[
    {
        "sha":  "795a74aefed762550e99bac8a897e61862a6e557",
        "filename":  "HANDOFF.md",
        "status":  "modified",
        "additions":  2,
        "deletions":  0,
        "changes":  2,
        "blob_url":  "https://github.com/RustCADs/RGE/blob/4bb9cf99b5a188fd5e8d9e28ddb7bcec77039e75/HANDOFF.md",
        "raw_url":  "https://github.com/RustCADs/RGE/raw/4bb9cf99b5a188fd5e8d9e28ddb7bcec77039e75/HANDOFF.md",
        "contents_url":  "https://api.github.com/repos/RustCADs/RGE/contents/HANDOFF.md?ref=4bb9cf99b5a188fd5e8d9e28ddb7bcec77039e75",
        "patch":  "@@ -1,5 +1,7 @@\n # RGE Handoff Document\n \n+\u003e **Snapshot**: 2026-06-03 (MENUARC-DOC-RECONCILE-A4 — View menu landed; the menu-breadth arc File / Edit / Play / View is COMPLETE): #299 (`be4896a`, VIEWMENU-RESETCAMERA-A4) added a **View** menu (Reset Camera) — the fourth and final standard menu — via the same W08 `MenuRegistry` path. **Unlike the prior three (pure wiring), View needed a NEW runtime action:** `EguiHost::new` declares a fourth extension point `editor.main_menu.view` and registers Reset Camera (`Command::ResetCamera`); the editor-shell drain routes it to the new **infallible** `EditorShell::reset_camera`, which reframes `editor_camera` to the isometric view of the live scene\u0027s AABB union (new `current_scene_bounds` helper sourcing live meshes the same way as the render path — prebuilt meshes, else the CAD projection — and reusing `compute_aabb_union` + `isometric_camera_for_bounds`), falling back to the default pose when nothing is frameable. Product semantics (owner-confirmed): frame the scene, mirroring the constructor\u0027s auto-frame, not a fixed default-pose jump. (The View additions tipped `crates/editor-egui-host/src/lib.rs` past 1000L — production ~913L + inline tests — so a `// SPLIT-EXEMPTION:` annotation was added per the `host.rs` precedent; a flagged **EGUIHOST-TEST-EXTRACTION** follow-up will extract the inline test module and retire it.) This supersedes the A3 snapshot below, which listed View as the lone remaining standard menu. STILL DEFERRED (NO standard menus remain — the breadth arc is closed): registry/accelerator-driven command execution (clicks still use the FIFO), accelerator-table execution/display/conflict population, plugin entries, dynamic predicates, and per-frame re-resolve (no dynamic toggle labels — e.g. Play⇄Pause, or a camera-state-aware View item — yet). The A3 / A2 / A1 snapshots below and older dated lines are historical and are not rewritten in place.\n+\n \u003e **Snapshot**: 2026-06-03 (MENUARC-DOC-RECONCILE-A3 — Play menu landed; the **View** menu is now the lone remaining standard menu deferred): #297 (`6ea5006`, MENUREGISTRY-PLAYMENU-A3) added a **Play** menu (Play / Pause / Stop / Step) to the host menu bar, driven by the same W08 `MenuRegistry` path as File + Edit — `EguiHost::new` declares a third extension point `editor.main_menu.play`, registers Play / Pause / Stop / Step as `MenuEntry`s, resolves once (one registry, all three points), and the editor-shell drain routes `Command::PlayStart` / `PlayPause` / `PlayStop` / `PlayStep` to `EditorShell::handle_button(ToolbarButtonId::Play / Pause / Stop / Step)` — the same PIE transition path the play-toolbar buttons drive (a click in a `PlayState` where its transition is a no-op, e.g. Stop while Editing → `PlayStateError::NoSnapshot`, is swallowed at debug BEFORE any mutation, mirroring the Space/Escape playback swallow). This supersedes the A2 snapshot below, which listed the Play menu as the deferred next feature beat. STILL DEFERRED (narrowed further): the **View** menu — now the lone remaining standard menu — needs a NEW camera-reset action first (e.g. via `isometric_camera_for_bounds`); registry/accelerator-driven command execution (clicks still use the FIFO), accelerator-table execution/display/conflict population, plugin entries, dynamic predicates, and per-frame re-resolve (no Play⇄Pause dynamic toggle label yet — v0 ships fixed labels). The A2 / A1 snapshots below and older dated lines are historical and are not rewritten in place.\n \n \u003e **Snapshot**: 2026-06-02 (MENUARC-DOC-RECONCILE-A2 — Edit menu landed; only View/Play menus remain deferred): #295 (`0bc6a0c`, MENUREGISTRY-EDITMENU-A2) added an **Edit** menu (Undo / Redo) to the host menu bar, driven by the same W08 `MenuRegistry` path as File — `EguiHost::new` declares `editor.main_menu.edit`, registers Undo / Redo as `MenuEntry`s, resolves once (one registry, both points), and the editor-shell drain routes `Command::Undo` / `Command::Redo` to `EditorShell::undo_command` / `redo_command`, behavior-identical to the existing `Ctrl+Z` / `Ctrl+Y` keystrokes. This supersedes the A1 snapshot below, which listed the Edit menu as deferred. STILL DEFERRED (narrowed further): the **View** menu (needs a NEW camera-reset action first, e.g. via `isometric_camera_for_bounds`) and the **Play** menu (needs only menu/UI wiring, NOT a new action — `Command::PlayStart` / `PlayStop` / `PlayPause` / `PlayStep` already exist and the PIE path is already runtime-wired through `EditorShell::handle_button`, Phase 5.3 CLOSED, so the Play menu is pure FIFO→`handle_button` wiring and is the intended next feature beat); registry/accelerator-driven command execution (clicks still use the FIFO), accelerator-table execution/display/conflict population, plugin entries, dynamic predicates, and per-frame re-resolve. The A1 snapshot below and older dated lines are historical and are not rewritten in place."
    },
    {
        "sha":  "feb8a0dc7175f483acb4f8c6572323f2de0a51c5",
        "filename":  "Status.md",
        "status":  "modified",
        "additions":  2,
        "deletions":  0,
        "changes":  2,
        "blob_url":  "https://github.com/RustCADs/RGE/blob/4bb9cf99b5a188fd5e8d9e28ddb7bcec77039e75/Status.md",
        "raw_url":  "https://github.com/RustCADs/RGE/raw/4bb9cf99b5a188fd5e8d9e28ddb7bcec77039e75/Status.md",
        "contents_url":  "https://api.github.com/repos/RustCADs/RGE/contents/Status.md?ref=4bb9cf99b5a188fd5e8d9e28ddb7bcec77039e75",
        "patch":  "@@ -1,5 +1,7 @@\n # Status\n \n+\u003e Snapshot: 2026-06-03 (MENUARC-DOC-RECONCILE-A4 — View menu landed; the menu-breadth arc File / Edit / Play / View is COMPLETE): #299 (`be4896a`, VIEWMENU-RESETCAMERA-A4) added a **View** menu (Reset Camera) — the fourth and final standard menu — via the same W08 `MenuRegistry` path. **Unlike the prior three (pure wiring), View needed a NEW runtime action:** `EguiHost::new` declares a fourth extension point `editor.main_menu.view` and registers Reset Camera (`Command::ResetCamera`); the editor-shell drain routes it to the new **infallible** `EditorShell::reset_camera`, which reframes `editor_camera` to the isometric view of the live scene\u0027s AABB union (new `current_scene_bounds` helper sourcing live meshes the same way as the render path — prebuilt meshes, else the CAD projection — and reusing `compute_aabb_union` + `isometric_camera_for_bounds`), falling back to the default pose when nothing is frameable. Product semantics (owner-confirmed): frame the scene, mirroring the constructor\u0027s auto-frame, not a fixed default-pose jump. (The View additions tipped `crates/editor-egui-host/src/lib.rs` past 1000L — production ~913L + inline tests — so a `// SPLIT-EXEMPTION:` annotation was added per the `host.rs` precedent; a flagged **EGUIHOST-TEST-EXTRACTION** follow-up will extract the inline test module and retire it.) This supersedes the A3 snapshot below, which listed View as the lone remaining standard menu. STILL DEFERRED (NO standard menus remain — the breadth arc is closed): registry/accelerator-driven command execution (clicks still use the FIFO), accelerator-table execution/display/conflict population, plugin entries, dynamic predicates, and per-frame re-resolve (no dynamic toggle labels — e.g. Play⇄Pause, or a camera-state-aware View item — yet). The A3 / A2 / A1 snapshots below and older dated lines are historical and are not rewritten in place.\n+\n \u003e Snapshot: 2026-06-03 (MENUARC-DOC-RECONCILE-A3 — Play menu landed; the **View** menu is now the lone remaining standard menu deferred): #297 (`6ea5006`, MENUREGISTRY-PLAYMENU-A3) added a **Play** menu (Play / Pause / Stop / Step) to the host menu bar, driven by the same W08 `MenuRegistry` path as File + Edit — `EguiHost::new` declares a third extension point `editor.main_menu.play`, registers Play / Pause / Stop / Step as `MenuEntry`s, resolves once (one registry, all three points), and the editor-shell drain routes `Command::PlayStart` / `PlayPause` / `PlayStop` / `PlayStep` to `EditorShell::handle_button(ToolbarButtonId::Play / Pause / Stop / Step)` — the same PIE transition path the play-toolbar buttons drive (a click in a `PlayState` where its transition is a no-op, e.g. Stop while Editing → `PlayStateError::NoSnapshot`, is swallowed at debug BEFORE any mutation, mirroring the Space/Escape playback swallow). This supersedes the A2 snapshot below, which listed the Play menu as the deferred next feature beat. STILL DEFERRED (narrowed further): the **View** menu — now the lone remaining standard menu — needs a NEW camera-reset action first (e.g. via `isometric_camera_for_bounds`); registry/accelerator-driven command execution (clicks still use the FIFO), accelerator-table execution/display/conflict population, plugin entries, dynamic predicates, and per-frame re-resolve (no Play⇄Pause dynamic toggle label yet — v0 ships fixed labels). The A2 / A1 snapshots below and older dated lines are historical and are not rewritten in place.\n \n \u003e Snapshot: 2026-06-02 (MENUARC-DOC-RECONCILE-A2 — Edit menu landed; only View/Play menus remain deferred): #295 (`0bc6a0c`, MENUREGISTRY-EDITMENU-A2) added an **Edit** menu (Undo / Redo) to the host menu bar, driven by the same W08 `MenuRegistry` path as File — `EguiHost::new` declares `editor.main_menu.edit`, registers Undo / Redo as `MenuEntry`s, resolves once (one registry, both points), and the editor-shell drain routes `Command::Undo` / `Command::Redo` to `EditorShell::undo_command` / `redo_command`, behavior-identical to the existing `Ctrl+Z` / `Ctrl+Y` keystrokes. This supersedes the A1 snapshot below, which listed the Edit menu as deferred. STILL DEFERRED (narrowed further): the **View** menu (needs a NEW camera-reset action first, e.g. via `isometric_camera_for_bounds`) and the **Play** menu (needs only menu/UI wiring, NOT a new action — `Command::PlayStart` / `PlayStop` / `PlayPause` / `PlayStep` already exist and the PIE path is already runtime-wired through `EditorShell::handle_button`, Phase 5.3 CLOSED, so the Play menu is pure FIFO→`handle_button` wiring and is the intended next feature beat); registry/accelerator-driven command execution (clicks still use the FIFO), accelerator-table execution/display/conflict population, plugin entries, dynamic predicates, and per-frame re-resolve. The A1 snapshot below and older dated lines are historical and are not rewritten in place."
    },
    {
        "sha":  "aee0d478ffc5990cb0b3140ece4a574171d01d17",
        "filename":  "change.md",
        "status":  "modified",
        "additions":  2,
        "deletions":  0,
        "changes":  2,
        "blob_url":  "https://github.com/RustCADs/RGE/blob/4bb9cf99b5a188fd5e8d9e28ddb7bcec77039e75/change.md",
        "raw_url":  "https://github.com/RustCADs/RGE/raw/4bb9cf99b5a188fd5e8d9e28ddb7bcec77039e75/change.md",
        "contents_url":  "https://api.github.com/repos/RustCADs/RGE/contents/change.md?ref=4bb9cf99b5a188fd5e8d9e28ddb7bcec77039e75",
        "patch":  "@@ -1654,3 +1654,5 @@ ew / partial / is_full_revolution / existing geometry tests. **Critical design c\n 2026-06-02 (MENUARC-DOC-RECONCILE-A2 — planning-substrate hygiene after A2 + menu rustdoc + lint-doc counts, docs-only). Forward-only reconciliation after #295 (`0bc6a0c`, MENUREGISTRY-EDITMENU-A2) added the **Edit** menu (Undo / Redo) via `MenuRegistry`. The host builds both menus from one registry (`build_main_menu_entries` declares `editor.main_menu.file` + `editor.main_menu.edit`, resolves once, caches both lists; `render` paints a second `menu_button(\"Edit\")`); `editor-shell` `drain_and_route_menu_commands` routes `Command::Undo` / `Command::Redo` to `EditorShell::undo_command` / `redo_command`, behavior-identical to `Ctrl+Z` / `Ctrl+Y`. This entry + new top snapshots in `Status.md` / `HANDOFF.md` + a new `plans/BASELINE.md` A2 subsection supersede the now-stale \"Edit menu deferred\" framing from the #292 / A1 reconcile (which predated A2). **Also folded in (post-2026-06-02 deep-audit doc hygiene):** fixed stale menu rustdoc in `crates/editor-egui-host/src/{lib.rs,handoff.rs}` (the \"Dispatch A only enqueues — nothing drains yet\" claim was false since #288 added the drain; the \"the File menu bar\" framing widened to \"File + Edit\"); and corrected VERIFIED-stale counts in `docs/§18/ARCHITECTURE_LINTS.md` — forbidden-dep **6 → 7 rules** (Rule 7 `editor-shell` loader-free landed #280), exemptions **60 → 28** total (all failure-class rollout-debt; **28 remain** of the original 81; no graph-foundation or reserved entries — verified with anchored `grep -c \u0027^\\[\\[exemption\\]\\]\u0027`, since the unanchored form over-counts by matching the file\u0027s schema comment). **Still deferred (narrowed):** the View menu (needs a NEW camera-reset action first) and the Play menu (needs only menu/UI wiring — Play is already runtime-wired via `EditorShell::handle_button`, with `Command::PlayStart` / `PlayStop` / `PlayPause` / `PlayStep` variants present, so it is pure FIFO→`handle_button` wiring with no new action, the intended next feature beat), accelerator-table execution/display/conflict, plugin entries, dynamic predicates, per-frame re-resolve. **History preserved:** the A1 (#292) reconcile entry above + all older dated entries are byte-identical (forward-only). **Scope:** docs + rustdoc/comments only — edits confined to `Status.md`, `HANDOFF.md`, `plans/BASELINE.md`, `change.md`, `crates/editor-egui-host/src/{lib.rs,handoff.rs}`, `docs/§18/ARCHITECTURE_LINTS.md`; no Rust source-logic / test / Cargo / routing change. Provenance: the 2026-06-02 post-menu-arc read-only 5-axis deep audit (health strong; authority intact; no kernel-cavity pressure). Gate: VERIFY OK 7/7 + 10/10 arch lints.\n \n 2026-06-03 (MENUARC-DOC-RECONCILE-A3 — planning-substrate hygiene after A3, docs-only). Forward-only reconciliation after #297 (`6ea5006`, MENUREGISTRY-PLAYMENU-A3) added the **Play** menu (Play / Pause / Stop / Step) via `MenuRegistry`. The host now builds all three menus from one registry (`build_main_menu_entries` declares `editor.main_menu.file` + `editor.main_menu.edit` + `editor.main_menu.play`, resolves once, caches all three lists; `render` paints a third `menu_button(\"Play\")`); `editor-shell` `drain_and_route_menu_commands` routes `Command::PlayStart` / `PlayPause` / `PlayStop` / `PlayStep` to `EditorShell::handle_button(ToolbarButtonId::Play / Pause / Stop / Step)` via a `route_play_button` helper — the same PIE path the play-toolbar buttons drive, with benign invalid-state `PlayStateError` (e.g. Stop while Editing → `NoSnapshot`) swallowed at debug before any mutation (mirrors the Space/Escape playback swallow). This entry + new top snapshots in `Status.md` / `HANDOFF.md` + a new `plans/BASELINE.md` A3 subsection supersede the now-stale \"Play menu deferred / the intended next feature beat\" framing from the A2 reconcile (which predated A3). **Menu rustdoc already current:** #297 itself widened the `editor-egui-host` menu rustdoc/comments from \"File + Edit\" to \"File + Edit + Play\" (its `bdd9531` correction round), so — unlike A2 — this docs reconcile touches NO source comments and NO `docs/§18/ARCHITECTURE_LINTS.md` counts. **Still deferred (narrowed to View alone):** the **View** menu is now the lone remaining standard menu and needs a NEW camera-reset action first (e.g. via `isometric_camera_for_bounds`) — unlike Play, View has no reachable shell action yet, so it is a feature beat, not pure wiring; plus accelerator-table execution/display/conflict, plugin entries, dynamic predicates, per-frame re-resolve (including a Play⇄Pause dynamic toggle label — v0 ships fixed labels). **History preserved:** the A2 (#296) reconcile entry above + all older dated entries are byte-identical (forward-only). **Scope:** docs-only — edits confined to `Status.md`, `HANDOFF.md`, `plans/BASELINE.md`, `change.md`; no Rust source-logic / test / Cargo / routing / rustdoc / lint-doc change. **Verification:** `git diff --check` clean; `git diff --stat 6ea5006..HEAD` = exactly the 4 docs; `.ai/dispatch.verify.ps1` exit 0 (`VERIFY OK: all 7 verification step(s) passed.`, 10/10 architecture lints 0 violations — Markdown-only change, no Rust touched). **Three required references present in this entry:** `plans/BASELINE.md` new \"2026-06-03 — Play menu now registry-produced (#297 A3)\" subsection, the merged PR #297 (`6ea5006`), and `ai_handoffs/MENUARC-DOC-RECONCILE-A3_TASK_2026-06-03_11-13-31+0300.md`.\n+\n+2026-06-03 (MENUARC-DOC-RECONCILE-A4 — planning-substrate hygiene after A4, docs-only). Forward-only reconciliation after #299 (`be4896a`, VIEWMENU-RESETCAMERA-A4) added the **View** menu (Reset Camera), closing the menu-breadth arc — File (A1) / Edit (A2) / Play (A3) / View (A4) all build from the one shared `MenuRegistry`. Unlike A1–A3 (pure wiring), View needed a NEW runtime action: `build_main_menu_entries` declares a fourth point `editor.main_menu.view` + registers `Command::ResetCamera` (the host now resolves all four points, caches four lists; `render` paints a fourth `menu_button(\"View\")`); `editor-shell` `drain_and_route_menu_commands` routes `Command::ResetCamera` to the new infallible `EditorShell::reset_camera`, which reframes `editor_camera` to `isometric_camera_for_bounds(current_scene_bounds)` (live scene AABB union sourced as in render_path Step 6 — prebuilt meshes, else the CAD projection mesh), falling back to `EditorCameraState::default()` when nothing is frameable. Product semantics (owner-confirmed): frame the scene, not a fixed default-pose jump. This entry + new top snapshots in `Status.md` / `HANDOFF.md` + a new `plans/BASELINE.md` A4 subsection supersede the now-stale \"View is the lone remaining standard menu / pending feature beat\" framing from the A3 reconcile (which predated A4). **Split-exemption:** the View additions tipped `crates/editor-egui-host/src/lib.rs` past 1000L (~913L production + inline tests), so a `// SPLIT-EXEMPTION:` annotation was added per the `host.rs` precedent; a flagged EGUIHOST-TEST-EXTRACTION follow-up will extract the inline test module and retire it. **Menu rustdoc already current:** #299 (incl. its `6cd222d` correction round) widened the `editor-egui-host` menu rustdoc/comments to \"File + Edit + Play + View\", so — like A3 — this docs reconcile touches NO source comments and NO `docs/§18/ARCHITECTURE_LINTS.md` counts. **Still deferred (NO standard menus remain):** generalized registry/accelerator-driven command execution (clicks still use the FIFO), accelerator-table execution/display/conflict, plugin entries, dynamic predicates, per-frame re-resolve (including dynamic toggle labels). **History preserved:** the A3 (#298) reconcile entry above + all older dated entries are byte-identical (forward-only). **Scope:** docs-only — edits confined to `Status.md`, `HANDOFF.md`, `plans/BASELINE.md`, `change.md`; no Rust source-logic / test / Cargo / routing / rustdoc / lint-doc change. **Verification:** `git diff --check` clean; `git diff --stat be4896a..HEAD` = exactly the 4 docs; `.ai/dispatch.verify.ps1` exit 0 (`VERIFY OK: all 7 verification step(s) passed.`, 10/10 architecture lints 0 violations — Markdown-only change, no Rust touched). **Three required references present in this entry:** `plans/BASELINE.md` new \"2026-06-03 — View menu now registry-produced; menu-breadth arc complete (#299 A4)\" subsection, the merged PR #299 (`be4896a`), and `ai_handoffs/MENUARC-DOC-RECONCILE-A4_TASK_2026-06-03_14-23-21+0300.md`."
    },
    {
        "sha":  "3cc2bae5dfa76c18ba8a30bd28dee779bd91fe96",
        "filename":  "plans/BASELINE.md",
        "status":  "modified",
        "additions":  16,
        "deletions":  0,
        "changes":  16,
        "blob_url":  "https://github.com/RustCADs/RGE/blob/4bb9cf99b5a188fd5e8d9e28ddb7bcec77039e75/plans%2FBASELINE.md",
        "raw_url":  "https://github.com/RustCADs/RGE/raw/4bb9cf99b5a188fd5e8d9e28ddb7bcec77039e75/plans%2FBASELINE.md",
        "contents_url":  "https://api.github.com/repos/RustCADs/RGE/contents/plans%2FBASELINE.md?ref=4bb9cf99b5a188fd5e8d9e28ddb7bcec77039e75",
        "patch":  "@@ -558,6 +558,22 @@ Until **at least one** of those fires, treat the reflection substrate as observe\n 4. Cross-check the editor\u0027s call graph against the `CommandBus::submit` / `Action::apply` / `Action::revert` signatures to determine whether user-visible CAD mutations can flow through the existing bus.\n 5. Test inventory across `editor-*` (`#[test]` count + integration vs unit breakdown + workflow coverage).\n \n+### 2026-06-03 — View menu now registry-produced; menu-breadth arc complete (#299 A4)\n+\n+**Forward-only follow-up (MENUARC-DOC-RECONCILE-A4).** Supersedes the A3 subsection below, which listed View as the lone remaining standard menu / a pending feature beat. A4 landed on main at `be4896a` via PR #299. **This closes the menu-breadth arc: File (A1) / Edit (A2) / Play (A3) / View (A4) are all built from the one shared `MenuRegistry`.**\n+\n+**Now CLOSED — the View menu (Reset Camera).** Unlike A1–A3 (pure FIFO→handler wiring), View needed a NEW runtime action. `crates/editor-egui-host/src/lib.rs` `build_main_menu_entries()` now declares a fourth point `editor.main_menu.view` (`VIEW_MENU_EXTENSION_POINT`), registers Reset Camera (`Command::ResetCamera`), resolves once, and caches all four `(label, Command)` lists; `render()` paints a fourth `menu_button(\"View\")`. `editor-shell` `drain_and_route_menu_commands` routes `Command::ResetCamera` to the new **infallible** `EditorShell::reset_camera` (no swallow — contrast A3\u0027s `route_play_button`), which reframes `editor_camera` to `isometric_camera_for_bounds(current_scene_bounds)` — the live scene\u0027s AABB union sourced the same way as `render_path.rs` Step 6 (prebuilt meshes, else the CAD projection mesh) — falling back to `EditorCameraState::default()` when nothing is frameable. Product semantics (owner-confirmed): frame the scene, mirroring the constructor\u0027s auto-frame, not a fixed default-pose jump.\n+\n+**Split-exemption.** The View additions tipped `crates/editor-egui-host/src/lib.rs` past 1000L (production ~913L + inline `#[cfg(test)]` ~120L), so a `// SPLIT-EXEMPTION:` annotation was added at the file head per the `host.rs` precedent (annotate in-the-moment; extract tests later). **Flagged follow-up:** an EGUIHOST-TEST-EXTRACTION dispatch to move the inline test module to a sibling file and retire the annotation.\n+\n+**Still open — explicitly NOT closed here (NO standard menus remain; the breadth arc is closed):**\n+- Generalized registry/accelerator-driven command execution remains deferred (menu clicks still flow through the host→shell FIFO + `editor-shell` routing, not a registry/accelerator execution path).\n+- accelerator-table execution/display/conflict population, plugin menu entries, dynamic predicates, and per-frame re-resolve remain unbuilt — including dynamic toggle labels (a Play⇄Pause label, or a camera-state-aware View item).\n+\n+**Historical preservation.** The A3 / A2 / A1 subsections below and all earlier dated entries are preserved byte-identical as dated history; their \"View … the lone remaining standard menu / pending feature beat\" lines are superseded forward by this subsection, not rewritten in place.\n+\n+**Scope:** docs only (`Status.md` + `HANDOFF.md` + `plans/BASELINE.md` + `change.md`); no Rust source-logic / test / Cargo / routing change (the menu rustdoc was widened to File + Edit + Play + View inside #299 itself, incl. its `6cd222d` correction round).\n+\n ### 2026-06-03 — Play menu now registry-produced (#297 A3)\n \n **Forward-only follow-up (MENUARC-DOC-RECONCILE-A3).** Supersedes the A2 subsection below, which listed the Play menu among the deferred items as the intended next feature beat. A3 landed on main at `6ea5006` via PR #297."
    }
]

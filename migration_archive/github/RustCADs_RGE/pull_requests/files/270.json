[
    {
        "sha":  "e63736aa12178ae2ab5129a66873fa97d9ec0292",
        "filename":  "crates/editor-shell/src/lifecycle/mod.rs",
        "status":  "modified",
        "additions":  12,
        "deletions":  0,
        "changes":  12,
        "blob_url":  "https://github.com/RustCADs/RGE/blob/59b097673fdfed1e92f6a062e48dc643f59d34ff/crates%2Feditor-shell%2Fsrc%2Flifecycle%2Fmod.rs",
        "raw_url":  "https://github.com/RustCADs/RGE/raw/59b097673fdfed1e92f6a062e48dc643f59d34ff/crates%2Feditor-shell%2Fsrc%2Flifecycle%2Fmod.rs",
        "contents_url":  "https://api.github.com/repos/RustCADs/RGE/contents/crates%2Feditor-shell%2Fsrc%2Flifecycle%2Fmod.rs?ref=59b097673fdfed1e92f6a062e48dc643f59d34ff",
        "patch":  "@@ -216,6 +216,7 @@ pub mod commands;\n pub mod open_request;\n pub mod playback;\n pub mod save_request;\n+pub mod window_title;\n \n pub use asset_reload::AssetReloadHook;\n pub use commands::{EditorKeyCommand, SetTimeScale};\n@@ -440,6 +441,11 @@ pub struct EditorShell {\n     /// via [`Self::with_scene_source_path`]; read via [`Self::scene_source_path`].\n     pub(crate) scene_source_path: Option\u003cPathBuf\u003e,\n \n+    /// EDITOR-WINDOW-TITLE — the last title handed to the winit window\u0027s\n+    /// `set_title`, so [`Self::sync_window_title`] only re-titles on a change\n+    /// (not every frame). `None` until the first sync.\n+    pub(crate) last_window_title: Option\u003cString\u003e,\n+\n     /// winit window the surface is bound to (kept alive for the surface\u0027s\n     /// `\u0027static` lifetime). `None` until `resumed`.\n     pub(crate) window: Option\u003cArc\u003cWindow\u003e\u003e,\n@@ -674,6 +680,7 @@ impl EditorShell {\n             save_dialog: None,\n             scene_save_hook: None,\n             scene_source_path: None,\n+            last_window_title: None,\n         }\n     }\n \n@@ -835,6 +842,7 @@ impl EditorShell {\n             save_dialog: None,\n             scene_save_hook: None,\n             scene_source_path: None,\n+            last_window_title: None,\n         }\n     }\n \n@@ -1088,6 +1096,7 @@ impl EditorShell {\n             save_dialog: None,\n             scene_save_hook: None,\n             scene_source_path: None,\n+            last_window_title: None,\n         }\n     }\n \n@@ -1895,6 +1904,9 @@ impl ApplicationHandler\u003c()\u003e for EditorShell {\n             }\n             WindowEvent::RedrawRequested =\u003e {\n                 self.tick_redraw();\n+                // EDITOR-WINDOW-TITLE — reflect the open scene + dirty state in\n+                // the title bar (set_title only fires on change).\n+                self.sync_window_title();\n                 // Phase 6.2 runtime integration — acquire the most\n                 // recently published snapshot from the per-ADR-117\n                 // `RenderHandoff` slot. `render_frame` currently"
    },
    {
        "sha":  "20f7345dc03f8a680697964cb32441686e12638c",
        "filename":  "crates/editor-shell/src/lifecycle/tests.rs",
        "status":  "modified",
        "additions":  62,
        "deletions":  0,
        "changes":  62,
        "blob_url":  "https://github.com/RustCADs/RGE/blob/59b097673fdfed1e92f6a062e48dc643f59d34ff/crates%2Feditor-shell%2Fsrc%2Flifecycle%2Ftests.rs",
        "raw_url":  "https://github.com/RustCADs/RGE/raw/59b097673fdfed1e92f6a062e48dc643f59d34ff/crates%2Feditor-shell%2Fsrc%2Flifecycle%2Ftests.rs",
        "contents_url":  "https://api.github.com/repos/RustCADs/RGE/contents/crates%2Feditor-shell%2Fsrc%2Flifecycle%2Ftests.rs?ref=59b097673fdfed1e92f6a062e48dc643f59d34ff",
        "patch":  "@@ -27,6 +27,7 @@\n //! No `pub(crate)` promotions were required for the extraction; the\n //! tests were already touching only public API surface.\n \n+use super::window_title::editor_window_title;\n use super::EditorShell;\n use crate::audit::AuditEvent;\n use crate::play_state::{PlayState, PlayStateTransition};\n@@ -2052,3 +2053,64 @@ fn save_outside_editing_with_source_is_noop() {\n         \"PIE-gated Save leaves the source untouched\"\n     );\n }\n+\n+// ---------------------------------------------------------------------------\n+// EDITOR-WINDOW-TITLE — window title reflects scene source + dirty state\n+// ---------------------------------------------------------------------------\n+\n+#[test]\n+fn window_title_no_source_clean() {\n+    assert_eq!(editor_window_title(None, false), \"RGE Editor\");\n+}\n+\n+#[test]\n+fn window_title_no_source_dirty() {\n+    assert_eq!(editor_window_title(None, true), \"RGE Editor *\");\n+}\n+\n+#[test]\n+fn window_title_with_source_clean() {\n+    assert_eq!(\n+        editor_window_title(Some(std::path::Path::new(\"/tmp/level.rge-scene\")), false),\n+        \"level.rge-scene — RGE Editor\"\n+    );\n+}\n+\n+#[test]\n+fn window_title_with_source_dirty() {\n+    assert_eq!(\n+        editor_window_title(Some(std::path::Path::new(\"/tmp/level.rge-scene\")), true),\n+        \"level.rge-scene * — RGE Editor\"\n+    );\n+}\n+\n+#[test]\n+fn window_title_uses_file_name_not_full_path() {\n+    // Only the file name appears, not the directory components.\n+    assert_eq!(\n+        editor_window_title(Some(std::path::Path::new(\"/a/b/c/scene.rge-scene\")), false),\n+        \"scene.rge-scene — RGE Editor\"\n+    );\n+}\n+\n+#[test]\n+fn sync_window_title_without_window_is_noop() {\n+    // A headless shell has no winit window; the per-frame title sync must not\n+    // panic and must leave the windowless shell untouched.\n+    let mut s = EditorShell::new();\n+    assert!(\n+        s.window.is_none(),\n+        \"precondition: headless shell has no window\"\n+    );\n+\n+    s.sync_window_title();\n+\n+    assert!(\n+        s.window.is_none(),\n+        \"sync_window_title must not create a window\"\n+    );\n+    assert!(\n+        s.last_window_title.is_none(),\n+        \"a windowless sync commits no title\"\n+    );\n+}"
    },
    {
        "sha":  "7e8622d10e7b34bd520631b811a249c28ed8f868",
        "filename":  "crates/editor-shell/src/lifecycle/window_title.rs",
        "status":  "added",
        "additions":  64,
        "deletions":  0,
        "changes":  64,
        "blob_url":  "https://github.com/RustCADs/RGE/blob/59b097673fdfed1e92f6a062e48dc643f59d34ff/crates%2Feditor-shell%2Fsrc%2Flifecycle%2Fwindow_title.rs",
        "raw_url":  "https://github.com/RustCADs/RGE/raw/59b097673fdfed1e92f6a062e48dc643f59d34ff/crates%2Feditor-shell%2Fsrc%2Flifecycle%2Fwindow_title.rs",
        "contents_url":  "https://api.github.com/repos/RustCADs/RGE/contents/crates%2Feditor-shell%2Fsrc%2Flifecycle%2Fwindow_title.rs?ref=59b097673fdfed1e92f6a062e48dc643f59d34ff",
        "patch":  "@@ -0,0 +1,64 @@\n+//! Window-title surfacing — reflect the open scene + dirty state in the winit\n+//! title bar (EDITOR-WINDOW-TITLE).\n+//!\n+//! The in-app Save chain (SCENE-SAVE-SUBSTRATE → SCENE-SAVE-WIRING →\n+//! SCENE-SAVE-SOURCE-PATH) gave the editor a tracked\n+//! [`EditorShell::scene_source_path`] and a Command-Bus dirty flag; this surface\n+//! makes both visible in the OS window title so the user can see which file\n+//! `Ctrl+S` writes and whether there are unsaved edits.\n+//!\n+//! - [`editor_window_title`] is the pure (no-I/O) title formatter — the only\n+//!   logic, fully unit-testable.\n+//! - [`EditorShell::sync_window_title`] reconciles the live title onto the winit\n+//!   window via `set_title`, only when it changed since the last sync (tracked\n+//!   by `last_window_title`). It runs once per frame from the\n+//!   `WindowEvent::RedrawRequested` branch of `window_event`, and no-ops on a\n+//!   windowless (headless) shell. Mirrors the pure-decision + reconcile shape of\n+//!   the binary\u0027s `glb_watcher_action` / `sync_glb_watcher`.\n+\n+use std::path::Path;\n+\n+use crate::lifecycle::EditorShell;\n+\n+/// The window title for the current scene source + dirty state.\n+///\n+/// - `Some(path)` with a readable file name →\n+///   `\"{file_name}{dirty} — RGE Editor\"` (e.g. `level.rge-scene * — RGE Editor`).\n+/// - `None` (default demo / `--glb` / no scene), or a path whose `file_name()`\n+///   is absent / non-UTF-8 → `\"RGE Editor{dirty}\"`.\n+///\n+/// `dirty` is `\" *\"` when `is_dirty`, else `\"\"`. Pure — the `set_title`\n+/// side-effect lives in [`EditorShell::sync_window_title`].\n+pub(crate) fn editor_window_title(scene_source_path: Option\u003c\u0026Path\u003e, is_dirty: bool) -\u003e String {\n+    let dirty = if is_dirty { \" *\" } else { \"\" };\n+    match scene_source_path\n+        .and_then(Path::file_name)\n+        .and_then(|name| name.to_str())\n+    {\n+        Some(name) =\u003e format!(\"{name}{dirty} — RGE Editor\"),\n+        None =\u003e format!(\"RGE Editor{dirty}\"),\n+    }\n+}\n+\n+impl EditorShell {\n+    /// Reconcile the winit window title with the live\n+    /// [`Self::scene_source_path`] + the Command-Bus dirty flag.\n+    ///\n+    /// Computes [`editor_window_title`] and pushes it to the window via\n+    /// `set_title` **only when it changed** since the last sync (tracked by\n+    /// `last_window_title`) — so a redraw whose title is unchanged costs no\n+    /// `set_title`. No-op when there is no window (headless `EditorShell::new()`\n+    /// / pre-`resumed`). Called once per frame from the\n+    /// `WindowEvent::RedrawRequested` branch of [`Self::window_event`].\n+    pub(crate) fn sync_window_title(\u0026mut self) {\n+        // No window yet (headless / pre-`resumed`): nothing to retitle.\n+        let Some(window) = self.window.clone() else {\n+            return;\n+        };\n+        let title = editor_window_title(self.scene_source_path(), self.command_bus().is_dirty());\n+        if self.last_window_title.as_deref() != Some(title.as_str()) {\n+            window.set_title(\u0026title);\n+            self.last_window_title = Some(title);\n+        }\n+    }\n+}"
    },
    {
        "sha":  "d63858b9ee1724a7821942406aae7717934b75c5",
        "filename":  "crates/editor-shell/src/render_path.rs",
        "status":  "modified",
        "additions":  6,
        "deletions":  0,
        "changes":  6,
        "blob_url":  "https://github.com/RustCADs/RGE/blob/59b097673fdfed1e92f6a062e48dc643f59d34ff/crates%2Feditor-shell%2Fsrc%2Frender_path.rs",
        "raw_url":  "https://github.com/RustCADs/RGE/raw/59b097673fdfed1e92f6a062e48dc643f59d34ff/crates%2Feditor-shell%2Fsrc%2Frender_path.rs",
        "contents_url":  "https://api.github.com/repos/RustCADs/RGE/contents/crates%2Feditor-shell%2Fsrc%2Frender_path.rs?ref=59b097673fdfed1e92f6a062e48dc643f59d34ff",
        "patch":  "@@ -286,6 +286,12 @@ impl EditorShell {\n \n         // Stash the winit-bound bits not covered by the shared helper.\n         self.window = Some(window);\n+        // EDITOR-WINDOW-TITLE — a freshly created window starts at the static\n+        // creation title (\"RGE Editor\"); invalidate the title cache so the next\n+        // `sync_window_title` re-applies the live title. Without this, a\n+        // suspend/resume that installs a NEW window would skip `set_title`\n+        // (cached title == desired title) and leave the new window mistitled.\n+        self.last_window_title = None;\n         self.surface_ctx = Some(surface_ctx);\n \n         // Phase 9 egui host integration (dispatch B) — construct the"
    }
]

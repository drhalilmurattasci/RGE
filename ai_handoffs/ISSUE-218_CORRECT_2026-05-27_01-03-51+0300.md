# Correction Packet

DISPATCH_ID: ISSUE-218
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-27_01-03-51+0300
RELATED_FILES:
- ai_handoffs/ISSUE-218_TASK_2026-05-27_00-36-30+0300.md
- ai_handoffs/ISSUE-218_EXEC_2026-05-27_00-50-31+0300.md
- ai_handoffs/ISSUE-218_EXEC_*.md
- ai_handoffs/ISSUE-218_EXEC_*.meta.json
- .ai/dispatch-ISSUE-218/codex.control.round0.json
- .ai/dispatch-ISSUE-218/verification.round0.log
- AI_DISPATCH_AUTOMATION.md
- ai_handoffs/AI_HANDOFF_PROTOCOL.md
- .github/workflows/fmt.yml
- .github/workflows/architecture.yml
- .github/workflows/deny.yml
- .github/workflows/tests.yml
- .github/workflows/bench.yml
- .ai/dispatch.verify.ps1
- ai_dispatch_logs/log_*.md
- .ai/dispatch-ISSUE-*/verification*.log
- plans/PLAN.md
- plans/IMPLEMENTATION.md
- plans/BASELINE.md
- Status.md
- HANDOFF.md
- change.md
- Cargo.toml
- Cargo.lock
- crates/**/Cargo.toml
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-218_TASK_2026-05-27_00-36-30+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-218_EXEC_2026-05-27_00-50-31+0300.md`
- Codex Control Review Result: `.ai/dispatch-ISSUE-218/codex.control.round0.json`, verdict `needs_changes`
- Verification Log: `.ai/dispatch-ISSUE-218/verification.round0.log`
- Prior Correction Packets: none

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections and nothing more. This is a
report-correction round only: write one new timestamped ISSUE-218 EXEC report
and its normal `.meta.json` sidecar if the completion tooling emits one. Do not
edit existing TASK or EXEC artifacts, source files, documentation files,
schemas, scripts, Cargo files, workflow files, dispatch logs, or any other
packet.

1. **Line-cite dispatch automation and protocol claims** - Control review
   `required_fixes[0]`. Required change: in the replacement EXEC report, add
   valid live `path:line` citations from both `AI_DISPATCH_AUTOMATION.md` and
   `ai_handoffs/AI_HANDOFF_PROTOCOL.md` before any claim about orchestrator
   dispatch state, missing `_REVIEW_` / `_CLOSEOUT_` packets, control-review
   storage, or manual protocol packet shape. Replace the prior unsupported
   `AI_DISPATCH_AUTOMATION.md:191-194` citation for the no
   `_REVIEW_` / `_CLOSEOUT_` claim. Acceptance: the replacement EXEC contains
   supporting citations for both files and does not infer dispatch completion
   from the `ai_handoffs/` listing.

2. **Complete workflow-definition citations** - Control review
   `required_fixes[1]`. Required change: add line-cited inspection evidence for
   every audited workflow definition required by the Task Packet:
   `.github/workflows/fmt.yml`, `.github/workflows/architecture.yml`,
   `.github/workflows/deny.yml`, `.github/workflows/tests.yml`, and
   `.github/workflows/bench.yml`. The missing citations for `fmt.yml`,
   `architecture.yml`, and `deny.yml` must be added; retain or refresh valid
   citations for `tests.yml` and `bench.yml`. Acceptance: static search of the
   replacement EXEC finds `path:line` citations for all five workflow files,
   and the workflow-definition section states the inspected commands/scope.

3. **Line-cite local dispatch evidence** - Control review
   `required_fixes[2]`. Required change: add current line-cited evidence from
   recent `ai_dispatch_logs/log_*.md` files and available
   `.ai/dispatch-ISSUE-*/verification*.log` outputs, including
   `.ai/dispatch-ISSUE-218/verification.round0.log`. Label historical logs as
   historical, report observed durations or failure patterns only when the cited
   lines support them, and distinguish orchestrator automatic verification from
   commands manually run for the audit. Acceptance: the replacement EXEC
   contains concrete line-number citations such as
   `ai_dispatch_logs/log_...md:123` and
   `.ai/dispatch-ISSUE-.../verification...log:456` for local dispatch claims.

4. **Tighten Phase 9 context citations** - Control review
   `required_fixes[3]`. Required change: revise the Phase 9 context section so
   every required source from the Task Packet is actually cited with live
   `path:line` evidence: `plans/PLAN.md`, `plans/IMPLEMENTATION.md`,
   `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, recent `change.md` entries,
   and relevant existing dispatch artifacts. Add the missing `HANDOFF.md`
   citation and separate current compile-time-pressure evidence from historical
   release, CI, or dispatch-doctrine evidence. Acceptance: each retained Phase 9
   context claim cites the source it relies on, including `HANDOFF.md`.

5. **Use current Cargo-file line citations for dependency claims** - Control
   review `required_fixes[4]`. Required change: replace the invalid
   `Cargo.toml:4-?` placeholder and add live line citations from current
   `Cargo.toml`, `Cargo.lock`, and relevant crate `Cargo.toml` files for every
   substantive dependency-surface claim. For dependency churn, include exact
   read-only git commands, scoped paths, commit IDs, and observed additions or
   removals; separate dependency churn from ordinary source or test churn.
   Acceptance: the replacement EXEC contains usable root `Cargo.toml:12`,
   `Cargo.lock:34`, and `crates/.../Cargo.toml:56` style citations wherever
   current Cargo metadata supports the dependency analysis.

6. **Make CI evidence complete per workflow** - Control review
   `required_fixes[5]`. Required change: tighten the CI evidence table or
   section so each audited workflow has the current run ID, job ID where
   available, branch, head SHA, conclusion, timestamp, and representative log
   output. If logs are unavailable, state the exact read-only source or command
   attempted, the unavailable-log result, and whether classification remains
   possible without guessing. Acceptance: each of the format, architecture,
   deny, workspace tests, and script bench compile workflows has complete
   metadata plus representative log evidence or an explicit unavailable-log
   result.

## Deferred Findings (NOT Approved for This Round)

None. The control review did not approve optional implementation work or
cleanup beyond making the ISSUE-218 EXEC report evidence-complete. No source,
documentation, Cargo, workflow, schema, script, dispatch-log, or prior-packet
edits are approved.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with this correction-specific addition: the
next ISSUE-218 EXEC report must satisfy all six approved evidence corrections
above while preserving the audit-only scope, the exact decision-block field
requirements, and the no-protected-file-change boundary.

## Re-Verification Gates

The Executor MUST run or document these gates after writing the corrected EXEC
report. Do not manually run full repository verification, cargo builds, cargo
tests, cargo fmt, cargo deny, benches, profilers, the autonomous queue, or a
nested dispatch for this audit; if the orchestrator wrapper reruns canonical
verification, report that generated verification log.

- Inspect the replacement EXEC for dispatch/protocol citations. Expected:
  citations for both `AI_DISPATCH_AUTOMATION.md:` and
  `ai_handoffs/AI_HANDOFF_PROTOCOL.md:` supporting orchestrator/manual-protocol
  claims.
- Inspect the replacement EXEC for workflow-definition citations. Expected:
  citations for `.github/workflows/fmt.yml:`,
  `.github/workflows/architecture.yml:`, `.github/workflows/deny.yml:`,
  `.github/workflows/tests.yml:`, and `.github/workflows/bench.yml:`.
- Inspect the replacement EXEC for local dispatch evidence citations.
  Expected: at least one recent line-number citation from an
  `ai_dispatch_logs/log_*.md` file and line-cited
  `.ai/dispatch-ISSUE-*/verification*.log` evidence, including
  `.ai/dispatch-ISSUE-218/verification.round0.log`.
- Inspect the replacement EXEC Phase 9 context. Expected: live citations for
  `plans/PLAN.md`, `plans/IMPLEMENTATION.md`, `plans/BASELINE.md`, `Status.md`,
  `HANDOFF.md`, recent `change.md`, and any retained prior dispatch artifacts.
- Inspect the replacement EXEC dependency section. Expected: no
  `Cargo.toml:4-?` placeholder and usable citations for root `Cargo.toml`,
  `Cargo.lock`, and relevant crate manifests supporting dependency claims.
- Inspect the replacement EXEC CI evidence. Expected: for each audited
  workflow, run ID, job ID where available, branch, head SHA, conclusion,
  timestamp, and representative log output or explicit unavailable-log result.
- Inspect the replacement EXEC decision block. Expected exactly one occurrence
  of each required field: `COMPILE_TIME_PRESSURE_VERDICT`,
  `BOUNDED_AUTONOMOUS_FOLLOW_UP`, `SMALLEST_SAFE_FOLLOW_UP`, and
  `FOLLOW_UP_BRANCH_MODE`, with allowed values from the Task Packet.
- `git diff --check` -> expected exit 0.
- `git diff --name-only` -> expected empty output for tracked files.
- `git status --short --untracked-files=all` -> expected no tracked
  production, documentation, script, Cargo, workflow, schema, scheduler, task
  brief, fixture, or protected-surface changes. New or changed files must be
  limited to dispatch-owned ISSUE-218 handoff artifacts and optional ignored
  `.ai/dispatch-ISSUE-218` scratch.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, plus:

- Halt with `NEEDS_HUMAN` if satisfying these corrections would require
  editing protected source, tests, documentation, Cargo files, workflows,
  schemas, scripts, scheduler configuration, task briefs, existing packets, or
  existing dispatch logs.
- Halt with `NEEDS_HUMAN` if live line citations from
  `AI_DISPATCH_AUTOMATION.md` and `ai_handoffs/AI_HANDOFF_PROTOCOL.md` cannot
  be obtained for dispatch-state/protocol claims.
- Halt with `NEEDS_HUMAN` if all five workflow definitions cannot be inspected
  and line-cited read-only.
- Halt with `NEEDS_HUMAN` if current local dispatch logs or verification logs
  cannot be inspected and cited without rewriting, copying, or normalizing
  them.
- Halt with `NEEDS_HUMAN` if the required Phase 9, Cargo dependency, or CI
  evidence cannot be made complete without guessing or expanding scope.
- Halt rather than writing a `_REVIEW_` or `_CLOSEOUT_` packet for this
  orchestrator dispatch.

## Planner Notes

The canonical orchestrator verification log passed all seven steps, and
control review found no tracked protected-file diff. The blocker is report
readiness: the first EXEC report under-cited required evidence and left several
current-evidence details incomplete. Keep this correction report-only. Preserve
the smallest-follow-up and branch-mode constraints from the Task Packet unless
refreshed evidence forces a truthful `NEEDS_HUMAN` result.

---

HANDOFF_STATUS: COMPLETE
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Task Packet

DISPATCH_ID: ISSUE-81
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-21_01-00-56+0300
RELATED_FILES:
- kernel/graph-foundation/tests/diff_correctness.rs
STATUS: OPEN

## Goal

Add focused integration-test coverage for GitHub issue #81: `GraphDiff::between` must report newly added edges as full edge records. The test should pin that an `added_edges` entry preserves the new edge's `src`, `dst`, and `data`, and that this added-edge-only scenario does not produce unrelated node, removed-edge, or changed-edge diff noise.

## Scope

### MAY edit
- kernel/graph-foundation/tests/diff_correctness.rs

### MUST NOT edit
- Any file outside `kernel/graph-foundation/tests/diff_correctness.rs`
- Production source under `kernel/graph-foundation/src/`
- Cargo manifests, lockfiles, workspace configuration, scripts, docs, schemas, or other handoff packets

### MAY add new files
- None

### MUST NOT add new files
- Any new test file, fixture, helper module, documentation file, source file, script, schema, or handoff packet

## Deliverables

- One new focused integration test in `kernel/graph-foundation/tests/diff_correctness.rs`, preferably named `diff_added_edge_preserves_new_edge_record_payload`.
- The test constructs two graph snapshots where the only semantic difference is one newly inserted edge between pre-existing nodes.
- The test asserts the `added_edges` entry for the new `EdgeId` preserves the new edge record's `src`, `dst`, and `data`.
- The test asserts there is no unrelated diff noise: no added, removed, or changed nodes; no removed edges; and no changed edges.

## Acceptance Criteria

- `kernel/graph-foundation/tests/diff_correctness.rs` contains exactly one new focused test for added-edge record preservation.
- The new test fails if `GraphDiff::between` records the added edge without the correct `src`, `dst`, or `data`.
- The new test fails if the added-edge-only scenario reports node noise, removed-edge noise, or changed-edge noise.
- `cargo test -p rge-kernel-graph-foundation --test diff_correctness` exits 0.

## Constraints / Non-Goals

- Do not change `GraphDiff`, `Graph`, `GraphSnapshot`, edge-record types, or any production behavior.
- Do not broaden this dispatch into implementation work unless the existing behavior fails; if it fails, halt and report the failure instead of editing production source.
- Do not add broad workspace tests, new fixtures, new helper modules, property tests, snapshots, or documentation.
- Do not rewrite or restructure existing tests except for the minimal local placement needed to add the new test.
- Do not commit, stage, or push changes.

## Verification Gates

The Executor MUST run and document the result of this command in their `EXECUTION_REPORT`:

- `cargo test -p rge-kernel-graph-foundation --test diff_correctness` -> expected exit 0

No additional verification gate is required for this dispatch unless the Executor chooses to run one for local confidence; any optional gate must be reported separately and must not expand the edit scope.

## Halt Conditions

The Executor MUST halt without editing outside the allowed file and request fresh guidance if any of the following occur:

- The added-edge record assertion cannot pass without changing production source.
- The required test command fails for reasons unrelated to the new test and the cause is not clearly within `kernel/graph-foundation/tests/diff_correctness.rs`.
- Implementing the requested coverage appears to require editing Cargo manifests, creating new files, changing public APIs, or modifying any handoff packet other than the Executor's own execution report.
- The working tree contains conflicting edits in `kernel/graph-foundation/tests/diff_correctness.rs` that make a minimal focused test addition unsafe to apply.

## Planner Notes

Existing `diff_correctness.rs` coverage already includes focused tests for changed-edge records and removed-edge record payload preservation. This dispatch should add the symmetric added-edge record coverage only, using the same local helper style (`n(...)`, `e(...)`) and the existing `Graph<String, u32>` pattern.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-81
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Execution Report

DISPATCH_ID: ISSUE-120
AUTHOR: Executor / Claude
TIMESTAMP: 2026-05-23_12-33-35+0300
RELATED_FILES:
- ai_handoffs/ISSUE-120_TASK_2026-05-23_12-25-14+0300.md — TASK packet consumed; no edits.
- crates/script-bench/BASELINE.md — read-only audit input; no edits.
- crates/script-bench/METHODOLOGY.md — read-only audit input; no edits.
- crates/script-bench/src/script_host.rs — read-only audit input; no edits.
- crates/script-bench/Cargo.toml — read-only audit input; no edits.
- plans/IMPLEMENTATION.md — read-only audit input; no edits.
- Status.md — read-only audit input; no edits.
- HANDOFF.md — read-only audit input; no edits.
- change.md — read-only audit input; no edits.
- ai_handoffs/ISSUE-120_EXEC_2026-05-23_12-33-35+0300.md — this packet (added).
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-120_TASK_2026-05-23_12-25-14+0300.md`

## Pre-Execution Review Consumed

No pre-execution review issued for this dispatch.

## What I Changed

### Source

- None. Read-only audit per TASK `MAY edit: None`.

### Tests

- None. Audit-only dispatch; no test changes permitted.

### Docs

- None. The single allowed artifact is this EXEC packet.

## Per-File Summary

This dispatch made no repository edits other than adding this EXEC packet (and
its automation-generated sidecar at finalize time, if applicable). All eight
required inspection files were read in place and grepped; no file was modified.

## Verification Results

This dispatch is intentionally cargo-free per TASK §"Verification Gates". The
applicable read-only gates were run:

- `git status --short --untracked-files=no` (pre-audit) → empty (no tracked
  changes from this dispatch at start).
- File inventory (`ls` of the eight required paths) → all eight present:
  `crates/script-bench/BASELINE.md`, `crates/script-bench/METHODOLOGY.md`,
  `crates/script-bench/src/script_host.rs`, `crates/script-bench/Cargo.toml`,
  `plans/IMPLEMENTATION.md`, `Status.md`, `HANDOFF.md`, `change.md`.
- Read `crates/script-bench/BASELINE.md` (547 lines) → full inspection.
- Read `crates/script-bench/METHODOLOGY.md` (312 lines) → full inspection.
- Read `crates/script-bench/src/script_host.rs` (1102 lines) → full inspection.
- Read `crates/script-bench/Cargo.toml` (68 lines) → full inspection.
- Read `plans/IMPLEMENTATION.md` (758 lines) → full inspection.
- Read `Status.md` / `HANDOFF.md` / `change.md` → targeted, line-range and
  grep-driven inspection because each is too large to read whole-file with the
  Read tool (Status.md 446.7 KB / 381 lines; HANDOFF.md ~1.65 MB / 1676 lines;
  change.md ~430 KB / 1632 lines). Grep was used to locate every occurrence of
  the required search terms; matching line ranges were then extracted with
  `awk` for full-text inspection.
- Grep over the required files for `peak_rss`, `vss_delta`,
  `ProcessMemoryMetrics`, `process_memory`, `memory-stats`,
  `phase3_memory_soak`, `phase_3_memory_soak_one_hour`,
  `FORMAL_MEMORY_SOAK_DURATION`, `one-hour`, `1-hour`, `soak`, `deferr`,
  `harness`, `2026-05-16`, `2026-05-17` → completed.
- `git status --short --untracked-files=all` (post-audit) → only this
  dispatch's own untracked artifacts present:
  ```
  ?? ai_handoffs/ISSUE-120_EXEC_2026-05-23_12-33-35+0300.md
  ?? ai_handoffs/ISSUE-120_TASK_2026-05-23_12-25-14+0300.md
  ?? ai_handoffs/ISSUE-120_TASK_2026-05-23_12-25-14+0300.meta.json
  ```
  No tracked modifications. EXEC sidecar will be produced by the finalize step
  if invoked; that is automation output, not task content.
- No `cargo` / `cargo test` / `cargo bench` / `cargo fmt` / `cargo-deny` /
  `.ai/dispatch.verify.ps1` / one-hour soak invocation occurred. Audit-only
  scope respected.

### Command log (read-only audit only)

```
git status --short --untracked-files=no
ls crates/script-bench/BASELINE.md crates/script-bench/METHODOLOGY.md \
   crates/script-bench/src/script_host.rs crates/script-bench/Cargo.toml \
   plans/IMPLEMENTATION.md Status.md HANDOFF.md change.md
Read crates/script-bench/BASELINE.md
Read crates/script-bench/METHODOLOGY.md
Read crates/script-bench/src/script_host.rs
Read crates/script-bench/Cargo.toml
Read plans/IMPLEMENTATION.md
wc -l Status.md HANDOFF.md change.md
Grep "peak_rss|vss_delta|ProcessMemoryMetrics|process_memory|memory-stats|
      phase3_memory_soak|phase_3_memory_soak_one_hour|FORMAL_MEMORY_SOAK_DURATION|
      one-hour|1-hour|soak|deferr|harness"
  in Status.md, HANDOFF.md, change.md (line numbers only — many matched lines
  are oversized and cannot be displayed inline; targeted extraction follows)
Grep "peak_rss|vss_delta|ProcessMemoryMetrics|memory-stats|
      phase3_memory_soak_memory|phase_3_memory_soak_one_hour|FORMAL_MEMORY_SOAK"
  -o -n in Status.md / HANDOFF.md / change.md (precise occurrence list)
awk 'NR==<line>' <Status.md|HANDOFF.md|change.md> | awk -v RS=";"|"." \
  '/peak_rss|vss_delta|soak-harness|memory soak|phase_3_memory_soak|harness/'
  on each matched line, to extract semicolon- or sentence-bounded fragments
  that exceed the Grep display width.
Grep "2026-05-16|memory-stats|ProcessMemoryMetrics"
  in change.md, Status.md, HANDOFF.md (all returned "No matches found").
Grep "2026-05-17|2026-05-16" in plans/IMPLEMENTATION.md (returned "No matches
  found").
new-handoff.ps1 -DispatchId ISSUE-120 -PacketType EXEC -Author Executor
  (scaffolded this EXEC packet at the path in TIMESTAMP above; no sidecar
  generated until -Finalize is run).
git rev-parse --short HEAD              # 4bd7ecc
git rev-parse --abbrev-ref HEAD         # ai-dispatch/ISSUE-120
git status --short --untracked-files=all # only this dispatch's own artifacts
```

## Evidence Table

Each row pins a material finding to a file path and line number that the
Executor inspected. All citations are against the working-tree state at commit
`4bd7ecc` (no edits introduced by this dispatch).

| # | Finding | File | Line(s) |
|---|---|---|---|
| 1 | `ProcessMemoryMetrics` struct (start/end/peak RSS, start/end/delta VSS, sample count) committed | `crates/script-bench/src/script_host.rs` | 227–247 |
| 2 | `MemorySoakReport::process_memory: Option<ProcessMemoryMetrics>` field committed | `crates/script-bench/src/script_host.rs` | 251–267 |
| 3 | `ScriptHostBench::memory_soak` folds per-cycle samples into `peak_rss` + end footprint, produces `process_memory` payload | `crates/script-bench/src/script_host.rs` | 583–651 |
| 4 | `sample_process_memory` wraps `memory_stats::memory_stats()` (Some on supported platforms, None elsewhere — honest unavailability) | `crates/script-bench/src/script_host.rs` | 757–772 |
| 5 | `memory_delta_bytes` signed end-minus-start delta, saturating into i64 | `crates/script-bench/src/script_host.rs` | 774–785 |
| 6 | `phase_3_memory_soak_one_hour` test prints `phase3_memory_soak_memory: …` line under `--nocapture` with the eight ProcessMemoryMetrics fields | `crates/script-bench/src/script_host.rs` | 1077–1092 |
| 7 | Bounded `memory_soak_reports_process_memory_metrics` test exercises the same metrics path with a sub-second floor, asserts numeric metrics on Windows | `crates/script-bench/src/script_host.rs` | 975–1041 |
| 8 | `memory-stats = "1"` dependency added with rationale comment (2026-05-16) | `crates/script-bench/Cargo.toml` | 20–31 |
| 9 | Committed formal one-hour metrics-enabled RUN 2026-05-17 — invocation, toolchain, exit code, exact `--nocapture` stdout, full evidence table including `peak_rss_bytes=10510336`, `vss_delta_bytes=1368064`, `cycles=3736934`, `elapsed_s=3600.00`, `samples=3736936`, `final_counter_sum=1000499500`, `restored_components=3736934000` | `crates/script-bench/BASELINE.md` | 140–211 |
| 10 | Platform mapping documented honestly (Windows working set + commit charge; Linux RSS + VSZ; other platforms None) | `crates/script-bench/BASELINE.md` | 183–192 |
| 11 | Prior 2026-05-12 RUN preserved as historical record; the metrics-enabled 2026-05-17 RUN explicitly complements it without rewriting history | `crates/script-bench/BASELINE.md` | 120–138, 200–204 |
| 12 | "Memory-soak process-memory metrics — harness revision 2026-05-16" section in BASELINE.md describes the harness-only capability addition and explicitly notes "This revision adds the harness capability only. It does not re-run the formal one-hour soak …" — that wording is now superseded by the 2026-05-17 RUN section above it; it is current-correct as historical context about the 2026-05-16 capability dispatch, NOT a present-tense gap claim | `crates/script-bench/BASELINE.md` | 405–442 |
| 13 | METHODOLOGY.md W5 section describes the 2026-05-16 process-memory sampling, `ProcessMemoryMetrics`, platform mapping, and `memory-stats` dependency | `crates/script-bench/METHODOLOGY.md` | 142–169 |
| 14 | METHODOLOGY.md Change log entry "Memory-soak process-memory metrics (2026-05-16)" | `crates/script-bench/METHODOLOGY.md` | 294–300 |
| 15 | **STALE (current-state)**: plans/IMPLEMENTATION.md Phase 3.4 exit criterion #3 CLOSED-marker dated 2026-05-12 includes parenthetical "(no explicit `peak_rss` / `vss_delta` capture in the harness — harness limitation flagged for future improvement)". The "harness limitation flagged for future improvement" framing was accurate as of 2026-05-12 but is now stale: harness has the metrics committed (rows 1–7), and a metrics-enabled formal one-hour RUN 2026-05-17 (row 9) is committed in BASELINE.md. The 2026-05-12 dated record itself is historical; the parenthetical reads as ongoing | `plans/IMPLEMENTATION.md` | 318 |
| 16 | **STALE (current-snapshot)**: Status.md current 2026-05-14 snapshot lists "Known v0 deferrals (NOT blockers; listed so absence is not mistaken for oversight): … `peak_rss` / `vss_delta` soak-harness improvement (per `crates/script-bench/BASELINE.md`)". This deferral does not match the committed harness + BASELINE.md state | `Status.md` | 7 (long line; semicolon-bounded fragment) |
| 17 | **STALE (current-snapshot)**: Status.md older Job-10 snapshot lists the same "pressure-driven future dispatch candidate" with identical wording. Preserved as historical text in the dated Job-10 closeout entry, but propagated into the head 2026-05-14 snapshot at row 16, which is the load-bearing live-state line | `Status.md` | 11 (long line; semicolon-bounded fragment) |
| 18 | **STALE (current-snapshot)**: HANDOFF.md current snapshot mirrors Status.md's "Known v0 deferrals" line, including `peak_rss` / `vss_delta` soak-harness improvement | `HANDOFF.md` | 7 (long line; semicolon-bounded fragment) |
| 19 | **STALE (current-snapshot)**: HANDOFF.md secondary snapshot mirrors Status.md's Job-10 pressure-driven candidate line | `HANDOFF.md` | 11 (long line; semicolon-bounded fragment) |
| 20 | **HISTORICAL (preserve)**: change.md 2026-05-12 entry for the soak gate closure records "Does NOT certify: explicit memory-growth metrics (no `peak_rss` / `vss_delta` …" — accurate description of that 2026-05-12 RUN; preserve as written | `change.md` | 1443, 1523, 1527, 1574 |
| 21 | **HISTORICAL (preserve)**: change.md 2026-05-14 Job-10 closeout entry lists `peak_rss` / `vss_delta` soak-harness improvement as a "pressure-driven future dispatch candidate". Accurate at that date; preserve as dated history | `change.md` | 1616 |
| 22 | **HISTORICAL (preserve)**: change.md 2026-05-14 v0 release certification entry lists `peak_rss` / `vss_delta` soak-harness improvement under "Known v0 deferrals (NOT blockers)". Accurate at that date; preserve as dated history | `change.md` | 1620 |
| 23 | No entry exists in Status.md / HANDOFF.md / change.md / plans/IMPLEMENTATION.md mentioning `2026-05-16` (harness revision date) or `2026-05-17` (metrics-enabled formal RUN date). Confirms the staleness in rows 15–19: those four docs have not been refreshed since the 2026-05-16 capability dispatch and the 2026-05-17 formal RUN landed in BASELINE.md | `Status.md`, `HANDOFF.md`, `change.md`, `plans/IMPLEMENTATION.md` | grep returned "No matches found" in all four |

## 5-Question Memory-Soak Reconciliation Answer Block

### Q1. Metrics Support

**Yes — `script-bench` harness code committed today supports
`peak_rss_bytes` / `vss_delta_bytes` process-memory metrics for the memory
soak.**

- `ProcessMemoryMetrics` carries `start_rss_bytes` / `end_rss_bytes` /
  `peak_rss_bytes` / `start_vss_bytes` / `end_vss_bytes` / `vss_delta_bytes`
  / `samples` at `crates/script-bench/src/script_host.rs:227–247`.
- `MemorySoakReport::process_memory: Option<ProcessMemoryMetrics>` is wired
  into the soak report at `crates/script-bench/src/script_host.rs:251–267`.
- `ScriptHostBench::memory_soak` calls `sample_process_memory()` at soak
  start, after each completed hot-reload cycle, and at soak end, folding the
  observed samples into `peak_rss` / the end footprint and producing the
  `Option<ProcessMemoryMetrics>` payload (`crates/script-bench/src/script_host.rs:583–651`).
- `sample_process_memory()` returns `Some((resident, virtual))` from
  `memory_stats::memory_stats()` on supported platforms and `None` elsewhere
  — honest unavailability, never a fabricated zero
  (`crates/script-bench/src/script_host.rs:757–772`).
- `phase_3_memory_soak_one_hour` prints a `phase3_memory_soak_memory:
  samples=… start_rss_bytes=… end_rss_bytes=… peak_rss_bytes=… start_vss_bytes=…
  end_vss_bytes=… vss_delta_bytes=…` line under `--nocapture`
  (`crates/script-bench/src/script_host.rs:1077–1092`).
- A bounded non-`#[ignore]`'d
  `memory_soak_reports_process_memory_metrics` test pins the wiring with a
  tiny scene and a sub-second floor; on Windows it asserts the metrics are
  populated (`crates/script-bench/src/script_host.rs:975–1041`).
- The required `memory-stats` dependency is committed at
  `crates/script-bench/Cargo.toml:20–31` (added 2026-05-16 with rationale
  about the workspace `unsafe_code = "forbid"` lint forcing the FFI into
  the dep crate).

This is committed harness support and bounded (sub-second) metric-path test
coverage — distinct from formal one-hour result evidence, which Q2 covers
separately.

### Q2. Formal One-Hour Evidence

**Yes — a committed formal one-hour `peak_rss_bytes` / `vss_delta_bytes`
result is in the repository.**

- **Recorded date**: 2026-05-17 (release-profile background test run on the
  Windows 11 / x86_64 recorder host; toolchain `cargo 1.92.0` / `rustc
  1.92.0` / `wasmtime 44.0.1`).
- **Invocation**:
  ```
  cargo test -p rge-script-bench --release --lib \
    script_host::tests::phase_3_memory_soak_one_hour \
    -- --ignored --nocapture
  ```
- **Exit code**: 0.
- **Cargo wall-clock**: 3600.46 s; test harness summary `ok. 1 passed; 0
  failed; 0 ignored; 0 measured; 28 filtered out; finished in 3600.00s`.
- **Cycle count (`cycles`)**: 3,736,934.
- **Sample count (`samples`)**: 3,736,936 (= start + one per completed cycle
  + end — matches the harness sampling cadence).
- **Elapsed seconds (`elapsed_s`)**: 3600.00.
- **`peak_rss_bytes`**: **10,510,336**.
- **`vss_delta_bytes`**: **1,368,064** (positive — commit charge grew on
  Windows; recorded verbatim and signed).
- **Companion endpoints**: `start_rss_bytes=8,859,648`,
  `end_rss_bytes=10,346,496`, `start_vss_bytes=1,327,104`,
  `end_vss_bytes=2,695,168`, `final_counter_sum=1,000,499,500`,
  `restored_components=3,736,934,000`
  (`restored_components == cycles * entity_count` invariant HELD).
- **Source of evidence**: `crates/script-bench/BASELINE.md:140–211` —
  "Formal 1-hour memory soak — RUN 2026-05-17 (process-memory metrics
  enabled)" section, including the exact `--nocapture` stdout block, the
  measurement table, the Windows working-set/commit-charge platform mapping,
  and a load-bearing scope-limitation note (CONSTRAINED-CERTIFIED on the
  recorder host only).

**Distinguishing harness-support from formal-result evidence**: Q1 establishes
that the harness can capture the metrics. Q2 establishes that a formal
one-hour recorder-host RUN with those metrics captured exists, dated
2026-05-17, with all eight ProcessMemoryMetrics fields recorded verbatim from
stdout. The 2026-05-12 RUN at `BASELINE.md:120–138` predates the metrics and
remains the pre-metrics historical record, unchanged.

### Q3. Deferral Text Status

Per-file classification of every relevant deferral / capability statement
across the six required docs. The audit distinguishes
**CURRENT-AND-CORRECT** (matches committed evidence), **STALE-CURRENT-STATE**
(present-tense wording the committed evidence contradicts), and
**HISTORICAL-ONLY** (dated record that was accurate when written; preserve
forward).

- `crates/script-bench/BASELINE.md`:
  - **140–211** (RUN 2026-05-17 section): **CURRENT-AND-CORRECT** — records
    the formal one-hour metrics-enabled run with exact stdout evidence and
    explicit "complements the 2026-05-12 row" framing that preserves history.
  - **120–138** (RUN 2026-05-12 section): **HISTORICAL-ONLY** — accurate
    description of the pre-metrics run. Preserve as-is.
  - **396–402** ("It does NOT certify … `peak_rss` / `vss_delta` for this
    2026-05-12 run …"): **HISTORICAL-ONLY** scoped to the 2026-05-12 RUN by
    its own wording ("for this 2026-05-12 run") and explicitly notes "The
    harness now does capture them — see 'Memory-soak process-memory metrics'
    below". Preserve as-is.
  - **405–442** ("Memory-soak process-memory metrics — harness revision
    2026-05-16"): **CURRENT-AND-CORRECT** as historical context for the
    2026-05-16 capability dispatch; its closing paragraph ("This revision
    adds the harness capability only. It does not re-run the formal one-hour
    soak and publishes no new one-hour memory baseline row") was accurate
    when written; the very next section in BASELINE.md (140–211) is the
    later 2026-05-17 RUN that supersedes the "no new one-hour memory
    baseline row" gap. No edit is required because the section's wording is
    explicitly scoped to "this revision" and the superseding RUN sits next
    to it in the file.

- `crates/script-bench/METHODOLOGY.md`:
  - **142–169** (W5 methodology description): **CURRENT-AND-CORRECT** —
    describes `ProcessMemoryMetrics`, `peak_rss_bytes`, `vss_delta_bytes`,
    platform mapping, and the `memory-stats` crate.
  - **294–300** (Change log "Memory-soak process-memory metrics 2026-05-16"):
    **HISTORICAL-ONLY / CURRENT-AND-CORRECT** dated change-log entry.

- `plans/IMPLEMENTATION.md`:
  - **318** (Phase 3.4 exit criterion #3 CLOSED-marker, dated 2026-05-12):
    **STALE-CURRENT-STATE in part**. The dated CLOSED-on-2026-05-12 framing
    is historical and accurate. The parenthetical
    "(no explicit `peak_rss` / `vss_delta` capture in the harness — harness
    limitation flagged for future improvement)" reads as a present-tense
    capability claim ("in the harness") and is now contradicted by committed
    code (`script_host.rs:227–247`, `script_host.rs:583–651`,
    `Cargo.toml:20–31`) plus the committed 2026-05-17 RUN evidence
    (`BASELINE.md:140–211`). This is the load-bearing stale present-state
    statement in IMPLEMENTATION.md. The bullet's other CLOSED markers
    (rows 316–317, 319) do not need adjustment.

- `Status.md`:
  - **7** (current 2026-05-14 head snapshot — "Known v0 deferrals (NOT
    blockers; listed so absence is not mistaken for oversight): … `peak_rss`
    / `vss_delta` soak-harness improvement (per `crates/script-bench/BASELINE.md`)"):
    **STALE-CURRENT-STATE**. Status.md's head snapshot is the document's
    live-state surface; the deferral is contradicted by committed harness
    code + the 2026-05-17 RUN row.
  - **11** (older Job-10 snapshot block in the same document — same wording,
    "pressure-driven future dispatch candidates"): **STALE-CURRENT-STATE
    when read as the live status block**, but the wording is verbatim
    duplicated into earlier snapshot framing. Treat the head copy at row 7
    as the load-bearing reconciliation target; the row-11 copy may stay
    inside its Job-10 dated context as historical, but if it is still
    treated as a live-status surface, it is equally stale and should be
    reconciled the same way.

- `HANDOFF.md`:
  - **7** (current snapshot): **STALE-CURRENT-STATE** — same `peak_rss` /
    `vss_delta` soak-harness deferral as Status.md row 7.
  - **11** (secondary snapshot): same treatment as Status.md row 11.

- `change.md`:
  - **1443, 1523, 1527, 1574**: **HISTORICAL-ONLY** — dated 2026-05-12
    entries describing the 2026-05-12 soak-gate-closure dispatch and its
    "Does NOT certify: explicit memory-growth metrics" wording. Accurate at
    that date; preserve as written.
  - **1616**: **HISTORICAL-ONLY** — dated 2026-05-14 Job-10 closeout entry
    listing `peak_rss` / `vss_delta` soak-harness improvement as a
    "pressure-driven future dispatch candidate". Accurate at that date;
    preserve.
  - **1620**: **HISTORICAL-ONLY** — dated 2026-05-14 v0 release
    certification entry listing the same under "Known v0 deferrals (NOT
    blockers)". Accurate at that date; preserve.

**Cross-cutting confirmation**: a grep for `2026-05-16` / `2026-05-17` /
`memory-stats` / `ProcessMemoryMetrics` in Status.md / HANDOFF.md / change.md
and `2026-05-17` / `2026-05-16` in `plans/IMPLEMENTATION.md` returned **no
matches** in any of the four — corroborating that Status / HANDOFF /
IMPLEMENTATION current-state surfaces have not been refreshed since the
2026-05-16 harness revision and the 2026-05-17 formal RUN landed in BASELINE.

### Q4. Remaining Source/Harness Need

**No. No source or harness change is required before docs can be
reconciled.** The committed evidence already satisfies what the stale
present-state wording calls "future improvement":

- The harness has `ProcessMemoryMetrics`, samples at start / per-cycle /
  end, exposes the metrics through `MemorySoakReport::process_memory`, and
  prints them under `--nocapture` (rows 1–7 of the evidence table).
- The `memory-stats` Cargo dependency is committed (Cargo.toml:20–31).
- A bounded non-`#[ignore]`'d test exercises the metrics path on every
  workspace test invocation (script_host.rs:975–1041).
- A formal one-hour metrics-enabled RUN on the recorder host (2026-05-17)
  is committed with full stdout evidence (BASELINE.md:140–211).

The only outstanding work the audit can identify is **documentation
reconciliation** in `plans/IMPLEMENTATION.md`, `Status.md`, and `HANDOFF.md`
to remove the now-stale present-tense "soak-harness improvement" framing and
to point Phase 3.4 exit criterion #3 at the 2026-05-17 RUN. No `script-bench`
source, test, fixture, Cargo, or workflow change is justified by the audit
evidence.

### Q5. Smallest Follow-Up

**Exactly one smallest follow-up: a single docs-only reconciliation dispatch
that removes the stale present-tense `peak_rss` / `vss_delta` soak-harness
deferral wording from the three current-state surfaces and points Phase 3.4
exit criterion #3 at the 2026-05-17 RUN, while preserving every dated
historical record in change.md unchanged.**

Concrete scope of that single follow-up:

1. `plans/IMPLEMENTATION.md:318` — keep the 2026-05-12 CLOSED record intact;
   remove the parenthetical "(no explicit `peak_rss` / `vss_delta` capture
   in the harness — harness limitation flagged for future improvement)" and
   add a reference to the 2026-05-17 metrics-enabled re-run in BASELINE.md
   (e.g. "Re-validated 2026-05-17 on recorder host with process-memory
   metrics captured — see `crates/script-bench/BASELINE.md` 'RUN 2026-05-17'
   section").
2. `Status.md:7` (current head snapshot) and the parallel line in
   `HANDOFF.md:7` — remove the `peak_rss` / `vss_delta` soak-harness
   improvement bullet from the "Known v0 deferrals" list and, if needed, add
   a short note that the 2026-05-17 RUN closed that pressure-driven
   candidate. Treat `Status.md:11` / `HANDOFF.md:11` the same way only if
   those copies are still read as live-status surfaces; otherwise leave them
   inside their dated Job-10 context as historical.
3. `change.md` — append a new dated entry (e.g. `2026-05-23 docs-only
   reconciliation per ISSUE-120 audit …`) but DO NOT modify any existing
   2026-05-12 / 2026-05-14 entry; the historical "Does NOT certify" /
   "pressure-driven future dispatch candidate" / "Known v0 deferrals"
   wording must be preserved as dated history.
4. `crates/script-bench/BASELINE.md` and `METHODOLOGY.md` — no edits
   required; both already reflect the 2026-05-16 harness capability and the
   2026-05-17 RUN evidence accurately.
5. No `crates/script-bench` source / test / bench / fixture / Cargo edits;
   no architectural-lint / workflow / schema changes; no fresh one-hour
   soak; no `.ai/dispatch.verify.ps1` invocation.

**Why this is the smallest follow-up**:

- **No source/harness edit is justified** by the audit evidence (Q4) —
  source already implements what the stale text calls "future improvement".
- **No fresh one-hour soak is justified** — `BASELINE.md:140–211` already
  carries a committed metrics-enabled formal RUN on the recorder host with
  the full set of `peak_rss_bytes` / `vss_delta_bytes` numbers and a
  load-bearing scope-limitation note.
- **The reconciliation target is exactly three present-state surfaces** —
  one line in `plans/IMPLEMENTATION.md`, one snapshot line in `Status.md`,
  and the parallel line in `HANDOFF.md`. Every other relevant mention is
  either correct (BASELINE.md, METHODOLOGY.md) or dated historical text in
  `change.md` that must be preserved.
- **Source and docs reconciliation are not combined** — per TASK
  Constraints, this dispatch must not recommend combining source/harness
  changes with documentation reconciliation in a single follow-up. Since
  Q4 establishes no source/harness change is needed, the recommendation is
  purely docs-only and the constraint is satisfied without a halt.
- **No fresh recorder-host one-hour soak is required to justify the doc
  edits** — the 2026-05-17 RUN is the load-bearing evidence row. Therefore
  this packet does NOT halt with `STATUS: NEEDS_HUMAN`; it routes to
  `REVIEWER_AI` with `HANDOFF_STATUS: COMPLETE` per the success path.

This follow-up itself MUST NOT be created as a TASK packet by this dispatch;
naming it is the entire Q5 deliverable per TASK §"Constraints / Non-Goals"
("Q5 must name exactly one smallest follow-up. It must not create a follow-up
TASK packet, GitHub issue comment, PR comment, code edit, or documentation
edit.").

## Deviations from Task Packet

None — execution stayed strictly within the Task Packet scope. No file was
edited except the addition of this EXEC packet; no Cargo / lint / verify /
soak command was run; no follow-up TASK / REVIEW / CORRECT / CLOSEOUT packet
was authored; no GitHub issue or PR was modified.

A practical note on the inspection technique used for the three large
status files: `Status.md` / `HANDOFF.md` / `change.md` could not be read
whole-file with the Read tool (file size + token limits) and contain many
extremely long lines that the Grep `content` mode could not display in full.
Inspection was completed by combining `Grep -o -n` (precise occurrence list
per file with line numbers) with targeted `awk 'NR==<line>'` extraction and
sentence/semicolon-bounded fragment filtering. This is a read-only inspection
technique that introduces no edits and produced enough context to classify
each occurrence as current, stale, or historical.

## Open Questions for Reviewer

- The Reviewer should confirm whether `Status.md:11` and `HANDOFF.md:11`
  copies of the deferral wording are intended to be read as live-status
  surfaces (in which case they are stale and need the same reconciliation as
  row 7) or as embedded historical context within their dated Job-10 /
  earlier-snapshot blocks (in which case they should be preserved as written).
  The audit classified row 7 as the load-bearing live-state line and treated
  row 11 conservatively as potentially preservable historical text, with a
  note in Q3 that it is equally stale if it is also live.
- The Reviewer should confirm whether the BASELINE.md
  `Memory-soak process-memory metrics — harness revision 2026-05-16` section
  (lines 405–442) warrants a one-line "superseded by RUN 2026-05-17 above"
  cross-reference. The audit classified it as CURRENT-AND-CORRECT because
  its wording is scoped to "this revision" and the superseding RUN sits
  immediately above it in the same file, but a Reviewer may prefer to fold
  that cross-reference into the smallest follow-up's scope.

## Worktree State

- Tracked files: clean (no modifications introduced by this dispatch).
- Untracked items (this dispatch's own artifacts only):
  - `ai_handoffs/ISSUE-120_TASK_2026-05-23_12-25-14+0300.md`
  - `ai_handoffs/ISSUE-120_TASK_2026-05-23_12-25-14+0300.meta.json`
  - `ai_handoffs/ISSUE-120_EXEC_2026-05-23_12-33-35+0300.md` (this packet)
  - An `ISSUE-120_EXEC_2026-05-23_12-33-35+0300.meta.json` sidecar will be
    produced if/when `new-handoff.ps1 -Finalize` is invoked; that is
    automation output, not task content.
- Branch: `ai-dispatch/ISSUE-120`
- Last commit: `4bd7ecc` chore(dispatch): queue task #18 memory-soak metrics reconciliation audit

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-120
AUTHOR: Executor / Claude
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---

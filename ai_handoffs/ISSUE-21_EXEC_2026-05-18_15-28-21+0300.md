# Execution Report

DISPATCH_ID: ISSUE-21
AUTHOR: Executor / Claude
TIMESTAMP: 2026-05-18_15-28-21+0300
RELATED_FILES:
- crates/cad-projection/tests/sweep_brep_face_id_lookup_smoke.rs — new integration test file (4 smoke tests)
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-21_TASK_2026-05-18_15-20-49+0300.md`

## Pre-Execution Review Consumed

No pre-execution review issued for this dispatch.

## What I Changed

### Source

- None. No production code, provider code, Sweep geometry, renderer/gfx,
  kernel code, docs, scripts, or manifests were touched.

### Tests

- `crates/cad-projection/tests/sweep_brep_face_id_lookup_smoke.rs` — new
  integration test file in the existing `rge-cad-projection` crate (no
  Cargo manifest change needed; Cargo auto-discovers `tests/*.rs`).
  Local helpers: `tol()`, `unit_square()` (`n = 4` CCW profile),
  `z_path(&[f32])` (monotonic-Z `Polyline3D` on the `+Z` axis), and
  `build_sweep_projection(profile, path) -> (CadGraph, CadProjection,
  World, EntityId)` which commits an `OperatorNode::Sweep`, spawns the
  B-Rep entity, and sets `BRepHandle.brep_owner = Some(TEST_OWNER)`.
  Four tests:
  - `sweep_projection_query_returns_brep_face_id_for_each_triangle`:
    pins the projected mesh to exactly 20 triangles
    (`2 * 4 * 2 + 2 * (4 - 2)`) for a unit square over `z_path(&[0.0,
    1.0, 2.0])`; the direct `SweepOp::brep_face_ids(TEST_OWNER)` call
    yields exactly 10 face IDs (`2 + 4 * (3 - 1)`); every triangle
    `0..20` resolves through `CadProjection::brep_face_id_for_triangle`
    to one of those 10 IDs.
  - `sweep_projection_query_canonical_face_order_for_square`: asserts
    the explicit canonical order — triangles `0..2` → direct face index
    `0` (`FirstCap`); `2..4` → index `1` (`LastCap`); for each
    `segment_index in 0..2` and `edge_index in 0..4`, triangles
    `4 + 2 * (segment_index * 4 + edge_index)` and `+ 1` → direct face
    index `2 + segment_index * 4 + edge_index`.
  - `sweep_query_returns_none_for_out_of_bounds_triangle`: triangle
    index `99` resolves to `None`.
  - `sweep_query_returns_none_when_brep_owner_is_none`: after setting
    `BRepHandle.brep_owner` to `None`, a valid triangle (`0`) lookup
    resolves to `None`.

### Docs

- None.

## Per-File Summary

Single new file. Modeled on `extrude_brep_face_id_lookup_smoke.rs` and the
`revolve` / `loft` peers; uses only the public APIs named in the Task
Packet (`CadGraph`, `OperatorNode::Sweep`, `Polygon2D`, `Polyline3D`,
`SweepOp`, `BRepProvider::brep_face_ids`, `BRepHandle`, `CadProjection`,
`World`). No new dependencies, fixtures, randomized inputs, snapshots,
feature flags, or helper modules.

## Verification Results

- `cargo test -p rge-cad-projection --test sweep_brep_face_id_lookup_smoke`
  → exit 0 — 4 passed / 0 failed / 0 ignored.

Observed contract matched the Task Packet exactly: 20 projected
triangles, 10 direct face IDs, canonical cap-first / side
segment-major / profile-edge-major order — no halt condition triggered.

Note on environment: `cargo` is not on `PATH` in this shell. The Rust
toolchain lives at `A:\RustCache\cargo` (`CARGO_HOME`) /
`A:\RustCache\rustup` (`RUSTUP_HOME`); the verification command was run
with those env vars set and `A:\RustCache\cargo\bin` prepended to `PATH`.
`cargo 1.92.0` / `rustc 1.92.0`.

## Deviations from Task Packet

None — execution stayed strictly within the Task Packet scope. Only the
single permitted new file was added; no MUST-NOT-edit path was touched.

## Open Questions for Reviewer

None.

## Worktree State

- Tracked files: clean (no tracked file modified).
- Untracked items:
  - `crates/cad-projection/tests/sweep_brep_face_id_lookup_smoke.rs` (the deliverable)
  - `ai_handoffs/ISSUE-21_TASK_2026-05-18_15-20-49+0300.md`
  - `ai_handoffs/ISSUE-21_TASK_2026-05-18_15-20-49+0300.meta.json`
  - `ai_handoffs/ISSUE-21_EXEC_2026-05-18_15-28-21+0300.md` (this packet)
- Branch: ai-dispatch/ISSUE-21
- Last commit: `10f4f6b` fix(cad): preserve TransformOp face labels

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-21
AUTHOR: Executor / Claude
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---

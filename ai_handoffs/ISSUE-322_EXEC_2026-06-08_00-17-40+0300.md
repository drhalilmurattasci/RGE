# Execution Report

DISPATCH_ID: ISSUE-322
AUTHOR: Executor / Claude
TIMESTAMP: 2026-06-08_00-17-40+0300
RELATED_FILES:
- plans/BASELINE.md — §13.3 provenance reworded (attribution path vs. four direct dependents); Pulley remediation candidate B re-scoped to all four; parsing-command record added to notes
- Status.md — top ISSUE-322 snapshot provenance reworded to name all four direct `wasmtime` dependents
- HANDOFF.md — top ISSUE-322 snapshot provenance reworded; remediation options B/C re-scoped to all four direct dependents
- change.md — corrected the in-place ISSUE-322 provenance phrasing and appended a dated correction-round entry recording both fixes and the parsing command
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-322_TASK_2026-06-07_23-34-23+0300.md`
Correction Packet: `ai_handoffs/ISSUE-322_CORRECT_2026-06-08_00-11-00+0300.md` (round 0, verdict `needs_changes`).

## Pre-Execution Review Consumed

No pre-execution review issued for this dispatch. This is a CORRECTION round executed against the Planner-signed correction packet `ISSUE-322_CORRECT_2026-06-08_00-11-00+0300.md`, which enumerated exactly two approved fixes (false `wasmtime` provenance claim; undocumented local parsing command). Executor concurs with both findings — the correction is documentation/audit-only and was applied within the unchanged Task Packet scope.

## Current-State Claims: Falsification Re-run

- Claim: `wasmtime` is declared as a *direct* dependency by four workspace crates, not two.
  Re-run: `Select-String -Path crates/*/Cargo.toml -Pattern wasmtime` (filtered to manifest dep lines) -> **exit 0**; four direct declarations confirmed: `crates/expr-wasm/Cargo.toml:16` (`features=["parallel-compilation"]`), `crates/runtime-wasmtime-engine/Cargo.toml:33` (`optional=true`, behind default-on `engine_wasmtime`), `crates/script-host/Cargo.toml:20` (plain `workspace=true`), `crates/script-bench/Cargo.toml:39` (`features=["winch"]`). Package names confirmed via `Select-String ... -Pattern '^\s*name\s*='`: `rge-expr-wasm`, `rge-runtime-wasmtime-engine`, `rge-script-host`, `rge-script-bench`.
- Claim: the previous "exactly two workspace crates" wording was only the critical-path attribution path (`cargo tree -i wasmtime`), not the complete direct-dependency set.
  Re-run: the prior EXEC (`ISSUE-322_EXEC_2026-06-07_23-50-34+0300.md:62-63`) records `cargo tree -i wasmtime -e normal` reaching `wasmtime` 44.0.1 via `rge-expr-wasm` + `rge-runtime-wasmtime-engine` (the units on the critical path), with `rge-script-host`/`rge-script-bench` noted only "transitively" — confirming the two-crate figure was the attribution path, while the manifest scan above is the full direct-dependent set.
- Claim: `.ai/dispatch-ISSUE-322/unit-data.json` is the `UNIT_DATA` array extracted from the preserved `cargo-timing-clean-release.html`, and the extraction reproduces it exactly.
  Re-run: the equivalent local extraction (command in Verification Results below) -> **exit 0**; regenerated 685 units vs. 685 in the existing `unit-data.json` (match=True), with `cranelift-codegen` 0.131.1 = 125.64 s preserved.

## What I Changed

### Source

- None. No Rust source, tests, benches, fixtures, Cargo manifests/lock, tooling, workflows, schemas, scheduler config, or dispatch automation were edited or added. This correction is documentation/audit-only, as required by the correction packet ("Do not change dependency graphs. Do not touch source ...").

### Tests

- None (no source change to test; documentation/audit repair).

### Docs

- `plans/BASELINE.md`: §13.3 "Provenance of the long pole" reworded to distinguish the `cargo tree -i wasmtime` critical-path **attribution path** (`rge-expr-wasm` + `rge-runtime-wasmtime-engine`) from the **four** workspace crates that declare `wasmtime` directly (adds `rge-script-host` `crates/script-host/Cargo.toml:20` and `rge-script-bench` `crates/script-bench/Cargo.toml:39`), and states that scope/Pulley/feature-gate remediation must cover all four. Remediation candidate **B (Pulley interpreter)** re-scoped from two crates to all four direct dependents, with an explicit note that scoping to only the first two leaves `script-host`/`script-bench` pulling cranelift/winch (and that `script-bench` pins `winch`). Candidate **C** already listed all four. Added a parsing-command provenance note to the §13.3 "Notes / caveats" so the per-unit table no longer relies on `unit-data.json` without an auditable command record.
- `Status.md`: top ISSUE-322 snapshot provenance reworded — names all four direct dependents and states remediation must cover all four, not just the attribution-path two.
- `HANDOFF.md`: top ISSUE-322 snapshot provenance reworded the same way; remediation options B and C re-scoped to all four direct dependents.
- `change.md`: corrected the in-place ISSUE-322 entry's provenance phrasing (attribution path vs. four direct dependents) and appended a dated `2026-06-08 (... correction round 0)` entry recording both approved fixes, the manifest evidence, the parsing-command record, explicit non-changes, and the re-verification gate results.

## Per-File Summary

The entire tracked diff is documentation/task-record. No dependency graph, manifest, or source was touched — the four direct-dependent facts come from read-only `Select-String` scans of the existing `crates/*/Cargo.toml` manifests, and the `unit-data.json` corroboration is a read-only extraction from the preserved (gitignored) `cargo-timing-clean-release.html`. The regenerated `.ai/dispatch-ISSUE-322/unit-data.regen.json` is a gitignored run-dir artifact and does not enter the tracked diff.

## Verification Results

Re-verification gates from the correction packet (§Re-Verification Gates):

- `Select-String -Path crates/*/Cargo.toml -Pattern wasmtime` → **exit 0**; shows direct `wasmtime` dependencies in `crates/expr-wasm` (:16), `crates/runtime-wasmtime-engine` (:33), `crates/script-host` (:20), and `crates/script-bench` (:39), as required.
- `Select-String -Path plans/BASELINE.md,Status.md,HANDOFF.md,change.md -Pattern 'exactly two workspace crates'` → **no matches** (PowerShell returned no match objects; recorded explicitly as "no matches").
- Local parsing/extraction command for `.ai/dispatch-ISSUE-322/unit-data.json` → **exit 0**. The original ad-hoc extraction command was not captured at measurement time (stated explicitly per the correction packet's fallback clause); an equivalent extraction was re-run against the preserved input artifact `.ai/dispatch-ISSUE-322/cargo-timing-clean-release.html` (the `const UNIT_DATA = [ … ];` array, lines 6414–16140):
  ```powershell
  $html = Get-Content '.ai/dispatch-ISSUE-322/cargo-timing-clean-release.html' -Raw
  $startTok = 'const UNIT_DATA = ['
  $si = $html.IndexOf($startTok); $arrStart = $si + $startTok.Length - 1   # position of '['
  $ei = $html.IndexOf("`n];", $arrStart)                                   # closing '];' line
  $arrText = $html.Substring($arrStart, ($ei - $arrStart) + 2)             # include closing ']'
  $units = $arrText | ConvertFrom-Json
  $units | ConvertTo-Json -Depth 6 | Set-Content '.ai/dispatch-ISSUE-322/unit-data.regen.json' -Encoding UTF8
  ```
  - Input artifact: `.ai/dispatch-ISSUE-322/cargo-timing-clean-release.html`
  - Output artifact: `.ai/dispatch-ISSUE-322/unit-data.regen.json`
  - Result: **exit 0** (pure-PowerShell pipeline; `$LASTEXITCODE` empty because no native process ran, `$?` = True). Corroboration: regenerated **685** units vs. **685** in the existing `unit-data.json` (match=True); `cranelift-codegen` 0.131.1 top unit duration = **125.64 s**, matching the §13.3 attribution.
- `git diff --check` → **exit 0**.
- `git diff --name-only` → exactly `.ai/dispatch.tasks.md`, `HANDOFF.md`, `Status.md`, `change.md`, `plans/BASELINE.md` (confined to the MAY-edit docs surface; `.ai/dispatch.tasks.md` carries only the prior round's task-86 DONE marker — not re-touched this round).
- Orchestrator canonical verify gate: not re-run in this Executor turn; no source/Cargo/test target changed, so the docs-only diff cannot affect it. If the orchestrator runs it, report its result; no out-of-scope fix was attempted.
- The full workspace test/bench gate was intentionally NOT added (per Task/Correction packets — documentation/audit repair, not a source change).

## Deviations from Task Packet

None — execution stayed strictly within the correction packet's two approved fixes and the unchanged Task Packet scope. Per the correction packet's fallback clause, the original exact `unit-data.json` parsing command could not be recovered from shell history/run logs, so this is stated explicitly and an equivalent extraction was re-run against the preserved HTML artifact and recorded above. No dependency graph, manifest, or source was edited; no `cargo clean`; `A:\RustCache\target` was not touched.

## Open Questions for Reviewer

- `runtime-wasmtime-engine`'s `wasmtime` dependency is `optional = true` behind the `engine_wasmtime` feature, which is **default-on** (`crates/runtime-wasmtime-engine/Cargo.toml:43,46`). I therefore counted it among the four direct dependents (the default `cargo build --workspace --release` pulls it), and noted the default-on caveat in the §13.3 prose. Flagging in case you prefer it labeled "conditionally direct (default feature)".
- The `change.md` correction entry still contains the descriptive phrase "precisely two workspace crates" when narrating the now-corrected claim (immediately followed by "but a direct manifest scan shows four workspace crates …"). This is intentional — a change-log correction should record what was wrong — and it does **not** match the forbidden literal `exactly two workspace crates` (gate returns no matches). Flag if you want even the descriptive paraphrase removed.

## Worktree State

- Tracked files: modified-but-uncommitted — `.ai/dispatch.tasks.md`, `HANDOFF.md`, `Status.md`, `change.md`, `plans/BASELINE.md` (docs/task-record only; not committed, per protocol).
- Untracked items: this EXEC report (+ sidecar on finalize), the prior dispatch packets/claim under `ai_handoffs/` (`ISSUE-322_TASK_*`, `ISSUE-322_EXEC_2026-06-07_23-50-34+0300*`, `ISSUE-322_CORRECT_2026-06-08_00-11-00+0300*`, `claims/ISSUE-322_*_claim.json`), and the gitignored `.ai/dispatch-ISSUE-322/` artifacts including the new `unit-data.regen.json` corroboration output.
- Branch: `ai-dispatch/ISSUE-322`
- Last commit: `b97eeb7` ai-dispatch ISSUE-321: Measure Phase 9 incremental p95 build time

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-322
AUTHOR: Executor / Claude
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---

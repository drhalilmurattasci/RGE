# Task Packet

DISPATCH_ID: ISSUE-401
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-15_04-13-50+0300
RELATED_FILES:
- crates/editor-shell/src/lifecycle/mod.rs
- crates/editor-shell/src/lifecycle/tests.rs
- .ai/dispatch.tasks.md
- Status.md
- HANDOFF.md
- plans/BASELINE.md
- change.md
- ai_handoffs/ISSUE-401_TASK_2026-06-15_04-13-50+0300.md
STATUS: OPEN

## Goal

Implement task 147 from `.ai/dispatch.tasks.md`: reset stale viewport
left-double-click state when View/PageUp Zoom In or View/PageDown Zoom Out is
executed. `EditorShell::zoom_camera_in` and `EditorShell::zoom_camera_out`
must clear pending `ViewportLeftDoubleClick` state before their existing
`zoom_camera_by` calls, while preserving current zoom math, command routing,
valid double-click framing, reset-camera, wheel, focus-loss, cursor-left,
orbit, pan, cursor-grab, viewport hit-testing, and face-pick behavior. After
implementation and verification, perform the required self-re-arm by appending
exactly one bounded docs/source-read-only audit task 148 or recording
`NEEDS_HUMAN_RECORDED`.

## Scope

### MAY edit
- `crates/editor-shell/src/lifecycle/mod.rs`
- `crates/editor-shell/src/lifecycle/tests.rs`
- `.ai/dispatch.tasks.md`
- `Status.md`
- `HANDOFF.md`
- `plans/BASELINE.md`
- `change.md`
- `ai_handoffs/ISSUE-401_*`
- `ai_dispatch_logs/*ISSUE-401*`

### MUST NOT edit
- Any Rust source or test file outside `crates/editor-shell/src/lifecycle/mod.rs`
  and `crates/editor-shell/src/lifecycle/tests.rs`.
- Cargo manifests or `Cargo.lock`.
- Command routing, menu definitions, shortcut execution, accelerator mappings,
  plugin runtime/discovery/loading code, remapping/persistence/fatal-policy
  behavior, OS clipboard behavior, CAD/projection/CommandBus mutation,
  undo/dirty/save-load authority, viewport hit testing, or face-pick policy.
- Reset-camera behavior, mouse-wheel behavior, focus-loss behavior,
  cursor-left behavior, right-button orbit math, middle-button pan math,
  cursor-grab/release behavior, or camera zoom math beyond clearing pending
  double-click state before the existing `zoom_camera_by` calls.
- Workflows, dispatch automation, guard, queue, scheduler, watcher,
  verification, health/trend scripts, schemas, ADR files, architecture-lint
  rules/config, packet templates, or unrelated existing handoff/log artifacts.

### MAY add new files
- `ai_handoffs/ISSUE-401_EXEC_*`
- `ai_handoffs/ISSUE-401_CORRECT_*`
- `ai_handoffs/ISSUE-401_*.meta.json`
- `ai_dispatch_logs/*ISSUE-401*`

### MUST NOT add new files
- New Rust source or test modules.
- New crates, Cargo manifests, workflows, schemas, ADRs, architecture-lint
  rules/config, dispatch automation scripts, packet templates, or non-ISSUE-401
  handoff/log artifacts.
- Task 149 or any task-149 generated artifact during this dispatch. Task 148,
  if appended, must be the self-re-arming audit that later selects task 149.

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - crates/editor-shell/src/lifecycle/mod.rs
  - crates/editor-shell/src/lifecycle/tests.rs
  - .ai/dispatch.tasks.md
  - Status.md
  - HANDOFF.md
  - plans/BASELINE.md
  - change.md
  - ai_handoffs/ISSUE-401_*
  - ai_dispatch_logs/*ISSUE-401*
MUST_NOT_EDIT:
  - crates/editor-shell/src/lifecycle/accelerator.rs
  - crates/editor-shell/src/render_path.rs
  - crates/editor-ui/**
  - crates/editor-actions/**
  - crates/editor-state/**
  - crates/cad-projection/**
  - Cargo.toml
  - Cargo.lock
  - .github/workflows/**
  - Invoke-AiDispatch*.ps1
  - Register-AiDispatchSchedule.ps1
  - Get-AiDispatchHealth.ps1
  - .ai/dispatch.verify.ps1
  - ai_handoffs/templates/**
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Deliverables

- `EditorShell::zoom_camera_in` clears pending viewport left-double-click state
  immediately before the existing `self.zoom_camera_by(0.8)` call.
- `EditorShell::zoom_camera_out` clears pending viewport left-double-click
  state immediately before the existing `self.zoom_camera_by(1.25)` call.
- Focused lifecycle tests in `crates/editor-shell/src/lifecycle/tests.rs` cover
  stale scene-wide framing prevention for first viewport left press, View/PageUp
  Zoom In, and a second in-threshold viewport left press.
- Focused lifecycle tests cover stale selected-CAD framing prevention for first
  viewport left press, View/PageDown Zoom Out, selected CAD bounds, and a second
  in-threshold viewport left press.
- `.ai/dispatch.tasks.md` is updated as the final step with exactly one bounded
  next-task-source AUDIT task 148 titled
  `Post-zoom-command-double-click-reset Phase 9 next-task source audit`, or
  exactly one `NEEDS_HUMAN_RECORDED:` line using an ISO date and a
  source-grounded reason.
- `Status.md`, `HANDOFF.md`, `plans/BASELINE.md`, and `change.md` are updated
  only as needed to record the ISSUE-401/task-147 outcome and the self-re-arm
  result.

## Acceptance Criteria

- Zoom In and Zoom Out command helpers clear stale `ViewportLeftDoubleClick`
  state before applying existing zoom behavior.
- The numeric zoom factors, `zoom_camera_by` implementation, camera target/up,
  FOV, clipping-plane, and degenerate-vector fallback behavior are unchanged.
- The existing mouse-wheel helper keeps its current reset-before-classification
  behavior so no-op or horizontal wheel events still clear stale double-click
  state; a duplicate reset on vertical wheel zoom is acceptable because reset is
  idempotent.
- Tests prove a pre-zoom first viewport left press cannot pair with a later
  in-threshold left press to frame either the whole scene or selected CAD
  bounds after View/PageUp Zoom In or View/PageDown Zoom Out.
- Existing valid consecutive-left-click scene framing and selected-CAD framing
  remain covered and unchanged in intent.
- Existing reset-camera stale reset, mouse-wheel stale reset, focus-loss stale
  reset, cursor-left stale reset, right-button orbit, middle-button pan, and
  cursor-grab behavior remain unchanged in intent.
- No command enum, menu, shortcut, accelerator, render-path routing, CAD,
  projection, CommandBus, undo/dirty/save-load, viewport hit-test, or face-pick
  behavior is changed.
- The final self-re-arm step appends exactly one bounded task 148 audit with
  explicit `MAY edit`, `MUST NOT edit`, `Done criteria`, `Verification`, and
  `Halt conditions` sections, and task 148's done criteria require appending
  exactly one bounded FEATURE task 149 or recording `NEEDS_HUMAN_RECORDED`.
  If no bounded in-policy task exists, record exactly one
  `NEEDS_HUMAN_RECORDED:` line using an ISO date and a source-grounded reason
  instead. Do not append task 149 during this dispatch.

## Current-State Claims: Falsification Required

Every negative / "unchanged" assertion this packet makes about current source
MUST list its inverse-search command and result. A negative claim without a
falsifying search is not a valid premise (Protocol Rule 8).

- Claim: Current `EditorShell::zoom_camera_in` / `zoom_camera_out` do not clear
  `ViewportLeftDoubleClick` state before their zoom behavior, and there is no
  existing Zoom In/Out-specific reset coverage in the lifecycle/routing files.
  Falsifying read: `rg -n -C 18 "fn zoom_camera_for_viewport_mouse_wheel|pub fn zoom_camera_in|pub fn zoom_camera_out|self\\.zoom_camera_by\\(" crates/editor-shell/src/lifecycle/mod.rs`
  shows `zoom_camera_in` at `crates/editor-shell/src/lifecycle/mod.rs:2134`
  calling only `self.zoom_camera_by(0.8)`, and `zoom_camera_out` at
  `crates/editor-shell/src/lifecycle/mod.rs:2142` calling only
  `self.zoom_camera_by(1.25)`, before the separate mouse-wheel helper starts
  at `:2146`.
  Falsifying search for existing command/reset coverage:
  `rg -n "zoom_camera_(in|out).*(double_click|stale|reset_viewport_left_double_click)|(double_click|stale|reset_viewport_left_double_click).*zoom_camera_(in|out)|PageUp.*(double_click|stale|reset_viewport_left_double_click)|(double_click|stale|reset_viewport_left_double_click).*PageUp|PageDown.*(double_click|stale|reset_viewport_left_double_click)|(double_click|stale|reset_viewport_left_double_click).*PageDown|Zoom In.*(double_click|stale|reset_viewport_left_double_click)|(double_click|stale|reset_viewport_left_double_click).*Zoom In|Zoom Out.*(double_click|stale|reset_viewport_left_double_click)|(double_click|stale|reset_viewport_left_double_click).*Zoom Out" crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/tests.rs crates/editor-shell/src/render_path.rs crates/editor-shell/src/lifecycle/accelerator.rs`
  -> no matches, exit 1.
- Claim: Current mouse-wheel behavior resets stale viewport left-double-click
  state before wheel classification, and no-op/horizontal/non-viewport wheel
  inputs do not call a zoom helper from the `None` arm. Preserve that behavior;
  a duplicate reset after this task is acceptable only for vertical wheel
  events that then route through `zoom_camera_in` / `zoom_camera_out`.
  Falsifying read: `rg -n -C 6 "fn zoom_camera_for_viewport_mouse_wheel|self\\.reset_viewport_left_double_click\\(\\)|Some\\(ViewportMouseWheelZoom::In\\)|Some\\(ViewportMouseWheelZoom::Out\\)|None => \\{\\}" crates/editor-shell/src/lifecycle/mod.rs`
  shows `zoom_camera_for_viewport_mouse_wheel` at
  `crates/editor-shell/src/lifecycle/mod.rs:2146`, the reset before the
  `match` at `:2151`, vertical wheel routing to `zoom_camera_in` /
  `zoom_camera_out` at `:2153-2154`, and the empty `None => {}` arm at `:2155`.
  Coverage read: `rg -n "viewport_mouse_wheel_signs_map_line_and_pixel_vertical_delta|viewport_mouse_wheel_horizontal_only_delta_is_no_op|viewport_mouse_wheel_false_viewport_hit_test_is_no_op|viewport_mouse_wheel_no_cursor_or_no_host_is_no_op|viewport_mouse_wheel_resets_stale_left_double_click_before_scene_frame|viewport_mouse_wheel_resets_stale_left_double_click_before_selected_cad_frame|viewport_middle_button_pan_wheel_zoom_still_requires_viewport_hit" crates/editor-shell/src/lifecycle/tests.rs`
  shows existing wheel direction/no-op/hit-test/stale-reset coverage at
  `crates/editor-shell/src/lifecycle/tests.rs:1296`, `:1360`, `:1374`,
  `:1388`, `:1417`, `:1447`, and `:1706`.
- Positive source evidence: `rg -n "Command::ZoomIn|Command::ZoomOut|PageUp|PageDown|zoom_camera_in|zoom_camera_out" crates/editor-ui/src/menus/default_menu.rs crates/editor-shell/src/lifecycle/accelerator.rs crates/editor-shell/src/render_path.rs crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/tests.rs`
  shows View menu entries at `crates/editor-ui/src/menus/default_menu.rs:350`
  and `:356`, PageUp/PageDown accelerator mappings at
  `crates/editor-shell/src/lifecycle/accelerator.rs:247-248`, routing to the
  helpers at `crates/editor-shell/src/render_path.rs:472-473`, the helper
  bodies at `crates/editor-shell/src/lifecycle/mod.rs:2134-2142`, and the
  existing route test at `crates/editor-shell/src/lifecycle/tests.rs:5380`.
- Positive source evidence: `rg -n "reset_camera_resets_stale_viewport_left_double_click|viewport_mouse_wheel_resets_stale_left_double_click|viewport_focus_loss_resets_stale_left_double_click|viewport_cursor_left_resets_stale_left_double_click|viewport_left_double_click_second_viewport_press_frames_prebuilt_scene|viewport_left_double_click_selected_cad_entity_frames_selected_bounds|zoom_camera_in_and_out_preserve_target_and_direction|menu_zoom_commands_route_via_view" crates/editor-shell/src/lifecycle/tests.rs`
  shows existing focused tests for valid double-click scene framing, selected
  CAD framing, reset-camera stale reset, mouse-wheel stale reset, focus-loss
  stale reset, cursor-left stale reset, zoom invariants, and menu zoom routing.

## Constraints / Non-Goals

- Do not infer GitHub queue state with `gh`, browser, network, or live GitHub
  API access. For audit/task-selection evidence, use the dispatcher-provided
  GitHub snapshot embedded in the ISSUE-401 body/task prompt, or an exact local
  artifact path copied from it.
- Do not remove the existing `reset_viewport_left_double_click` call from the
  mouse-wheel helper; it is required for no-op/horizontal wheel reset behavior.
- Do not alter `zoom_camera_by`, zoom factors, camera math, route draining,
  command enums, menu definitions, accelerator mappings, render-path routing,
  CAD selection/projection, CommandBus, undo/dirty/save-load, viewport hit
  testing, or face-pick policy.
- Do not implement task 148 or append task 149 in this dispatch. Task 148 is
  only the next docs/source-read-only audit source.
- Do not broaden this into shortcut remapping, plugin execution, OS clipboard,
  CAD mutation, or unrelated editor-usability work.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- `cargo test -p rge-editor-shell --lib viewport_left_double_click` -> expected
  exit 0.
- `cargo test -p rge-editor-shell --lib menu_zoom_commands_route_via_view` ->
  expected exit 0.
- `cargo check -p rge-editor-shell --lib` -> expected exit 0.
- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `rg -n "^145\\.|^146\\.|^147\\.|^148\\.|^149\\." .ai/dispatch.tasks.md`
  before edits and after edits -> expected to show task 147 before, then task
  147 plus exactly one task 148 after, unless exactly one
  `NEEDS_HUMAN_RECORDED` line is recorded instead.
- `git diff --name-only` -> expected paths stay inside the MAY-edit surface
  plus generated ISSUE-401 artifacts.
- `git diff --check` -> expected exit 0.
- `.\new-handoff.ps1 -Finalize -PacketPath` on the generated ISSUE-401 EXEC
  packet with `-DryRun` -> expected exit 0.

## Halt Conditions

The Executor MUST halt (without committing) and request fresh guidance if any
of the following occur:

- Implementing the reset requires changing any file outside the MAY-edit list.
- `EditorShell::zoom_camera_in`, `EditorShell::zoom_camera_out`,
  `zoom_camera_by`, `reset_viewport_left_double_click`, menu command draining,
  or the lifecycle test helpers have moved enough that this is no longer a
  two-helper lifecycle/test change.
- Preserving no-op/horizontal mouse-wheel reset behavior would require changing
  wheel semantics or removing existing wheel reset coverage.
- Existing reset-camera, wheel, orbit, pan, focus-loss, cursor-left, valid
  consecutive-double-click, selected-CAD framing, viewport hit-testing, or
  face-pick behavior would need to change beyond clearing stale state in Zoom
  In/Out helpers.
- More than one behavioral follow-up is required to make Zoom In/Out stale
  double-click handling coherent.
- The self-re-arm audit task 148 cannot be specified without live GitHub
  access, crossing a policy/architecture boundary, or appending more than one
  next task. In that case, record `NEEDS_HUMAN_RECORDED` instead of forcing a
  task.

## Planner Notes

Task 146's audit identified this as the next smallest bounded editor-usability
follow-up. Resetting in the public zoom helpers keeps View menu and PageUp/
PageDown accelerator routes aligned because both already reach
`Command::ZoomIn` / `Command::ZoomOut` and then `EditorShell::zoom_camera_in` /
`zoom_camera_out`. The wheel path currently resets before it decides whether to
zoom; keep that reset because it covers wheel inputs that do not call the zoom
helpers.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-401
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

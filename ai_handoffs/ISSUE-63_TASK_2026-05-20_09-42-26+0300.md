# Task Packet

DISPATCH_ID: ISSUE-63
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_09-42-26+0300
RELATED_FILES:
- crates/editor-ui/tests/node_graph_phase8_three_domain_smoke.rs
- crates/editor-ui/Cargo.toml
- crates/editor-ui/tests/node_graph_material_smoke.rs
- crates/editor-ui/tests/node_graph_anim_graph_smoke.rs
- crates/editor-ui/tests/node_graph_operator_smoke.rs
- crates/editor-ui/src/widgets/node_graph.rs
- crates/material-graph/src/lib.rs
- crates/anim-graph/src/lib.rs
- crates/cad-core/src/graph/operator_graph.rs
STATUS: OPEN

## Goal

Add the GitHub issue #63 editor-ui integration smoke proving one generic `NodeGraphWidget` path can model all three Phase 8 graph domains through the existing `rge_kernel_graph_foundation::VizAdapter` bridge. This is a test-only consolidation smoke over the material, animation, and CAD operator graph adapters already covered individually by issues #60, #61, and #62; production `editor-ui` and graph-domain code must remain unchanged and domain-agnostic.

## Scope

### MAY edit
- `crates/editor-ui/Cargo.toml`, only inside `[dev-dependencies]`, and only if one of the test-only dependencies needed by the new integration test is missing: `rge-material-graph`, `rge-anim-graph`, or `rge-cad-core`.

### MUST NOT edit
- Any production source file, including `crates/editor-ui/src/**`, `crates/material-graph/**`, `crates/anim-graph/**`, `crates/cad-core/**`, `kernel/graph-foundation/**`, renderer/gfx crates, runtime crates, editor-shell crates, or any graph-domain crate.
- Existing integration tests, including `crates/editor-ui/tests/node_graph_material_smoke.rs`, `crates/editor-ui/tests/node_graph_anim_graph_smoke.rs`, and `crates/editor-ui/tests/node_graph_operator_smoke.rs`; they are reference material only.
- Workspace-root `Cargo.toml`, `Cargo.lock`, any crate manifest other than the narrow `crates/editor-ui/Cargo.toml` allowance above, architecture-lint files, schemas, scripts, CI files, documentation, `.gitignore`, generated artifacts, or any existing handoff packet.

### MAY add new files
- `crates/editor-ui/tests/node_graph_phase8_three_domain_smoke.rs`

### MUST NOT add new files
- Any source, manifest, fixture, snapshot, asset, doc, schema, script, CI, generated, or workspace file other than `crates/editor-ui/tests/node_graph_phase8_three_domain_smoke.rs`.
- New crates, architecture-lint rules, workspace dependency entries, or generated artifacts.

## Deliverables

- Add `crates/editor-ui/tests/node_graph_phase8_three_domain_smoke.rs` as one editor-ui integration test target.
- In that test, build three small real fixtures using public APIs: a `rge_material_graph::MaterialGraph`, a `rge_anim_graph::AnimGraph`, and a `rge_cad_core::OperatorGraph`.
- Feed each fixture to the existing `NodeGraphWidget` bridge, for example `NodeGraphWidget::new().model_from(&graph)` if `model_from` remains an instance method.
- Assert each model preserves the expected node count and edge count.
- Assert each model preserves node labels and node kinds from the relevant adapter.
- Assert each model preserves edge endpoints and edge labels from the relevant adapter.
- Use the stable `NodeId` and `EdgeId` values returned by each graph's construction APIs for lookups; avoid relying on insertion order because adapter iteration may be sorted by ids.
- If, and only if, a required dev-dependency is missing from `crates/editor-ui/Cargo.toml`, add the minimal existing workspace path dependency in `[dev-dependencies]`: `rge-material-graph = { path = "../material-graph" }`, `rge-anim-graph = { path = "../anim-graph" }`, or `rge-cad-core = { path = "../cad-core" }`.

## Acceptance Criteria

- `crates/editor-ui/tests/node_graph_phase8_three_domain_smoke.rs` exists and is the only new source/test file.
- The new test uses real `MaterialGraph`, `AnimGraph`, and `OperatorGraph` fixtures; it does not use a fake adapter or standalone graph-foundation test double.
- The material fixture has at least three material nodes and at least two typed-port edges. The test asserts material node display names, node kind `MaterialNode`, exact edge endpoints, and exact labels such as `color->color` and `vector->texture`.
- The animation fixture has at least two animation states and at least one transition. The test asserts animation state display names, node kind `AnimState`, exact transition endpoints, and the transition label such as `start_run`.
- The operator fixture has two distinct cuboid nodes feeding one Boolean union node on ports `0` and `1`. The test asserts node display names/kinds `Cuboid` and `Boolean`, both edge endpoint pairs, and exact edge labels `input[0]` and `input[1]`.
- The test proves the editor-ui bridge by compiling and calling the existing `NodeGraphWidget` model path for all three domain graphs without production API changes.
- `crates/editor-ui/Cargo.toml` is unchanged if the three dev-dependencies already exist. If edited, it is changed only inside `[dev-dependencies]` and only to add missing test-only dependencies for the three existing workspace graph crates.
- `Cargo.lock` is not changed.
- No production `editor-ui` code mentions `rge-material-graph`, `rge-anim-graph`, `rge-cad-core`, renderer crates, runtime crates, or other graph-domain crates as part of this dispatch.
- No behavior or public API changes are made to `NodeGraphWidget`, `NodeGraphModel`, `VizAdapter`, `MaterialGraph`, `AnimGraph`, `OperatorGraph`, graph-foundation, renderer, runtime, architecture-lint, schema, script, documentation, or CI files.
- `cargo test -p rge-editor-ui --test node_graph_phase8_three_domain_smoke` exits 0.

## Constraints / Non-Goals

- This dispatch is test-only except for the narrow optional editor-ui `[dev-dependencies]` allowance.
- Do not change `NodeGraphWidget`, `NodeGraphModel`, `VizAdapter`, domain graph adapters, graph labels, graph ordering, graph-foundation ids, or any production API to make the test easier.
- Do not add rendering, egui UI, layout, pan/zoom, selection, ports UI, persistence, material editor behavior, animation editor behavior, CAD editor behavior, graph traversal, evaluation, validation, mutation behavior, or runtime behavior.
- Do not promote material, animation, or CAD graph crates to production dependencies of editor-ui.
- Do not add workspace dependency entries, feature flags, dependency version bumps, lockfile changes, architecture-lint exemptions, or new graph-foundation APIs.
- Do not merge, delete, rename, or rewrite the existing single-domain smoke tests.
- Do not fix unrelated warnings, formatting churn, failing tests, or dependency metadata outside the listed scope.

## Verification Gates

The Executor MUST run and document the result of this command in their `EXECUTION_REPORT`:

- `cargo test -p rge-editor-ui --test node_graph_phase8_three_domain_smoke` -> expected exit 0.

If `crates/editor-ui/Cargo.toml` is edited or time permits, the Executor MAY also run and document:

- `cargo run -q -p rge-tool-architecture-lints -- all` -> expected exit 0.
- `cargo fmt --check -p rge-editor-ui` -> expected exit 0.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- The smoke cannot be implemented entirely within `crates/editor-ui/tests/node_graph_phase8_three_domain_smoke.rs` plus the narrow optional `crates/editor-ui/Cargo.toml` dev-dependency edits allowed above.
- The test appears to require any production source edit, production dependency, workspace manifest edit, lockfile edit, feature flag, architecture-lint exemption, or change to `NodeGraphWidget`, `VizAdapter`, `MaterialGraph`, `AnimGraph`, `OperatorGraph`, graph-foundation, renderer, runtime, schema, script, documentation, or CI files.
- A required dev-dependency is missing and adding it would cause `Cargo.lock` or any manifest other than `crates/editor-ui/Cargo.toml` to change.
- The architecture lint gate, if run, rejects the editor-ui dev-dependencies on the graph-domain crates and resolving that rejection would require any change outside this packet's MAY-edit scope.
- The existing public `NodeGraphWidget` model does not expose node display names, node kinds, edge labels, edge endpoints, or counts strongly enough for the smoke to assert them without changing production APIs.
- The required verification gate fails for reasons unrelated to this task and the fix would require edits outside the MAY-edit or MAY-add scope.
- Existing uncommitted changes in any in-scope file make it unclear how to proceed without overwriting someone else's work.

## Planner Notes

Use the three existing single-domain smokes as reference patterns, not as files to modify:

- `crates/editor-ui/tests/node_graph_material_smoke.rs`
- `crates/editor-ui/tests/node_graph_anim_graph_smoke.rs`
- `crates/editor-ui/tests/node_graph_operator_smoke.rs`

The intended test can either contain one `#[test]` that builds and checks all three fixtures, or small helper functions plus one test that exercises all three domains. Keep assertions domain-specific and explicit: material port labels, animation transition labels, and operator input-port labels should all be checked. The value of this dispatch is proving the same editor-ui `NodeGraphWidget` model path handles all Phase 8 graph domains through `VizAdapter`; it is not a request to add new editor UI behavior.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-63
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

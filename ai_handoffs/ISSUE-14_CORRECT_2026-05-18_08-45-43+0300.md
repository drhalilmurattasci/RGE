# Correction Packet

DISPATCH_ID: ISSUE-14
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18_08-45-43+0300
RELATED_FILES:
- crates/audio/src/falloff.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-14_TASK_2026-05-18_08-40-14+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-14_EXEC_2026-05-18_08-44-25+0300.md`
- Latest Review Report: Not applicable for this orchestrator dispatch; no `_REVIEW_` packet is emitted.
- Verification Failure: `.ai/dispatch.verify.ps1` failed at step `[1] cargo +nightly fmt --check`, specifically `cargo +nightly fmt --all -- --check`, with `Incorrect newline style in \\?\A:\RCAD\RGE\crates\audio\src\falloff.rs`.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. Each correction references the specific verification failure it
addresses.

1. **Normalize `falloff.rs` newline style** - Verification gate failure:
   `cargo +nightly fmt --all -- --check` reported incorrect newline style in
   `crates/audio/src/falloff.rs`. Required change: normalize only the line
   endings in `crates/audio/src/falloff.rs` to the newline style accepted by
   rustfmt for this repository. Do not change Rust tokens, comments,
   indentation, tests, public APIs, behavior, or any other file. Acceptance:
   `cargo +nightly fmt --all -- --check` exits 0, and the correction contains
   no textual or semantic change beyond newline-style normalization.

## Deferred Findings (NOT Approved for This Round)

None. The post-execution verification gate reported only the newline-style
format failure above, and no other correction is approved for this round.

## Updated Acceptance Criteria

The ISSUE-14 feature acceptance criteria remain unchanged. For this correction
round only, one additional formatting-only edit is allowed in
`crates/audio/src/falloff.rs` to satisfy the canonical verification gate. The
dispatch may pass only if the canonical verification gate exits 0 afterward.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0.

If the full canonical verification script cannot be run in the environment, the
Executor MUST run the failed format command above, document the exact reason the
full script could not run, and report that limitation in the next execution
report.

## Halt Conditions (Updated if Any)

- Halt if passing the format gate requires changing any Rust token, comment,
  indentation, test, public API, behavior, or file other than
  `crates/audio/src/falloff.rs`.
- Halt if canonical verification reports any additional failure after the
  newline-style correction; do not broaden the correction without a fresh
  Planner packet.

## Planner Notes

This correction is limited to the canonical verification blocker reported after
execution. Do not revisit the ISSUE-14 test implementation, broaden test
coverage, or make unrelated formatting changes. Preserve the existing
`crates/io-image/src/format_detect.rs` task work and write the next execution
report after the approved correction attempt.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-14
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Correction Packet

DISPATCH_ID: ISSUE-48
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19T14:51:53+03:00
RELATED_FILES:
- crates/material-graph/src/lib.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-48_TASK_2026-05-19_14-45-14+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-48_EXEC_2026-05-19_14-51-11+0300.md`
- Latest Review Report: none; orchestrator dispatch failed in the post-execution verification gate before a control review packet.
- Prior Correction Packets: none.
- Verification Failure: `.ai/dispatch.verify.ps1` step `[1] cargo +nightly fmt --check` reported incorrect newline style in `crates/material-graph/src/lib.rs`.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. Each correction references the specific verification failure it addresses.

1. **Normalize material-graph lib newline style** - Verification gate failure from `cargo +nightly fmt --check`. Required change: normalize only `crates/material-graph/src/lib.rs` to the newline style accepted by rustfmt, without changing Rust semantics or modifying unrelated files. Acceptance: `cargo +nightly fmt --all -- --check` exits 0 and `.ai/dispatch.verify.ps1` exits 0.

## Deferred Findings (NOT Approved for This Round)

No findings are deferred in this correction round. The post-execution
verification gate named a single formatting issue, and no broader source,
architecture, supply-chain, test, or doctest correction is approved here.

## Updated Acceptance Criteria

Unchanged from Task Packet. The existing post-execution verification gate must
return exit 0 after the approved newline-style correction.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from Task Packet. If fixing the newline style would require changing
any file other than `crates/material-graph/src/lib.rs`, halt and report the
unexpected scope requirement in the next execution report.

## Planner Notes

This correction is intentionally narrow because the verification gate stopped
at rustfmt before architecture lints, supply-chain checks, workspace tests, or
doctests could run. Do not make semantic changes, refactors, or additional
formatting edits beyond the reported newline-style fix unless rustfmt itself
rewrites `crates/material-graph/src/lib.rs` as part of the same correction.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-48
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

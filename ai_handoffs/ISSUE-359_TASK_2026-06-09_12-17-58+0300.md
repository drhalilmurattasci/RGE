# Task Packet

DISPATCH_ID: ISSUE-359
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-09_12-17-58+0300
RELATED_FILES:
- .ai/dispatch.tasks.md
- Status.md
- HANDOFF.md
- plans/BASELINE.md
- change.md
- crates/editor-egui-host/src/**
- crates/editor-shell/src/**
- crates/editor-ui/src/**
- editor/rge-editor/src/**
STATUS: OPEN

## Goal

Run task 110 as a docs/source-read audit after ISSUE-358 completed task 109
(`9c789f9`, host-local keyboard shortcuts help). The audit must re-read current
docs and source, re-run the listed falsifying searches, compare the remaining
Phase 9/editor-usability candidate classes, and then either append exactly one
bounded implementation follow-up as task 111 or record `NEEDS_HUMAN` with
source-backed evidence. This dispatch must not implement the selected follow-up.

## Scope

### MAY edit
- `.ai/dispatch.tasks.md`
- `Status.md`
- `HANDOFF.md`
- `plans/BASELINE.md`
- `change.md`
- `ai_handoffs/ISSUE-359_*.md`
- `ai_handoffs/ISSUE-359_*.meta.json`
- `ai_handoffs/claims/ISSUE-359_*.json`
- `.ai/dispatch-ISSUE-359/**`
- `ai_dispatch_logs/log_*ISSUE-359*.md`

### MUST NOT edit
- `crates/**`
- `kernel/**`
- `runtime/**`
- `editor/**`
- `Cargo.toml`
- `**/Cargo.toml`
- `Cargo.lock`
- `.github/workflows/**`
- `Invoke-AiDispatch*.ps1`
- `Register-AiDispatchSchedule.ps1`
- `Get-AiDispatchHealth.ps1`
- `Watch-AiDispatch.ps1`
- `Watch-DispatchStages.ps1`
- `Wait-GitHubActions.ps1`
- `new-handoff.ps1`
- `.ai/dispatch.verify.ps1`
- `.ai/codex_control.schema.json`
- `ai_handoffs/templates/**`
- Any source, test, schema, workflow, ADR, architecture-lint, dispatch
  automation, plugin runtime/discovery/loading implementation, or existing
  handoff/log artifact that is not an ISSUE-359 artifact.

### MAY add new files
- `ai_handoffs/ISSUE-359_*.md`
- `ai_handoffs/ISSUE-359_*.meta.json`
- `ai_handoffs/claims/ISSUE-359_*.json`
- `.ai/dispatch-ISSUE-359/**`
- `ai_dispatch_logs/log_*ISSUE-359*.md`

### MUST NOT add new files
- `crates/**`
- `kernel/**`
- `runtime/**`
- `editor/**`
- `.github/workflows/**`
- `ai_handoffs/templates/**`
- New ADR docs, new schemas, new Cargo manifests, new source/test files, new
  architecture-lint files, new plugin runtime/discovery/loading files, or any
  non-ISSUE-359 handoff/log artifacts.

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - .ai/dispatch.tasks.md
  - Status.md
  - HANDOFF.md
  - plans/BASELINE.md
  - change.md
  - ai_handoffs/ISSUE-359_*.md
  - ai_handoffs/ISSUE-359_*.meta.json
  - ai_handoffs/claims/ISSUE-359_*.json
  - .ai/dispatch-ISSUE-359/**
  - ai_dispatch_logs/log_*ISSUE-359*.md
MUST_NOT_EDIT:
  - crates/**
  - kernel/**
  - runtime/**
  - editor/**
  - Cargo.toml
  - **/Cargo.toml
  - Cargo.lock
  - .github/workflows/**
  - Invoke-AiDispatch*.ps1
  - Register-AiDispatchSchedule.ps1
  - Get-AiDispatchHealth.ps1
  - Watch-AiDispatch.ps1
  - Watch-DispatchStages.ps1
  - Wait-GitHubActions.ps1
  - new-handoff.ps1
  - .ai/dispatch.verify.ps1
  - .ai/codex_control.schema.json
  - ai_handoffs/templates/**
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Deliverables

- An implementation-read-only source audit recorded in the EXEC packet and, as
  needed, the allowed status/task docs.
- Exactly one of:
  - append task 111 to `.ai/dispatch.tasks.md` as one bounded implementation
    follow-up with MAY-edit/MUST-NOT-edit scope, current-state falsification
    searches, required behavior, done criteria, verification, and halt
    conditions; or
  - record `NEEDS_HUMAN` with concrete source evidence explaining why no
    candidate can safely become one bounded implementation task.
- Status surfaces (`Status.md`, `HANDOFF.md`, `plans/BASELINE.md`, `change.md`)
  updated only as needed to describe the audit result and queue posture.
- An EXEC packet that documents the required searches, candidate comparison,
  chosen task 111 or `NEEDS_HUMAN`, `git diff --check`, and
  `git diff --name-only`.

## Acceptance Criteria

- No Rust source, tests, Cargo files, workflows, schemas, ADRs, automation
  scripts, architecture-lint files, or plugin runtime/discovery/loading code are
  edited.
- The audit re-runs every falsifying search listed below and records the command
  plus source-backed result in the EXEC packet or status update.
- The candidate comparison explicitly covers every candidate class listed in
  this packet and explains why the selected task 111 is smaller/safer than the
  deferred alternatives, or why all remaining candidates require human policy.
- `.ai/dispatch.tasks.md` ends with exactly one new task 111 or an explicit
  `NEEDS_HUMAN` record for task 110. It must not append task 112 or implement
  task 111.
- `git diff --check` exits 0.
- `git diff --name-only` is documented and is confined to the allowed docs plus
  ISSUE-359 artifacts.

## Current-State Claims: Falsification Required

Every claim below must be rechecked by the Executor against the current tree.
The planning-time spot checks were run with `git grep` on 2026-06-09.

- Claim: task 109 completed host-local shortcut help in `editor-egui-host`, so
  task 111 must not reselect another shortcut-help window/discoverability slice.
  Falsifying search:
  `git grep -n -E "shortcut_help|shortcut_help_open|Keyboard Shortcuts|ShortcutHelpRow|ShortcutHelpGroup|shortcut_help_rows|show_shortcut_help_window" -- crates/editor-egui-host/src Status.md HANDOFF.md plans/BASELINE.md .ai/dispatch.tasks.md`
  Planning-time result: matches include `mod shortcut_help`,
  `shortcut_help_open`, `shortcut_help_rows`, `shortcut_help_window`,
  `Keyboard Shortcuts`, `ShortcutHelpRow`, `ShortcutHelpGroup`, and the task 109
  completion/re-arm docs.

- Claim: core menu, palette, and accelerator activations still cross the
  host-shell boundary through `MenuCommandHandoff` and
  `EditorShell::route_menu_command`; replacing that route is broader than a
  UI-only task unless the current source shows a tiny safe slice.
  Falsifying search:
  `git grep -n -E "MenuCommandHandoff|drain_and_route_menu_commands|route_menu_command|command_palette_window|enqueue_command_palette_activation|enabled_command_for_shortcut|default_editor_menu" -- crates/editor-egui-host/src crates/editor-shell/src crates/editor-ui/src editor/rge-editor/src`
  Planning-time result: matches show `MenuCommandHandoff` in the host,
  `command_palette_window` and `enqueue_command_palette_activation` in
  `editor-egui-host`, `drain_and_route_menu_commands` and
  `route_menu_command` in `editor-shell`, and canonical shortcut resolution via
  `default_editor_menu` / `enabled_command_for_shortcut` in `editor-ui` and
  `editor-shell`.

- Claim: extension/plugin commands still stop at the task-102 injected handler
  seam; no real plugin runtime/discovery/loading path is wired into the editor
  command route.
  Falsifying search:
  `git grep -n -E "ExtensionCommandHandler|ExtensionCommandEvent|Command::Custom|Command::Plugin|PluginHost|PluginContext|plugin-discovery|runtime-wasmtime|rge_kernel_plugin_host" -- crates/editor-shell/src crates/editor-egui-host/src crates/editor-ui/src editor/rge-editor/src`
  Planning-time result: matches show `ExtensionCommandHandler` /
  `ExtensionCommandEvent` and `Command::Custom` / `Command::Plugin` routing
  tests; `PluginContext` / `rge_kernel_plugin_host` matches are limited to the
  `editor-ui` plugin adapter substrate, not a wired editor command route.

- Claim: several stale roadmap candidates may already have partial source
  closure and must be rechecked before selection, especially shortcut
  conflicts/keybinding policy, shell-local clipboard, close/quit save behavior,
  camera commands, and CAD/editor mutation routes.
  Falsifying search:
  `git grep -n -E "Shortcut Conflicts|shortcut_conflicts|keybinding|has_clipboard_entities|Command::Close|Command::Quit|unsaved|dirty|Command::ResetCamera|Command::ZoomIn|Command::ZoomOut|Command::Cut|Command::Copy|Command::Paste|Command::Duplicate|cad|CadGraph" -- crates/editor-egui-host/src crates/editor-shell/src crates/editor-ui/src editor/rge-editor/src`
  Planning-time result: matches show existing shortcut-conflict projection,
  dirty/save-status/window-title surfaces, shell-local clipboard predicates,
  routed Close/Quit/Edit commands, routed ResetCamera/ZoomIn/ZoomOut, and CAD
  graph/projection substrates. These are evidence to compare, not authorization
  to implement those areas in task 110.

- Claim: task 110 is the only current open autonomous backlog entry and task
  111 does not already exist, so this audit must either append exactly one new
  task 111 or record `NEEDS_HUMAN` instead of skipping, renumbering, or
  appending task 112.
  Falsifying searches:
  `git grep -n -E "^[0-9]+\. \*\*[^\[]" -- .ai/dispatch.tasks.md`
  `git grep -n -E "^111\. " -- .ai/dispatch.tasks.md`
  Planning-time result: the first search returned only
  `.ai/dispatch.tasks.md:9605:110. **Post-shortcut-help Phase 9 next-task source audit.**`;
  the second search exited 1 with no matches. These results are planning-time
  evidence only; the Executor must re-run both searches against the current
  tree before editing `.ai/dispatch.tasks.md`.

## Candidate Classes To Compare

- Host-shell FIFO replacement or generalized registry execution beyond the
  current `MenuCommandHandoff` to `EditorShell::route_menu_command` path.
- Extension/plugin command execution beyond the injected handler seam,
  including whether any substrate-only step exists before real runtime or
  discovery/loading work.
- Keybinding/conflict policy, shortcut diagnostics, or other shortcut surfaces
  after task 109's read-only help window.
- Unsaved close/quit prompting and save-state UX beyond current save/dirty
  routing.
- OS clipboard or typed editor clipboard behavior beyond the current
  shell-local entity clipboard.
- Authoritative CAD graph/projection mutation with undo/dirty integration.
- Camera/navigation UI beyond existing reset/zoom commands and scene-aware
  labels.
- Any other Phase 9/editor-usability candidate that current source shows is
  smaller and safer than the listed deferred areas.

## Constraints / Non-Goals

- This is not a source implementation dispatch. Do not implement task 111.
- Do not infer from stale backlog prose or from `ai_handoffs/` filenames; read
  current docs and source.
- Do not select another shortcut-help window/discoverability task; task 109
  closed that slice unless current source contradicts the claim.
- Do not select a follow-up that requires Cargo/workflow/automation/schema/ADR
  edits, real plugin runtime/discovery/loading, command-route replacement, OS
  clipboard integration, authoritative CAD mutation, or undo/dirty policy
  unless the audit can scope it as one narrow source-backed implementation task.
- Do not modify scheduler state, publish mode, GitHub issue labels, or
  automation scripts.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- The five required `git grep -n -E ...` falsifying searches above, or exact
  `rg` equivalents if `rg` is available.
- `git diff --check` -> expected exit 0.
- `git diff --name-only` -> expected only `.ai/dispatch.tasks.md`,
  `Status.md`, `HANDOFF.md`, `plans/BASELINE.md`, `change.md`, and ISSUE-359
  artifacts.

Rust/Cargo build and test gates are intentionally not required because this
dispatch is implementation-read-only. If any Rust source/test/Cargo file changes,
that is a scope violation, not a reason to run implementation gates.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of
the following occur:

- Selecting task 111 would require implementation edits during task 110.
- Current source shows all plausible next slices require human
  product/architecture policy before implementation.
- The selected follow-up would need Cargo/workflow/automation/schema/ADR edits,
  real plugin runtime/discovery/loading, command-route replacement, OS clipboard
  integration, authoritative CAD mutation, or undo/dirty policy and cannot be
  scoped as one narrow source-backed task.
- The audit requires editing any file outside the MAY-edit surface.
- `git diff --name-only` shows any source, test, Cargo, workflow, schema, ADR,
  automation, architecture-lint, or unrelated handoff/log artifact.

## Planner Notes

Task 110 is the next open autonomous backlog entry after ISSUE-358. `Status.md`
and `HANDOFF.md` both record that ISSUE-358 landed task 109 as commit
`9c789f9`, then the automation wrapper exhausted the previous task brief because
no task 110 existed. `.ai/dispatch.tasks.md` now contains task 110 with the
same falsifying-search set and candidate classes mirrored here.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-359
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Task Packet

DISPATCH_ID: ISSUE-14
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18_08-40-14+0300
RELATED_FILES:
- crates/io-image/src/format_detect.rs
STATUS: OPEN

## Goal

Implement GitHub issue #14 by adding focused test-only coverage for `detect_format` magic-byte matching in `crates/io-image/src/format_detect.rs`. The dispatch should prove that a byte slice containing only the first 3 bytes of the PNG signature is treated as an incomplete prefix and returns `None`, pinning the existing full-signature requirement without changing runtime behavior.

## Scope

### MAY edit
- `crates/io-image/src/format_detect.rs`, only inside the existing `#[cfg(test)] mod tests`.

### MUST NOT edit
- Any file other than `crates/io-image/src/format_detect.rs`.
- Any production/runtime code in `crates/io-image/src/format_detect.rs`, including `ImageFormat` and `detect_format`.
- Cargo manifests, lockfiles, schemas, scripts, docs, CI files, `.gitignore`, or existing handoff packets.

### MAY add new files
- One protocol-required execution report matching `ai_handoffs/ISSUE-14_EXEC_*.md`.
- The matching execution-report `.meta.json` sidecar if generated by `new-handoff.ps1 -Finalize`.

### MUST NOT add new files
- No new source files, integration tests, fixtures, docs, schemas, scripts, Cargo entries, or architecture-lint files.
- No handoff packets other than the protocol-required `ISSUE-14_EXEC` report and its finalize-generated sidecar.

## Deliverables

- Add one unit test in the existing `#[cfg(test)] mod tests` in `crates/io-image/src/format_detect.rs`.
- The new test MUST use exactly the first 3 bytes of the PNG signature: `[0x89, b'P', b'N']`.
- The new test MUST assert `detect_format(&bytes) == None`.
- A completed execution report packet for this dispatch.

## Acceptance Criteria

- The only implementation file changed is `crates/io-image/src/format_detect.rs`.
- All implementation changes are test-only and remain inside the existing `#[cfg(test)] mod tests`.
- A test named `truncated_png_prefix_is_not_detected`, or an equivalently clear name, exists and uses exactly `[0x89, b'P', b'N']` as the input bytes.
- The truncated PNG prefix test asserts that `detect_format(&bytes)` returns `None`.
- No changes are made to `detect_format`, `ImageFormat`, public APIs, runtime behavior, dependencies, Cargo configuration, documentation, or repository scripts.
- Existing `format_detect` tests continue to pass.

## Constraints / Non-Goals

- This is a test-only dispatch. Do not modify runtime detection logic to satisfy the new test.
- Do not broaden PNG detection coverage beyond the strict first-3-byte truncated-prefix case requested by issue #14.
- Do not add fixtures, integration tests, helper modules, generated files, dependencies, or Cargo manifest entries.
- Do not rewrite or refactor unrelated tests in `format_detect.rs`.
- Do not change magic-byte constants, codec routing, loading behavior, error types, or any image decoding code.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `cargo +nightly fmt --check -p rge-io-image` -> expected exit 0.
- `cargo test -p rge-io-image --lib truncated_png_prefix_is_not_detected --no-fail-fast` -> expected exit 0, with the new truncated PNG prefix test passing.
- `cargo test -p rge-io-image --lib format_detect --no-fail-fast` -> expected exit 0, with all `format_detect` tests passing.
- `cargo test -p rge-io-image --lib --no-fail-fast` -> expected exit 0.
- `git diff -- crates/io-image/src/format_detect.rs` -> expected diff is limited to test code inside `#[cfg(test)] mod tests`.
- `git diff --check -- crates/io-image/src/format_detect.rs` -> expected exit 0.

If a required command cannot be run in the environment, the Executor MUST document the exact command, the failure reason, and any partial verification that was completed.

## Halt Conditions

The Executor MUST halt without modifying runtime behavior and request fresh guidance if any of the following occur:

- The requested test fails against the existing production implementation and passing it would require changing `detect_format` or any other runtime code.
- Any required edit outside `crates/io-image/src/format_detect.rs` appears necessary.
- Any required edit outside the existing `#[cfg(test)] mod tests` in `crates/io-image/src/format_detect.rs` appears necessary.
- Any new dependency, fixture, integration test file, Cargo manifest change, generated file, or documentation change appears necessary.
- Formatting or tests fail for reasons unrelated to the requested test-only change and cannot be isolated within the allowed scope.

## Planner Notes

This task was filed automatically by `Invoke-AiDispatchAuto.ps1` from `.ai/dispatch.tasks.md` for GitHub issue #14. Revision number: 0. There is no prior Claude gate result. Keep the implementation narrow: add only the truncated PNG prefix regression test and leave magic-byte detection behavior untouched.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-14
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

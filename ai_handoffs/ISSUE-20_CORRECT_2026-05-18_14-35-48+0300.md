# Correction Packet

DISPATCH_ID: ISSUE-20
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18_14-35-48+0300
RELATED_FILES:
- crates/cad-core/src/operators/sweep.rs
- crates/cad-core/src/topology/edge_resolve.rs
- crates/cad-core/tests/brep_edge_chain_resolution_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-20_TASK_2026-05-18_14-24-57+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-20_EXEC_2026-05-18_14-34-47+0300.md`
- Latest Review Report: none; the post-execution verification gate failed before Codex control review.
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections -- nothing more, nothing
less. Each correction references the failed post-execution verification gate.

1. **Normalize rustfmt newline style** -- Verification gate finding:
   `cargo +nightly fmt --all -- --check` failed with "Incorrect newline
   style" for `crates/cad-core/src/operators/sweep.rs`,
   `crates/cad-core/src/topology/edge_resolve.rs`, and
   `crates/cad-core/tests/brep_edge_chain_resolution_smoke.rs`. Required
   change: normalize only the newline style of those three Rust files to the
   style accepted by `cargo +nightly fmt --all -- --check`. Do not make
   semantic edits, do not change imports or formatting beyond rustfmt's newline
   normalization requirement, and do not modify the ISSUE-20 test harness
   unless the verification command itself formats it. Acceptance:
   `cargo +nightly fmt --all -- --check` exits 0, and the canonical dispatch
   verification gate proceeds past the format step.

## Deferred Findings (NOT Approved for This Round)

No review findings are deferred. No Codex control review ran because the
canonical verification gate failed first.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with one additional correction-round
acceptance bar: the repository must pass the canonical dispatch verification
gate after the newline-style normalization.

## Re-Verification Gates

The Executor MUST re-run these gates after the corrections:

- `cargo +nightly fmt --all -- --check` -> expected exit 0
- `.ai/dispatch.verify.ps1` -> expected exit 0

If `.ai/dispatch.verify.ps1` fails after the format step for an unrelated
pre-existing issue, stop and report the exact failing step in the new
`EXECUTION_REPORT` without expanding scope.

## Halt Conditions (Updated if Any)

Unchanged from Task Packet, plus:

- Halt if making the format gate pass requires semantic Rust changes,
  implementation changes, test assertion changes, Cargo manifest changes, or
  edits outside the three files named in `RELATED_FILES`.
- Halt if the newline-style correction cannot be confined to the three
  rustfmt-reported files.

## Planner Notes

This correction is limited to the failed verification gate. The previous
execution report states that the ISSUE-20 behavioral test passed and that the
edited gate file was not flagged by rustfmt. The only approved action in this
round is to normalize newline style for the three rustfmt-reported files so the
canonical CI-format step can pass. Do not revisit Sweep behavior, edge IDs,
face IDs, generator logic, or any other task implementation detail in this
correction round.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-20
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

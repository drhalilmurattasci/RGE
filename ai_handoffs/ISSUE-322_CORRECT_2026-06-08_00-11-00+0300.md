# Correction Packet

DISPATCH_ID: ISSUE-322
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-08_00-11-00+0300
RELATED_FILES:
- plans/BASELINE.md
- Status.md
- HANDOFF.md
- change.md
- ai_handoffs/ISSUE-322_EXEC_2026-06-07_23-50-34+0300.md
- ai_handoffs/ISSUE-322_EXEC_*.md
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-322_TASK_2026-06-07_23-34-23+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-322_EXEC_2026-06-07_23-50-34+0300.md`
- Latest Review Result: Codex control review JSON supplied for round 0, verdict `needs_changes`
- Prior CORRECT packets in this dispatch: none

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing less.

1. **Correct the false `wasmtime` provenance claim** - Codex control finding: the docs and EXEC claim that `wasmtime` is pulled by exactly two workspace crates, but direct manifest reads show direct `wasmtime` dependencies in `crates/expr-wasm/Cargo.toml:16`, `crates/runtime-wasmtime-engine/Cargo.toml:33`, `crates/script-host/Cargo.toml:20`, and `crates/script-bench/Cargo.toml:39`.
   Required change: update `plans/BASELINE.md:408`, `Status.md:3`, `HANDOFF.md:3`, and `change.md:1773` so they no longer say or imply "exactly two workspace crates" pull `wasmtime`. The corrected wording must distinguish the main clean-release attribution path from the full set of direct workspace manifest dependencies, including `rge-script-host` and `rge-script-bench`. Update the remediation candidates, especially the Pulley/default-feature option, so any next task is not scoped only to `rge-expr-wasm` and `rge-runtime-wasmtime-engine` when `script-host` and `script-bench` also directly depend on `wasmtime`.
   Acceptance: `Select-String -Path plans/BASELINE.md,Status.md,HANDOFF.md,change.md -Pattern 'exactly two workspace crates'` returns no matches, and the updated text names all four direct manifest dependencies or explicitly says the previous two-crate wording was only the attribution path, not the complete direct-dependency set.

2. **Record the material local parsing command** - Codex control finding: the report records parsed timing data at `.ai/dispatch-ISSUE-322/unit-data.json`, but the measurement command list does not document the local parsing/extraction command, despite the TASK packet requiring every material measurement, attribution, and local parsing command to be recorded with exit status.
   Required change: document the command that produced `.ai/dispatch-ISSUE-322/unit-data.json`, including exit status, in the corrected docs and/or in the follow-up correction EXEC packet. If the original exact command cannot be recovered from shell history or run logs, state that explicitly and re-run an equivalent local parsing/extraction command against the preserved `.ai/dispatch-ISSUE-322/cargo-timing-clean-release.html` artifact to regenerate or corroborate `.ai/dispatch-ISSUE-322/unit-data.json`; record that exact command, exit status, input artifact, and output artifact.
   Acceptance: the follow-up EXEC report contains an entry for the local parsing/extraction command with exit status 0, input path, and output path, and the docs no longer rely on `unit-data.json` without an auditable command record.

## Deferred Findings (NOT Approved for This Round)

None. The control review identified only the two required fixes above for this correction round.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with these clarification requirements for review pass:

- The docs must not contain the unqualified "exactly two workspace crates" `wasmtime` provenance claim.
- The remediation list must account for all direct workspace manifest dependencies on `wasmtime` when proposing Pulley/default-feature or dependency-scope follow-up work.
- The follow-up EXEC report must document the material local parsing/extraction command for `.ai/dispatch-ISSUE-322/unit-data.json` with exit status.

## Re-Verification Gates

The Executor MUST re-run and document these gates after the corrections:

- `Select-String -Path crates/*/Cargo.toml -Pattern wasmtime` -> expected exit 0; result must show direct `wasmtime` dependencies in `crates/expr-wasm`, `crates/runtime-wasmtime-engine`, `crates/script-host`, and `crates/script-bench`.
- `Select-String -Path plans/BASELINE.md,Status.md,HANDOFF.md,change.md -Pattern 'exactly two workspace crates'` -> expected no matches. If PowerShell returns exit 0 for no matches, record "no matches"; if it returns no output, record that explicitly.
- The local parsing/extraction command for `.ai/dispatch-ISSUE-322/unit-data.json` -> expected exit 0.
- `git diff --check` -> expected exit 0.
- `git diff --name-only` -> expected tracked changes confined to `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, `change.md`, `.ai/dispatch.tasks.md`, and this dispatch's protocol/audit artifacts.

Do not add source, test, bench, Cargo, workflow, schema, tooling, scheduler, or dispatch-automation verification work. If the orchestrator canonical verification gate runs again, report its result, but do not broaden this correction beyond the documentation/audit fixes above.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet. Additionally, halt and return to Planner if the original or equivalent local parsing/extraction command cannot be documented with enough detail for another agent to rerun it, or if correcting the `wasmtime` provenance would require editing Cargo manifests or source files.

## Planner Notes

This correction is documentation/audit repair only. Do not implement any remediation candidate. Do not change dependency graphs. Do not touch source, tests, benches, Cargo manifests, lockfiles, scripts, workflows, schemas, scheduler configuration, or dispatch automation. Keep the follow-up EXEC packet focused on the two approved fixes and the verification commands above.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-322
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Task Packet

DISPATCH_ID: ISSUE-6
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-17_23-08-35+0300
RELATED_FILES:
- AI_DISPATCH_PARALLEL.md
- Invoke-AiDispatchQueue.ps1
- Invoke-AiDispatchAuto.ps1
STATUS: OPEN

## Goal

Document GitHub issue #6, "Document dispatch lock serialization." Add a short
note to `AI_DISPATCH_PARALLEL.md` explaining that `Invoke-AiDispatchQueue.ps1`
and `Invoke-AiDispatchAuto.ps1` each hold a single-run lock, so overlapping
scheduled and manual ticks serialize instead of colliding.

## Scope

This is a docs-only task. It is not a code, script-behavior, schema, or
automation-policy change.

### MAY edit

- `AI_DISPATCH_PARALLEL.md`, only to add or adjust a concise note about the
  single-run locks held by `Invoke-AiDispatchQueue.ps1` and
  `Invoke-AiDispatchAuto.ps1`, and the resulting serialization of overlapping
  scheduled and manual ticks.

### MUST NOT edit

- `Invoke-AiDispatchQueue.ps1`
- `Invoke-AiDispatchAuto.ps1`
- `Invoke-AiDispatchLoop.ps1`, `Watch-AiDispatch.ps1`, `new-handoff.ps1`, or
  any other PowerShell script.
- `AI_DISPATCH_AUTOMATION.md`, `Status.md`, `HANDOFF.md`, `AGENTS.md`,
  `CLAUDE.md`, `README.md`, `change.md`, ADRs, plans, schemas, templates,
  workflows, `.gitignore`, `.mcp.json`, or files under `.ai/`.
- Any Rust source, tests, benchmarks, Cargo manifests, lockfiles, shaders,
  assets, generated outputs, or files under `crates/**`, `kernel/**`,
  `runtime/**`, `editor/**`, `tools/**`, or `plans/**`.
- Any existing handoff packet, state file, sidecar, queue log, run-dir file, or
  prior issue packet.

### MAY add new files

- Exactly one execution report matching `ai_handoffs/ISSUE-6_EXEC_*.md`.
- The matching `ai_handoffs/ISSUE-6_EXEC_*.meta.json` sidecar, only if produced
  by `.\new-handoff.ps1 -Finalize -PacketPath` while finalizing the execution
  report.

### MUST NOT add new files

- New source files, tests, docs, ADRs, scripts, schemas, templates, workflows,
  queue logs, scratch files, or packets other than the single allowed ISSUE-6
  EXEC packet and its generated sidecar.

### MUST NOT commit

- Do not stage, commit, merge, or push. Leave the docs edit and execution
  report uncommitted for the queue or human reviewer.

## Deliverables

- A concise note in `AI_DISPATCH_PARALLEL.md` that names
  `Invoke-AiDispatchQueue.ps1` and `Invoke-AiDispatchAuto.ps1` and states that
  each holds a single-run lock.
- The note explains that overlapping scheduled and manual ticks serialize
  instead of colliding.
- The note matches the document's existing concise style and is placed where it
  naturally fits the parallel-versus-serial dispatch guidance.
- An EXECUTION_REPORT documenting the exact docs change, verification results,
  and final working-tree status.
- No code, script-behavior, schema, template, workflow, source, test, or
  unrelated documentation changes.

## Acceptance Criteria

- `AI_DISPATCH_PARALLEL.md` contains the requested lock-serialization note.
- The note explicitly mentions both `Invoke-AiDispatchQueue.ps1` and
  `Invoke-AiDispatchAuto.ps1`.
- The note says each script holds a single-run lock.
- The note explains that overlapping scheduled and manual ticks serialize
  instead of colliding.
- The wording is short and consistent with the existing document style.
- No code block, command example, or behavior change is introduced for this
  issue.
- Final tracked changes are limited to `AI_DISPATCH_PARALLEL.md`, plus the
  ISSUE-6 EXEC packet and optional generated sidecar.

## Constraints / Non-Goals

- Do not change lock implementation, queue behavior, auto-run scheduling,
  branch policy, publish policy, labels, markers, schemas, templates, or packet
  protocol.
- Do not broaden the parallel fan-out design or add a new dispatch runner.
- Do not audit or rewrite unrelated dispatch documentation.
- Do not contact GitHub, run the queue, run the auto scheduler, mutate labels,
  fetch, push, or require network access.
- Do not update historical packet contents or prior issue artifacts.

## Verification Gates

The Executor MUST run and document the result of each gate in the
EXECUTION_REPORT:

- `git status --short --branch`
- `Select-String -Path AI_DISPATCH_PARALLEL.md -Pattern 'Invoke-AiDispatchQueue.ps1','Invoke-AiDispatchAuto.ps1','single-run lock','scheduled','manual','serialize','collid'`
  - Expected: output shows the new concise note and supports the acceptance
    criteria.
- `git diff -- AI_DISPATCH_PARALLEL.md`
  - Expected: a narrow docs-only diff that adds or revises only the requested
    note.
- `git diff -- Invoke-AiDispatchQueue.ps1 Invoke-AiDispatchAuto.ps1`
  - Expected: no diff.
- `git diff --check -- AI_DISPATCH_PARALLEL.md`
  - Expected: exit 0.
- `git status --short --branch`
- After writing the EXEC packet:
  `$execPacket = (Get-ChildItem -LiteralPath ai_handoffs -Filter 'ISSUE-6_EXEC_*.md' | Sort-Object LastWriteTime -Descending | Select-Object -First 1).FullName; .\new-handoff.ps1 -Finalize -PacketPath $execPacket -DryRun`
  - Expected: exit 0.

## Halt Conditions

The Executor MUST halt without committing, pushing, or broadening scope and
write an EXECUTION_REPORT with `STATUS: BLOCKED` or `STATUS: NEEDS_HUMAN` if
any of the following occur:

- The requested note cannot be made accurately by editing only
  `AI_DISPATCH_PARALLEL.md`.
- Local read-only inspection of `Invoke-AiDispatchQueue.ps1` or
  `Invoke-AiDispatchAuto.ps1` contradicts the single-run-lock behavior described
  in this task.
- Making the docs accurate appears to require any code, script behavior,
  schema, template, workflow, source, test, or unrelated docs change.
- Existing tracked edits in `AI_DISPATCH_PARALLEL.md` make a minimal
  non-destructive patch unsafe or ambiguous.
- Verification would require GitHub network access, `gh` authentication changes,
  a real queue run, a real scheduler run, fetch, push, label mutation, or issue
  mutation.
- Any verification gate fails after the allowed docs edit and cannot be fixed by
  changing only `AI_DISPATCH_PARALLEL.md`.

## Planner Notes

- No prior Claude gate exists for this revision.
- Keep the edit smaller than the issue description: one short note is enough.
- The scripts are related files only for read-only verification of the claimed
  lock behavior; they are not in scope for edits.

---

HANDOFF_STATUS: COMPLETE
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Task Packet

DISPATCH_ID: ISSUE-214
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-26_20-39-49+0300
REVISION: 0
RELATED_FILES:
- GitHub issue #214
- AI_DISPATCH_AUTOMATION.md
- ai_handoffs/AI_HANDOFF_PROTOCOL.md
- plans/PLAN.md
- plans/IMPLEMENTATION.md
- plans/BASELINE.md
- Status.md
- HANDOFF.md
- change.md
- Cargo.toml
- kernel/graph-foundation/src/invalidation.rs
- kernel/graph-foundation/tests/invalidation_dag.rs
- tools/invalidation-profiler/src/main.rs
- crates/cad-core/src/tessellation/cache.rs
- crates/cad-core/src/tessellation/mod.rs
- crates/cad-core/tests/labeled_tessellation_pipeline.rs
- crates/cad-projection/src/projection_cache/mod.rs
- crates/cad-projection/src/lib.rs
- crates/gfx/src/frame_graph/resource_map.rs
- crates/gfx/src/frame_graph/texture_pool.rs
- crates/gfx/src/frame_graph/buffer_pool.rs
- crates/gfx/src/frame_graph/compile/
- crates/gfx/tests/frame_graph_smoke.rs
- crates/gfx/tests/frame_graph_umbrella_smoke.rs
- crates/script-bench/BASELINE.md
- crates/script-bench/METHODOLOGY.md
- crates/script-bench/benches/hot_reload_swap.rs
- crates/script-host/src/swap.rs
- crates/script-host/tests/swap_smoke.rs
- crates/hot-reload-watcher/src/lib.rs
- ai_dispatch_logs/
- ai_handoffs/ISSUE-214_EXEC_*.md
- ai_handoffs/ISSUE-214_EXEC_*.meta.json
STATUS: OPEN

## Goal

Perform a read-only Phase 9 invalidation-economics audit for GitHub issue
#214. Assess cache invalidation and recompute economics across
`kernel/graph-foundation`, `cad-core` tessellation caches, `cad-projection`
projection caches, `gfx` frame-graph resource maps, and script hot-reload
evidence. Use current code, docs, dispatch artifacts, and line-cited evidence
only. Classify whether any measured or structural invalidation cost deserves
the next dispatch, then name exactly one smallest safe follow-up or
`NEEDS_HUMAN`. This dispatch must not change tracked source, tests,
documentation, Cargo files, workflows, schemas, automation scripts, scheduler
configuration, or `.ai/dispatch.tasks.md`.

## Scope

### MAY edit
- None. This is an audit-only dispatch.

### MUST NOT edit
- `plans/PLAN.md`
- `plans/IMPLEMENTATION.md`
- `plans/BASELINE.md`
- `Status.md`
- `HANDOFF.md`
- `change.md`
- `Cargo.toml`
- `Cargo.lock`
- `**/Cargo.toml`
- `.github/workflows/**`
- `.ai/dispatch.tasks.md`
- `.ai/dispatch.verify.ps1`
- `.ai/*.schema.json`
- `Invoke-AiDispatchAuto.ps1`
- `Invoke-AiDispatchQueue.ps1`
- `Invoke-AiDispatchLoop.ps1`
- `Register-AiDispatchSchedule.ps1`
- `Get-AiDispatchHealth.ps1`
- `Get-AiDispatchTrends.ps1`
- `Watch-AiDispatch.ps1`
- `Wait-GitHubActions.ps1`
- `new-handoff.ps1`
- `ai_handoffs/AI_HANDOFF_PROTOCOL.md`
- `ai_handoffs/templates/**`
- This task packet,
  `ai_handoffs/ISSUE-214_TASK_2026-05-26_20-39-49+0300.md`, after the
  executor starts.
- Any existing handoff packet or sidecar.
- Any existing `ai_dispatch_logs/**` artifact.
- `crates/**`
- `kernel/**`
- `editor/**`
- `runtime/**`
- `tools/**`
- `tests/**`
- `docs/**`
- `tasks/**`
- `plugins/**`
- `files/**`
- `golden-projects/**`
- `OLD/**`
- `.claude/worktrees/**`
- Any tracked source file, Rust test, documentation file, Cargo metadata file,
  workflow, schema, automation script, scheduler config, task brief, existing
  handoff artifact, existing dispatch log, generated fixture, sandbox
  worktree, or unrelated local scratch file.

### MAY add new files
- `ai_handoffs/ISSUE-214_EXEC_*.md`
- `ai_handoffs/ISSUE-214_EXEC_*.meta.json`
- `ai_dispatch_logs/log_*.md`
- `.ai/dispatch-ISSUE-214/**`
- `.ai/dispatch-trace/Invoke-AiDispatchAuto-*.jsonl`
- `.ai/dispatch-trace/Invoke-AiDispatchQueue-*.jsonl`

### MUST NOT add new files
- New source, test, documentation, schema, Cargo, workflow, task brief,
  automation, scheduler, fixture, generated asset, ADR, plan, status,
  architecture-lint, benchmark, screenshot, ZIP, or persistent scratch files.
- New handoff packets or sidecars for any dispatch other than `ISSUE-214`.
- `REVIEW`, `CORRECT`, or `CLOSEOUT` packets for this orchestrator dispatch.
- Temporary source-layout inventories, extracted logs, copied evidence files,
  or measurement outputs inside the repository. If scratch is required, place
  it outside the repo and remove it before writing the EXEC report.

## Deliverables

- One new `ISSUE-214` EXEC report containing the read-only audit.
- A decision block with exactly these fields:
  `INVALIDATION_ECONOMICS_VERDICT`,
  `COST_CLASS`, and `SMALLEST_SAFE_FOLLOW_UP`.
- `INVALIDATION_ECONOMICS_VERDICT` must use exactly one of:
  `warrants_next_dispatch`, `does_not_warrant_next_dispatch`,
  `NEEDS_HUMAN`.
- `COST_CLASS` must use exactly one of: `measured`, `structural`, `mixed`,
  `none`, `NEEDS_HUMAN`.
- `SMALLEST_SAFE_FOLLOW_UP` must name exactly one action or be exactly
  `NEEDS_HUMAN`. If the follow-up touches code, tests, scripts, Cargo
  metadata, workflows, schemas, scheduler configuration, or automation
  behavior, it must state `PublishMode branch only unless later authorized by a
  human`.
- A line-cited evidence table covering every required surface:
  `kernel/graph-foundation`, `crates/cad-core` tessellation cache,
  `crates/cad-projection` projection cache, `crates/gfx` frame-graph resource
  maps, and script hot-reload evidence.
- A measured-evidence section that cites any current benchmarks, baselines,
  profiler code, dispatch logs, or tests that quantify invalidation or
  recompute cost. If current measured evidence is absent for a surface, state
  that explicitly and classify the surface using structural evidence only.
- A structural-evidence section that traces invalidation keys, cache lifetimes,
  clear/rebuild behavior, recompute boundaries, and fanout where the current
  code makes those visible.
- A candidate comparison section explaining why the selected follow-up is
  smaller or more urgent than the other inspected invalidation-economics
  candidates.
- A Phase 9 context section citing `plans/PLAN.md`, `plans/IMPLEMENTATION.md`,
  `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, recent `change.md`, and
  relevant prior dispatch artifacts. It must distinguish live current evidence
  from historical or host-specific measurements.
- A final verification section reporting `git diff --check`,
  `git status --short --untracked-files=all`, and static inspection proving no
  tracked production/doc/script/Cargo/workflow/schema/task-brief files changed.

## Acceptance Criteria

- The EXEC report is audit-only and does not modify tracked source, tests,
  docs, Cargo metadata, workflows, schemas, scripts, scheduler configuration,
  or `.ai/dispatch.tasks.md`.
- The report classifies invalidation-economics pressure using exactly one
  `INVALIDATION_ECONOMICS_VERDICT` value and exactly one `COST_CLASS` value
  from the deliverables section.
- The report names exactly one smallest safe follow-up or exactly
  `NEEDS_HUMAN`; it does not produce a backlog list.
- Every substantive claim about current code, docs, scripts, measurements, or
  dispatch artifacts is backed by `path:line` evidence. If a source-layout
  claim comes from directory or Cargo inventory rather than a line-bearing
  file, the report says so explicitly.
- The audit reads and line-cites `AI_DISPATCH_AUTOMATION.md` and
  `ai_handoffs/AI_HANDOFF_PROTOCOL.md` before judging dispatch state, and it
  does not treat missing orchestrator `_REVIEW_` or `_CLOSEOUT_` packets as
  evidence of an unfinished dispatch.
- The audit reads and line-cites `plans/PLAN.md`, `plans/IMPLEMENTATION.md`,
  `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, and recent `change.md`
  entries to establish Phase 9 context.
- The audit inspects and line-cites graph-foundation invalidation evidence at
  minimum from `kernel/graph-foundation/src/invalidation.rs`,
  `kernel/graph-foundation/tests/invalidation_dag.rs`, and
  `tools/invalidation-profiler/src/main.rs`.
- The audit inspects and line-cites CAD tessellation/projection cache evidence
  at minimum from `crates/cad-core/src/tessellation/cache.rs`,
  `crates/cad-core/src/tessellation/mod.rs`,
  `crates/cad-core/tests/labeled_tessellation_pipeline.rs`, and
  `crates/cad-projection/src/projection_cache/mod.rs`.
- The audit inspects and line-cites frame-graph resource-map evidence at
  minimum from `crates/gfx/src/frame_graph/resource_map.rs`,
  `crates/gfx/src/frame_graph/texture_pool.rs`,
  `crates/gfx/src/frame_graph/buffer_pool.rs`,
  `crates/gfx/tests/frame_graph_smoke.rs`, and
  `crates/gfx/tests/frame_graph_umbrella_smoke.rs`.
- The audit inspects and line-cites script hot-reload evidence at minimum from
  `crates/script-bench/BASELINE.md`,
  `crates/script-bench/METHODOLOGY.md`,
  `crates/script-bench/benches/hot_reload_swap.rs`,
  `crates/script-host/src/swap.rs`,
  `crates/script-host/tests/swap_smoke.rs`, and
  `crates/hot-reload-watcher/src/lib.rs`.
- The audit inspects relevant existing dispatch artifacts read-only, including
  prior Phase 9 audit artifacts when relevant. It must cite current packet/log
  lines, not stale copied line numbers.
- The report distinguishes measured invalidation cost from structural
  invalidation risk, ordinary deferred implementation, host-specific benchmark
  data, and known doctrine or audit-only gaps.
- The report does not run new benchmarks, profilers, full repository
  verification, the autonomous queue, or a nested dispatch as part of this
  audit.
- `git diff --check` exits 0.
- `git status --short --untracked-files=all` shows no tracked
  production/doc/script/Cargo/workflow/schema/task-brief changes. Any new files
  must be limited to this dispatch's own EXEC report, optional matching
  sidecar/log artifacts, and ignored `.ai` dispatch scratch.

## Constraints / Non-Goals

- Do not implement a fix.
- Do not edit source, tests, docs, Cargo files, workflows, schemas, scripts,
  scheduler config, task brief files, prior handoff packets, prior logs, or
  existing dispatch artifacts.
- Do not update `plans/PLAN.md`, `plans/IMPLEMENTATION.md`,
  `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, `change.md`, or any other
  documentation.
- Do not add or run new benchmarks, profilers, fixtures, generated reports,
  screenshots, ZIPs, or persistent scratch files in the repository.
- Do not run publish, push, merge, branch-delete, label-edit, GitHub issue
  mutation, scheduler registration, or scheduler modification commands.
- Do not create a branch, commit, PR, issue comment, label change, or close the
  GitHub issue.
- Do not run the autonomous queue or a nested dispatch as verification.
- Do not run full repository verification manually for this audit. The
  task-specific gates are read-only inspection plus diff/status checks; the
  orchestrator may still run its canonical gate automatically after execution.
- Do not infer invalidation cost from file names alone. Use line-cited code,
  docs, tests, measured baselines, and dispatch artifacts together.
- Do not convert a host-specific or dated benchmark into a current measured
  regression unless the report also cites provenance, date, and applicability.
- Treat unrelated dirty worktree state as out of scope. Do not clean, revert,
  or modify changes you did not make; report them if they affect confidence.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- `Get-Content -Raw AI_DISPATCH_AUTOMATION.md` and
  `Get-Content -Raw ai_handoffs/AI_HANDOFF_PROTOCOL.md` -> expected readable;
  use them to avoid mistaking orchestrator packet shape for dispatch
  incompleteness.
- `Get-Content -Raw plans/PLAN.md`,
  `Get-Content -Raw plans/IMPLEMENTATION.md`, and
  `Get-Content -Raw plans/BASELINE.md` -> expected readable; use them to
  establish Phase 9 pressure context and any baseline evidence.
- `Get-Content -Raw Status.md` and `Get-Content -Raw HANDOFF.md` -> expected
  readable; use them to establish current state, closed gates, known deferrals,
  and next-work posture.
- Inspect recent `change.md` entries with line numbers immediately before
  writing the final EXEC report. Use live line positions, not copied line
  numbers from prior packets or logs.
- Inspect graph-foundation invalidation surfaces read-only:
  `kernel/graph-foundation/src/invalidation.rs`,
  `kernel/graph-foundation/tests/invalidation_dag.rs`, and
  `tools/invalidation-profiler/src/main.rs`.
- Inspect CAD cache surfaces read-only:
  `crates/cad-core/src/tessellation/cache.rs`,
  `crates/cad-core/src/tessellation/mod.rs`,
  `crates/cad-core/tests/labeled_tessellation_pipeline.rs`,
  `crates/cad-projection/src/projection_cache/mod.rs`, and related
  `crates/cad-projection/src/**` call sites only as needed for line-cited
  reachability.
- Inspect frame-graph resource-map surfaces read-only:
  `crates/gfx/src/frame_graph/resource_map.rs`,
  `crates/gfx/src/frame_graph/texture_pool.rs`,
  `crates/gfx/src/frame_graph/buffer_pool.rs`,
  `crates/gfx/src/frame_graph/compile/**`,
  `crates/gfx/tests/frame_graph_smoke.rs`, and
  `crates/gfx/tests/frame_graph_umbrella_smoke.rs`.
- Inspect script hot-reload evidence read-only:
  `crates/script-bench/BASELINE.md`,
  `crates/script-bench/METHODOLOGY.md`,
  `crates/script-bench/benches/hot_reload_swap.rs`,
  `crates/script-host/src/swap.rs`,
  `crates/script-host/tests/swap_smoke.rs`, and
  `crates/hot-reload-watcher/src/lib.rs`.
- Inspect relevant existing dispatch artifacts read-only. Ignore `OLD/**`,
  `.ai/dispatch-*/**`, `.claude/worktrees/**`, `.git/**`, and build output
  when assessing repository state unless a specific artifact is cited as
  provenance.
- `git diff --check` -> expected exit 0.
- `git status --short --untracked-files=all` -> expected no tracked
  production/doc/script/Cargo/workflow/schema/task-brief changes. Any new files
  must be limited to this dispatch's own handoff/log artifacts and optional
  ignored `.ai` dispatch scratch.
- Static no-source-change inspection: run `git diff --name-only` and a
  targeted read-only inspection of protected tracked surfaces such as
  `crates`, `kernel`, `editor`, `runtime`, `tools`, `docs`, `plans`,
  `Status.md`, `HANDOFF.md`, `change.md`, `Cargo.toml`, `Cargo.lock`,
  `.github/workflows`, `.ai/dispatch.tasks.md`, `.ai/dispatch.verify.ps1`,
  `.ai/*.schema.json`, and root `*.ps1` automation scripts. Expected result:
  no tracked production/doc/script/Cargo/workflow/schema/task-brief diff.

## Halt Conditions

The Executor MUST halt and return `NEEDS_HUMAN` without editing any source,
docs, tests, scripts, schemas, workflows, Cargo files, scheduler config, task
briefs, prior packets, or prior logs if any of the following occur:

- Required evidence files are missing or unreadable.
- The required code-surface inspection cannot be completed read-only.
- The evidence cannot support exactly one
  `INVALIDATION_ECONOMICS_VERDICT` value from the allowed set.
- The evidence cannot support exactly one `COST_CLASS` value from the allowed
  set.
- The smallest safe follow-up cannot be narrowed to exactly one action.
- The smallest safe follow-up would require code/script/Cargo/workflow/schema
  changes but cannot be constrained to branch-mode pending later human
  authorization.
- The audit needs an implementation, documentation, Cargo, workflow, schema,
  script, scheduler, task-brief, prior-artifact, benchmark, profiler run, or
  GitHub mutation to be truthful.
- Required line citations cannot be produced for document-, code-, script-, or
  artifact-based claims.
- Measured evidence is too stale, host-specific, or ambiguous to support the
  requested classification, and structural evidence alone is also insufficient.
- Any GitHub mutation, push, merge, branch deletion, label change, issue
  comment, scheduler command, benchmark run, profiler run, or nested dispatch
  seems necessary.
- `git diff --check` fails for a reason caused by this dispatch.
- `git status --short --untracked-files=all` shows new or modified files
  outside this dispatch's own handoff/log artifacts and optional ignored `.ai`
  scratch, unless they are clearly pre-existing unrelated user changes.

If a halt condition fires, the Executor must still write the `EXECUTION_REPORT`
if possible, set header `STATUS: NEEDS_HUMAN`, include the exact halt reason
and current status output, and route the footer to `NEXT_ROLE: HUMAN_ARBITER`.

## Planner Notes

This is delegated-main smoke batch task 3 of 10 from `.ai/dispatch.tasks.md`,
but it is still audit-only. The desired outcome is not to manufacture a code
task; it is to decide whether invalidation economics now present a sharper
next dispatch than the other Phase 9 pressure axes.

Prefer current code and current line-numbered docs over historical summaries.
Prior dispatch logs can be used as provenance, but the report must label them
as historical unless the cited artifact itself proves current applicability.
For benchmark or baseline evidence, cite the methodology and the recorded
measurement lines, and say whether they are current enough to support a
measured-cost verdict.

Under the orchestrator protocol, no `REVIEW` or `CLOSEOUT` packet is expected
from this TASK. The queue's Codex control review is stored in the run
directory, not as a handoff packet.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-214
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Task Packet

DISPATCH_ID: ISSUE-20
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18_14-24-57+0300
RELATED_FILES:
- crates/cad-core/tests/phase_7_2_gate_closure.rs
- crates/cad-core/src/operators/sweep.rs
- crates/cad-core/src/topology/edge_resolve.rs
STATUS: OPEN

## Goal

Extend the Phase 7.2 rebuild gate in `crates/cad-core/tests/phase_7_2_gate_closure.rs` so the deterministic random chain generator includes `SweepOp` as one generated root kind. The gate must exercise Sweep chains through both face-ID and edge-ID rebuild checks: numeric profile/path-coordinate changes are topology-preserving, while profile-count or path-segment-count changes are topology-changing. This closes GitHub issue #20 without duplicating the implementation work already filed for #18/#19.

## Scope

### MAY edit
- `crates/cad-core/tests/phase_7_2_gate_closure.rs`

### MUST NOT edit
- `crates/cad-core/src/operators/sweep.rs`
- `crates/cad-core/src/topology/edge_resolve.rs`
- `crates/cad-core/src/topology/edge_id.rs`
- `crates/cad-core/src/topology/provider.rs`
- Any Sweep geometry, validation, face-label, face-ID, edge-ID, or resolver implementation
- Any production source file under `crates/cad-core/src/**`
- Any Cargo manifest, docs, schemas, scripts, `.gitignore`, snapshots, generated artifacts, or handoff packet other than the protocol-required `EXECUTION_REPORT`

### MAY add new files
- None, except the protocol-required `EXECUTION_REPORT` packet.

### MUST NOT add new files
- New tests outside `crates/cad-core/tests/phase_7_2_gate_closure.rs`
- New source modules, crates, docs, ADRs, architecture lints, schemas, scripts, snapshots, or additional handoff packets beyond the required `EXECUTION_REPORT`

## Deliverables

- Add `SweepOp` as a fifth generated root kind in the existing Phase 7.2 gate harness.
- Add only local test-harness code needed in `phase_7_2_gate_closure.rs`, such as imports for `SweepOp`/`Polyline3D`, a `RootKind::Sweep` case, a `RootSeed::Sweep` variant, and local helpers for valid Sweep profiles/paths.
- Build Sweep roots with a convex `Polygon2D` profile and a strictly monotonic-Z `Polyline3D` path so `SweepOp::evaluate` remains valid.
- Preserve the existing deterministic-seed structure and 100-chain x 10-rebuild stress shape unless a minimal local adjustment is needed to make Sweep coverage deterministic.
- Track Sweep coverage in the gate and assert that at least one generated chain has `RootKind::Sweep`.
- Update the root-kind sanity coverage test so it proves the generator visits all five root kinds, including Sweep.
- For Sweep topology-preserving mutations, change only numeric coordinates while keeping `profile_n` and path point/segment count unchanged. Valid examples: profile radius/coordinate changes, path XY drift, or path Z-coordinate changes that keep strict monotonic-Z and the same point count.
- For Sweep topology-changing mutations, change `profile_n` or the path point/segment count while keeping the resulting Sweep inputs valid.
- Keep the existing face+edge vector expectations: preserving mutations assert byte-identical face and edge ID vectors; changing mutations assert vector-level inequality.

## Acceptance Criteria

- `phase_7_2_gate_closure.rs` imports and constructs `SweepOp` through existing public `rge_cad_core` APIs only.
- The deterministic gate run sees at least one Sweep chain and fails loudly if Sweep is not generated.
- Sweep preserving mutations keep both `profile_n` and path segment count stable and assert unchanged face+edge IDs.
- Sweep changing mutations alter either profile count or path segment count and assert changed face+edge ID vectors.
- Existing Cuboid, Extrude, Revolve, Loft, and Transform-chain behavior remains covered and semantically unchanged except for any necessary enum/count updates caused by adding Sweep.
- No Sweep geometry, B-Rep identity, graph resolver, topology provider, or public API implementation is changed.
- The dispatch-specific test command passes:
  `cargo test -p rge-cad-core --test phase_7_2_gate_closure -- --nocapture`

## Constraints / Non-Goals

- Do not implement, repair, or redesign Sweep edge IDs. If Sweep edge IDs are unavailable, this task must block instead of expanding into issue #19's scope.
- Do not change Sweep mesh generation, monotonic-Z rules, winding correction, structural hash, face-label ordering, `SweepFaceTag`, `BRepFaceId`, `BRepEdgeId`, or `BRepEdgeProvider`.
- Do not change graph-level edge or face resolution behavior.
- Do not add a new random dependency or change Cargo manifests; keep using the existing local `TinyRng`.
- Do not broaden the Phase 7.2 gate to Boolean, Fillet, RoundFillet, or any operator not already in the harness plus Sweep.
- Do not weaken existing assertions or lower the mutation counts to make the test pass.

## Verification Gates

The Executor MUST run and document the result of each command in the `EXECUTION_REPORT`:

- `cargo test -p rge-cad-core --test phase_7_2_gate_closure -- --nocapture` -> expected exit 0, with output showing the Phase 7.2 gate completed and with the updated harness asserting at least one Sweep chain was generated.

If the executor also runs formatting or broader crate tests, document them, but do not edit files outside the MAY-edit list to satisfy those optional checks.

## Halt Conditions

The Executor MUST halt without committing and write an `EXECUTION_REPORT` with `STATUS: BLOCKED` if any of the following occur:

- `brep_edge_ids_for_node` cannot resolve `OperatorNode::Sweep` successfully, or `SweepOp` does not expose usable B-Rep edge IDs through the current public API. Report this as the missing #19 prerequisite; do not implement it here.
- Adding Sweep to the gate requires editing any file other than `crates/cad-core/tests/phase_7_2_gate_closure.rs`.
- A passing test appears to require changing Sweep geometry, validation, face IDs, edge IDs, topology resolver code, public topology traits, or Cargo manifests.
- Valid Sweep topology-preserving mutations cannot be expressed without changing `profile_n` or path point/segment count.
- Valid Sweep topology-changing mutations cannot be expressed by changing `profile_n` or path point/segment count.
- The dispatch-specific cargo test fails for behavior unrelated to this test-harness change after the implementation is complete.

## Planner Notes

Issue #19 is the prerequisite for this work: Sweep edge IDs must already be available before this gate can include Sweep. The current repository context appears to include `SweepOp` in the direct edge resolver path, but the executor must verify that through the public APIs while staying read-only outside the test file. If that prerequisite is missing or regressed, block and report it rather than reimplementing Sweep identity work.

Implementation guidance for the test harness: Sweep can mirror the existing Extrude/Loft profile generation for `profile_n`, and can use a local path helper that produces strictly increasing Z points. A path with `m` points has `m - 1` segments; preserving mutations must keep `m` fixed, while changing mutations may pick a different valid `m`.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-20
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

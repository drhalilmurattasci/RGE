# Correction Packet

DISPATCH_ID: ISSUE-30
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_01-29-37+0300
RELATED_FILES:
- crates/cad-projection/tests/face_selection_partition_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-30_TASK_2026-05-19_01-22-53+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-30_EXEC_2026-05-19_01-28-59+0300.md`
- Latest Review Report: none; the canonical post-execution verification gate failed before Codex control review.
- Prior CORRECT packets in this dispatch: none.
- Blocking verification gate: `cargo +nightly fmt --all -- --check`, started 2026-05-19T01:29:36.5613090+03:00, failed with incorrect newline style in `\\?\A:\RCAD\RGE\crates\cad-projection\tests\face_selection_partition_smoke.rs`.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. This correction round is based only on the post-execution verification
gate failure listed above.

1. **Normalize rustfmt newline style** - Verification gate finding: `cargo +nightly fmt --all -- --check` failed because `crates/cad-projection/tests/face_selection_partition_smoke.rs` has an incorrect newline style. Required change: convert only `crates/cad-projection/tests/face_selection_partition_smoke.rs` to the newline style accepted by rustfmt. Do not change Rust tokens, imports, test names, assertions, comments, production code, manifests, docs, scripts, schemas, or any other source file. Acceptance: `cargo +nightly fmt --all -- --check` exits 0, and the canonical `.ai/dispatch.verify.ps1` gate exits 0.

## Deferred Findings (NOT Approved for This Round)

1. **None** - No review findings are deferred. No REVIEW packet was produced before the verification gate failed, and the only approved correction is the format-only newline normalization above.

## Updated Acceptance Criteria

Unchanged from Task Packet, with one correction-round addition: the canonical
dispatch verification gate must pass after the newline-style normalization.
The accepted source edit for this correction round is limited to
`crates/cad-projection/tests/face_selection_partition_smoke.rs` newline style.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0, with no incorrect-newline-style report for `crates/cad-projection/tests/face_selection_partition_smoke.rs`.
- `.ai/dispatch.verify.ps1` -> expected exit 0 across the canonical format, architecture lint, supply-chain, workspace test, and doctest checks.
- `git status --short --untracked-files=all` -> expected tracked edits limited to the prior ISSUE-30 task change in `crates/cad-projection/tests/face_picking_smoke.rs`, the newline-style-only correction in `crates/cad-projection/tests/face_selection_partition_smoke.rs`, and required ISSUE-30 handoff packets/sidecars.

## Halt Conditions (Updated if Any)

Unchanged from Task Packet, plus halt and report back if fixing the newline
style would require semantic Rust changes, edits outside
`crates/cad-projection/tests/face_selection_partition_smoke.rs`, weakening or
removing any test, or modifying production source, manifests, docs, scripts,
schemas, or unrelated handoff packets. If `.ai/dispatch.verify.ps1` still
fails after the newline-style correction for any new reason, do not broaden
scope without fresh Planner guidance.

## Planner Notes

Verification stopped at the first canonical gate, before architecture,
supply-chain, workspace test, or doctest checks ran. Keep this correction
format-only. Leave the ISSUE-30 face-picking smoke behavior unchanged, write
the required follow-up EXECUTION_REPORT, and do not add any new source, test,
documentation, schema, script, or packet beyond the protocol-required
execution report for this correction round.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-30
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

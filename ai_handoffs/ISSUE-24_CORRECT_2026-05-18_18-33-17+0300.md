# Correction Packet

DISPATCH_ID: ISSUE-24
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18_18-33-17+0300
RELATED_FILES:
- crates/cad-projection/tests/transform_brep_face_lookup_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-24_TASK_2026-05-18_18-23-10+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-24_EXEC_2026-05-18_18-32-29+0300.md`
- Latest Review Report: none; this correction is based on the post-execution verification gate failure.
- Prior Correction Packets: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections and nothing more.

1. **Normalize blocking newline style** - Post-execution verification gate failure at `cargo +nightly fmt --all -- --check`. Required change: convert `crates/cad-projection/tests/transform_brep_face_lookup_smoke.rs` to the newline style accepted by rustfmt, without changing Rust tokens, assertions, imports, comments, test structure, or behavior. Acceptance: `cargo +nightly fmt --all -- --check` exits 0 and `git diff -- crates/cad-projection/tests/transform_brep_face_lookup_smoke.rs` shows only line-ending normalization.

## Deferred Findings (NOT Approved for This Round)

None. No other findings or fixes are approved for this correction round.

## Updated Acceptance Criteria

The Task Packet acceptance criteria remain unchanged except for this narrow correction: the Executor may edit `crates/cad-projection/tests/transform_brep_face_lookup_smoke.rs` only to normalize line endings required by the canonical verification gate. No semantic source change, test rewrite, production change, manifest change, documentation change, or additional file addition is approved.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0.
- `git diff -- crates/cad-projection/tests/transform_brep_face_lookup_smoke.rs` -> expected only line-ending normalization.
- `git status --short` -> expected to show the previously added ISSUE-24 smoke test plus the newline-normalized `crates/cad-projection/tests/transform_brep_face_lookup_smoke.rs`, with no other new changes from this correction.

## Halt Conditions (Updated if Any)

Halt and report back if normalizing `crates/cad-projection/tests/transform_brep_face_lookup_smoke.rs` requires any token-level Rust edit, if `cargo +nightly fmt --all -- --check` still fails after newline normalization, or if `.ai/dispatch.verify.ps1` fails for any reason other than the already reported newline-style issue.

## Planner Notes

The correction is limited to the verification blocker reported by the canonical gate: rustfmt rejected the newline style in `crates/cad-projection/tests/transform_brep_face_lookup_smoke.rs`. This packet does not approve revisiting the ISSUE-24 smoke implementation, broad formatting churn, production edits, or cleanup outside the one blocking file.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-24
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Task Packet

DISPATCH_ID: ISSUE-397
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-15_00-53-43+0300
RELATED_FILES:
- `crates/editor-shell/src/lifecycle/mod.rs`
- `crates/editor-shell/src/lifecycle/tests.rs`
- `.ai/dispatch.tasks.md`
- `Status.md`
- `HANDOFF.md`
- `plans/BASELINE.md`
- `change.md`
- `ai_handoffs/ISSUE-397_*`
STATUS: OPEN

## Goal

Implement GitHub issue #397 / task 143: reset pending `ViewportLeftDoubleClick` state when viewport mouse-wheel input is handled, before the existing wheel zoom/no-op result can pair with a later left press. A first viewport left press, then mouse wheel, then a second in-threshold viewport left press must not frame the scene or selected CAD bounds. Preserve existing wheel zoom and no-op cases, ordinary viewport double-click framing, focus-loss and cursor-left reset/cancellation, right-button orbit, and middle-button pan behavior.

As the final implementation step, re-arm the autonomous task brief by appending exactly one bounded task-144 docs/source-read-only audit, or record a bounded `NEEDS_HUMAN_RECORDED` line if no in-policy next task exists.

## Scope

### MAY edit
- `crates/editor-shell/src/lifecycle/mod.rs`
- `crates/editor-shell/src/lifecycle/tests.rs`
- `.ai/dispatch.tasks.md`
- `Status.md`
- `HANDOFF.md`
- `plans/BASELINE.md`
- `change.md`
- `ai_handoffs/ISSUE-397_EXEC_*.md`
- `ai_handoffs/ISSUE-397_EXEC_*.meta.json`
- `.ai/dispatch-ISSUE-397/**`
- `ai_dispatch_logs/log_ISSUE-397*.md`

### MUST NOT edit
- `crates/editor-shell/src/lifecycle/viewport_navigation.rs`
- `crates/editor-shell/src/render_path.rs`
- `crates/editor-shell/src/pick_path.rs`
- `crates/editor-shell/src/camera.rs`
- `crates/editor-ui/**`
- `crates/editor-egui-host/**`
- `crates/editor-actions/**`
- `crates/cad-core/**`
- `crates/cad-projection/**`
- `crates/plugin-discovery/**`
- `runtime/**`
- `Cargo.toml`
- `Cargo.lock`
- `**/Cargo.toml`
- `.github/workflows/**`
- `.ai/dispatch.verify.ps1`
- `.ai/*.schema.json`
- `Invoke-AiDispatch*.ps1`
- `Register-AiDispatchSchedule.ps1`
- `Watch-AiDispatch.ps1`
- `Watch-DispatchStages.ps1`
- `Wait-GitHubActions.ps1`
- `Get-AiDispatchHealth.ps1`
- `Get-AiDispatchTrends.ps1`
- `new-handoff.ps1`
- `ai_handoffs/templates/**`
- Any source, docs, schemas, scripts, Cargo metadata, handoff packets, run logs, or audit artifacts outside the MAY-edit and MAY-add surfaces for ISSUE-397.

### MAY add new files
- `ai_handoffs/ISSUE-397_EXEC_*.md`
- `ai_handoffs/ISSUE-397_EXEC_*.meta.json`
- `.ai/dispatch-ISSUE-397/**`
- `ai_dispatch_logs/log_ISSUE-397*.md`

### MUST NOT add new files
- `crates/**`
- `docs/**`
- `plans/**`
- `tasks/**`
- `.github/**`
- `ai_handoffs/ISSUE-398_*`
- New Rust files, tests, fixtures, crates, Cargo files, workflows, schemas, ADRs, architecture-lint files, packet templates, dispatch automation scripts, or generated artifacts for any dispatch other than ISSUE-397.

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - crates/editor-shell/src/lifecycle/mod.rs
  - crates/editor-shell/src/lifecycle/tests.rs
  - .ai/dispatch.tasks.md
  - Status.md
  - HANDOFF.md
  - plans/BASELINE.md
  - change.md
  - ai_handoffs/ISSUE-397_EXEC_*.md
  - ai_handoffs/ISSUE-397_EXEC_*.meta.json
  - .ai/dispatch-ISSUE-397/**
  - ai_dispatch_logs/log_ISSUE-397*.md
MUST_NOT_EDIT:
  - crates/editor-shell/src/lifecycle/viewport_navigation.rs
  - crates/editor-shell/src/render_path.rs
  - crates/editor-shell/src/pick_path.rs
  - crates/editor-shell/src/camera.rs
  - crates/editor-ui/**
  - crates/editor-egui-host/**
  - crates/editor-actions/**
  - crates/cad-core/**
  - crates/cad-projection/**
  - crates/plugin-discovery/**
  - runtime/**
  - Cargo.toml
  - Cargo.lock
  - **/Cargo.toml
  - .github/workflows/**
  - .ai/dispatch.verify.ps1
  - .ai/*.schema.json
  - Invoke-AiDispatch*.ps1
  - Register-AiDispatchSchedule.ps1
  - Watch-AiDispatch.ps1
  - Watch-DispatchStages.ps1
  - Wait-GitHubActions.ps1
  - Get-AiDispatchHealth.ps1
  - Get-AiDispatchTrends.ps1
  - new-handoff.ps1
  - ai_handoffs/templates/**
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Deliverables

- A minimal editor-shell lifecycle change in `crates/editor-shell/src/lifecycle/mod.rs` that resets pending `ViewportLeftDoubleClick` state on viewport mouse-wheel handling before a later left press can observe stale first-press state.
- Focused lifecycle test coverage in `crates/editor-shell/src/lifecycle/tests.rs` for first viewport left press -> mouse wheel -> second in-threshold viewport left press, proving the second press does not frame scene-wide camera bounds or selected CAD bounds.
- Existing wheel zoom tests still cover vertical zoom-in/out and horizontal-only, false-viewport-hit, no-cursor, and no-host no-op behavior, with those no-op cases now allowed only to additionally clear pending left-double-click state.
- Existing ordinary left-double-click, selected-CAD double-click, focus-loss reset/cancellation, cursor-left reset/cancellation, right-button orbit, and middle-button pan tests remain unchanged in intent.
- Task/status bookkeeping updates marking task 143 complete in `.ai/dispatch.tasks.md`, `Status.md`, `HANDOFF.md`, `plans/BASELINE.md`, and `change.md`.
- Self-rearm update in `.ai/dispatch.tasks.md`: exactly one bounded next-task-source audit as task 144 named `Post-wheel-double-click-reset Phase 9 next-task source audit`, or one `NEEDS_HUMAN_RECORDED` line if no bounded in-policy next task exists.
- An ISSUE-397 EXEC handoff packet documenting changed files, verification results, the self-rearm outcome, and any deviations.

## Acceptance Criteria

- `WindowEvent::MouseWheel` or the existing `zoom_camera_for_viewport_mouse_wheel` lifecycle helper resets pending `ViewportLeftDoubleClick` state before handling or completing the wheel zoom/no-op decision.
- A first viewport left press, then any handled mouse-wheel input, then a second in-threshold viewport left press does not call `frame_cad_selection_or_reset_camera()`, `reset_camera()`, or selected-CAD framing through stale double-click state.
- The new tests distinguish stale scene-wide framing and stale selected-CAD framing from no-op behavior, and fail against the current pre-fix behavior.
- Existing vertical mouse-wheel zoom over the viewport still zooms in/out and preserves camera invariants.
- Existing horizontal-only wheel input, false-viewport-hit wheel input, no-cursor wheel input, and no-host wheel input remain camera no-ops except for clearing pending left-double-click state.
- Ordinary valid consecutive left presses over the viewport still use the existing `ViewportLeftDoubleClick` detector, preserve face-pick handling order, and frame selected CAD bounds first before falling back to scene-wide `reset_camera()`.
- Focus-loss and cursor-left behavior remain unchanged: both still reset pending left-double-click state and keep their existing active orbit/pan cancellation and cursor-grab release semantics.
- Right-button orbit and middle-button pan behavior remain unchanged, including their existing stale-double-click reset behavior on non-left mouse buttons.
- No command routing, menu, shortcut, command enum, CommandBus, CAD/projection mutation, save/load, undo/dirty, camera math, viewport hit-test, face-pick policy, plugin, clipboard, Cargo, workflow, schema, automation, or packet-template behavior is changed.
- The self-rearm edit appends exactly one bounded task-144 audit with explicit `MAY edit`, `MUST NOT edit`, `Done criteria`, `Verification`, and `Halt conditions` sections, plus the copied Self-rearm final-step requirement, or appends exactly one `NEEDS_HUMAN_RECORDED` line instead.

## Current-State Claims: Falsification Required

Every negative or unchanged assertion this packet makes about current repository state lists its inverse-search command and observed result here. The Executor must re-run and record any claim it relies on after editing.

- Claim: The current mouse-wheel event path handles viewport wheel zoom/no-op without resetting pending viewport left-double-click state.
  Falsifying search: `rg -n "WindowEvent::MouseWheel|zoom_camera_for_viewport_mouse_wheel|reset_viewport_left_double_click\\(\\)" crates/editor-shell/src/lifecycle/mod.rs` -> current matches show `zoom_camera_for_viewport_mouse_wheel` at `mod.rs:2145`, `WindowEvent::MouseWheel` at `mod.rs:2841-2843`, and `reset_viewport_left_double_click()` only on focus loss, cursor left, and non-left mouse-input branches at `mod.rs:2301`, `mod.rs:2312`, and `mod.rs:2875-2892`; no reset occurs in the wheel branch or helper.
- Claim: There is no current wheel/double-click stale-reset coverage.
  Falsifying search: `rg -n "wheel.*double|double.*wheel|mouse_wheel.*left_double|left_double.*mouse_wheel|viewport_mouse_wheel.*double|double_click.*wheel" crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/tests.rs` -> no matches, rg exit 1.
- Claim: Existing wheel tests cover direction mapping, positive/negative zoom, horizontal-only no-op, false-viewport-hit no-op, no-cursor no-op, and no-host no-op.
  Falsifying search: `rg -n "zoom_camera_for_viewport_mouse_wheel|viewport_mouse_wheel_signs|viewport_mouse_wheel_positive|viewport_mouse_wheel_negative|viewport_mouse_wheel_horizontal|viewport_mouse_wheel_false|viewport_mouse_wheel_no_cursor" crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/tests.rs` -> current matches include the helper at `mod.rs:2145`, event routing at `mod.rs:2843`, and wheel tests at `tests.rs:1239`, `tests.rs:1280`, `tests.rs:1290`, `tests.rs:1303`, `tests.rs:1317`, and `tests.rs:1331`.
- Claim: Existing ordinary viewport double-click, selected-CAD double-click, focus-loss stale reset, and cursor-left stale reset coverage already exists and should remain valid.
  Falsifying search: `rg -n "viewport_left_double_click_(second|selected|multiple|cad_no|unresolved|empty|outside|time|single)|viewport_focus_loss_resets_stale_left_double_click|viewport_cursor_left_resets_stale_left_double_click" crates/editor-shell/src/lifecycle/tests.rs` -> current matches include ordinary and selected-CAD double-click tests at `tests.rs:891-1073`, focus-loss stale reset tests at `tests.rs:1813` and `tests.rs:1834`, and cursor-left stale reset coverage at `tests.rs:1967`.
- Claim: The task brief currently has task 141 done, task 142 done, task 143 open, and no task 144 heading.
  Falsifying search: `rg -n "^141\\.|^142\\.|^143\\.|^144\\." .ai/dispatch.tasks.md` -> current matches show task 141 done at `.ai/dispatch.tasks.md:13514`, task 142 done at `.ai/dispatch.tasks.md:13600`, and task 143 open at `.ai/dispatch.tasks.md:13788`; no `^144.` match.
- Claim: Queue/already-filed state must not be fetched live inside the executor sandbox.
  Falsifying read: the dispatcher snapshot embedded in the ISSUE-397 goal states it was generated by `Invoke-AiDispatchAuto.ps1` at `2026-06-15T00:53:13.9884725+03:00` before ISSUE-397 was created, for repo `RustCADs/RGE`, and reports no open `ai-dispatch` issues, no open failed autonomous issues, and autonomous issues already filed through closed #396; this packet makes live GitHub access a halt condition rather than a verification step.

## Constraints / Non-Goals

- Do not edit `crates/editor-shell/src/lifecycle/viewport_navigation.rs`; use the existing lifecycle-owned reset helper and detector state from `mod.rs`.
- Do not change double-click thresholds, detector semantics, selected-CAD framing semantics, scene-wide `reset_camera()` semantics, face-pick ordering, or mouse-button reset behavior.
- Do not change wheel delta interpretation, zoom factors, camera math, viewport hit testing, cursor-position tracking, or no-op camera behavior for non-zooming wheel cases.
- Do not alter focus-loss, focus-gained, cursor-left, active orbit, active pan, or cursor-grab release behavior.
- Do not add new public APIs, commands, menu items, shortcuts, command-palette entries, route owners, dependencies, Cargo metadata, schemas, workflows, automation, or packet-template changes.
- Do not use `gh`, browser, network, or live GitHub state for queue/already-filed claims; use the dispatcher-provided GitHub snapshot embedded in the issue body if such evidence is needed.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `cargo test -p rge-editor-shell --lib viewport_mouse_wheel` -> expected exit 0, including the new stale double-click reset coverage and existing wheel zoom/no-op tests.
- `cargo test -p rge-editor-shell --lib viewport_left_double_click` -> expected exit 0.
- `cargo test -p rge-editor-shell --lib viewport_focus_loss` -> expected exit 0.
- `cargo test -p rge-editor-shell --lib viewport_cursor_left` -> expected exit 0.
- `cargo test -p rge-editor-shell --lib viewport_right_button_orbit` -> expected exit 0.
- `cargo test -p rge-editor-shell --lib viewport_middle_button_pan` -> expected exit 0.
- `rg -n "^141\\.|^142\\.|^143\\.|^144\\.|^145\\.|Post-wheel-double-click-reset|Self-re-arm|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md` -> expected task 143 marked complete and exactly one task 144 audit appended, or exactly one `NEEDS_HUMAN_RECORDED` line if no bounded in-policy next task exists.
- `git diff --name-only` -> expected only paths within the MAY-edit list plus generated ISSUE-397 artifacts.
- `git diff --check` -> expected exit 0.
- `.\new-handoff.ps1 -Finalize -PacketPath $execPacket -DryRun`, where `$execPacket` is the actual ISSUE-397 EXEC packet path.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- The stale double-click reset cannot be implemented in `crates/editor-shell/src/lifecycle/mod.rs` using the existing lifecycle reset helper.
- The fix requires editing any path outside the MAY-edit list, including `crates/editor-shell/src/lifecycle/viewport_navigation.rs`.
- The implementation would change command routing, menus, shortcuts, command enum variants, camera math, viewport hit testing, face-pick policy, CAD projection or CommandBus state, save/load behavior, plugin runtime/discovery, OS clipboard behavior, Cargo metadata, workflows, schemas, automation, packet templates, or architecture-lint configuration.
- Preserving existing wheel zoom/no-op behavior conflicts with resetting pending left-double-click state on wheel handling.
- Preserving focus-loss reset/cancellation, cursor-left reset/cancellation, right-button orbit, or middle-button pan behavior conflicts with the requested wheel reset.
- Focused tests cannot distinguish stale scene-wide or selected-CAD double-click framing from no-op behavior after wheel input.
- More than one behavioral follow-up is required to make the wheel double-click reset coherent.
- The self-rearm audit task 144 cannot be appended without crossing the docs/source-read-only scope required by the Self-rearm protocol; append one `NEEDS_HUMAN_RECORDED` line instead of forcing a task.
- Verification fails for reasons inside the MAY-edit surface and cannot be fixed without expanding scope.
- Verification fails only because of an unrelated pre-existing issue outside this dispatch's scope; document the failure and halt rather than editing outside scope.

## Self-Rearm Requirements For Task 144

After the implementation is complete and verified, follow the Self-re-arm protocol in `.ai/dispatch.tasks.md`: append exactly one bounded next-task-source AUDIT task as task 144, named `Post-wheel-double-click-reset Phase 9 next-task source audit`, or, if no bounded in-policy next task exists, append one `NEEDS_HUMAN_RECORDED:` line with the actual ISO date and source-grounded reason filled in.

The task-144 audit must be docs/source-read-only. Mirror task 142's audit structure and scope discipline, including candidate-class source evidence, dispatcher-snapshot discipline for queue/already-filed claims, status-doc cross-checks, and explicit `MAY edit`, `MUST NOT edit`, `Done criteria`, `Verification`, and `Halt conditions` sections. Do not copy task 142's task-number-specific criteria: task 144 is itself a self-re-arming audit, so its done criteria MUST require appending exactly one bounded FEATURE task 145 carrying a copied Self-rearm final-step requirement, or a single `NEEDS_HUMAN_RECORDED:` line. Copy this Self-rearm final-step requirement into the audit task 144 you author. Edit `.ai/dispatch.tasks.md` to do this; it is already in this packet's MAY-edit list.

## Planner Notes

The smallest expected implementation is to call `reset_viewport_left_double_click()` in the `WindowEvent::MouseWheel` branch before `zoom_camera_for_viewport_mouse_wheel(&delta, over_viewport)`, or at the start of the helper if tests can still directly exercise all existing wheel cases. Resetting before the existing zoom/no-op match keeps wheel behavior unchanged while preventing a stale first left press from surviving wheel input.

The regression tests can mirror the existing focus-loss stale reset tests, replacing `handle_window_focus_change(false)` with mouse-wheel handling. Include both scene-wide and selected-CAD variants so the test fails whether stale state would frame all or frame selected CAD bounds.

Use the dispatcher-provided GitHub state snapshot embedded in the issue body for any queue/already-filed evidence. It was generated by `Invoke-AiDispatchAuto.ps1` at `2026-06-15T00:53:13.9884725+03:00` before ISSUE-397 was created and reports no open `ai-dispatch` issue, no open failed autonomous issue, and autonomous issues already filed through closed #396.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-397
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Task Packet

DISPATCH_ID: ISSUE-395
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-14_23-13-44+0300
RELATED_FILES:
- `crates/editor-shell/src/lifecycle/mod.rs`
- `crates/editor-shell/src/lifecycle/tests.rs`
- `.ai/dispatch.tasks.md`
- `Status.md`
- `HANDOFF.md`
- `plans/BASELINE.md`
- `change.md`
- `ai_handoffs/ISSUE-395_*`
STATUS: OPEN

## Goal

Implement GitHub issue #395 / task 141: reset pending viewport left-double-click state when the window loses focus, so a left press before `WindowEvent::Focused(false)` cannot combine with a later in-threshold left press after focus returns to frame the scene or selected CAD bounds. Preserve the existing focus-loss drag cancellation and cursor-grab release behavior, and keep focus-gained handling as a no-op for active drag state.

As the final implementation step, re-arm the autonomous task brief by appending exactly one bounded task-142 audit, or record a bounded `NEEDS_HUMAN_RECORDED` line if no in-policy next task exists.

## Scope

### MAY edit
- `crates/editor-shell/src/lifecycle/mod.rs`
- `crates/editor-shell/src/lifecycle/tests.rs`
- `.ai/dispatch.tasks.md`
- `Status.md`
- `HANDOFF.md`
- `plans/BASELINE.md`
- `change.md`
- `ai_handoffs/ISSUE-395_EXEC_*.md`
- `ai_handoffs/ISSUE-395_EXEC_*.meta.json`
- `.ai/dispatch-ISSUE-395/**`
- `ai_dispatch_logs/log_ISSUE-395*.md`

### MUST NOT edit
- `crates/editor-shell/src/lifecycle/viewport_navigation.rs`
- `crates/editor-shell/src/render_path.rs`
- `crates/editor-shell/src/pick_path.rs`
- `crates/editor-shell/src/camera.rs`
- `crates/editor-ui/**`
- `crates/editor-egui-host/**`
- `crates/editor-actions/**`
- `crates/cad-core/**`
- `crates/cad-projection/**`
- `crates/plugin-discovery/**`
- `runtime/**`
- `Cargo.toml`
- `Cargo.lock`
- `**/Cargo.toml`
- `.github/workflows/**`
- `.ai/dispatch.verify.ps1`
- `.ai/*.schema.json`
- `Invoke-AiDispatch*.ps1`
- `Register-AiDispatchSchedule.ps1`
- `Watch-AiDispatch.ps1`
- `Watch-DispatchStages.ps1`
- `Wait-GitHubActions.ps1`
- `Get-AiDispatchHealth.ps1`
- `Get-AiDispatchTrends.ps1`
- `new-handoff.ps1`
- `ai_handoffs/templates/**`
- Any unrelated existing `ai_handoffs/**`, `.ai/dispatch-*/**`, or `ai_dispatch_logs/**` artifact outside ISSUE-395 output.

### MAY add new files
- `ai_handoffs/ISSUE-395_EXEC_*.md`
- `ai_handoffs/ISSUE-395_EXEC_*.meta.json`
- `.ai/dispatch-ISSUE-395/**`
- `ai_dispatch_logs/log_ISSUE-395*.md`

### MUST NOT add new files
- `crates/**`
- `docs/**`
- `plans/**`
- `tasks/**`
- `.github/**`
- New Rust files, tests, fixtures, crates, Cargo files, workflows, schemas, ADRs, architecture-lint files, packet templates, dispatch automation scripts, or generated artifacts for any dispatch other than ISSUE-395.

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - crates/editor-shell/src/lifecycle/mod.rs
  - crates/editor-shell/src/lifecycle/tests.rs
  - .ai/dispatch.tasks.md
  - Status.md
  - HANDOFF.md
  - plans/BASELINE.md
  - change.md
  - ai_handoffs/ISSUE-395_EXEC_*.md
  - ai_handoffs/ISSUE-395_EXEC_*.meta.json
  - .ai/dispatch-ISSUE-395/**
  - ai_dispatch_logs/log_ISSUE-395*.md
MUST_NOT_EDIT:
  - crates/editor-shell/src/lifecycle/viewport_navigation.rs
  - crates/editor-shell/src/render_path.rs
  - crates/editor-shell/src/pick_path.rs
  - crates/editor-shell/src/camera.rs
  - crates/editor-ui/**
  - crates/editor-egui-host/**
  - crates/editor-actions/**
  - crates/cad-core/**
  - crates/cad-projection/**
  - crates/plugin-discovery/**
  - runtime/**
  - Cargo.toml
  - Cargo.lock
  - **/Cargo.toml
  - .github/workflows/**
  - .ai/dispatch.verify.ps1
  - .ai/*.schema.json
  - Invoke-AiDispatch*.ps1
  - Register-AiDispatchSchedule.ps1
  - Watch-AiDispatch.ps1
  - Watch-DispatchStages.ps1
  - Wait-GitHubActions.ps1
  - Get-AiDispatchHealth.ps1
  - Get-AiDispatchTrends.ps1
  - new-handoff.ps1
  - ai_handoffs/templates/**
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Deliverables

- A minimal editor-shell lifecycle change in `crates/editor-shell/src/lifecycle/mod.rs` that resets pending `ViewportLeftDoubleClick` state on `handle_window_focus_change(false)` / `WindowEvent::Focused(false)`.
- Focused lifecycle test coverage in `crates/editor-shell/src/lifecycle/tests.rs` for first viewport left press -> focus loss -> second in-threshold viewport left press, proving the second press does not frame the scene-wide camera or selected CAD bounds.
- Existing focus-loss drag tests still cover active orbit cancellation, active pan cancellation, combined drag cancellation with a single cursor-grab release, no-release behavior when no drag is active, and focus-gained no-op behavior.
- Task/status bookkeeping updates marking task 141 complete in `.ai/dispatch.tasks.md`, `Status.md`, `HANDOFF.md`, `plans/BASELINE.md`, and `change.md`.
- Self-rearm update in `.ai/dispatch.tasks.md`: exactly one bounded next-task-source audit as task 142 named `Post-focus-loss-double-click-reset Phase 9 next-task source audit`, or one `NEEDS_HUMAN_RECORDED` line if no bounded in-policy next task exists.
- An ISSUE-395 EXEC handoff packet documenting changed files, verification results, the self-rearm outcome, and any deviations.

## Acceptance Criteria

- `handle_window_focus_change(false)` resets pending `ViewportLeftDoubleClick` state before or while preserving the existing `cancel_viewport_drags_for_focus_loss()` behavior.
- `WindowEvent::Focused(false)` continues to route through `handle_window_focus_change(false)`.
- `handle_window_focus_change(true)` remains a no-op for active viewport drag state and must not reset, stop, or release active drag state.
- Active orbit and pan drags still cancel on focus loss, and cursor-grab release behavior remains exactly as tested by the existing `viewport_focus_loss_*` lifecycle tests.
- Cursor-left behavior remains unchanged: it still clears `cursor_pos`, resets pending left double-click state, and cancels active viewport drags.
- Existing viewport left-double-click behavior remains unchanged except for the new focus-loss stale-state reset: qualifying double-clicks still use the existing detector, still preserve face-pick handling order, and still frame selected CAD bounds first before falling back to scene-wide `reset_camera()`.
- The new test must distinguish a stale double-click camera frame from no-op behavior, and must fail on the current pre-fix behavior.
- No command routing, menu, shortcut, command enum, CommandBus, CAD/projection mutation, save/load, undo/dirty, camera math, viewport hit-test, face-pick policy, wheel zoom, right-button orbit math, middle-button pan math, plugin, clipboard, Cargo, workflow, schema, automation, or packet-template behavior is changed.
- The self-rearm edit appends exactly one bounded task-142 audit with explicit `MAY edit`, `MUST NOT edit`, `Done criteria`, `Verification`, and `Halt conditions` sections, plus the copied Self-rearm final-step requirement, or appends exactly one `NEEDS_HUMAN_RECORDED` line instead.

## Current-State Claims: Falsification Required

Every negative or unchanged assertion this packet makes about current repository state lists its inverse-search command and observed result here. The Executor must re-run and record any claim it relies on after editing.

- Claim: The current focus-loss path cancels viewport drags but does not reset pending viewport left-double-click state.
  Falsifying search: `rg -n "handle_window_focus_change|cancel_viewport_drags_for_focus_loss|handle_cursor_left|reset_viewport_left_double_click" crates/editor-shell/src/lifecycle/mod.rs` -> current matches show `handle_window_focus_change(false)` calling only `cancel_viewport_drags_for_focus_loss()` at `mod.rs:2299-2303`, while `handle_cursor_left()` calls `reset_viewport_left_double_click()` at `mod.rs:2309-2312`.
- Claim: There is existing focused coverage for focus-loss drag cancellation and focus-gained no-op behavior.
  Falsifying search: `rg -n "viewport_focus_loss" crates/editor-shell/src/lifecycle/tests.rs` -> current matches include orbit cancellation, pan cancellation, combined-drag single release, no-active-drag no release, and `viewport_focus_loss_focused_true_preserves_active_drag_state` around `tests.rs:1711-1808`.
- Claim: There is existing cursor-left stale double-click reset coverage, but no focus-loss stale double-click reset coverage.
  Falsifying searches: `rg -n "viewport_cursor_left_resets_stale_left_double_click" crates/editor-shell/src/lifecycle/tests.rs` -> current match at `tests.rs:1914`; `rg -n "focus_loss.*double|double_click.*focus|reset_viewport_left_double_click\\(\\).*focus|Focused\\(false\\).*double" crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/tests.rs` -> no matches, rg exit 1.
- Claim: The current double-click path uses the existing `ViewportLeftDoubleClick` detector, runs face-pick handling before framing, and frames via selected CAD bounds or scene-wide reset.
  Falsifying search: `rg -n "ViewportLeftDoubleClick|handle_viewport_left_press|should_fire_face_pick|handle_left_click|frame_cad_selection_or_reset_camera|reset_camera" crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/tests.rs` -> current matches include the detector field at `mod.rs:600-604`, `handle_viewport_left_press` at `mod.rs:2157-2174`, and selected-CAD double-click tests beginning at `tests.rs:910`.
- Claim: The task brief currently contains task 141 with the required self-rearm language for task 142.
  Falsifying search: `rg -n "^141\\.|Self-re-arm|Post-focus-loss-double-click-reset|^142\\." .ai/dispatch.tasks.md` -> current matches include task 141 and the task-142 self-rearm requirement; no task 142 heading exists before this dispatch.

## Constraints / Non-Goals

- Do not edit `viewport_navigation.rs`; use the existing `reset_viewport_left_double_click()` / `ViewportLeftDoubleClick::reset()` path from lifecycle code.
- Do not change double-click thresholds, detector semantics, selected-CAD framing semantics, scene-wide `reset_camera()` semantics, or face-pick ordering.
- Do not alter focus-gained behavior. `handle_window_focus_change(true)` must remain a no-op for active drag state.
- Do not change cursor-left semantics beyond ensuring the new focus-loss behavior is consistent with the stale double-click reset already covered there.
- Do not add new public APIs, commands, menu items, shortcuts, command-palette entries, route owners, dependencies, Cargo metadata, schemas, workflows, automation, or packet-template changes.
- Do not use `gh`, browser, network, or live GitHub state for queue/already-filed claims; use the dispatcher-provided GitHub snapshot embedded in the issue body if such evidence is needed.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `cargo test -p rge-editor-shell --lib viewport_focus_loss` -> expected exit 0, including the new stale double-click reset coverage and existing drag/focus-gained coverage.
- `cargo test -p rge-editor-shell --lib viewport_cursor_left` -> expected exit 0.
- `cargo test -p rge-editor-shell --lib viewport_left_double_click` -> expected exit 0.
- `rg -n "^141\\.|^142\\.|^143\\.|Post-focus-loss-double-click-reset|Self-re-arm|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md` -> expected task 141 marked complete and exactly one task 142 audit appended, or exactly one `NEEDS_HUMAN_RECORDED` line if no bounded in-policy next task exists.
- `git diff --name-only` -> expected only paths within the MAY-edit list plus generated ISSUE-395 artifacts.
- `git diff --check` -> expected exit 0.
- `.\new-handoff.ps1 -Finalize -PacketPath $execPacket -DryRun`, where `$execPacket` is the actual ISSUE-395 EXEC packet path.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- The stale double-click reset cannot be implemented in `crates/editor-shell/src/lifecycle/mod.rs` using the existing lifecycle reset helper.
- The fix requires editing any path outside the MAY-edit list, including `crates/editor-shell/src/lifecycle/viewport_navigation.rs`.
- The implementation would change command routing, menus, shortcuts, command enum variants, camera math, viewport hit testing, face-pick policy, CAD projection or CommandBus state, save/load behavior, plugin runtime/discovery, OS clipboard behavior, Cargo metadata, workflows, schemas, automation, packet templates, or architecture-lint configuration.
- Preserving focus-loss drag cancellation or cursor-grab release behavior conflicts with resetting pending left double-click state.
- Preserving focus-gained no-op behavior conflicts with the requested focus-loss reset.
- Focused tests cannot distinguish stale double-click framing from no-op behavior after focus loss.
- More than one behavioral follow-up is required to make the stale focus-loss double-click reset coherent.
- The self-rearm audit task 142 cannot be appended without crossing the docs/source-read-only scope required by the Self-rearm protocol; append one `NEEDS_HUMAN_RECORDED` line instead of forcing a task.
- Verification fails for reasons inside the MAY-edit surface and cannot be fixed without expanding scope.
- Verification fails only because of an unrelated pre-existing issue outside this dispatch's scope; document the failure and halt rather than editing outside scope.

## Self-Rearm Requirements For Task 142

After the implementation is complete and verified, follow the Self-rearm protocol in `.ai/dispatch.tasks.md`: append exactly one bounded next-task-source AUDIT task as task 142, named `Post-focus-loss-double-click-reset Phase 9 next-task source audit`, or, if no bounded in-policy next task exists, append one `NEEDS_HUMAN_RECORDED:` line with the actual ISO date and source-grounded reason filled in.

The task-142 audit must be docs/source-read-only. Mirror task 140's audit structure and scope discipline, including candidate-class source evidence, dispatcher-snapshot discipline for queue/already-filed claims, status-doc cross-checks, and explicit `MAY edit`, `MUST NOT edit`, `Done criteria`, `Verification`, and `Halt conditions` sections. Do not copy task 140's pre-protocol no-successor criteria: do not require "no task 143" and do not require `rg ^143` to return no matches.

Because task 142 is itself a self-rearming audit, its done criteria MUST require appending exactly one bounded FEATURE task 143 carrying a copied Self-rearm final-step requirement. Copy this Self-rearm final-step requirement into the audit task 142 you author. Edit `.ai/dispatch.tasks.md` to do this; it is already in this packet's MAY-edit list.

## Planner Notes

The smallest expected implementation is to call `reset_viewport_left_double_click()` from the `!focused` branch of `handle_window_focus_change`, before or alongside `cancel_viewport_drags_for_focus_loss()`. Keep the existing cancellation helper behavior intact.

The regression test can mirror the existing `viewport_cursor_left_resets_stale_left_double_click` shape, replacing `handle_cursor_left()` with `handle_window_focus_change(false)`. Add selected-CAD coverage if needed to prove selected bounds are not framed; at minimum the test must prove a second in-threshold press after focus loss does not frame either the scene-wide camera or selected CAD bounds.

Use the dispatcher-provided GitHub state snapshot embedded in the issue body for any queue/already-filed evidence. It was generated by `Invoke-AiDispatchAuto.ps1` at `2026-06-14T23:13:13.0084171+03:00` before ISSUE-395 was created and reports no open `ai-dispatch` issue, no open failed autonomous issue, and autonomous issues already filed through closed #394.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-395
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

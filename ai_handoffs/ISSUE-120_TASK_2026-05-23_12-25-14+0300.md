# Task Packet

DISPATCH_ID: ISSUE-120
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-23_12-25-14+0300
RELATED_FILES:
- crates/script-bench/BASELINE.md
- crates/script-bench/METHODOLOGY.md
- crates/script-bench/src/script_host.rs
- crates/script-bench/Cargo.toml
- plans/IMPLEMENTATION.md
- Status.md
- HANDOFF.md
- change.md
- ai_handoffs/ISSUE-120_EXEC_*.md
STATUS: OPEN

## Goal

Perform GitHub issue #120 as a read-only memory-soak reconciliation audit for
`script-bench` `peak_rss_bytes` / `vss_delta_bytes` evidence and stale deferral
wording. The Executor must distinguish committed harness support from committed
formal one-hour result evidence, identify which deferral text is stale versus
still-current history, and recommend exactly one smallest follow-up. The
Executor must not edit source, tests, docs, manifests, scripts, workflows, or
existing packets.

## Scope

### MAY edit
- None.

### MUST NOT edit
- `crates/script-bench/BASELINE.md`
- `crates/script-bench/METHODOLOGY.md`
- `crates/script-bench/src/script_host.rs`
- `crates/script-bench/Cargo.toml`
- `plans/IMPLEMENTATION.md`
- `Status.md`
- `HANDOFF.md`
- `change.md`
- Any other source, test, bench, fixture, documentation, manifest, lockfile,
  workflow, script, schema, status, gitignore, lint, dependency, or repository
  metadata file.
- Any existing handoff packet, including this TASK packet.
- Any handoff packet other than the single required
  `ai_handoffs/ISSUE-120_EXEC_*.md` execution report.

### MAY add new files
- Exactly one execution report matching `ai_handoffs/ISSUE-120_EXEC_*.md`.
  Do not hand-create sidecars; if the orchestration layer finalizes the EXEC
  packet, its generated `.meta.json` sidecar is automation output rather than
  task content.

### MUST NOT add new files
- New source files, tests, docs, fixtures, generated artifacts, persisted logs,
  screenshots, Cargo entries, dependency files, crates, architecture lints,
  schemas, scripts, workflows, TASK packets, REVIEW packets, CORRECT packets,
  CLOSEOUT packets, or follow-up TASK packets.

## Deliverables

- One `EXECUTION_REPORT` packet under `ai_handoffs/` and no other repository
  changes.
- A command log in the EXEC report listing every local read/search/status
  command used for the audit.
- Mandatory read-only inspection of all of these files:
  `crates/script-bench/BASELINE.md`,
  `crates/script-bench/METHODOLOGY.md`,
  `crates/script-bench/src/script_host.rs`,
  `crates/script-bench/Cargo.toml`,
  `plans/IMPLEMENTATION.md`,
  `Status.md`,
  `HANDOFF.md`, and
  `change.md`.
- A concise evidence table or bullet list that cites file paths and line
  numbers for each material finding, including the harness code, Cargo
  dependency support, formal one-hour run evidence if present, and stale or
  current deferral wording.
- The exact answer section shown below, with all five headings present in this
  order:

```markdown
## 5-Question Memory-Soak Reconciliation Answer Block

### Q1. Metrics Support

### Q2. Formal One-Hour Evidence

### Q3. Deferral Text Status

### Q4. Remaining Source/Harness Need

### Q5. Smallest Follow-Up
```

- Q1 must answer whether committed `script-bench` harness code supports
  `peak_rss_bytes` / `vss_delta_bytes` process-memory metrics, citing
  `crates/script-bench/src/script_host.rs` and `crates/script-bench/Cargo.toml`.
- Q2 must answer whether the repository contains committed formal one-hour
  `peak_rss_bytes` / `vss_delta_bytes` result evidence, citing the relevant
  baseline/status records. It must explicitly distinguish a committed harness
  capable of recording metrics from committed one-hour recorder-host result
  evidence.
- Q3 must classify stale-or-current deferral wording across the required docs.
  It must preserve dated historical records as historical context and identify
  only present-tense/current-state wording as stale if the evidence supports
  that conclusion.
- Q4 must answer whether any source or harness change remains necessary before
  docs can be reconciled. It must not propose source/harness edits unless the
  audit evidence proves they are still needed.
- Q5 must recommend exactly one smallest follow-up and explain why it is the
  smallest. If the smallest follow-up would require a fresh one-hour
  recorder-host soak before any repository edit is justified, Q5 must say so
  and the EXEC report must halt with `STATUS: NEEDS_HUMAN`,
  `HANDOFF_STATUS: NEEDS_HUMAN`, and `NEXT_ROLE: HUMAN_ARBITER`.

## Acceptance Criteria

- The dispatch is audit-only. No source, test, documentation, manifest,
  lockfile, workflow, script, schema, status file, existing packet, or non-EXEC
  handoff packet is edited or added.
- Exactly one new EXEC packet is produced for this dispatch.
- The EXEC packet footer contains exactly one line-anchored
  `HANDOFF_STATUS: COMPLETE` marker and routes `NEXT_ROLE: REVIEWER_AI` when
  the audit succeeds, unless a halt condition requires `HANDOFF_STATUS:
  BLOCKED` or `NEEDS_HUMAN`.
- The EXEC packet includes the exact
  `## 5-Question Memory-Soak Reconciliation Answer Block` section and the exact
  Q1-Q5 headings listed in Deliverables.
- Q1 cites the committed process-memory metric implementation, including
  `ProcessMemoryMetrics`, `MemorySoakReport::process_memory`,
  `sample_process_memory`, the one-hour print path, and the `memory-stats`
  dependency or equivalent evidence if the file has changed.
- Q2 cites committed one-hour result evidence if present, including the
  recorded run date, invocation, elapsed time, cycle count, sample count,
  `peak_rss_bytes`, and `vss_delta_bytes`. If no such formal one-hour metric
  result exists, Q2 must state that clearly and distinguish that gap from
  harness support.
- Q3 covers `crates/script-bench/BASELINE.md`,
  `crates/script-bench/METHODOLOGY.md`, `plans/IMPLEMENTATION.md`, `Status.md`,
  `HANDOFF.md`, and `change.md`; it must identify each relevant stale,
  current, or historical-only deferral statement with file path and line number.
- Q4 clearly states whether a source/harness dispatch is still needed, and why.
- Q5 names exactly one smallest follow-up. It must not create a follow-up TASK
  packet, GitHub issue comment, PR comment, code edit, or documentation edit.
- The audit distinguishes these concepts throughout: committed harness code,
  bounded non-hour metric smoke coverage, committed formal one-hour
  `peak_rss_bytes` / `vss_delta_bytes` evidence, and stale documentation
  wording.
- The final repository state has no tracked modifications from this audit.
  With untracked files included, only this dispatch's own TASK packet, TASK
  sidecar if already generated by automation, EXEC packet, and EXEC sidecar if
  generated by automation may appear as dispatch artifacts.

## Constraints / Non-Goals

- Do not run Cargo commands, tests, formatters, benchmarks, architecture lints,
  cargo-deny, `.ai/dispatch.verify.ps1`, or the one-hour memory soak.
- Do not edit `crates/script-bench` source, tests, benches, fixtures, docs, or
  `Cargo.toml`.
- Do not edit `Cargo.lock`, workflows, scripts, status docs, plans, or existing
  handoff packets.
- Do not implement source/harness changes and documentation reconciliation in
  the same follow-up recommendation. If both appear necessary, the Executor
  must halt and explain the split rather than recommending a combined dispatch.
- Do not infer missing result evidence from harness capability, bounded tests,
  or prior non-metric one-hour runs.
- Do not treat old dated snapshots as incorrect merely because later evidence
  supersedes them. Current-state docs may need reconciliation; historical
  records should usually be preserved and superseded forward.
- Do not commit, push, open a PR, edit GitHub issue #120, or create additional
  packets beyond the single EXEC report.

## Verification Gates

The Executor MUST run and document each applicable gate in the
`EXECUTION_REPORT`. These gates are intentionally read-only and use no Cargo
commands:

- `git status --short --untracked-files=no` before audit work, expected no
  tracked changes from this dispatch. If tracked changes are present, document
  them and halt rather than cleaning or reverting them.
- Inventory the required files and confirm all eight required inspection files
  exist.
- Read `crates/script-bench/BASELINE.md`.
- Read `crates/script-bench/METHODOLOGY.md`.
- Read `crates/script-bench/src/script_host.rs`.
- Read `crates/script-bench/Cargo.toml`.
- Read `plans/IMPLEMENTATION.md`.
- Read `Status.md`.
- Read `HANDOFF.md`.
- Read `change.md`.
- Search the required files for `peak_rss`, `vss_delta`, `ProcessMemoryMetrics`,
  `process_memory`, `memory-stats`, `phase3_memory_soak`,
  `phase_3_memory_soak_one_hour`, `FORMAL_MEMORY_SOAK_DURATION`,
  `one-hour`, `1-hour`, `soak`, `deferr`, and `harness`.
- After writing the EXEC packet, run `git status --short --untracked-files=all`.
  Expected tracked modifications: none. Expected untracked dispatch artifacts:
  this dispatch's own TASK packet, TASK sidecar if already generated by
  automation, EXEC packet, and EXEC sidecar if generated by automation.

No local cargo build, cargo test, cargo fmt, cargo-deny, benchmark compile,
architecture-lint run, `.ai/dispatch.verify.ps1`, docs generation, source
generation, one-hour soak, or repository edit is allowed for this audit-only
dispatch.

## Halt Conditions

The Executor MUST halt and write an EXEC packet rather than expanding scope if
any of the following occur:

- A truthful Q1-Q5 answer requires running Cargo, tests, formatters, benchmarks,
  architecture lints, `.ai/dispatch.verify.ps1`, or the one-hour memory soak.
- A truthful Q1-Q5 answer requires editing any file other than the single
  allowed EXEC report.
- The smallest follow-up requires a fresh one-hour recorder-host soak before
  any repository edit is justified. In this case, set header
  `STATUS: NEEDS_HUMAN`, footer `HANDOFF_STATUS: NEEDS_HUMAN`, and
  `NEXT_ROLE: HUMAN_ARBITER`.
- The smallest follow-up would combine `script-bench` source/harness changes
  with documentation reconciliation. Halt instead; those must be separate
  dispatches if both are actually required.
- The required files disagree in a way that cannot be classified from committed
  text alone without guessing.
- Required evidence cannot be inspected because a required file is missing,
  unreadable, or too malformed to classify.
- More than one EXEC report would be required to explain the audit result.

If a halt condition is hit after enough evidence has been gathered, the EXEC
report must still include the five-question answer block to the extent possible,
set header `STATUS: BLOCKED` unless `NEEDS_HUMAN` is more accurate, set footer
`HANDOFF_STATUS: BLOCKED` or `NEEDS_HUMAN`, and route `NEXT_ROLE: PLANNER_AI` or
`HUMAN_ARBITER` as appropriate.

## Planner Notes

This packet is intentionally read-only. The audit target is not whether the
code can be improved, but whether the repository's current story is internally
consistent about `script-bench` memory-soak process-memory metrics.

Known tension to inspect carefully:

- `crates/script-bench/src/script_host.rs` and `crates/script-bench/Cargo.toml`
  appear to contain committed process-memory metric support for the memory-soak
  harness.
- `crates/script-bench/BASELINE.md` appears to contain both a metrics-enabled
  2026-05-17 one-hour run row and older/later wording that may still frame
  `peak_rss` / `vss_delta` as future or deferred.
- `plans/IMPLEMENTATION.md`, `Status.md`, `HANDOFF.md`, and `change.md` include
  dated records from before and around the metrics work. The Executor must
  classify those records as historical-only or stale current-state wording
  based on committed evidence, not by rewriting history.

Under the orchestrator protocol, no `_REVIEW_` or `_CLOSEOUT_` packet is
expected from this TASK. The only new packet produced by execution should be the
`ISSUE-120_EXEC_*.md` report.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-120
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

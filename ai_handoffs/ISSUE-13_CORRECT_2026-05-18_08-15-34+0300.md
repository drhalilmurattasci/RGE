# Correction Packet

DISPATCH_ID: ISSUE-13
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18_08-15-34+0300
RELATED_FILES:
- crates/cad-core/src/operators/transform.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-13_TASK_2026-05-18_08-10-13+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-13_EXEC_2026-05-18_08-14-57+0300.md`
- Latest Review Report: none generated. The post-execution canonical verification gate failed before Codex control review.
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less.

1. **Normalize transform.rs newline style** - Verification gate finding:
   `cargo +nightly fmt --all -- --check` failed with
   `Incorrect newline style in \\?\A:\RCAD\RGE\crates\cad-core\src\operators\transform.rs`.
   Required change: normalize only the line endings in
   `crates/cad-core/src/operators/transform.rs` to the newline style accepted
   by `rustfmt`. Do not change Rust tokens, formatting, comments, imports,
   module structure, behavior, or any other file. Acceptance:
   `cargo +nightly fmt --all -- --check` exits 0, and a whitespace-insensitive
   diff of `crates/cad-core/src/operators/transform.rs` shows no content change
   beyond newline normalization.

## Deferred Findings (NOT Approved for This Round)

None. No review findings were produced; the only known blocker is the canonical
format gate failure above.

## Updated Acceptance Criteria

Unchanged from the Task Packet except for this correction-specific allowance:
`crates/cad-core/src/operators/transform.rs` may receive a line-ending-only
normalization so the canonical verification gate can pass. The existing
`crates/audio/src/falloff.rs` changes from the execution round must not be
expanded.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `git diff --ignore-space-at-eol -- crates/cad-core/src/operators/transform.rs` -> expected no content diff beyond newline normalization.
- `.\.ai\dispatch.verify.ps1` -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, with this correction-round addition: halt and
route back to Planner if making `cargo +nightly fmt --all -- --check` pass
would require any non-newline content change in
`crates/cad-core/src/operators/transform.rs`, any edit to another file, or any
semantic/runtime change.

## Planner Notes

This correction is deliberately narrow. The ISSUE-13 execution report indicates
the requested `AudioFalloff` test-only work landed and its local gates passed,
but the orchestrator's canonical verification failed before control review on a
newline-style problem in `crates/cad-core/src/operators/transform.rs`. Normalize
that file's newline style only, preserve the existing ISSUE-13 test changes,
and rerun the canonical verification gate.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-13
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

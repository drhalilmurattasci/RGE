# Task Packet

DISPATCH_ID: ISSUE-49
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_15-16-06+0300
RELATED_FILES:
- crates/material-graph/src/lib.rs
- crates/material-graph/Cargo.toml
- Cargo.lock
STATUS: OPEN

## Goal

Advance Phase 8 graph-foundation pressure testing for the material graph wrapper only by adding a focused smoke test that proves a populated `MaterialGraph` can be captured through `GraphSnapshot`, serialized to RON, deserialized, restored back to the substrate `Graph`, and retain structure plus material payload identity.

## Scope

### MAY edit
- `crates/material-graph/src/lib.rs`
- `crates/material-graph/Cargo.toml`, only if adding serde derives requires a direct crate dependency
- `Cargo.lock`, only for the mechanical Cargo-regenerated dependency graph update caused by adding `serde = { workspace = true }` to `crates/material-graph/Cargo.toml`; this means adding the existing locked `serde` package as a dependency edge for `rge-material-graph` only, with no version bumps, new packages, or resolver churn

### MUST NOT edit
- Any file outside `crates/material-graph/src/lib.rs`, `crates/material-graph/Cargo.toml`, and the narrow `Cargo.lock` exception above
- `Cargo.toml` at the repository root
- `Cargo.lock` except for the narrow mechanical dependency-edge update explicitly allowed above
- `kernel/graph-foundation/**`
- `crates/anim-graph/**`
- `crates/material-graph-editor/**`
- Material runtime, renderer, editor, WGSL, asset, ECS, or cross-graph crates
- Documentation, schemas, scripts, `.gitignore`, CI files, or existing handoff packets

### MAY add new files
- Protocol output only: the Executor's `ai_handoffs/ISSUE-49_EXEC_*.md` report and its `.meta.json` sidecar if finalized by `new-handoff.ps1`

### MUST NOT add new files
- New source files
- New integration test files
- New crates
- New fixtures or snapshots
- New architecture lints, ADRs, design docs, or task packets

## Deliverables

- Add one focused unit test in `crates/material-graph/src/lib.rs`, inside the existing `#[cfg(test)] mod tests`, that builds a populated `MaterialGraph` with at least three material nodes and at least two typed-port edges.
- The test must capture the private inner graph with `rge_kernel_graph_foundation::GraphSnapshot`, serialize it through `GraphSnapshot::to_ron`, deserialize it through `GraphSnapshot::from_ron`, restore it through `GraphSnapshot::to_graph`, and assert the restored substrate `Graph` preserves:
  - node count
  - edge count
  - node identity, including each `NodeId` and stored material node key
  - edge identity, including each `EdgeId`, source node, destination node, and `MaterialEdge` port payload
- Add only the minimum serde derives needed for the snapshot RON round trip, likely on `PortType`, `MaterialEdge`, and the private `MaterialNode`.
- If serde derives are added, add `serde = { workspace = true }` to `crates/material-graph/Cargo.toml`; allow Cargo to update `Cargo.lock` only for the corresponding `rge-material-graph` -> `serde` dependency edge. Do not add a direct `ron` dependency; use the graph-foundation `GraphSnapshot::to_ron` and `GraphSnapshot::from_ron` APIs.
- Do not add public snapshot or inner-graph accessors to `MaterialGraph`; the in-crate test may use private fields and private payload types.

## Acceptance Criteria

- `cargo test -p rge-material-graph` exits 0.
- The new test exercises the full path `MaterialGraph` -> `GraphSnapshot` -> RON text -> `GraphSnapshot` -> `Graph`.
- The assertions compare real restored payloads, not only labels or counts.
- Existing material-graph tests continue to pass.
- The diff is limited to the allowed material-graph files, the optional mechanical `Cargo.lock` dependency-edge update described in Scope, and the protocol `EXEC` packet produced by the Executor.

## Constraints / Non-Goals

- Do not add material evaluation, shader/WGSL generation, material runtime behavior, editor behavior, renderer integration, asset loading, traversal, cycle detection, graph diff behavior, or AnimGraph work.
- Do not change `GraphSnapshot`, `Graph`, `NodeId`, `EdgeId`, or any kernel graph-foundation API.
- Do not introduce a public `MaterialGraph` snapshot API, public `inner()` accessor, or wrapper restore constructor.
- Do not broaden the dispatch into cross-wrapper snapshot coverage; this issue is material graph only.
- Do not update dependency versions or workspace dependency policy.
- Do not hand-edit `Cargo.lock`; if it changes, it must be the normal Cargo output from the scoped manifest change and must stay within the allowed dependency-edge exception.

## Verification Gates

The Executor MUST run and document the result of this command in their `EXECUTION_REPORT`:

- `cargo test -p rge-material-graph` -> expected exit 0

Optional local checks such as `cargo fmt --check -p rge-material-graph` may be run if useful, but they are not a substitute for the required gate above.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- The change cannot be completed by editing only `crates/material-graph/src/lib.rs`, if needed `crates/material-graph/Cargo.toml`, and the narrow mechanical `Cargo.lock` dependency-edge update allowed above.
- The implementation appears to require changes to `kernel/graph-foundation`, workspace `Cargo.toml`, any non-mechanical `Cargo.lock` update, or any graph wrapper other than material graph.
- `Cargo.lock` changes in any way beyond adding the existing locked `serde` package as a dependency edge for `rge-material-graph`, including version bumps, new package entries, removed dependencies, or resolver churn.
- The smoke test cannot use `GraphSnapshot::to_ron` and `GraphSnapshot::from_ron` without adding a direct `ron` dependency to `rge-material-graph`.
- Preserving payload identity requires adding or exposing new public `MaterialGraph` APIs.
- `cargo test -p rge-material-graph` fails for reasons unrelated to the allowed scoped change.

## Planner Notes

The existing material graph tests live in `crates/material-graph/src/lib.rs`, and the test module can access the private `MaterialGraph.graph` field and private `MaterialNode` payload. Prefer comparing collected node id plus node payload pairs and edge id plus edge record pairs from the original and restored graphs so the test pins exact payload identity, including each `MaterialEdge`. The graph-foundation crate already exposes `GraphSnapshot::from_graph`, `to_ron`, `from_ron`, and `to_graph`; this dispatch is only to prove the material wrapper's payloads participate correctly.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-49
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

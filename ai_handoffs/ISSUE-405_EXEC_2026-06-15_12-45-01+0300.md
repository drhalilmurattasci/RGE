# Execution Report

DISPATCH_ID: ISSUE-405
AUTHOR: Executor / Codex
TIMESTAMP: 2026-06-15_12-45-01+0300
RELATED_FILES:
- crates/editor-shell/src/lifecycle/commands.rs - added the bounded headless CAD cuboid add entry point and typed result/error surface.
- crates/editor-shell/src/lifecycle/mod.rs - re-exported the CAD add result/error types.
- crates/editor-shell/src/lifecycle/tests.rs - added focused lifecycle tests for add, render/frame/select paths, rejection, and restore/projection cleanup.
- .ai/dispatch.tasks.md - appended exactly one self-rearm audit task 151.
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-405_TASK_2026-06-15_12-22-35+0300.md`

## Pre-Execution Review Consumed

No pre-execution review issued for this dispatch.

## Current-State Claims: Falsification Re-run

- Claim: Task 150 is present in `.ai/dispatch.tasks.md`, and no task 151
  heading is present before this dispatch's self-rearm step.
  Re-run before edits: `rg -n "^150\.|^151\." .ai/dispatch.tasks.md` -> task
  150 present at line 14864 and no task 151 heading. After self-rearm,
  `rg -n "^151\." .ai/dispatch.tasks.md` -> exactly one task 151 heading at
  line 14961.
- Claim: The allowed editor-shell files do not expose an add-cuboid shell
  command/entry point under obvious add/insert names before this dispatch.
  Re-run before edits:
  `rg -n "AddCuboid|add_cuboid|insert_cuboid|add_cad_cuboid" crates/editor-shell/src/lifecycle/commands.rs crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/tests.rs crates/editor-shell/src/render_path.rs`
  -> no matches (exit 1).
- Claim: Existing frame/select CAD paths are based on `render_mesh_for`.
  Re-run:
  `rg -n "render_mesh_for|selected_cad_scene_bounds|current_scene_bounds|frame_cad_selection_or_reset_camera" crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/tests.rs crates/cad-projection/src/render_adapter.rs`
  -> current source includes `render_adapter.rs:90`, `lifecycle/mod.rs:2069`,
  `lifecycle/mod.rs:2077`, `lifecycle/mod.rs:2084`, `lifecycle/mod.rs:2098`,
  `lifecycle/mod.rs:2122`, plus the new tests that exercise those paths.
- Claim: The CommandBus public action path is world-only and is not the surface
  for this CAD add.
  Re-run:
  `rg -n "fn apply\(|fn revert\(|struct CommandBus|pub fn submit|pub fn undo|pub fn redo|pub fn mark_saved|Action" crates/editor-actions/src/action.rs crates/editor-actions/src/bus.rs crates/editor-shell/src/lifecycle/commands.rs`
  -> `Action::apply` / `Action::revert` still take `&mut World`,
  `CommandBus::submit` / `undo` / `redo` still take `&mut World`, and there is
  no diff under `crates/editor-actions/src`.

## What I Changed

### Source

- `crates/editor-shell/src/lifecycle/commands.rs`: added
  `EditorShell::add_cad_cuboid_to_empty_scene`, `CadCuboidAddResult`, and
  `CadCuboidAddError`. The method rejects any existing/partial CAD state or
  existing render content, then performs the required sequence:
  `CadGraph::begin_operation` -> `OperatorGraph::add_operator(OperatorNode::Cuboid(CuboidOp::default()))`
  -> `OperatorGraph::set_root` -> `CadGraph::commit` ->
  `CadProjection::spawn_brep_entity` -> `CadProjection::tick`.
- `crates/editor-shell/src/lifecycle/mod.rs`: re-exported
  `CadCuboidAddResult` and `CadCuboidAddError` with the existing lifecycle
  command types.

### Tests

- `crates/editor-shell/src/lifecycle/tests.rs::add_cad_cuboid_to_empty_scene_projects_and_uses_existing_frame_paths`:
  asserts exactly one default cuboid root, exactly one BRep entity, exactly one
  `render_mesh_for` mesh, scene framing through `reset_camera`, selected-CAD
  framing after adding the entity to `coord.selection`, and no prebuilt render
  mesh substitution.
- `crates/editor-shell/src/lifecycle/tests.rs::add_cad_cuboid_to_empty_scene_rejects_existing_and_partial_state`:
  asserts a second add, partial CAD state, and render-only state return clear
  `SceneNotEmpty` errors without replacing existing content.
- `crates/editor-shell/src/lifecycle/tests.rs::add_cad_cuboid_restore_to_pre_add_head_cleans_projection_state`:
  captures `pre_add_head`, adds and projects the cuboid, calls
  `CadGraph::restore_to(pre_add_head)`, despawns the spawned entity through
  `CadProjection::despawn_brep_entity`, ticks projection, and asserts no stale
  BRep entity or render mesh remains.

### Docs

- `.ai/dispatch.tasks.md`: appended exactly one bounded next-task-source audit
  task 151. It is source/docs-read-only, forbids Rust/test/Cargo/automation
  changes, requires dispatcher-provided GitHub snapshot evidence, and copies
  the self-rearm requirement forward for task 152 or `NEEDS_HUMAN_RECORDED`.

## Per-File Summary

The implementation stayed in the editor-shell lifecycle surface. No
`render_path.rs` edit was needed because the add path produces a projected CAD
mesh immediately, and the existing headless `current_scene_bounds` /
`selected_cad_scene_bounds` code reads that mesh via `render_mesh_for`.

## Verification Results

- `cargo test -p rge-editor-shell --lib --no-fail-fast` -> exit 0; 311 passed,
  0 failed, 1 ignored.
- `cargo check -p rge-editor-shell --lib` -> exit 0.
- `cargo +nightly fmt --all -- --check` -> exit 0.
- `rg -n "CuboidOp|begin_operation|spawn_brep_entity|restore_to|despawn_brep_entity" crates/editor-shell/src`
  -> exit 0; matches include the new implementation in
  `lifecycle/commands.rs` and cleanup tests in `lifecycle/tests.rs`.
- `rg -n "fn undo_command|CommandBus|Action " crates/editor-actions/src` ->
  exit 0 read-only confirmation; no diff under `crates/editor-actions/**`.
- `rg -n "^151\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md` -> exit 0; one
  new `151.` heading at line 14961 plus historical/template
  `NEEDS_HUMAN_RECORDED` references already present in the task brief.
- `rg -n "^151\." .ai/dispatch.tasks.md` -> exit 0; exactly one `151.`
  heading at line 14961.
- `git diff --name-only` -> exit 0; tracked diff limited to
  `.ai/dispatch.tasks.md`, `crates/editor-shell/src/lifecycle/commands.rs`,
  `crates/editor-shell/src/lifecycle/mod.rs`, and
  `crates/editor-shell/src/lifecycle/tests.rs`.
- `git diff --check` -> exit 0; only Git's line-ending warning for
  `.ai/dispatch.tasks.md`, no whitespace errors.

## Deviations from Task Packet

None - execution stayed within the Task Packet scope. I did not edit
`render_path.rs` because the existing projection mesh and bounds paths were
sufficient for the scoped headless entry point and tests.

## Open Questions for Reviewer

- While developing the restore cleanup test, I observed that
  `CadGraph::restore_to(pre_add_head)` restores the empty graph snapshot
  (`node_count == 0`) but the public root accessor may still report the prior
  root when the target checkpoint had no root. CAD core is out of scope for
  ISSUE-405, so this execution asserts the scoped requirement that no stale
  BRep entity or render mesh remains after projection cleanup. Task 151 calls
  out CAD restore semantics as an audit candidate for human-reviewed follow-up
  classification.

## Worktree State

- Tracked files: modified but uncommitted:
  `.ai/dispatch.tasks.md`,
  `crates/editor-shell/src/lifecycle/commands.rs`,
  `crates/editor-shell/src/lifecycle/mod.rs`,
  `crates/editor-shell/src/lifecycle/tests.rs`.
- Untracked items before finalizing this report:
  `ai_handoffs/ISSUE-405_TASK_2026-06-15_12-22-35+0300.md`,
  `ai_handoffs/ISSUE-405_TASK_2026-06-15_12-22-35+0300.meta.json`,
  `ai_handoffs/claims/ISSUE-405_2026-06-15_12-22-33-0267552+0300_claim.json`,
  `ai_handoffs/ISSUE-405_EXEC_2026-06-15_12-45-01+0300.md`.
- Branch: `ai-dispatch/ISSUE-405`
- Last commit: `a09b725` Re-arm dispatch with approved gated first CAD mutation (task 150)

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-405
AUTHOR: Executor / Codex
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---

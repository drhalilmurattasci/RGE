# Task Packet

DISPATCH_ID: ISSUE-322
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-07_23-34-23+0300
RELATED_FILES:
- plans/BASELINE.md
- Status.md
- HANDOFF.md
- change.md
- .ai/dispatch.tasks.md
- tools/compile-timing.ps1
- .ai/dispatch-ISSUE-322/**
STATUS: OPEN

## Goal

GitHub issue #322: attribute the Phase 9 section 13.3 clean release build miss. The existing certified clean release workspace build measurement is 156.591s wall / cargo 2m 36s against the <=120s budget. This dispatch must turn that miss into an actionable, evidence-backed remediation plan by identifying the largest compile-time cost drivers, without changing source behavior or implementing any remediation.

This is an audit/report dispatch, not an implementation dispatch. It may update only the planning/status documents and task marker listed below; source-code MAY edit is none.

## Scope

### MAY edit
- `plans/BASELINE.md`
- `Status.md`
- `HANDOFF.md`
- `change.md`
- `.ai/dispatch.tasks.md`

### MUST NOT edit
- `crates/**`
- `kernel/**`
- `runtime/**`
- `editor/**`
- `tools/**`
- `Cargo.toml`
- `Cargo.lock`
- `**/Cargo.toml`
- `.github/workflows/**`
- `.ai/dispatch.verify.ps1`
- `.ai/*.schema.json`
- `Invoke-AiDispatch*.ps1`
- `Register-AiDispatchSchedule.ps1`
- `Get-AiDispatchHealth.ps1`
- `Watch-AiDispatch.ps1`
- `Wait-GitHubActions.ps1`
- `new-handoff.ps1`
- `ai_handoffs/ISSUE-322_TASK_2026-06-07_23-34-23+0300.md`
- any existing `ai_handoffs/**` packet or claim artifact
- shared cache contents under `A:\RustCache\target`

### MAY add new files
- `.ai/dispatch-ISSUE-322/**`
- `B:\sdk\rge-clean-hotspots-ISSUE-322-*`
- `ai_handoffs/ISSUE-322_EXEC_*.md`
- `ai_handoffs/ISSUE-322_EXEC_*.meta.json`

### MUST NOT add new files
- new source, test, bench, fixture, crate, tool, script, workflow, schema, ADR, or Cargo metadata files under `crates/**`, `kernel/**`, `runtime/**`, `editor/**`, `tools/**`, `.github/**`, `.ai/*.schema.json`, or any `Cargo.toml` / `Cargo.lock` location
- any new tracked timing artifact outside the allowed docs/task-record surface
- any `ai_handoffs/**` packet other than this dispatch's required `ISSUE-322_EXEC_*` report and sidecar
- any file under or replacing the shared `A:\RustCache\target`

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - plans/BASELINE.md
  - Status.md
  - HANDOFF.md
  - change.md
  - .ai/dispatch.tasks.md
  - .ai/dispatch-ISSUE-322/**
  - ai_handoffs/ISSUE-322_EXEC_*.md
  - ai_handoffs/ISSUE-322_EXEC_*.meta.json
MUST_NOT_EDIT:
  - crates/**
  - kernel/**
  - runtime/**
  - editor/**
  - tools/**
  - Cargo.toml
  - Cargo.lock
  - **/Cargo.toml
  - .github/workflows/**
  - .ai/dispatch.verify.ps1
  - .ai/*.schema.json
  - Invoke-AiDispatch*.ps1
  - Register-AiDispatchSchedule.ps1
  - Get-AiDispatchHealth.ps1
  - Watch-AiDispatch.ps1
  - Wait-GitHubActions.ps1
  - new-handoff.ps1
  - ai_handoffs/ISSUE-322_TASK_2026-06-07_23-34-23+0300.md
  - ai_handoffs/claims/**
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Deliverables

- A forward-only Phase 9 section 13.3 hotspot-attribution update in `plans/BASELINE.md` that records:
  - the attribution method and exact commands used;
  - the command exit status and whether the run was a fresh isolated-target remeasurement or attribution-only timing;
  - the dominant clean release build cost drivers, ordered largest first;
  - whether the certified 156.591s miss is preserved or superseded by a fresh isolated-target measurement;
  - the smallest next remediation candidates, expected risk, and whether each is suitable for automation.
- A new top snapshot in `Status.md` summarizing the attribution result and the next remediation options.
- A matching new top snapshot in `HANDOFF.md` with the same current-state conclusion and next handoff options.
- A dated `change.md` entry recording the audit, measurement commands, non-changes, and verification.
- `.ai/dispatch.tasks.md` task 86 marked done only after the docs above are updated; do not rewrite unrelated task entries.
- An `EXECUTION_REPORT` packet for ISSUE-322 documenting all measurement commands run, their exit codes, artifact paths created, and any cleanup performed.

## Acceptance Criteria

- The final tracked diff is confined to `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, `change.md`, `.ai/dispatch.tasks.md`, and this dispatch's protocol/audit artifacts.
- The audit identifies the largest compile-time cost drivers from `tools/compile-timing.ps1`, Cargo-supported timing output, Cargo metadata, or a clearly explained combination of those sources.
- The audit preserves the 156.591s clean release miss as the current certified measurement unless the Executor actually runs a fresh clean release workspace build from a new isolated target directory.
- If a fresh isolated-target run is performed, the docs state the target path, command, exit code, wall time, Cargo "Finished" time if present, and whether the fresh result supersedes 156.591s.
- If no fresh isolated-target run is performed, the docs explicitly say the run was attribution-only and the certified measurement remains 156.591s.
- The remediation list is intentionally small: each candidate must name the affected crate/dependency/build phase, expected compile-time upside, source-behavior risk, review risk, and automation suitability (`yes`, `no`, or `only after a separate task`).
- The docs must not claim the section 13.3 clean-build gate is closed unless a fresh isolated-target `cargo build --workspace --release` result is <=120s.
- `.ai/dispatch.tasks.md` marks only task 86 done and does not seed or modify any unrelated task.
- No source behavior, Cargo dependency graph, tooling behavior, workflow behavior, scheduler behavior, or dispatch automation behavior changes.

## Current-State Claims: Falsification Required

Every negative or unchanged assertion this packet relies on is paired with a falsifying search/read that another agent can rerun.

- Claim: Task 86 is currently the open backlog item for clean release build hotspot attribution, not already marked DONE.
  Falsifying search: `Select-String -Path '.ai/dispatch.tasks.md' -Pattern '86. **Phase 9 clean release build hotspot attribution.' -SimpleMatch` -> one match at `.ai/dispatch.tasks.md:7999`, heading has no `[DONE ...]` marker.
- Claim: The current certified clean release miss is 156.591s and remains open for remediation.
  Falsifying search: `Select-String -Path 'plans/BASELINE.md','Status.md','HANDOFF.md','change.md' -Pattern '156.591' -SimpleMatch` -> matches in the Phase 9 baseline/status/handoff/change records, including `plans/BASELINE.md:296` and `plans/BASELINE.md:317`.
- Claim: The task brief forbids `cargo clean` and shared `A:\RustCache\target` deletion.
  Falsifying search: `Select-String -Path '.ai/dispatch.tasks.md' -Pattern 'cargo clean','A:\RustCache\target' -SimpleMatch` -> matches the task 86 allowed-surface block at `.ai/dispatch.tasks.md:8015-8016`.
- Claim: `tools/compile-timing.ps1` is available as the repeatable non-destructive timing harness.
  Falsifying read: `Get-Content -LiteralPath 'tools/compile-timing.ps1'` -> script exists, supports `-Mode`, `-Iterations`, `-Release`, `-TimeoutSeconds`, `-JsonPath`, and states a warm-cache/no-`cargo clean` policy.

## Constraints / Non-Goals

- Do not implement any remediation candidate in this dispatch.
- Do not edit Rust source, tests, benches, Cargo manifests, lockfiles, tooling, workflows, schemas, scheduler configuration, or dispatch automation scripts.
- Do not run `cargo clean`.
- Do not delete, move, rewrite, or otherwise mutate the shared `A:\RustCache\target` cache.
- Do not replace the 156.591s certified clean release measurement with a warm-cache, no-op, partial, failed, timed-out, or non-isolated measurement.
- Do not claim a root cause from intuition alone; tie each hotspot to Cargo timing output, dependency/build metadata, or a documented limitation in the available evidence.
- Do not leave a source edit behind after exploratory measurement. If any temporary touch is considered, halt first because source touching is out of scope for this dispatch.
- Do not broaden `.ai/dispatch.tasks.md` beyond marking task 86 done.

## Verification Gates

The Executor MUST run and document the result of each applicable gate in the `EXECUTION_REPORT`:

- Every measurement or attribution command used must exit 0. This includes `tools/compile-timing.ps1`, direct `cargo build` / Cargo timing commands, `cargo metadata`, or any local parsing command that materially supports the attribution.
- `git diff --check` -> expected exit 0.
- `git diff --name-only` -> expected tracked changes confined to `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, `change.md`, `.ai/dispatch.tasks.md`, and this dispatch's protocol/audit artifacts.
- If a fresh isolated target under `B:\sdk` is created, verify the resolved path starts with `B:\sdk\rge-clean-hotspots-ISSUE-322-` before deleting it, and report whether it was removed or intentionally left as scratch.

Do not add the full workspace test/bench gate to this task. The deliverable is a docs/task-record audit of compile-time attribution, not a source change. If the orchestrator's canonical verify gate runs anyway and fails on an out-of-scope Rust/test target, do not attempt an out-of-scope fix; report the failure in the EXEC packet.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- Attribution would require editing source, tests, Cargo files, tooling, workflows, schemas, scheduler config, or dispatch automation scripts.
- Any command path would require `cargo clean`, deleting shared `A:\RustCache\target`, or mutating another dispatch's scratch target.
- A fresh isolated target path cannot be proven to resolve under `B:\sdk\rge-clean-hotspots-ISSUE-322-*`.
- Measurement commands needed for the attribution repeatedly fail, time out, or produce no usable timing data, and the remaining evidence is insufficient to identify cost drivers honestly.
- The docs would need to claim the clean-build gate is closed without a fresh <=120s isolated-target release build.
- The final tracked diff contains files outside the MAY edit surface, excluding this dispatch's required protocol/audit artifacts.
- The Executor discovers that the 156.591s baseline is contradicted by existing tracked documentation; in that case, do not choose a new value silently.

## Planner Notes

Prefer the smallest honest attribution over an over-engineered profiling exercise. A useful outcome is a ranked table that tells the next planner which remediation slice to file first, which candidates are too risky for automation, and what measurement would prove improvement later.

Cargo-supported timing output may be used directly if `tools/compile-timing.ps1` cannot emit the needed per-unit attribution. If doing so requires a fresh clean release build, use a new isolated `CARGO_TARGET_DIR` under `B:\sdk\rge-clean-hotspots-ISSUE-322-*`; do not use or wipe the shared `A:\RustCache\target` for clean-build certification.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-322
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Correction Packet

DISPATCH_ID: ISSUE-202
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-26T08:19:29+03:00
RELATED_FILES:
- Invoke-AiDispatchLoop.ps1
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-202_TASK_2026-05-26_08-06-23+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-202_EXEC_2026-05-26_08-14-17+0300.md`
- Latest Review Report: Codex control review JSON for round 0; no `_REVIEW_` packet is emitted for orchestrator dispatches.
- Prior Correction Packets: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly this correction -- nothing more, nothing less.

1. **Escape the valid worked example backticks** -- Codex control review round 0 found that the added valid example in the `Invoke-PlanFill` double-quoted here-string renders as `- Invoke-AiDispatchLoop.ps1 ` instead of a Markdown backtick-wrapped path token. Required change: in `Invoke-AiDispatchLoop.ps1`, update only the existing `Invoke-PlanFill` planner prompt Rules list so the source correctly escapes literal PowerShell backticks around `Invoke-AiDispatchLoop.ps1`. The source path token should be bracketed by doubled PowerShell backticks so the runtime prompt evaluates to the single-backtick Markdown form shown in Updated Acceptance Criteria. Do not change runtime logic, parsers, templates, schemas, docs, or queue behavior. Acceptance: evaluating the added prompt snippet through PowerShell here-string semantics shows the valid example line with `Invoke-AiDispatchLoop.ps1` wrapped in Markdown backticks.

## Deferred Findings (NOT Approved for This Round)

None. The control review identified only the prompt-example escaping defect above.

## Updated Acceptance Criteria

Unchanged from Task Packet, with this explicit clarification: the valid worked example in the runtime planner prompt MUST contain this exact Markdown backtick-wrapped path token:

```text
- `Invoke-AiDispatchLoop.ps1`
```

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `git diff --check` -> expected exit 0.
- PowerShell parser validation for `Invoke-AiDispatchLoop.ps1` -> expected exit 0 and `PARSE_OK`.
- PowerShell evaluation of the added `Invoke-PlanFill` prompt snippet -> expected exit 0 and the valid example line renders with `Invoke-AiDispatchLoop.ps1` wrapped in Markdown backticks.
- `.ai/dispatch.verify.ps1` -> expected exit 0 and final `VERIFY OK`.

## Halt Conditions (Updated if Any)

Unchanged from Task Packet.

## Planner Notes

Keep the correction narrowly scoped to the existing `Invoke-PlanFill` planner prompt Rules list. The canonical verification log already passed before this semantic review failure, so the only required source change is the PowerShell escaping needed to preserve literal Markdown backticks in the runtime prompt.

---

HANDOFF_STATUS: COMPLETE
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

# Task Packet

DISPATCH_ID: ISSUE-72
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_17-29-21+0300
RELATED_FILES:
- crates/editor-ui/tests/phase8_graph_diff_smoke.rs
- crates/editor-ui/tests/node_graph_script_graph_smoke.rs
- crates/script-graph/src/lib.rs
STATUS: OPEN

## Goal

Extend the Phase 8 editor-ui GraphDiff integration smoke so it covers `rge_script_graph::ScriptGraph` through the existing `rge_kernel_graph_foundation::VizAdapter` projection. The current smoke proves MaterialGraph, AnimGraph, and OperatorGraph in both per-domain and combined-checkpoint diff paths; this dispatch adds ScriptGraph to the same test-only coverage without changing production editor-ui, graph-domain crates, manifests, or docs.

## Scope

### MAY edit
- crates/editor-ui/tests/phase8_graph_diff_smoke.rs

### MUST NOT edit
- crates/editor-ui/src/**
- crates/script-graph/src/**
- crates/material-graph/src/**
- crates/anim-graph/src/**
- crates/cad-core/src/**
- crates/kernel-graph-foundation/src/**
- Cargo.toml
- Cargo.lock
- crates/**/Cargo.toml
- docs/**
- .ai/**
- .gitignore
- Any source, documentation, schema, script, manifest, lockfile, existing packet, or `.gitignore` outside the MAY-edit file, except for the protocol-required execution report packet described below.

### MAY add new files
- One protocol-required execution report packet matching `ai_handoffs/ISSUE-72_EXEC_*.md`.
- The matching `.meta.json` sidecar for that execution report if `new-handoff.ps1 -Finalize` writes one.

### MUST NOT add new files
- new tests outside `crates/editor-ui/tests/phase8_graph_diff_smoke.rs`
- new docs, ADRs, schemas, scripts, manifests, fixtures, snapshots, or generated artifacts
- any additional TASK, REVIEW, CORRECT, or CLOSEOUT packet for this dispatch

## Deliverables

- Update `crates/editor-ui/tests/phase8_graph_diff_smoke.rs` to import and exercise `rge_script_graph::{ScriptEdge, ScriptGraph}`.
- Add ScriptGraph to the existing per-domain GraphDiff smoke path by building an old ScriptGraph snapshot through `snapshot_via_adapter`, adding exactly one script node and one script edge, and asserting the diff reports exactly that one added node and one added edge with no removals or changes.
- Extend the existing combined-checkpoint helper/test so the combined snapshot includes the script domain under its own deterministic namespace and the combined diff reports MaterialGraph, AnimGraph, OperatorGraph, and ScriptGraph together.
- Keep the implementation test-only and use the existing public `VizAdapter` projection; do not expose or access ScriptGraph private substrate internals from editor-ui.

## Acceptance Criteria

- The per-domain smoke proves ScriptGraph adds exactly one node and one edge via `GraphDiff::between(&old_snapshot, &new_snapshot)` where both snapshots came from `snapshot_via_adapter(&script_graph)`.
- The per-domain ScriptGraph assertions pin the added node by the `NodeId` returned from `ScriptGraph::add_node`, with `DiffNode { display_name: "exit", kind: "ScriptNode" }`, and pin the added edge by the `EdgeId` returned from `ScriptGraph::connect`, with `EdgeRecord { src: <existing body node>, dst: <new exit node>, data: "done" }`.
- The per-domain ScriptGraph diff has empty `removed_nodes`, `removed_edges`, `changed_nodes`, and `changed_edges`; `node_change_count() == 1`; `edge_change_count() == 1`; and `!diff.is_empty()`.
- The combined checkpoint diff mutates each of MaterialGraph, AnimGraph, OperatorGraph, and ScriptGraph by exactly one node and one edge after the old checkpoint is captured.
- The combined checkpoint diff reports exactly four added nodes and four added edges total, with no removals or changed records, and includes identity assertions for the namespaced ScriptGraph node and edge.
- Existing MaterialGraph, AnimGraph, and OperatorGraph assertions remain equivalent in meaning; update test names/comments only as needed to avoid stale "three domain" wording.
- No production source, graph-domain source, Cargo manifest, docs, scripts, schemas, or additional packet files are modified, except for the protocol-required EXEC report and optional EXEC sidecar.

## Constraints / Non-Goals

- Do not change `VizAdapter`, `GraphDiff`, `GraphSnapshot`, `ScriptGraph`, or any production editor-ui API.
- Do not add a new dev-dependency or edit any manifest; `rge-script-graph` is already available to editor-ui tests.
- Do not create a separate ScriptGraph smoke file for this dispatch; extend the existing Phase 8 GraphDiff smoke only.
- Do not broaden this into runtime script execution, editor behavior, graph rendering, invalidation, serialization, or architecture-lint work.
- Do not commit, push, stage files, or generate files outside the protocol-required EXEC report and optional EXEC sidecar.

## Verification Gates

The Executor MUST run and document the result of this command in their `EXECUTION_REPORT`:

- `cargo test -p rge-editor-ui --test phase8_graph_diff_smoke -- --nocapture` -> expected exit 0

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- The change appears to require editing any file outside `crates/editor-ui/tests/phase8_graph_diff_smoke.rs`.
- The test cannot import or use `rge_script_graph::{ScriptEdge, ScriptGraph}` without editing a Cargo manifest.
- Proving the invariant requires accessing ScriptGraph private internals or changing `ScriptGraph`, `VizAdapter`, `GraphDiff`, `GraphSnapshot`, or production editor-ui source.
- The combined checkpoint can only be made to pass by weakening or removing existing MaterialGraph, AnimGraph, or OperatorGraph assertions.
- The required verification command fails for reasons that require source, manifest, docs, script, schema, or packet edits outside the allowed file.

## Planner Notes

Use the existing `node_graph_script_graph_smoke.rs` and `crates/script-graph/src/lib.rs` tests as references for ScriptGraph's public construction pattern: create `entry` and `body`, connect them with `ScriptEdge::new("flow")`, snapshot via the adapter, then add `exit` and connect `body -> exit` with `ScriptEdge::new("done")`. In the combined helper, use the existing namespacing functions with a distinct `"script"` domain label so ScriptGraph ids cannot collide with the other projected domains.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-72
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Task Packet

DISPATCH_ID: ISSUE-55
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_01-01-33+0300
RELATED_FILES:
- crates/cad-core/tests/operator_graph_structural_diff_smoke.rs
- crates/cad-core/tests/operator_graph_snapshot_smoke.rs
- crates/cad-core/src/graph/operator_graph.rs
- kernel/graph-foundation/src/diff.rs
- kernel/graph-foundation/src/snapshot.rs
STATUS: OPEN

## Goal

Add one focused cad-core integration smoke test for GitHub issue #55 proving that `OperatorGraph` snapshots can be compared with graph-foundation `GraphDiff` and that a simple CAD operator graph mutation reports only the newly added operator node and newly added edge. This extends the existing snapshot smoke coverage from issue #54 into structural-diff coverage without adding CAD-domain diff APIs.

## Scope

### MAY edit
- `crates/cad-core/tests/operator_graph_structural_diff_smoke.rs` only.

### MUST NOT edit
- Any existing file, including `crates/cad-core/src/graph/operator_graph.rs`.
- `crates/cad-core/tests/operator_graph_snapshot_smoke.rs`; it may be inspected only as a style and API precedent.
- `kernel/graph-foundation/**`; these files may be inspected only to understand `GraphDiff`, `GraphSnapshot`, `EdgeRecord`, `NodeId`, and `EdgeId`.
- `crates/cad-core/src/operators/**`; these files may be inspected only to construct public `CuboidOp`, `TransformOp`, `OperatorNode`, and `EdgeKind` values.
- `crates/cad-core/Cargo.toml`, `Cargo.toml`, `Cargo.lock`, or any manifest/lockfile.
- Renderer, editor, runtime, graph-viewer, asset, ECS, material, animation, tooling, docs, schemas, scripts, CI files, `.gitignore`, architecture-lint tooling, or any existing handoff packet.

### MAY add new files
- `crates/cad-core/tests/operator_graph_structural_diff_smoke.rs`.
- Protocol output only: the Executor's `ai_handoffs/ISSUE-55_EXEC_*.md` report and its `.meta.json` sidecar if finalized by `new-handoff.ps1`.

### MUST NOT add new files
- Any source file outside `crates/cad-core/tests/operator_graph_structural_diff_smoke.rs`.
- New fixtures, snapshots/goldens, generated artifacts, crates, modules, docs, ADRs, schemas, scripts, Cargo entries, task/correction/review/closeout packets, or files under graph-foundation/material-graph/anim-graph.

## Deliverables

- Add exactly one new cad-core integration test file: `crates/cad-core/tests/operator_graph_structural_diff_smoke.rs`.
- Add exactly one test in that file. Required test name: `operator_graph_structural_diff_reports_only_added_operator_and_edge`.
- The test must construct an `OperatorGraph` using only public cad-core APIs.
- The initial graph must contain exactly two operator nodes:
  - one `OperatorNode::Cuboid(CuboidOp { ... })`
  - one `OperatorNode::Transform(TransformOp { ... })`
- The initial graph must contain exactly one edge created with `OperatorGraph::connect(cuboid_id, first_transform_id, 0)`.
- The test must capture the old snapshot with `GraphSnapshot::from_graph(graph.inner())` before mutation.
- The test must mutate the same `OperatorGraph` by adding exactly one new operator node and exactly one new edge. A suitable shape is adding a second distinct `OperatorNode::Transform(TransformOp { ... })` and connecting `first_transform_id` to `second_transform_id` on port `0`.
- The test must capture the new snapshot with `GraphSnapshot::from_graph(graph.inner())` after mutation.
- The test must compute `GraphDiff::between(&old_snapshot, &new_snapshot)`.
- The test must assert that the diff reports exactly one added node, exactly one added edge, and no removed or changed nodes or edges.
- The test must assert that the sole added node is the new operator id returned by `add_operator` and that its payload equals the expected new `OperatorNode`.
- The test must assert that the sole added edge is the new edge id returned by `connect` and that its record has the expected source, destination, and `EdgeKind::Input(0)` payload.
- The test should also assert derived counts where useful: `node_change_count() == 1`, `edge_change_count() == 1`, and `!diff.is_empty()`.

## Acceptance Criteria

- The only implementation file added or changed is `crates/cad-core/tests/operator_graph_structural_diff_smoke.rs`.
- No existing source file, manifest, lockfile, docs file, script, schema, CI file, or packet is changed.
- The test uses `rge_kernel_graph_foundation::GraphSnapshot` against `OperatorGraph::inner()` and `rge_kernel_graph_foundation::GraphDiff::between`.
- The mutation between snapshots adds exactly one operator and exactly one edge; it must not replace, remove, or otherwise mutate existing node or edge payloads.
- The test validates exact added-node and added-edge identity and payloads, not only counts.
- The test does not add a new snapshot, diff, graph-inspection, or test-only access API to cad-core.
- `cargo test -p rge-cad-core --test operator_graph_structural_diff_smoke` exits 0.

## Constraints / Non-Goals

- This dispatch is test coverage only.
- Do not change `OperatorGraph`, `OperatorNode`, `CuboidOp`, `TransformOp`, `EdgeKind`, graph-foundation `Graph`, graph-foundation `GraphSnapshot`, graph-foundation `GraphDiff`, `EdgeRecord`, id types, serialization behavior, or public APIs.
- Do not add a cad-core structural-diff wrapper, domain-diff abstraction, renderer-tier diff, editor diff, graph-viewer diff, checkpoint diff, or persistence diff.
- Do not broaden this into snapshot RON round-trip coverage; GitHub issue #54 already owns that smoke test.
- Do not implement, modify, or test `VizAdapter`; GitHub issue #53 owns that separately.
- Do not add traversal, evaluation, cycle detection, graph mutation semantics, editor behavior, renderer behavior, graph-viewer behavior, runtime behavior, checkpoint behavior, cache behavior, persistence behavior, ECS behavior, asset behavior, or material/animation graph behavior.
- Do not add dependencies, feature flags, manifests, lockfile updates, generated files, architecture-lint policy, docs, ADRs, or CI changes.

## Verification Gates

The Executor MUST run and document the result of this command in their `EXECUTION_REPORT`:

- `cargo test -p rge-cad-core --test operator_graph_structural_diff_smoke` -> expected exit 0.

The Executor SHOULD also run and document this scoped formatting check if the nightly formatter is available:

- `cargo +nightly fmt --package rge-cad-core -- --check` -> expected exit 0.

If the local shell cannot find `cargo`, the Executor may use the repository's established Rust toolchain location (`A:\RustCache\cargo\bin\cargo.exe`) if present, matching `.ai/dispatch.verify.ps1`; otherwise halt and report the missing tool.

If a broader workspace verification wrapper or workspace formatting check fails solely because of pre-existing out-of-scope files, the Executor must not fix those files. Record the out-of-scope failure clearly in the execution report after the required scoped cad-core test gate has been attempted.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- The change cannot be completed by adding or editing only `crates/cad-core/tests/operator_graph_structural_diff_smoke.rs`.
- The implementation appears to require edits to `OperatorGraph`, graph-foundation, operator definitions, manifests, lockfiles, docs, scripts, schemas, CI files, architecture-lint tooling, or any existing file.
- The test cannot use public cad-core and graph-foundation APIs to build the graph, snapshot it, mutate it with one added operator and one added edge, snapshot again, and diff the snapshots.
- The solution requires a public API expansion, including test-only accessors or new cad-core wrappers around `GraphDiff`.
- The solution requires changing serialization derives, graph id derivation, edge record shapes, snapshot APIs, diff APIs, checkpoint APIs, traversal/evaluation behavior, or public method signatures.
- Cargo attempts to modify any manifest, lockfile, generated file, or file outside the MAY edit scope.
- `cargo test -p rge-cad-core --test operator_graph_structural_diff_smoke` fails after the scoped test addition and the failure is not an obvious typo that can be fixed within `crates/cad-core/tests/operator_graph_structural_diff_smoke.rs`.
- A pre-existing environment problem, such as missing cargo after checking the established RustCache path, prevents running the required verification gate.

## Planner Notes

This task was filed automatically by `Invoke-AiDispatchAuto.ps1` from `.ai/dispatch.tasks.md` for GitHub issue #55. Revision number: 0. There is no prior Claude gate result.

Useful public imports for the integration test are expected to include `rge_cad_core::{CuboidOp, EdgeKind, OperatorGraph, OperatorNode, TransformOp}` and `rge_kernel_graph_foundation::{EdgeRecord, GraphDiff, GraphSnapshot}`. `OperatorGraph::inner()` is already public and should be sufficient for `GraphSnapshot::from_graph`, so no cad-core API change should be needed. Keep the graph small: old snapshot has two nodes and one edge, new snapshot has three nodes and two edges, and the diff should show only the one added node and one added edge.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-55
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

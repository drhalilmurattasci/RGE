# Task Packet

DISPATCH_ID: ISSUE-402
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-15_05-32-29+0300
RELATED_FILES:
- .ai/dispatch.tasks.md
- Status.md
- HANDOFF.md
- plans/BASELINE.md
- change.md
- ai_handoffs/ISSUE-402_*
- ai_dispatch_logs/log_*.md
- crates/editor-ui/src/menus/**
- crates/editor-egui-host/src/**
- crates/editor-shell/src/lifecycle/**
- crates/editor-shell/src/render_path.rs
- crates/editor-actions/**
- crates/cad-projection/**
- crates/plugin-discovery/**
- runtime/**
STATUS: OPEN

## Goal

Run task 148 as a docs/source-read-only Phase 9/editor-usability audit after
ISSUE-401 / task 147's Zoom In/Out stale double-click reset. Compare the
remaining editor-usability candidate classes against current local source and
status docs, then append exactly one bounded FEATURE task 149 to
`.ai/dispatch.tasks.md`, or record a single source-grounded
`NEEDS_HUMAN_RECORDED: YYYY-MM-DD - concise reason` line if no bounded
in-policy feature exists. Use only the dispatcher-provided GitHub state
snapshot embedded in this issue body for queue/already-filed evidence; do not
call `gh`, browser, network, or the GitHub API.

## Scope

This is an audit and task-selection dispatch. Reading Rust source and status
docs is required. Editing production source, tests, Cargo metadata, automation,
schemas, workflows, ADRs, packet templates, plugin/command/clipboard/CAD/camera
behavior, or unrelated handoff/log artifacts is not allowed.

### MAY edit
- `.ai/dispatch.tasks.md`
- `Status.md`
- `HANDOFF.md`
- `plans/BASELINE.md`
- `change.md`
- `ai_handoffs/ISSUE-402_*`
- `ai_dispatch_logs/log_*.md`

### MUST NOT edit
- Rust source or tests under `crates/**`, `kernel/**`, `editor/**`,
  `runtime/**`, `tools/**`, `plugins/**`, `tests/**`, or `examples/**`.
- Cargo manifests or `Cargo.lock`.
- `.github/workflows/**`.
- `.ai/*.schema.json`.
- `.ai/dispatch.verify.ps1`.
- Dispatch automation, guard, queue, scheduler, watcher, verification, health,
  trend, or handoff scripts.
- Schemas, ADR files, architecture-lint rules/config, packet templates, or
  unrelated existing handoff/log artifacts.
- Plugin runtime/discovery/loading code, command routing, shortcut execution,
  remapping/persistence/fatal-policy behavior, OS clipboard behavior,
  CAD/projection/CommandBus mutation, undo/dirty/save-load authority,
  camera/navigation behavior, camera math, viewport hit testing, face-pick
  policy, wheel zoom, right-button orbit math, middle-button pan math,
  focus-loss behavior, cursor-left behavior, reset-camera behavior, or Zoom
  In/Out behavior.

### MAY add new files
- `ai_handoffs/ISSUE-402_EXEC_*.md`
- `ai_handoffs/ISSUE-402_EXEC_*.meta.json`
- `ai_handoffs/ISSUE-402_CORRECT_*.md`
- `ai_handoffs/ISSUE-402_CORRECT_*.meta.json`
- `ai_dispatch_logs/log_*.md`

### MUST NOT add new files
- New Rust source, tests, fixtures, Cargo manifests, workflows, schemas, ADRs,
  architecture-lint rules/config, dispatch automation scripts, packet
  templates, or non-generated documentation files.
- Any handoff/log artifact not generated for ISSUE-402.
- Task 150 or any task-150 generated artifact during this dispatch. Task 149,
  if appended, must be the self-re-arming feature that later appends task 150
  as a bounded audit or records `NEEDS_HUMAN_RECORDED`.

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - .ai/dispatch.tasks.md
  - Status.md
  - HANDOFF.md
  - plans/BASELINE.md
  - change.md
  - ai_handoffs/ISSUE-402_*
  - ai_dispatch_logs/log_*.md
MUST_NOT_EDIT:
  - crates/**
  - kernel/**
  - editor/**
  - runtime/**
  - tools/**
  - plugins/**
  - tests/**
  - examples/**
  - Cargo.toml
  - Cargo.lock
  - **/Cargo.toml
  - .github/workflows/**
  - .ai/*.schema.json
  - .ai/dispatch.verify.ps1
  - Invoke-*.ps1
  - Register-AiDispatchSchedule.ps1
  - Get-AiDispatchHealth.ps1
  - Get-AiDispatchTrends.ps1
  - Watch-*.ps1
  - Wait-GitHubActions.ps1
  - new-handoff.ps1
  - ai_handoffs/templates/**
  - docs/adr/**
  - plans/PLAN.md
  - plans/IMPLEMENTATION.md
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Deliverables

- A recorded task-148 audit in the allowed docs/brief surface that compares all
  required candidate classes using current local source reads.
- Exactly one bounded FEATURE task 149 appended to `.ai/dispatch.tasks.md`, or
  exactly one `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - concise reason` line appended
  if no bounded in-policy next feature exists.
- If task 149 is appended, it must include explicit `MAY edit`,
  `MUST NOT edit`, `Done criteria`, `Verification`, and `Halt conditions`
  sections.
- If task 149 is appended, it must carry the Self-re-arm final-step requirement
  so the next FEATURE dispatch appends exactly one bounded AUDIT task 150 or
  records `NEEDS_HUMAN_RECORDED`.
- Concise status updates in the allowed status docs recording the task-148
  audit outcome and the task-149-or-human result.
- A completed ISSUE-402 EXEC packet describing the audit, source evidence,
  verification, and final task-149-or-human outcome.

## Acceptance Criteria

- The pre-edit task-heading check is recorded using
  `rg -n "^145\.|^146\.|^147\.|^148\.|^149\." .ai/dispatch.tasks.md`.
- Queue/already-filed-task claims cite only the dispatcher-provided snapshot
  from this ISSUE-402 issue body. The usable snapshot facts are: generated by
  `Invoke-AiDispatchAuto.ps1` at `2026-06-15T05:12:54.9060513+03:00` for
  `RustCADs/RGE`; no open `ai-dispatch` issues before ISSUE-402 was created; no
  open failed autonomous issues; autonomous issues already filed through closed
  ISSUE-401 / task 147; no live `gh`, browser, network, or GitHub API command
  is run for those claims.
- The audit cross-checks `.ai/dispatch.tasks.md`, `Status.md`, `HANDOFF.md`,
  `plans/BASELINE.md`, and `change.md` for the post-task-147 state before
  editing them.
- The audit compares these candidate classes from current source and records
  positive source references, and for every negative or unchanged source claim
  either records the falsifying search/read plus observed result or states the
  item as an open question rather than a premise:
  - keybinding remapping, preferences, conflict policy, and fatal-policy
    behavior;
  - host-shell command routing through `MenuCommandHandoff` /
    `EditorShell::route_menu_command`;
  - real plugin command execution beyond the injected extension-command seam;
  - OS or typed clipboard behavior beyond the current shell-local clipboard;
  - CAD/editor mutation through `CommandBus`, projection, undo/dirty, and
    save/load authority;
  - camera/navigation follow-up after wheel zoom, orbit, pan, frame-all,
    selected-CAD framing, cursor grab/release, focus-loss drag cancellation,
    cursor-left cancellation, focus-loss stale double-click reset,
    mouse-wheel stale double-click reset, reset-camera stale double-click
    reset, and Zoom In/Out stale double-click reset.
- The selected task 149, if appended, is one bounded FEATURE task only. It must
  not implement itself, open a second follow-up, or require a cross-boundary
  policy/architecture decision to be coherent.
- If no bounded task 149 can be specified safely, the executor appends exactly
  one `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - concise reason` line instead of
  forcing a task.
- No implementation work for task 149 is done, and no Rust/source/test/Cargo
  files are edited.

## Current-State Claims: Falsification Required

This planner packet intentionally avoids negative source claims as premises.
The executor must ground the audit in fresh local source reads and must attach
the inverse search/read that would falsify every negative or "unchanged" source
claim used to select or reject task 149.

- Claim: GitHub queue/already-filed evidence is provided by the dispatcher
  snapshot embedded in the ISSUE-402 body, not by live GitHub state.
  Falsifying search: not applicable to repository source; executor must cite
  the snapshot facts listed in `Acceptance Criteria` and must not run `gh`,
  browser, network, or GitHub API commands for this evidence.
- Claim: Task 148 is the audit being executed and task 149 is the only allowed
  next task number.
  Falsifying search:
  `rg -n "^145\.|^146\.|^147\.|^148\.|^149\." .ai/dispatch.tasks.md` ->
  observed pre-planning result:
  `14101:145. **[DONE 2026-06-15 via ISSUE-399] Reset stale viewport left-double-click state on View/Home reset camera.**`;
  `14217:146. **[DONE 2026-06-15 via ISSUE-400] Post-reset-camera-double-click-reset Phase 9 next-task source audit.**`;
  `14426:147. **[DONE 2026-06-15 via ISSUE-401] Reset stale viewport left-double-click state on View/PageUp/PageDown Zoom In/Out.**`;
  `14548:148. **Post-zoom-command-double-click-reset Phase 9 next-task source audit.**`.
  No `149.` match was returned. Executor must record the post-edit result in
  the EXEC packet after appending task 149 or the human-needed marker.
- Claim: Status docs currently describe task 147 as done and task 148 as the
  open follow-up.
  Falsifying read:
  `rg -n "ISSUE-401|task 147|task 148|Zoom In|Zoom Out|double-click" Status.md HANDOFF.md plans/BASELINE.md change.md`
  -> observed matches include `Status.md:3`, `HANDOFF.md:3`,
  `plans/BASELINE.md:5`, and `change.md:1939` documenting ISSUE-401/task-147,
  plus `plans/BASELINE.md:12-13` and the top snapshots in `Status.md` and
  `HANDOFF.md` naming task 148 as the open audit. Executor must re-run and
  record the exact status-doc cross-check before editing.

## Constraints / Non-Goals

- Do not implement task 149.
- Do not edit production source, tests, Cargo metadata, workflows, schemas,
  automation scripts, packet templates, ADRs, or unrelated handoff/log
  artifacts.
- Do not use live GitHub state. The dispatcher-provided snapshot embedded in
  the ISSUE-402 issue body is the only authority for queue/already-filed
  evidence.
- Do not select more than one next feature. If the next coherent work requires a
  batch, policy decision, new architecture boundary, or multiple independent
  changes, record `NEEDS_HUMAN_RECORDED` instead.
- Do not broaden task 149 into shortcut remapping/persistence/fatal-policy,
  plugin discovery/loading/runtime execution, OS clipboard integration,
  CAD/CommandBus mutation, undo/dirty/save-load authority, or camera/navigation
  behavior unless the audit proves a single bounded in-policy feature can be
  scoped without crossing those boundaries.
- Preserve the Self-re-arm chain. If task 149 is appended, its final step must
  require appending exactly one bounded AUDIT task 150 or a single
  `NEEDS_HUMAN_RECORDED` line.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- `rg -n "^145\.|^146\.|^147\.|^148\.|^149\." .ai/dispatch.tasks.md` before
  edits and after edits.
- Status-doc cross-check against `.ai/dispatch.tasks.md`, `Status.md`,
  `HANDOFF.md`, `plans/BASELINE.md`, and `change.md` for `ISSUE-401`,
  `task 147`, `task 148`, `task 149`, `Zoom In`, `Zoom Out`, and
  `double-click`.
- Candidate-class source greps/readouts recorded in the audit for each
  required class listed in `Acceptance Criteria`. At minimum, cover shortcut
  remapping/preferences/fatal policy, `MenuCommandHandoff` /
  `route_menu_command`, extension/plugin-command execution seams, OS/typed
  clipboard terms, `CommandBus`/CAD/projection/save/dirty/undo authority, and
  camera/navigation/double-click reset surfaces.
- `git diff --name-only` showing only allowed docs/brief paths plus generated
  ISSUE-402 artifacts.
- `git diff --check`.
- Resolve the single `ai_handoffs/ISSUE-402_EXEC_*.md` packet path and run
  `.\new-handoff.ps1 -Finalize -PacketPath $execPacket -DryRun`.

Cargo/test/lint gates are not requested for this docs/source-read-only audit.
If the orchestrator's canonical verification gate later runs and fails on a
target outside this task's edit scope, the executor must treat any correction
request to fix source/tests/Cargo/automation as out of scope and halt per the
conditions below.

## Halt Conditions

The Executor MUST halt and request fresh guidance if any of the following
occur:

- The dispatcher-provided GitHub-state snapshot cannot be cited without live
  `gh`, browser, network, or GitHub API access.
- The audit would require editing any path outside `MAY edit` or adding any
  file outside `MAY add new files`.
- Any implementation work for task 149 appears necessary during this audit.
- More than one feature follow-up would be required to make the selected
  boundary coherent.
- The final `.ai/dispatch.tasks.md` state would contain both a task 149 and a
  `NEEDS_HUMAN_RECORDED` line, more than one task 149, or any task 150.
- Any verification/correction round asks the executor to fix a source, test,
  Cargo, workflow, schema, automation, packet-template, or unrelated artifact
  failure outside this task's docs/source-read-only scope.

If no bounded task 149 can be specified without crossing policy or architecture
boundaries, do not halt merely to ask for permission to invent one. Record the
single `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - concise reason` line allowed by this
packet and stop.

## Planner Notes

Task 147 landed as ISSUE-401 and re-armed the brief with task 148, a
post-Zoom-command-double-click-reset source audit. The dispatcher snapshot in
this issue body is the only GitHub evidence source for this dispatch; its
relevant facts are repeated in this packet so the executor does not need to
query GitHub. Treat camera/navigation details as source claims to verify during
the audit, not as authorization to change camera behavior.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-402
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Correction Packet

DISPATCH_ID: ISSUE-254
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-28T23:47:27+03:00
RELATED_FILES:
- ai_handoffs/ISSUE-254_EXEC_2026-05-28_23-36-58+0300.md
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-254_TASK_2026-05-28_22-57-20+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-254_EXEC_2026-05-28_23-36-58+0300.md`
- Prior Correction Packet: `ai_handoffs/ISSUE-254_CORRECT_2026-05-28_23-30-28+0300.md`
- Codex Control Review: `.ai/dispatch-ISSUE-254/codex.control.round1.json`
- Verification Log: `.ai/dispatch-ISSUE-254/verification.round1.log`

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections, nothing more and nothing
less. Each correction addresses the Codex control review verdict
`needs_changes` for round 1.

1. **Self-contained corrected audit answer** - Codex control found that the
   latest EXEC report incorporated the five Task-required audit sections only by
   reference to a superseded EXEC packet. Required change: write a new corrected
   `EXECUTION_REPORT` that contains the five Task-required audit sections as a
   self-contained corrected answer block in the new EXEC packet. Apply the
   round1 `MenuRegistry` and `fs::write` evidence corrections inline in that
   block rather than referring readers back to
   `ai_handoffs/ISSUE-254_EXEC_2026-05-28_23-06-49+0300.md` or to
   `ai_handoffs/ISSUE-254_CORRECT_2026-05-28_23-30-28+0300.md`.
   Acceptance: the new EXEC report can stand alone as the corrected audit
   artifact and includes the five required audit sections directly, with no
   incorporation-by-reference needed to understand the corrected answer.

2. **Accurate verification substeps** - Codex control found that the latest
   EXEC report incorrectly claimed a standalone workspace build ran. Required
   change: correct the new EXEC report's `Verification Results` section to
   match `.ai/dispatch-ISSUE-254/verification.round1.log` and
   `.ai/dispatch.verify.ps1`: step 4 was `cargo deny check`, step 5 was
   `cargo test --workspace --all-targets --no-fail-fast -j 1`, and no
   standalone `cargo build --workspace --all-targets` step ran. Acceptance: the
   verification section lists the canonical round1 gate accurately and contains
   no claim that `cargo build --workspace --all-targets` ran as an independent
   verification step.

## Deferred Findings (NOT Approved for This Round)

None. Codex control identified only the two EXEC-report documentation fixes
approved above.

## Updated Acceptance Criteria

Unchanged from Task Packet, with this clarification for the correction round:
the deliverable is a corrected EXEC report only. Source, docs, schemas, scripts,
and older packet files remain out of scope.

## Re-Verification Gates

The Executor MUST re-run or re-check these gates after the corrections:

- `git status --short --branch -uall` -> expected exit 0; no tracked source,
  docs, schemas, or scripts modified by this correction round.
- `git diff --stat; git diff --name-only; git diff --check` -> expected clean
  tracked diff and no diff-check errors.
- Inspect `.ai/dispatch-ISSUE-254/verification.round1.log` for `VERIFY OK` and
  `--- ok:` lines -> expected evidence that all seven canonical verification
  steps passed.
- Inspect `.ai/dispatch-ISSUE-254/verification.round1.log` for
  `cargo deny check` and `cargo build` -> expected evidence that `cargo deny
  check` was step 4, no standalone `cargo build --workspace --all-targets` hit
  exists, and the new EXEC report matches that fact.
- Re-read the new EXEC report -> expected self-contained corrected audit answer
  block with the five required audit sections and inline round1 `MenuRegistry`
  and `fs::write` corrections.

## Halt Conditions (Updated if Any)

Unchanged from Task Packet. Halt if any correction would require editing source,
docs, schemas, scripts, older packets, or files outside the new EXEC report.

## Planner Notes

The work tree and round1 verification evidence are already green. This
correction round is limited to making the next EXEC report the canonical audit
artifact: it must carry the corrected audit answer itself and must document the
canonical verification substeps exactly as the round1 log and verification
script recorded them.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-254
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Task Packet

DISPATCH_ID: ISSUE-48
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_14-45-14+0300
RELATED_FILES:
- crates/anim-graph/src/lib.rs
- kernel/graph-foundation/src/viz_adapter.rs
- crates/material-graph/src/lib.rs
STATUS: OPEN

## Goal

Implement GitHub issue #48 by adding a read-only
`rge_kernel_graph_foundation::VizAdapter` implementation for `AnimGraph`.
The adapter must expose the existing animation graph structure to graph-viewer
consumers without changing animation state or transition payload shapes,
without adding runtime/editor/renderer behavior, and without duplicating
structural state already owned by the graph-foundation substrate.

## Scope

### MAY edit
- `crates/anim-graph/src/lib.rs` only.

Within that file, the Executor may add the `VizAdapter` import and
implementation, any small private helper strictly needed for the adapter, and
focused inline `#[cfg(test)]` unit tests for the adapter.

### MUST NOT edit
- Any file other than `crates/anim-graph/src/lib.rs`.
- `kernel/graph-foundation/**`, including `kernel/graph-foundation/src/viz_adapter.rs`.
- `crates/material-graph/**`; it is read-only precedent only.
- Renderer-tier crates, including `crates/gfx/**`, `crates/gfx-ir/**`,
  `crates/brep-render/**`, and `crates/components-render/**`.
- Any Phase 9 work, Phase 9 planning files, or Phase 9 implementation surface.
- Cargo manifests, lockfiles, schemas, scripts, CI files, docs, status files,
  `.gitignore`, or existing handoff packets.
- Any animation runtime, animation editor, clip, IK, retarget, events, ECS,
  asset, material, renderer, or gfx crate.

### MAY add new files
- One protocol-required execution report matching `ai_handoffs/ISSUE-48_EXEC_*.md`.
- The matching execution-report `.meta.json` sidecar if generated by
  `new-handoff.ps1 -Finalize`.

### MUST NOT add new files
- No new Rust source files, integration-test files, fixtures, snapshots,
  docs, schemas, scripts, Cargo entries, architecture-lint files, renderer
  assets, Phase 9 files, or handoff packets other than the required ISSUE-48
  execution report and its finalize-generated sidecar.

## Deliverables

- Implement `rge_kernel_graph_foundation::VizAdapter` for `AnimGraph` in
  `crates/anim-graph/src/lib.rs`.
- Adapter `node_count` and `edge_count` must delegate to the existing
  `AnimGraph` or substrate counts and must not introduce duplicate count state.
- Adapter `nodes()` must iterate existing animation graph nodes through
  `Graph::nodes()`. Each `NodeView` must use:
  - `id`: the existing substrate `NodeId`.
  - `display_name`: the existing `AnimState.key` string.
  - `kind`: the stable static string `"AnimState"`.
- Adapter `edges()` must iterate existing animation graph edge records through
  `Graph::edges()`. Each `EdgeView` must use:
  - `id`: the existing substrate `EdgeId`.
  - `src`: the edge record source node id.
  - `dst`: the edge record destination node id.
  - `label`: the existing `AnimTransition.trigger` string.
- The adapter must borrow from existing payloads or records. It must not add
  new graph storage, cached view state, cloned structural records, side maps,
  public traversal APIs, or new payload fields.
- Add focused inline unit tests that exercise the adapter through
  `&dyn VizAdapter` for counts, deterministic node views, and deterministic
  edge views.
- A completed execution report packet for this dispatch.

## Acceptance Criteria

- The only implementation file changed is `crates/anim-graph/src/lib.rs`.
- `AnimGraph` implements `VizAdapter` and is usable through `&dyn VizAdapter`.
- Existing public construction APIs remain unchanged: do not change
  `AnimGraph::new`, `Default`, `add_state`, `add_transition`, `node_count`,
  `edge_count`, `AnimState`, `AnimTransition`, or their signatures.
- Existing node and edge payload shapes remain unchanged: do not add or remove
  fields on `AnimState` or `AnimTransition`, and do not change their field
  meanings.
- Node views are backed by existing animation state payloads: `display_name`
  is the state key, `kind` is exactly `"AnimState"`, and node iteration order
  is the deterministic substrate order returned by `Graph::nodes()` rather
  than insertion order.
- Edge views are backed by existing graph edge records: ids, sources, and
  destinations come from `Graph::edges()` records; labels come only from the
  existing transition trigger string; and edge iteration order is the
  deterministic substrate order returned by `Graph::edges()` rather than
  insertion order.
- Unit tests include:
  - an adapter count test that observes node and edge counts through
    `&dyn VizAdapter`;
  - a node-view test that asserts ids, display names, kind values, and
    deterministic substrate order;
  - an edge-view test that asserts edge ids, src/dst ids, trigger labels, and
    deterministic substrate order.
- Existing animation graph behavior remains intact, including stable state
  ids, duplicate state rejection, successful transition insertion, duplicate
  transition rejection, differing-trigger edge distinctness, dangling-endpoint
  rejection, and empty/default counts.

## Constraints / Non-Goals

- Do not change graph-foundation APIs, trait definitions, view structs, graph
  storage, ID derivation, duplicate detection, or iteration order.
- Do not copy the material graph implementation mechanically into
  material-graph; use it only as read-only precedent for adapter shape and
  tests.
- Do not add animation traversal, topological sorting, cycle detection,
  evaluation, active-state runtime, blend-tree behavior, clip sampling,
  transition conditions, guards, weights, timing, editor widgets, renderer
  integration, asset loading, ECS behavior, serialization, snapshot/diff
  behavior, or invalidation behavior.
- Do not add dependencies, feature flags, Cargo manifest changes, lockfile
  changes, architecture-lint changes, or new test files.
- Do not update GitHub issue metadata, labels, comments, branches, commits, or
  remotes.
- Keep any helper functions private to `crates/anim-graph/src/lib.rs` unless
  the existing `VizAdapter` trait requires otherwise.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- `cargo +nightly fmt --check -p rge-anim-graph` -> expected exit 0.
- `cargo test -p rge-anim-graph --all-targets --no-fail-fast` -> expected exit 0.
- `cargo run -q -p rge-tool-architecture-lints -- all` -> expected exit 0.
- `git diff -- crates/anim-graph/src/lib.rs` -> expected diff is limited to
  the `VizAdapter` import/implementation, any strictly needed private helper,
  and focused inline adapter unit tests.
- `git diff --check -- crates/anim-graph/src/lib.rs` -> expected exit 0.
- `git status --short --untracked-files=all` -> expected no unexpected
  tracked or untracked changes outside `crates/anim-graph/src/lib.rs` and the
  required ISSUE-48 execution-report packet/sidecar.

If a required command cannot be run in the environment, the Executor MUST
document the exact command, the failure reason, and any partial verification
that was completed.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of
the following occur:

- Implementing the adapter appears to require editing any file other than
  `crates/anim-graph/src/lib.rs` plus the required execution report packet.
- Implementing the adapter appears to require changing `kernel/graph-foundation`
  APIs or the `VizAdapter`, `NodeView`, or `EdgeView` definitions.
- Passing tests appears to require changing `AnimState` or `AnimTransition`
  payload shapes, public construction API signatures, ID derivation, duplicate
  detection, or graph-foundation behavior.
- The adapter cannot expose transition trigger labels by borrowing the existing
  `AnimTransition.trigger` string under the current `EdgeView` lifetime shape.
- Any graph traversal, runtime animation behavior, editor-widget work,
  renderer-tier work, Phase 9 work, dependency change, Cargo manifest change,
  or new Rust file appears necessary.
- `cargo test -p rge-anim-graph --all-targets --no-fail-fast` fails for
  reasons unrelated to the scoped adapter implementation and cannot be
  resolved within `crates/anim-graph/src/lib.rs`.
- `cargo run -q -p rge-tool-architecture-lints -- all` reports an enforcement
  failure that is unrelated to the scoped adapter implementation.
- Any existing test or lint outside `rge-anim-graph` must be weakened, ignored,
  deleted, renamed, or rebaselined.
- Verification shows unexpected tracked or untracked changes outside the
  MAY-edit scope and required ISSUE-48 execution-report packet/sidecar.

## Planner Notes

This task was filed automatically by `Invoke-AiDispatchAuto.ps1` from
`.ai/dispatch.tasks.md` for GitHub issue #48. Revision number: 0. There is no
prior Claude gate result.

`crates/material-graph/src/lib.rs` is the closest read-only precedent from
ISSUE-47. The animation version should be simpler: node labels come from
`AnimState.key`, every node uses one stable kind string, and edge labels can
borrow `AnimTransition.trigger.as_str()` directly instead of using a static
port-pair label helper.

The implementation must remain a view adapter only. It should not expand
`AnimGraph` toward an animation state machine, blend tree, editor model,
renderer integration, or Phase 9 substrate.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-48
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Execution Report

DISPATCH_ID: ISSUE-214
AUTHOR: Executor / Codex (manual salvage)
TIMESTAMP: 2026-05-26_21-45-00+0300
RELATED_FILES:
- ai_handoffs/ISSUE-214_TASK_2026-05-26_20-39-49+0300.md â€” TASK packet
  under correction-round execution (read-only Phase 9
  invalidation-economics audit)
- ai_handoffs/ISSUE-214_TASK_2026-05-26_20-39-49+0300.meta.json â€” TASK
  sidecar (pre-existing, untouched)
- ai_handoffs/ISSUE-214_EXEC_2026-05-26_20-53-50+0300.md â€” prior EXEC
  report (left in place per CORRECT packet, this report supersedes it
  for review)
- ai_handoffs/ISSUE-214_EXEC_2026-05-26_20-53-50+0300.meta.json â€” prior
  EXEC sidecar (pre-existing, untouched)
- ai_handoffs/ISSUE-214_CORRECT_2026-05-26_21-09-39+0300.md â€” Planner
  CORRECT packet authorizing this report-only correction round
- ai_handoffs/ISSUE-214_CORRECT_2026-05-26_21-09-39+0300.meta.json â€”
  CORRECT sidecar (pre-existing, untouched)
- AGENTS.md â€” orchestrator vs. manual protocol doctrine
- AI_DISPATCH_AUTOMATION.md â€” per-run scratch and post-run inspection
- ai_handoffs/AI_HANDOFF_PROTOCOL.md â€” manual-protocol packet types
- plans/PLAN.md â€” Â§1.10 / Â§1.10.4 entropy metrics; Â§13.x baselines
- plans/IMPLEMENTATION.md â€” Phase 9 Â§9 eight evaluation axes
- plans/BASELINE.md â€” Â§1.10.4 invalidation-radius preflight; Â§13.3
  compile-time preflight
- Status.md â€” pillar table and Phase 7.3 gate-closure row
- HANDOFF.md â€” Phase 9 framing snapshot
- change.md â€” 2026-05-21 Phase 9 preflight entries (live re-read this
  dispatch â€” canonical PowerShell view, see Â§Citation Hygiene)
- kernel/graph-foundation/src/invalidation.rs â€” `Invalidation` router
  surface (234 LoC)
- kernel/graph-foundation/tests/invalidation_dag.rs â€” 4-test DAG
  propagation gate (117 LoC)
- tools/invalidation-profiler/src/main.rs â€” 5-line stub binary
- crates/cad-core/src/tessellation/cache.rs â€” `TessellationCache`
  (`(structural_hash, tolerance)`-keyed memoization, 268 LoC)
- crates/cad-core/src/tessellation/mod.rs â€” 19-line module re-export
- crates/cad-core/tests/labeled_tessellation_pipeline.rs â€” labeled
  cache-flow gate (415 LoC)
- crates/cad-projection/src/projection_cache/mod.rs â€” `ProjectionCache`
  (per-entity dirty bits + head-tracking, 319 LoC)
- crates/cad-projection/src/lib.rs â€” `CadProjection::tick` orchestrator
  (995 LoC)
- crates/gfx/src/frame_graph/resource_map.rs â€” per-frame ResourceMap
  builder + Arc-dedup contract (358 LoC)
- crates/gfx/src/frame_graph/texture_pool.rs â€” N=2 ring-buffered
  transient-texture pool (282 LoC)
- crates/gfx/src/frame_graph/buffer_pool.rs â€” N=2 ring-buffered
  transient-buffer pool (272 LoC)
- crates/gfx/src/frame_graph/compile/algorithm.rs â€” Kahn topo sort +
  lifetime + greedy aliasing (512 LoC)
- crates/gfx/src/frame_graph/compile/mod.rs â€” compile public surface
  (43 LoC)
- crates/gfx/tests/frame_graph_smoke.rs â€” 3-pass smoke (208 LoC)
- crates/gfx/tests/frame_graph_umbrella_smoke.rs â€” cross-frame
  composition smoke (189 LoC)
- crates/script-bench/BASELINE.md â€” formal Phase 3.3/3.4 hot-reload p95
  + 1 h memory soak measurements
- crates/script-bench/METHODOLOGY.md â€” workload construction +
  reproducibility methodology
- crates/script-bench/benches/hot_reload_swap.rs â€” criterion bench
  driving the formal 1000Ã—100 gate
- crates/script-host/src/swap.rs â€” `capture_state` / `restore_state`
  swap protocol (170 LoC)
- crates/script-host/tests/swap_smoke.rs â€” swap protocol smoke
- crates/hot-reload-watcher/src/lib.rs â€” 4-line empty stub crate
- ai_dispatch_logs/log_2026-05-26_15-53-41+0300.md â€” ISSUE-210
  (freeze-validity) control-review audit log (read-only provenance)
- ai_dispatch_logs/log_2026-05-26_20-15-22+0300.md â€” ISSUE-212
  (abstraction-pain) control-review audit log (read-only provenance)
- ai_handoffs/ISSUE-212_EXEC_2026-05-26_20-30-00+0300.md â€” prior
  delegated-main smoke batch EXEC (read-only precedent for format and
  citation-hygiene convention)
- .ai/dispatch-ISSUE-214/verification.round0.log â€” prior orchestrator
  canonical gate log (read-only; preserved unchanged)
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-214_TASK_2026-05-26_20-39-49+0300.md`

Active Correction Packet:
`ai_handoffs/ISSUE-214_CORRECT_2026-05-26_21-09-39+0300.md` (round 0).

## Pre-Execution Review Consumed

No pre-execution review issued for this dispatch. The Codex advisory
pre-flight checklist (P1â€“P8 / V1â€“V8) shipped alongside the original
executor prompt was treated as advisory only per its own boundary
statement and does not expand the TASK packet's scope. Per orchestrator
doctrine (`AGENTS.md:26-36`), an orchestrator dispatch writes only
`TASK` + `EXEC` packets (plus a `CORRECT` packet if a correction round
runs); no `REVIEW` packet is expected from this TASK. The TASK packet
plus the round-0 CORRECT packet are jointly authoritative for this
report.

This is a human-directed manual salvage of a Planner-approved
report-correction round: the prior EXEC at
`ai_handoffs/ISSUE-214_EXEC_2026-05-26_20-53-50+0300.md` is left in
place per the CORRECT packet's preservation envelope, the corrected
EXEC at `ai_handoffs/ISSUE-214_EXEC_2026-05-26_21-17-21+0300.md`
is left in place as failed-round evidence, and this report supersedes
both for review.

## What I Changed

### Source

- None. This is an audit-only dispatch. The TASK packet declared
  `### MAY edit: None`
  (`ai_handoffs/ISSUE-214_TASK_2026-05-26_20-39-49+0300.md:58-59`) and
  the CORRECT packet's approved-corrections section restricts this
  round to writing one corrected follow-up `ISSUE-214` EXEC packet plus
  its matching `.meta.json` sidecar
  (`ai_handoffs/ISSUE-214_CORRECT_2026-05-26_21-09-39+0300.md:29-39`).

### Tests

- None.

### Docs

- None.

### New audit-only artifacts

- `ai_handoffs/ISSUE-214_EXEC_2026-05-26_21-45-00+0300.md` (this file).
- Matching `.meta.json` sidecar produced by
  manual salvage after this body is finalized.

No `_REVIEW_` or `_CLOSEOUT_` packet is created (orchestrator doctrine:
`AGENTS.md:26-36`; per-run scratch including the Codex control JSON
lands in `.ai/dispatch-<ID>/codex.control.round*.json`, never as a
packet â€” `AI_DISPATCH_AUTOMATION.md:961-973`). The manual `TASK ->
EXEC -> REVIEW -> CORRECT -> CLOSEOUT` chain
(`ai_handoffs/AI_HANDOFF_PROTOCOL.md:128-145` enumerates the
`<PACKET_TYPE>` set) is a different shape and does not apply here. The
optional `ai_dispatch_logs/log_*.md` and `.ai/dispatch-ISSUE-214/**`
artifacts allowed by the TASK packet's `### MAY add new files` list
are produced by the orchestrator harness around the run, not by this
EXEC report.

## Decision block

INVALIDATION_ECONOMICS_VERDICT: does_not_warrant_next_dispatch
COST_CLASS: structural
SMALLEST_SAFE_FOLLOW_UP: Docs-only Phase 9 `cad-projection`
invalidation-density PREFLIGHT â€” append one new section to
`plans/BASELINE.md` mirroring the 2026-05-21 Â§1.10.4 / Â§1.1 /
Editor-usability / Live-inspector preflight entries (`change.md:1633` /
`:1635` / `:1637` / `:1639` / `:1641`), plus the standard append-only
chronology entry in `change.md`. The section captures the current
structural shape (head-advance â‡’ all entities dirty per
`crates/cad-projection/src/projection_cache/mod.rs:164-176`), states
explicitly that no measured density exists today, and codifies revisit
triggers under which a real density measurement or per-node dirty
bookkeeping becomes warranted. The follow-up MUST NOT modify source,
tests, Cargo, scripts, schemas, workflows, scheduler configuration, or
`.ai/dispatch.tasks.md`. PublishMode branch only unless later
authorized by a human.

This is the standard PLAN-and-BASELINE chronology shape used by every
2026-05-21 Phase 9 preflight (two-file docs-only: one new BASELINE.md
section + one new `change.md` row). It is NOT single-file; the
follow-up is described consistently as docs-only two-file throughout
this report. The PublishMode-branch constraint is included even though
both files are documentation because `plans/BASELINE.md` carries Phase
9 doctrine and the prior delegated-main smoke batch precedent â€”
ISSUE-212 EXEC at
`ai_handoffs/ISSUE-212_EXEC_2026-05-26_20-30-00+0300.md:529-534` â€”
selected branch-mode for the analogous ADR follow-up. A human reviewer
may reclassify a pure documentation-of-current-structure preflight as
main-mode-eligible if desired; this report does not pre-decide that.

## Phase 9 context (live current-evidence citations)

Phase 9 is currently framed as the production-pressure phase that
reality-checks the eight Â§9 evaluation axes â€” "Invalidation economics"
being one of them â€” but it is not the active implementation phase; the
substrate has been in a "stable, well-closed state" since the
2026-05-14 v0 release certification and the MAIN-ORDERED 10-job queue
closeout.

- `plans/IMPLEMENTATION.md:589-606` enumerates Phase 9's eight
  evaluation axes; line `:596` reads: "**Invalidation economics** â€”
  projection-density metric (Â§1.10.4) reality-checked".
- `plans/IMPLEMENTATION.md:675` (the risk-driven phase-ordering
  rationale table) names "**cad-projection invalidation economics**"
  as the Phase 7 highest-secondary-risk validation point â€” the
  deferred-risk follow-on to that validation is the Phase 9 Â§9 axis
  above.
- `plans/IMPLEMENTATION.md:696` lists the Phase 9 metric-to-track row:
  `cad-projection invalidation density | counter | <30% per minor bump`.
- `plans/IMPLEMENTATION.md:23` separately frames the same metric as a
  highest-risk Phase 9 entry: "**Invalidation economics** â€”
  projection invalidation density (Â§1.10.4 entropy metric); could
  detonate cad-projection".
- `plans/PLAN.md:522`: Â§1.10's entropy-metric table â€”
  "cad-projection invalidation density | >30% per minor bump".
- `plans/PLAN.md:531-533`: the same table lists "Incremental
  invalidation radius (crates rebuilt after touching one core type)
  (NEW v0.7) | >30% of workspace = lint warn" and notes "The last
  metric (invalidation radius) is potentially more important than raw
  compile time â€” it measures the cost of changing core types."
- `plans/PLAN.md:1156`: Â§1.10 explicitly enumerates the ten v0.7-added
  entropy metrics including "**cad-projection invalidation density**".
- `plans/BASELINE.md:328-336`: Â§1.10.4 Incremental-invalidation-radius
  preflight (Phase 9), 2026-05-21 â€” read-only `cargo metadata`
  measurement; thresholds, methodology, headline finding.
- `plans/BASELINE.md:367-376`: top-10 reverse-dep closures â€”
  **`rge-kernel-graph-foundation` 18 / 95 = 18.9 %** (highest fanout);
  `kernel/diagnostics` 15.8 %; `kernel/ecs` 10.5 %; `cad-core` 5.3 %.
  All well under the 30 % Â§1.10.4 lint-warn threshold.
- `plans/BASELINE.md:389-405`: explicit revisit triggers â€” re-run only
  when (a) any crate's normal-closure crosses 25 % or (b)
  isolated-stubs population drops below 30 of 95. Neither has fired.
- `plans/BASELINE.md:254-263`: Â§13.3 Compile-time baseline (Phase 9
  preflight), 2026-05-21 â€” first recorded compile-time reference for
  the workspace; warm-cache cargo-check numbers; explicitly labeled
  NOT proof of clean-build / incremental-p95 budgets satisfied.
- `Status.md:173`: "Hot-reload p95 on 1000 Counter entities â€”
  **9.761ms p95** (10Ã— headroom vs <100ms) â€” 2026-05-11 formal
  script-bench gate". (Superseded post-toolchain bump by the 0.818 ms
  re-validation recorded at `crates/script-bench/BASELINE.md:88-96`;
  see Â§Measured evidence.)
- `Status.md:183`: Phase 7.3 cad-projection gate row â€”
  "**CLOSED** 2026-05-11 â€” `phase_7_3_gate_closure.rs` umbrella test:
  10 `BRepHandle` entities Ã— 100 random edits; per-edit head-advance +
  invalidation-within-one-tick + `ProjectedMesh` â†”
  `cad_core::evaluate()` byte-equality".
- `HANDOFF.md:1500`: "**The real next frontier is Phase 9 (production
  pressure)** per â€¦" â€” confirms current-state framing in the
  most-recent snapshot.
- `change.md:1633` (live, canonical PowerShell view â€” see Â§Citation
  Hygiene): 2026-05-21 Â§13.3 compile-time preflight entry.
- `change.md:1635`: 2026-05-21 Â§1.10.4 invalidation-radius preflight
  entry â€” recorded headline finding "no crate is anywhere near PLAN
  Â§1.10.4's 30 %-of-workspace lint-warn threshold today".
- `change.md:1637` / `:1639` / `:1641`: 2026-05-21 live-inspector /
  editor-usability / Â§1.1 reflection-scale preflight entries.
- `AI_DISPATCH_AUTOMATION.md:1097-1104`: Â§17.4 current operating
  policy â€” `branch`-mode remains the default for production source;
  main-mode is opt-in and bounded.

**Live current-evidence vs historical / host-specific note.** The
2026-05-21 Â§1.10.4 measurement and the 2026-05-12 / 2026-05-17
script-bench measurements are recorder-host-specific point samples.
This audit does NOT promote those host-specific numbers into current
workload regressions (none would qualify as regressions in any case â€”
script-host p95 sits at 0.8 % of budget and graph-foundation closure
sits well under the Â§1.10.4 threshold). Where this audit cites them, it
labels them as recorder-host historical baselines, not as current
production-traffic measurements.

## Measured evidence

| Surface | Recorder-host measured cost? | Citation (path:line â€” live this dispatch) |
|---|---|---|
| `kernel/graph-foundation` invalidation radius | YES â€” 2026-05-21 read-only `cargo metadata`. Highest fanout `rge-kernel-graph-foundation` 18 / 95 = **18.9 %**, well under 30 % Â§1.10.4 threshold (~11.1 pp headroom). Recorder-host historical baseline; host-independent because it's a graph-shape metric. | `plans/BASELINE.md:367` (top row); `:389-393` (status: no breach); `change.md:1635` (2026-05-21 entry). |
| `kernel/graph-foundation` BFS invalidation throughput | **NO measured throughput.** Substrate has tests for structural correctness only (4-node DAG + diamond). No microbenchmark, no profiler measurement, no recorded run. | `kernel/graph-foundation/tests/invalidation_dag.rs:26-117` (four `#[test]` functions; no `#[bench]`); `tools/invalidation-profiler/src/main.rs:1-5` (5-line stub binary). |
| `crates/cad-core` `TessellationCache` hit-rate | **NO measured production hit-rate.** Cache exposes `hits()` / `misses()` / `hit_rate()` accounting (`cache.rs:170-191`) used by tests for correctness, but no recorder-host or in-vivo telemetry has been captured into `plans/BASELINE.md`. | `crates/cad-core/src/tessellation/cache.rs:128-198` (struct + counters + hit_rate + clear); `crates/cad-core/tests/labeled_tessellation_pipeline.rs` (415 LoC â€” labeled-flow correctness gate, not perf). |
| `crates/cad-projection` invalidation density (PLAN Â§1.10) | **NO measured density.** PLAN Â§1.10's "cad-projection invalidation density >30 % per minor bump" entropy metric has never been sampled; `plans/BASELINE.md` carries no row for it. | Search for "cad-projection invalidation density" / "projection density" across `plans/BASELINE.md` returns zero substantive matches (only a doc-cross-reference at `plans/BASELINE.md:626`); `plans/BASELINE.md:328` ff. measures invalidation **radius** (a different Â§1.10.4 metric). |
| `crates/cad-projection` head-advance reprojection wall-time | **NO measured wall-time.** Phase 7.3 closure gate measured **structural correctness** (1000 random edits Ã— invariant assertions), not wall-clock per-tick latency. | `Status.md:183` ("**CLOSED** 2026-05-11 â€¦ per-edit head-advance + invalidation-within-one-tick + ProjectedMesh â†” cad_core::evaluate() byte-equality"); `crates/cad-projection/src/lib.rs:705-751` (`tick_after_commit_projects_mesh` + `tick_no_op_when_head_unchanged` integration tests â€” structural, not timed). |
| `crates/gfx` frame-graph compile/recompile cost | **NO measured cost.** Smoke tests verify recompile-is-byte-identical (`frame_graph_smoke.rs`) and cross-frame composition correctness (`frame_graph_umbrella_smoke.rs`), but no per-frame compile-time wall-clock; the recompile is one-shot at graph-shape change. | `crates/gfx/tests/frame_graph_smoke.rs:1-60` (3-pass shadow/scene/lighting smoke head); `crates/gfx/tests/frame_graph_umbrella_smoke.rs:14-25` (chapter-scope honesty paragraph: "runtime-perf re-validation â€¦ is deferred until real pass-record sites grow transient-resource consumers"). |
| `crates/gfx` aliasing-group reuse | **Smoked, not benchmarked.** Tests prove the Arc-dedup contract (`build_resource_map_dedup_single_group_returns_arc_equal_handles`) but no allocation-throughput or VRAM-residency number is recorded. | `crates/gfx/src/frame_graph/resource_map.rs:187-228` (RM-1 dedup test); `crates/gfx/src/frame_graph/texture_pool.rs:47-50` (N=2 frames-in-flight constant + comment); `crates/gfx/src/frame_graph/buffer_pool.rs:47-53` (same constant). |
| Script hot-reload p95 (Phase 3.3) | **YES â€” formal recorder-host gate measurement.** 2026-05-11 baseline p95 0.796 ms; 2026-05-12 re-validation p95 0.818 ms / max 0.982 ms / avg 0.793 ms â€” **0.8 % of <100 ms PLAN Â§5.6 budget** (~125Ã— headroom). | `crates/script-bench/BASELINE.md:84-95` (formal gate table); `:96-99` (verdict "**PASS** against PLAN Â§5.6's <100 ms p95 budget"); `crates/script-bench/benches/hot_reload_swap.rs:18-42` (criterion bench harness wiring the 1000Ã—100 gate). |
| Script hot-reload 1-hour memory soak (Phase 3.4 #3) | **YES â€” 2026-05-17 formal one-hour soak with process-memory metrics.** elapsed 3600.00 s, cycles 3736934, restored 3736934000 == expected, peak_rss 10,510,336 B (~10.5 MiB), vss_delta +1,368,064 B. No OOM / panic / hang. | `crates/script-bench/BASELINE.md:140-179` (2026-05-17 run table); `:206-211` (CONSTRAINED-CERTIFIED scope-limitation paragraph); `crates/script-bench/METHODOLOGY.md:23` (`FORMAL_MEMORY_SOAK_DURATION = 1 hour`). |
| Hot-reload **watcher** (`crates/hot-reload-watcher`) | **NO measurement** â€” the crate is a 4-line empty stub; no implementation to measure. | `crates/hot-reload-watcher/src/lib.rs:1-4` ("`rge-hot-reload-watcher` â€” stub crate. Architecture frozen at v0.8; implementation pending per IMPLEMENTATION.md."). |

**Measured-evidence summary.** Only the **script hot-reload** axis has
a current measured cost gate against a PLAN budget on the recorder
host; that gate sits at 0.8 % of budget (~125Ã— headroom) with a 1-hour
soak also CONSTRAINED-CERTIFIED on the recorder host. The other four
required surfaces (`kernel/graph-foundation` propagation throughput,
`cad-core` tessellation cache hit-rate, `cad-projection` invalidation
density, `gfx` frame-graph compile/aliasing wall-time) have **no
current measured cost** â€” they are exercised by smoke / correctness
tests only, and the profiler crate that would feed PLAN Â§1.10's
"cad-projection invalidation density" remains a 5-line stub.

## Structural evidence

| Surface | Invalidation key / boundary | Clear/rebuild behavior | Fanout shape | Citation (live `path:line`) |
|---|---|---|---|---|
| `kernel/graph-foundation::Invalidation` | Caller-supplied `dependents_of(node) -> Vec<NodeId>` closure; substrate has no edge ownership of its own. | BFS with `BTreeSet` visited-set; each dirtied node delivered once per `mark_dirty` call; listeners notified per node in registration order. | Per-call dedup via visited-set; listener fan-out is `O(listeners * dirty_count)`. | `kernel/graph-foundation/src/invalidation.rs:108-131` (`mark_dirty` body â€” BFS + visited-set dedup); `:64-67` (`listeners: BTreeMap<ListenerHandle, Box<dyn InvalidationListener>>`); `:29-35` (`InvalidationListener: Send + 'static` trait). |
| `tools/invalidation-profiler/` | n/a â€” empty stub binary. | n/a â€” `main` prints a placeholder. | n/a. | `tools/invalidation-profiler/src/main.rs:1-5` (5-line stub). |
| `crates/cad-core::TessellationCache` | `CacheKey { structural_hash: [u8; 32], tolerance: Tolerance }` â€” BLAKE3 over operator + parameters + recursive input hashes; tolerance quantized at 1e9 to make epsilon-different f32 tolerances hash equal. | No automatic eviction or LRU; `clear()` drops all entries and resets counters; otherwise the cache grows unbounded by unique keys. Insertion stores `Arc<Tessellation>` so multiple readers share allocations. | Per-entry only â€” every distinct `(structural_hash, tolerance)` allocates one slot; counter is caller-driven (`record_hit` / `record_miss` from `OperatorGraph::evaluate`). | `crates/cad-core/src/tessellation/cache.rs:108-118` (`CacheKey` definition); `:41-87` (`Tolerance` quantization for epsilon-equality); `:128-198` (`TessellationCache` struct + counters + `clear`); `:6-8` (module-doc: "owned by the application (typically cad-projection) and threaded through `OperatorGraph::evaluate` explicitly"). |
| `crates/cad-projection::ProjectionCache` | Per-entity `dirty: BTreeSet<EntityId>` + `last_seen_checkpoint: Option<CheckpointId>` head-tracking. | `observe_checkpoint(head, all_entities)` marks every known entity dirty when the head differs from `last_seen_checkpoint`; `insert_mesh` clears the dirty bit for one entity; `clear_meshes` drops mesh storage but preserves `dirty` and `last_seen_checkpoint`; `clear_dirty()` clears just the dirty set. | **Coarse: head-advance â‡’ all entities dirty.** Module-head doc-comment names this explicitly as the v0 simplification deferring per-node fine-grained dependency tracking to a future dispatch. | `crates/cad-projection/src/projection_cache/mod.rs:15-18` (module-doc: "the last-seen `cad-core` checkpoint â€” when this differs from `CadGraph::head()` we mark every known entity dirty (head-advanced â‡’ everything dirty). Per-node fine-grained dependency tracking is a future-dispatch concern."); `:164-176` (`observe_checkpoint` body â€” marks every entity in `all_entities` dirty on head change); `:67-82` (struct fields, including `dirty: BTreeSet<EntityId>` and `last_seen_checkpoint`); `:22-24` (design-note: `BTreeMap` chosen for deterministic iteration over HashMap-marginal speed). |
| `crates/cad-projection::CadProjection::tick` | Reads `cad.head()`, observes checkpoint, iterates `dirty` snapshot, calls `projection_geometry::project` per entity, updates `BRepHandle.mesh_id` / `.last_projected_checkpoint`, clears dirty. | The head-unchanged-no-op behavior is explicitly tested by `tick_no_op_when_head_unchanged` (`lib.rs:735-751`); subsequent ticks at the same head reproject zero entities. | One re-projection per dirty entity per tick; head-change-burst pattern means the first tick after any checkpoint advance re-projects every known entity. | `crates/cad-projection/src/lib.rs:502-554` (`tick` body); `:508-514` (`observe_checkpoint(head, known_entities)`); `:517` (`let dirty: Vec<EntityId> = ...`); `:526-544` (re-projection loop calling `projection_geometry::project` + `insert_mesh` + `BRepHandle` update); `:545` (`self.cache.clear_dirty()`); `:735-751` (`tick_no_op_when_head_unchanged` integration test). |
| `crates/gfx::FrameGraph::compile` | RAW dependency adjacency derived from `PassNode { name, reads, writes }`. | One-shot recompile via `compile_passes`: writers map â†’ unwritten-read validation â†’ Kahn topological sort with BTreeSet tiebreak â†’ execution-order walk for lifetimes â†’ greedy aliasing assignment ordered by `ResourceId`. Recompile is byte-identical for unchanged shape. No incremental compile path; topology change triggers full recompile. | Lifetime span `[first_use, last_use]` per `ResourceId`; aliasing-group merge is `O(resources * groups)`; greedy ordering is deterministic. | `crates/gfx/src/frame_graph/compile/algorithm.rs:28-142` (`compile_passes` body; six steps); `:115-134` (greedy aliasing assignment); `crates/gfx/src/frame_graph/compile/mod.rs:1-43` (module doc enumerating the six-step algorithm). |
| `crates/gfx::ResourceMap` build | Caller-driven per frame; iterates `compiled.aliasing_groups()` once, calls `acquire(...)` once per group, fans the resulting `Arc` to every member. | Fresh map per frame; structural-enforced dedup contract â€” same Arc per aliasing group, distinct Arc per group. | `O(groups + resources)` per build; no caching of the map across frames â€” invalidated whenever a new `CompiledFrameGraph` is supplied. | `crates/gfx/src/frame_graph/resource_map.rs:91-123` (`build_resource_map`); `:43-51` (`ResourceMap { texture_map, buffer_map }` struct); `:73-90` (dedup-contract docstring); `:182-228` (RM-1 dedup test). |
| `crates/gfx::TexturePool` / `BufferPool` lifetime | Per-pool N=2 frames-in-flight ring; `(TextureDescriptor, AliasingGroupId)` (or `(BufferDescriptor, AliasingGroupId)`) is the free-list key. | `begin_frame()` rotates `current_slot`, drains the new slot's `active` allocations back into its free-list; subsequent `acquire(...)` consumes free-list before allocating. | Per-slot free-list keyed by `(descriptor, group)` â€” same descriptor + same group reuses; descriptor or group difference allocates fresh. | `crates/gfx/src/frame_graph/texture_pool.rs:53-80` (`SlotState` + `TexturePool` struct); `:108-114` (`begin_frame` rotates and drains active â†’ free-list); `:124-139` (`acquire` consumes free-list or `allocate_texture`); `crates/gfx/src/frame_graph/buffer_pool.rs:55-85` (mirror layout); `:47-53` (the FRAMES_IN_FLIGHT=2 constant; comment notes it's intentionally duplicated rather than hoisted). |
| `crates/script-host` swap protocol | Per-tick "capture â†’ drop instance â†’ reinstantiate new module â†’ restore" boundary. Snapshot payload is RON-encoded counters-only (`HashMap<i64-handle-as-string, i64>`). | `capture_state` walks every Counter-bearing entity via `World::query::<Counter>()`; `restore_state` parses RON, builds an `i64 â†’ EntityId` handle map, re-inserts `Counter` for each live handle (silently skips despawned). | Per-tick snapshot is O(entities-with-Counter); restoration is O(entities-with-Counter Ã— hash-lookup). | `crates/script-host/src/swap.rs:7-13` (module-doc â€” "Prototype limitations: Only `Counter` components are captured. Generalising the snapshot to every reflected type requires type-erased archetype iteration in `kernel/ecs` â€” a Phase 4-Foundation extension"); `:109-125` (`capture_state`); `:142-170` (`restore_state` with handle-map build + insert + silent skip). |
| `crates/script-bench` formal hot-reload gate | 1000 Counter entities Ã— 100 cycles Ã— poison-then-restore protocol per cycle; release-profile criterion bench + formal test gate. | Bench drives `ScriptHostBench::hot_reload_preservation(HotReloadConfig::formal())` repeatedly; correctness invariant `restored_components == cycles * entity_count` asserted. | `O(entities * cycles)`; the bench amortizes per-cycle setup overhead across 100 swaps. | `crates/script-bench/benches/hot_reload_swap.rs:18-42` (criterion group; both `native_rust` and `script_host_counter_1000x100` rows); `crates/script-bench/METHODOLOGY.md:20-24` (workload constants, including `FORMAL_HOT_RELOAD_ENTITY_COUNT = 1000` and `HOT_RELOAD_CYCLES = 100`). |
| `crates/hot-reload-watcher` | n/a â€” empty stub crate. | n/a. | n/a â€” no current consumer pressure on the watcher substrate. | `crates/hot-reload-watcher/src/lib.rs:1-4` (4-line stub with module-doc deferring implementation per IMPLEMENTATION.md). |

**Structural-evidence summary.** Five structurally distinct invalidation
shapes are visible:

1. **`kernel/graph-foundation`** is a pure caller-driven router (caller
   supplies `dependents_of`); the substrate does NOT own edge state.
   BFS-deduplication is correct by construction and tested. No measured
   cost gate exists; the Â§1.10.4 reverse-dep-radius preflight is the
   closest current evidence and shows ~11.1 pp headroom.
2. **`crates/cad-core::TessellationCache`** is an unbounded hash-keyed
   memoization with explicit caller-owned hit/miss accounting and a
   manual `clear()` boundary; tolerance is quantized so epsilon-different
   f32 keys collide. Structural risk: unbounded growth without an LRU
   policy. No current pressure (no measured size or hit-rate).
3. **`crates/cad-projection::ProjectionCache`** uses
   **head-advance â‡’ every-entity-dirty** as its explicit MVP
   invalidation policy. This is the sharpest structural cavity in the
   audit's required surfaces, but it is named in code as a deliberate
   v0 simplification (`mod.rs:15-18`) and the Phase 7.3 gate-closure
   test cleared it for 10 entities Ã— 100 mutations structurally â€”
   without a wall-time or density measurement.
4. **`crates/gfx` frame-graph** runs a one-shot recompile per topology
   change with deterministic ordering and per-frame Arc-deduped
   resource-map; transient pools rotate on a tight N=2 ring. No
   incremental compile; no per-frame compile-time wall-time gate.
   Structural state is locked by ADR-118 D2-D6 and protected by smokes.
5. **Script hot-reload** is the only axis with both a measured Phase 3
   gate (0.8 % of budget) and an in-substrate snapshot mechanism â€” the
   capture-snapshot-restore protocol is correct on Counter-only payload
   per the module-doc's explicit prototype limitation
   (`crates/script-host/src/swap.rs:7-13`).

## Candidate comparison

Five plausible follow-ups surface from the structural-evidence
inventory. Each is evaluated against (a) does it act on current
measured cost? (b) is it ordinary deferred implementation? (c) is it
doctrine-frozen and intentionally a cavity? (d) is it the smallest
reversible action that advances the Â§1.10 / Â§1.10.4 evaluation axis
this dispatch was scoped to assess?

| Candidate | Why considered | Why **not** sharpest (and, for the WINNER, why selected) |
|---|---|---|
| **(WINNER) Docs-only two-file Phase 9 cad-projection invalidation-density preflight** â€” append one new `## Phase 9 PREFLIGHT` section to `plans/BASELINE.md` + one chronology entry to `change.md` | PLAN Â§1.10's "cad-projection invalidation density >30 % per minor bump" entropy metric (`plans/PLAN.md:522`) is named, has no current baseline, and the most structurally interesting cavity in this audit's required surfaces (head-advance â‡’ all-entities dirty, `projection_cache/mod.rs:15-18`) lives in cad-projection. The 2026-05-21 docs-only preflight precedent (`change.md:1633` / `:1635` / `:1637` / `:1639` / `:1641`) shows exactly this shape â€” capture current structural posture, name explicit revisit triggers, defer the lint/harness to a future dispatch under pressure â€” and that precedent landed cleanly across the workspace without touching code, tests, Cargo, or scripts. Each of those 2026-05-21 entries is two-file (a new `plans/BASELINE.md` section + a `change.md` entry), which is the same shape proposed here. | n/a â€” selected. This is the smallest reversible docs-only action (one new section appended to `plans/BASELINE.md` plus one append-only chronology entry in `change.md`) that closes the gap between PLAN Â§1.10's named metric and the workspace's actual structural state, without committing to a measurement harness, profiler implementation, or any code/source/test/Cargo edit. It mirrors the Â§1.10.4 preflight pattern exactly (a `Phase 9 PREFLIGHT` entry: methodology + thresholds + headline finding + revisit triggers), and the "no current pressure crossed" headline of this audit is exactly the verdict the entry would record. PublishMode branch only unless later authorized by a human (since `plans/BASELINE.md` carries Phase 9 doctrine). |
| Implement `tools/invalidation-profiler/` to measure Â§1.10.4 radius and Â§1.10 cad-projection density | The stub at `tools/invalidation-profiler/src/main.rs:1-5` is exactly the harness PLAN Â§1.10's density metric would need. Tempting because it would close the "no measured density" gap. | Premature mechanism. The 2026-05-21 Â§1.10.4 preflight (`plans/BASELINE.md:328-432`) explicitly defers `tools/invalidation-profiler/` wiring until a two-arm revisit trigger fires (`:400-405`): either any crate crosses 25 % normal-closure (today's top: 18.9 %) or isolated-stubs drops below 30 / 95 (today: 57 / 95 isolated). Neither trigger has fired. Implementing the profiler now would be substrate-ahead-of-pressure work, violating PLAN Â§1.10's pressure-driven doctrine. |
| Add an LRU eviction policy to `crates/cad-core::TessellationCache` | The cache grows unbounded by unique `(structural_hash, tolerance)` keys (`cache.rs:128-156`); long editor sessions could in principle accumulate stale entries. | Ordinary deferred implementation, not abstraction pain. The cache exposes `clear()` for caller-driven boundaries (`cache.rs:194-198`) and is **always recomputable on miss** (the cache key fully encodes inputs, per `cache.rs:10-12`). No current measurement shows the size becoming a problem, and the cache is owned by cad-projection which currently has only one editor session at a time. PLAN doctrine asks for pressure-driven optimization, not anticipatory eviction. |
| Replace `crates/cad-projection`'s head-advance-coarse policy with per-node fine-grained dirty bookkeeping | The MVP shape is named at `projection_cache/mod.rs:16-18` as a "future-dispatch concern". Tempting because it's the single structurally coarsest invalidation rule in the audit. | (a) No current measured density to motivate it. (b) Per-node dirty bookkeeping requires changes across `cad-projection/src/lib.rs` (tick), `projection_cache/mod.rs` (struct + observe_checkpoint), and probably a feedback edge from cad-core operator-graph evaluation â€” much larger than a single bounded reversible action. (c) The Phase 7.3 gate closure (`Status.md:183`) certifies the current shape for 10 entities Ã— 100 mutations structurally; the WINNER above captures that current shape in a docs-only preflight that the future per-node-bookkeeping implementation dispatch will be authored against. |
| Add a per-frame compile-time wall-clock gate to `crates/gfx::FrameGraph::compile()` | A bench would close the "no measured frame-graph compile cost" gap. | (a) The frame-graph chapter (`frame_graph_umbrella_smoke.rs:19-25`) explicitly defers runtime-perf re-validation until real pass-record sites grow transient-resource consumers â€” none today (`editor-shell::render_frame` bypasses the substrate, per the resource_map dispatch doc at `resource_map.rs:16-28`). (b) The recompile path is one-shot at topology change, not per-frame; per-frame cost is the per-frame `build_resource_map` call (`resource_map.rs:91-123`), which is `O(groups + resources)`, dominated by the wgpu allocation it brackets, and trivial to recompute. |

The WINNER is sharper than each alternative because (a) it is a
two-file docs-only append (one new section in `plans/BASELINE.md` plus
one new chronology entry in `change.md`), which is the exact shape used
by every 2026-05-21 Phase 9 preflight, (b) it mirrors the established
2026-05-21 preflight cadence exactly so the protocol-precedent is
documented, (c) it produces a load-bearing baseline artifact that a
future implementation dispatch (whether profiler-implementation or
per-node dirty bookkeeping) will be authored against, and (d) it does
not commit the workspace to any code/Cargo/script/scheduler/schema
change under ambient pressure that has not yet crossed any documented
threshold. The follow-up's file list and scope is identical in every
location it is described in this report: `plans/BASELINE.md` (new
section) + `change.md` (new entry), docs-only, two-file, PublishMode
branch.

## Verification Results

This audit-only correction-round dispatch's verification gates are
read-only per the TASK packet's `## Verification Gates` section
(lines 251-308) plus the CORRECT packet's re-verification gates
(`ai_handoffs/ISSUE-214_CORRECT_2026-05-26_21-09-39+0300.md:49-63`):

- `Get-Content -Raw AGENTS.md` â†’ readable (58 lines); used as the
  canonical statement of orchestrator vs. manual protocol doctrine.
  `AGENTS.md:26-36` records the exact rule "Orchestrator dispatch
  writes only a `TASK` and an `EXEC` packet (plus a `CORRECT` packet
  only if a correction round runs) â€¦ It emits **no `_REVIEW_` and no
  `_CLOSEOUT_` packets, ever.**" plus the consequence paragraph
  "missing `_REVIEW_` / `_CLOSEOUT_` packets is normal and expected â€”
  it is **not** evidence of an unfinished dispatch."
- `Get-Content -Raw AI_DISPATCH_AUTOMATION.md` â†’ readable (1352
  lines); used here only as a citation source for run-directory
  scratch (`:961-973`, including the
  `.ai/dispatch-<DISPATCH_ID>/codex.control.round*.json` per-run
  scratch location and the post-run inspection list) and Â§17.4
  current operating policy (`:1097-1104`). The prior EXEC's
  unsupported packet-shape citation (a 1041-1062 range covering a
  separate read-only-audit halt lesson, not the orchestrator
  packet-shape rule) has been removed from every claim about
  orchestrator packet shape in this corrected report.
- `Get-Content -Raw ai_handoffs/AI_HANDOFF_PROTOCOL.md` â†’ readable
  (448 lines); used here only for the file-naming convention and the
  manual-protocol packet-type set at `:128-145`, which states the
  `<DISPATCH_ID>_<PACKET_TYPE>_<TIMESTAMP>` filename pattern and
  enumerates `<PACKET_TYPE>: one of TASK, EXEC, REVIEW, CORRECT,
  CLOSEOUT`. That enumeration is the manual chain; the orchestrator
  chain (this dispatch) does not include REVIEW or CLOSEOUT per
  `AGENTS.md:26-36`. The machine-readable completion footer
  requirements (`:188-260`) were re-confirmed before authoring this
  report.
- `Get-Content -Raw plans/PLAN.md` â†’ readable (1508 lines); Â§1.10 /
  Â§1.10.4 / Â§13.2 / Â§13.3 entropy metrics re-read (line citations in
  Â§Phase 9 context above).
- `Get-Content -Raw plans/IMPLEMENTATION.md` â†’ readable (758 lines);
  Phase 9 Â§9 evaluation axes confirmed at `:589-606`; the metric row
  at `:696` confirmed; the Phase 7 risk-table row at `:675` confirmed.
- `Get-Content -Raw plans/BASELINE.md` â†’ readable (753 lines); Â§1.10.4
  preflight at `:328` ff. and Â§13.3 preflight at `:254` ff. re-read
  with explicit page-by-page line citations recorded in Â§Phase 9
  context.
- `Get-Content -Raw Status.md` â†’ readable (file is 450 KB with many
  long lines; canonical Phase 7.3 row at `:183` and hot-reload row at
  `:173` confirmed via targeted Grep).
- `Get-Content -Raw HANDOFF.md` â†’ readable (file is ~156 KB / 1678
  canonical PowerShell lines); Phase 9 framing at `:1500` confirmed.
- `change.md` live re-read this correction round (canonical PowerShell
  view â€” see Â§Citation Hygiene): 2026-05-21 Phase 9 preflight entries
  at **1633 / 1635 / 1637 / 1639 / 1641**, anchors re-confirmed via
  `Select-String -Path change.md -Pattern '^2026-05-21'`.
- Graph-foundation invalidation surfaces (read-only re-scan):
  `kernel/graph-foundation/src/invalidation.rs:1-234` (full read of
  the 234-LoC `Invalidation` router + the test foot);
  `kernel/graph-foundation/tests/invalidation_dag.rs:1-117` (full read
  of the 117-LoC 4-test integration foot);
  `tools/invalidation-profiler/src/main.rs:1-5` (full read of the
  5-line stub â€” line range corrected from prior EXEC's `:1-6`).
- CAD cache surfaces (read-only re-scan):
  `crates/cad-core/src/tessellation/cache.rs:1-268` (full read; the
  file is 268 lines â€” prior EXEC's RELATED_FILES line `(269 LoC)` was
  one-off and is corrected to `(268 LoC)` in this report's
  RELATED_FILES section);
  `crates/cad-core/src/tessellation/mod.rs:1-19` (full read);
  `crates/cad-projection/src/projection_cache/mod.rs:1-319` (full
  read);
  `crates/cad-projection/src/lib.rs:490-555` (full read of the `tick`
  orchestrator body around the head-observe + dirty-walk + reproject
  loop) and `:700-751` (full read of `tick_after_commit_projects_mesh`
  + `tick_no_op_when_head_unchanged` â€” both end at line 751, not 749;
  the prior EXEC's `:735-749` / `:705-749` truncated the closing
  brace and are corrected to `:735-751` / `:705-751`).
- Frame-graph surfaces (read-only re-scan):
  `crates/gfx/src/frame_graph/resource_map.rs:1-358` (full read; the
  `ResourceMap` struct itself spans `:43-51`, corrected from the
  prior EXEC's `:42-51` which leaked one trailing doc-line);
  `crates/gfx/src/frame_graph/texture_pool.rs:1-282` (full read);
  `crates/gfx/src/frame_graph/buffer_pool.rs:1-272` (full read);
  `crates/gfx/src/frame_graph/compile/algorithm.rs:1-190` (full read
  of the algorithm body plus test-foot start);
  `crates/gfx/src/frame_graph/compile/mod.rs:1-43` (full read; the
  module is 43 lines â€” line range corrected from prior EXEC's `:1-44`);
  `crates/gfx/tests/frame_graph_smoke.rs:1-60` (head read);
  `crates/gfx/tests/frame_graph_umbrella_smoke.rs:1-80` (head read).
- Script hot-reload surfaces (read-only re-scan):
  `crates/script-bench/BASELINE.md:1-300` (head + Phase 3.3 + Phase
  3.4 formal gate sections);
  `crates/script-bench/METHODOLOGY.md:1-80` (head);
  `crates/script-bench/benches/hot_reload_swap.rs:1-45` (full read);
  `crates/script-host/src/swap.rs:1-170` (full read; the prototype-
  limitations doc-block runs `:7-13`, corrected from prior EXEC's
  `:7-14` â€” line 14 is the blank line ending the doc block, not part
  of the limitations text);
  `crates/hot-reload-watcher/src/lib.rs:1-4` (full read).
- Prior dispatch artifacts:
  `ai_dispatch_logs/log_2026-05-26_15-53-41+0300.md` (head read for
  ISSUE-210 freeze-validity audit provenance);
  `ai_dispatch_logs/log_2026-05-26_20-15-22+0300.md` (head read for
  ISSUE-212 abstraction-pain audit provenance);
  `ai_handoffs/ISSUE-212_EXEC_2026-05-26_20-30-00+0300.md:1-642`
  (full read for format + citation-hygiene precedent).
- `.ai/dispatch-ISSUE-214/verification.round0.log` â†’ readable; the
  prior orchestrator canonical gate's last lines record
  `VERIFY OK: all 7 verification step(s) passed.` The log file is
  preserved unchanged; no new verification command was run.
- `git diff --check` â†’ **exit 0** (no whitespace errors on the working
  tree).
- `git status --short --branch --untracked-files=all` (post-salvage write):
  branch `ai-dispatch/ISSUE-214`; untracked items consist of the
  pre-existing ISSUE-214 TASK / prior-EXEC / CORRECT packets and
  sidecars, this manual-salvage EXEC packet plus its sidecar, and the
  orchestrator queue log. Unrelated root-level scratch was parked
  outside the worktree before this successor report was finalized.
  No tracked source / test / doc / Cargo / workflow / schema / script
  / scheduler / task-brief modifications.
- `git diff --stat` â†’ empty.
- `git diff --name-only` â†’ empty (no tracked-file diffs).
- Grep for the prior EXEC's unsupported packet-shape citation
  (`AI_DISPATCH_AUTOMATION.md` plus the 1041 1062 range) inside this
  corrected EXEC report â†’ **zero matches** (acceptance criterion 1 of
  the CORRECT packet satisfied â€” all packet-shape citations in this
  report now use `AGENTS.md:26-36`, `AI_DISPATCH_AUTOMATION.md:961-973`,
  or `ai_handoffs/AI_HANDOFF_PROTOCOL.md:128-145` as appropriate; no
  packet-shape claim relies on the unsupported prior range).
- Line-range re-scan against current file lengths:
  `tools/invalidation-profiler/src/main.rs` = 5 lines (cited `:1-5`,
  was `:1-6`); `crates/gfx/src/frame_graph/compile/mod.rs` = 43 lines
  (cited `:1-43`, was `:1-44`); other tightenings recorded inline
  above (`cache.rs:6-8` not `:7-9`; `cache.rs:10-12` not `:9-12`;
  `projection_cache/mod.rs:22-24` not `:23-25`; `lib.rs:705-751` and
  `:735-751` not `:705-749` / `:735-749`; `resource_map.rs:43-51` not
  `:42-51`; `swap.rs:7-13` not `:7-14`). No cited line range now
  exceeds its current live file length.
- Targeted protected-surface inspection (read-only): the following
  paths from the TASK packet's `### MUST NOT edit` list were verified
  untouched by visual inspection of `git status` + `git diff
  --name-only`: `plans/PLAN.md`, `plans/IMPLEMENTATION.md`,
  `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, `change.md`,
  `Cargo.toml`, `Cargo.lock`, `**/Cargo.toml`,
  `.github/workflows/**`, `.ai/dispatch.tasks.md`,
  `.ai/dispatch.verify.ps1`, `.ai/*.schema.json`,
  `Invoke-AiDispatchAuto.ps1`, `Invoke-AiDispatchQueue.ps1`,
  `Invoke-AiDispatchLoop.ps1`, `Register-AiDispatchSchedule.ps1`,
  `Get-AiDispatchHealth.ps1`, `Get-AiDispatchTrends.ps1`,
  `Watch-AiDispatch.ps1`, `Wait-GitHubActions.ps1`, `new-handoff.ps1`,
  `ai_handoffs/AI_HANDOFF_PROTOCOL.md`, `ai_handoffs/templates/**`,
  this TASK packet itself, every other existing handoff packet /
  sidecar (including the prior EXEC `_2026-05-26_20-53-50+0300` pair
  and the round-0 CORRECT pair) / dispatch log, and the entire
  `crates/**` / `kernel/**` / `editor/**` / `runtime/**` / `tools/**`
  / `tests/**` / `docs/**` / `tasks/**` / `plugins/**` / `files/**` /
  `golden-projects/**` / `OLD/**` / `.claude/worktrees/**` tree.
- **No new benchmarks, profilers, full repository verification, the
  autonomous queue, or any nested dispatch were run as part of this
  correction round** (per CORRECT packet `## Re-Verification Gates`
  closing paragraph and TASK packet `## Constraints / Non-Goals`
  lines 224-247).

**No tracked source, tests, documentation, Cargo metadata, workflow,
schema, automation script, scheduler configuration, task-brief, prior
handoff packet (including the prior EXEC and the round-0 CORRECT
packet), or prior dispatch-log file was modified by this correction
round.** New untracked items are limited to this corrected EXEC packet
and its `.meta.json` sidecar.

## Citation hygiene

Per the orchestrator dispatch protocol and following the ISSUE-212 EXEC
precedent
(`ai_handoffs/ISSUE-212_EXEC_2026-05-26_20-30-00+0300.md:301-388`), all
line numbers cited against `change.md` and `HANDOFF.md` in this report
were re-located by live read on the **current branch
(`ai-dispatch/ISSUE-214`, HEAD `e8219e0`)** immediately before
finalizing this section, using the canonical PowerShell `Select-String`
gate. Every other `path:line` citation in this report was re-scanned
against its live file length as part of the CORRECT packet's
acceptance criterion 2; the changes from the prior EXEC are itemized
in the `Verification Results` section above.

- **Live `change.md` last-line / line count (canonical PowerShell view,
  this dispatch):** `Select-String -Path change.md -Pattern '.*' |
  Select-Object -Last 1` â†’ **`LineNumber 1647`**.
- **Live positions of the relevant 2026-05-21 Phase 9 preflight entries
  on the current branch (PowerShell `Select-String -Path change.md
  -Pattern '^2026-05-21'`):**

  | Topic | Live line | Live anchor (entry-start text, first â‰¤120 chars) |
  |---|---:|---|
  | Phase 9 Â§13.3 Compile-time preflight | **change.md:1633** | `2026-05-21 (Phase 9 Â§13.3 compile-time preflight â€” warm-cache cargo check baseline recorded in plans/BASELINE.md, IM` |
  | Phase 9 Â§1.10.4 Invalidation-radius preflight | **change.md:1635** | `2026-05-21 (Phase 9 Â§1.10.4 invalidation-radius preflight â€” read-only cargo metadata measurement recorded in plans/BA` |
  | Phase 9 Live-inspector wiring preflight | **change.md:1637** | `2026-05-21 (Phase 9 live-inspector wiring preflight â€” read-only audit of egui host integration status recorded in plans` |
  | Phase 9 Editor-usability preflight | **change.md:1639** | `2026-05-21 (Phase 9 editor-usability preflight â€” read-only audit of the editor's user-facing surface recorded in plans/` |
  | Phase 9 Â§1.1 Reflection-scale honesty preflight | **change.md:1641** | `2026-05-21 (Phase 9 Â§1.1 reflection-scale honesty preflight â€” read-only adoption-state audit recorded in plans/BASELINE` |

- **Live `HANDOFF.md` last-line / line count (canonical PowerShell
  view, this dispatch):** **`LineNumber 1678`**.
- **Live `HANDOFF.md` Phase 9 framing line:** `Select-String -Path
  HANDOFF.md -Pattern 'real next frontier'` â†’ **`HANDOFF.md:1500`**
  (`> **The real next frontier is Phase 9 (production pressure)** per`).
- These canonical PowerShell line numbers match the ISSUE-212 EXEC's
  re-read
  (`ai_handoffs/ISSUE-212_EXEC_2026-05-26_20-30-00+0300.md:310-326`)
  exactly. POSIX `awk` / `grep` report different counts against the
  same on-disk bytes because of bare CR characters inside long
  entries; this is repo state, not a working-tree anomaly. The
  canonical PowerShell line numbers are the load-bearing reference.

## Deviations from Task Packet

None â€” execution stayed strictly within the original TASK packet scope
and the CORRECT packet's narrowed report-correction envelope. No
tracked source, tests, docs, Cargo metadata, workflows, schemas,
automation scripts, scheduler configuration, task-brief files, prior
EXEC packet, prior EXEC sidecar, or CORRECT packet was modified by this
correction round. New files are limited to this corrected EXEC packet
and its matching `.meta.json` sidecar.

No `REVIEW` or `CLOSEOUT` packet was created (orchestrator dispatch â€”
see `AGENTS.md:26-36`). The optional `ai_dispatch_logs/log_*.md` and
`.ai/dispatch-ISSUE-214/**` artifacts allowed by the TASK packet's
`### MAY add new files` list (lines 109-115) were not generated by
this EXEC report; the orchestrator harness produces them around the
run.

## Open Questions for Reviewer

- **Verdict classification.** This report classifies the audit as
  `does_not_warrant_next_dispatch` / `COST_CLASS: structural` because
  (a) the only measured axis (script hot-reload) sits at 0.8 % of
  budget and (b) the Â§1.10.4 reverse-dep radius preflight shows
  ~11.1 pp headroom under the lint-warn threshold, neither of which
  represents current pressure. The follow-up named in the decision
  block is still proposed (a docs-only two-file PREFLIGHT) so that
  PLAN Â§1.10's "cad-projection invalidation density" metric has a
  recorded structural baseline against which a future implementation
  dispatch can be authored. If the reviewer prefers verdict
  `warrants_next_dispatch` (e.g. interpreting "structural" alone as
  warranting a measurement dispatch), the proposed follow-up does not
  change â€” it is the same docs-only two-file PREFLIGHT.
- **Smallest-safe-follow-up wording.** The decision block names the
  follow-up's scope ("docs-only two-file Phase 9 cad-projection
  invalidation-density PREFLIGHT â€” `plans/BASELINE.md` new section +
  `change.md` chronology entry") and the candidate-comparison WINNER
  row names the same two files in the same scope. The follow-up is
  NOT single-file; it is the two-file docs-only chronology shape used
  by every 2026-05-21 Phase 9 preflight. The natural envelope for the
  follow-up TASK packet, following `change.md:1635`'s precedent: MAY
  edit `plans/BASELINE.md` (single new section appended after the
  existing Â§1.10.4 / Â§1.1 / Editor-usability / Live-inspector
  preflights) + `change.md` (single new entry); MUST NOT edit any
  source / test / Cargo / workflow / schema / script / scheduler /
  task-brief / prior-packet file.
- **PublishMode framing.** The decision block selects
  `PublishMode branch only unless later authorized by a human`
  because `plans/BASELINE.md` carries Phase 9 doctrine and the prior
  delegated-main smoke batch precedent (ISSUE-212 EXEC at lines
  529-534) selected branch-mode for the analogous ADR follow-up. A
  reviewer may reclassify a pure documentation-of-current-structure
  preflight as main-mode-eligible; this report does not pre-decide
  that choice.
- **Line-counting tooling provenance (FYI, not a blocking question).**
  This report uses the canonical PowerShell `Select-String` view of
  `change.md` and `HANDOFF.md` (1647 last line; 1678 last line).
  POSIX `awk` / `grep` report different counts against the same
  on-disk bytes because of bare CR characters inside long entries
  (documented at length in
  `ai_handoffs/ISSUE-212_EXEC_2026-05-26_20-30-00+0300.md:343-363`).
  This is repo state, not a working-tree anomaly; the byte-level
  cause has not changed since the ISSUE-212 read.

## Worktree State

- Tracked files: clean (no tracked modifications).
- Untracked items (after this report and its sidecar exist on disk):
  - `ai_handoffs/ISSUE-214_TASK_2026-05-26_20-39-49+0300.md`
    (pre-existing TASK packet from the Planner)
  - `ai_handoffs/ISSUE-214_TASK_2026-05-26_20-39-49+0300.meta.json`
    (pre-existing TASK sidecar)
  - `ai_handoffs/ISSUE-214_EXEC_2026-05-26_20-53-50+0300.md`
    (prior EXEC report â€” left in place per the CORRECT packet's
    preservation envelope; this corrected report supersedes it for
    review)
  - `ai_handoffs/ISSUE-214_EXEC_2026-05-26_20-53-50+0300.meta.json`
    (prior EXEC sidecar â€” left in place per the CORRECT packet)
  - `ai_handoffs/ISSUE-214_CORRECT_2026-05-26_21-09-39+0300.md`
    (Planner CORRECT packet authorizing this correction round)
  - `ai_handoffs/ISSUE-214_CORRECT_2026-05-26_21-09-39+0300.meta.json`
    (CORRECT packet sidecar)
  - `ai_handoffs/ISSUE-214_EXEC_2026-05-26_21-17-21+0300.md`
    (failed-round corrected EXEC report; preserved as evidence)
  - `ai_handoffs/ISSUE-214_EXEC_2026-05-26_21-17-21+0300.meta.json`
    (failed-round corrected EXEC sidecar; preserved as evidence)
  - `ai_handoffs/ISSUE-214_EXEC_2026-05-26_21-45-00+0300.md`
    (this manual-salvage EXEC report)
  - `ai_handoffs/ISSUE-214_EXEC_2026-05-26_21-45-00+0300.meta.json`
    (this manual-salvage EXEC sidecar)
  - `ai_dispatch_logs/log_2026-05-26_21-28-14+0300.md`
    (orchestrator queue log for the blocked run)
- Branch: `ai-dispatch/ISSUE-214`
- Last commit: `e8219e0` ai-dispatch ISSUE-212: salvage abstraction-pain
  audit
- `git rev-list --left-right --count main...HEAD` â†’ branch is at the
  same commit as `main` (no divergence introduced by this dispatch).

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-214
AUTHOR: Executor / Codex (manual salvage)
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---

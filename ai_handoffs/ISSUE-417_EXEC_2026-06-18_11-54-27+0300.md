# Execution Report

DISPATCH_ID: ISSUE-417
AUTHOR: Executor / Codex
TIMESTAMP: 2026-06-18_11-54-27+0300
RELATED_FILES:
- crates/editor-actions/src/action.rs - added generic action-context family contract while preserving default World-only Action shape.
- crates/editor-actions/src/bus.rs - made BusEntry and CommandBus context-family aware.
- crates/editor-actions/src/lib.rs - re-exported the new context/view types.
- crates/editor-actions/src/undo_stack.rs - made UndoStack carry the same action-context family.
- crates/editor-actions/tests/test_actions.rs - updated test merge helper to use the metadata-only ActionView.
- crates/editor-shell/src/lifecycle/commands.rs - added shell-owned CAD action/context, routed first cuboid add through CommandBus, and preserved World-only submit compatibility.
- crates/editor-shell/src/lifecycle/mod.rs - typed the shell command bus to the shell action-context family.
- crates/editor-shell/src/lifecycle/tests.rs - added bus-routed CAD add/undo/redo regression coverage.
- .ai/dispatch.tasks.md - appended exactly one gated audit task 161.
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-417_TASK_2026-06-18_11-14-23+0300.md`

## Pre-Execution Review Consumed

No pre-execution review issued for this dispatch.

## Current-State Claims: Falsification Re-run

- Claim: `rge-editor-actions` has no editor-shell, CAD-core, cad-projection, CAD graph/projection/world, or B-Rep references.
  Re-run: `rg -n "editor_shell|editor-shell|cad_core|cad-core|cad_projection|cad-projection|CadGraph|CadProjection|cad_world|BRepHandle" crates/editor-actions/src crates/editor-actions/Cargo.toml` -> no matches, exit 1 as expected.
- Claim: the original editor-actions bus/action API was World-only and now has a generic context contract without losing `Box<dyn Action>`.
  Re-run: `rg -n "CommandBus|Action|submit\(|undo_command|redo_command|mark_saved_command|add_cad_cuboid_to_empty_scene" crates/editor-actions/src crates/editor-shell/src/lifecycle/commands.rs` -> matches show `ActionContext`, `ActionContextFamily`, `WorldActionContext`, generic `Action<F = WorldActionContext>`, `BusEntry<F>`, `CommandBus<F>`, shell `EditorShellActionContext`, `WorldOnlyShellAction`, and `command_bus.submit(action, &mut context)`.
- Claim: `EditorShell::add_cad_cuboid_to_empty_scene` is now bus-routed.
  Re-run: the same routing grep shows `AddCadCuboidToEmptyScene`, `impl Action<EditorShellActionContext>`, `submit_shell_action(Box::new(action))`, and `command_bus.submit(action, &mut context)`.
- Claim: rollback ingredients remain available without editing CAD crates.
  Re-run: source reads in `crates/editor-shell/src/lifecycle/commands.rs` use existing `CadGraph::restore_to`, `CadProjection::despawn_brep_entity`, `CadProjection::spawn_brep_entity`, and `CadProjection::tick`; no CAD crate files changed.
- Claim: stale-id normalization and empty-scene rejection behavior remain covered.
  Re-run: `rg -n "clear_stale_tracked_cad_entity_live_without_render_mesh_noop|clear_stale_tracked_cad_entity_stale_after_restore_despawn_clears_tracked_id|add_cad_cuboid_to_empty_scene_clears_stale_tracked_id_only_then_adds" crates/editor-shell/src/lifecycle/tests.rs` -> matched all three regression tests. `rg -n "add_cad_cuboid_to_empty_scene_rejects_existing_and_partial_state|SceneNotEmpty|render content|uploaded meshes|prebuilt_render_meshes|meshes\.is_empty" crates/editor-shell/src/lifecycle/commands.rs crates/editor-shell/src/lifecycle/tests.rs` -> matched the source guards and regression assertions.
- Claim: task 161 was not already present and no task 162 should be appended.
  Re-run: `rg -n "^160\.|^161\.|^162\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md` -> found task 160 and the newly appended task 161 heading; no `^162.` heading. Historical and task-instruction `NEEDS_HUMAN_RECORDED` references remain; task 160 did not record a new marker.
- Queue/already-filed autonomous-task evidence: followed the task packet instruction and did not call `gh`, browser, network, GitHub API, or any live remote lookup.

## What I Changed

### Source

- `crates/editor-actions/src/action.rs`: added `ActionContext`, `ActionContextFamily`, `WorldActionContext`, `ActionView`, and `ActionViewRef`; made `Action` generic over a context family with `WorldActionContext` as the default so existing `impl Action` and `Box<dyn Action>` remain object-safe.
- `crates/editor-actions/src/bus.rs`: made `BusEntry`, `CommandBus::submit`, `undo`, `redo`, and stack access context-family aware; preserved `CommandBus::new()` and `with_coalesce_window()` for World-only call sites and added `new_for_context()` for richer owners.
- `crates/editor-actions/src/undo_stack.rs`: carried the context family through the stack while preserving default World-only construction.
- `crates/editor-shell/src/lifecycle/commands.rs`: added `EditorShellActionContext`, `ShellActionContext`, `WorldOnlyShellAction`, `CommandBusView`, `UndoStackView`, and `AddCadCuboidToEmptyScene`; routed `add_cad_cuboid_to_empty_scene` through `submit_shell_action` and the bus. Undo restores the captured `pre_add_head`, despawns the B-Rep entity, re-ticks projection, and clears the tracked CAD entity id.
- `crates/editor-shell/src/lifecycle/mod.rs`: changed the shell bus field to `CommandBus<commands::EditorShellActionContext>` and initialized it with `CommandBus::new_for_context()`.

### Tests

- `crates/editor-shell/src/lifecycle/tests.rs::bus_routed_cad_cuboid_add_undo_redo_keeps_scene_coherent_and_moves_dirty_mark`: proves bus-routed add, undo cleanup with no stale entity/render mesh, redo coherence, and dirty/save-mark cursor movement.
- `crates/editor-shell/src/lifecycle/tests.rs::bus_routed_cad_cuboid_rejected_non_empty_add_does_not_grow_bus_stack`: proves rejected non-empty add does not submit a bus action or move the saved cursor.
- `crates/editor-actions/tests/test_actions.rs`: updated the merge helper to accept the new metadata-only `ActionView`.

### Docs

- `.ai/dispatch.tasks.md`: appended exactly one task 161 audit block. It is source/docs-read-only, forbids task 162 and feature work, and requires a future `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - source-grounded reason` marker plus a human-approval recommendation block.

## Per-File Summary

- `crates/editor-actions/src/action.rs`, `bus.rs`, `undo_stack.rs`, `lib.rs`: generic context substrate only; no editor-shell/CAD/projection references or dependency changes.
- `crates/editor-shell/src/lifecycle/commands.rs`: shell-owned CAD action/context and bus view; World-only actions are wrapped so existing `submit_action(Box<dyn Action>)` callers remain source-compatible.
- `crates/editor-shell/src/lifecycle/tests.rs`: focused headless tests only; no GPU/wgpu construction.

## Verification Results

- `cargo test -p rge-editor-actions --all-targets` -> exit 0. Results: 23 lib tests, 4 audit-ledger tests, 3 coalesce tests, 3 compound tests, 5 dirty/save-mark tests, 2 byte-identical smoke tests, and 0 test-actions tests all passed.
- `cargo test -p rge-editor-shell --lib bus_routed_cad_cuboid --no-fail-fast` -> exit 0, 2 passed / 0 failed / 0 ignored.
- `cargo test -p rge-editor-shell --lib add_cad_cuboid_to_empty_scene --no-fail-fast` -> exit 0, 3 passed / 0 failed / 0 ignored.
- `cargo test -p rge-editor-shell --lib clear_stale_tracked_cad_entity --no-fail-fast` -> exit 0, 4 passed / 0 failed / 0 ignored.
- `cargo test -p rge-editor-shell --lib cad_scene_inspection --no-fail-fast` -> exit 0, 4 passed / 0 failed / 0 ignored.
- `cargo check -p rge-editor-actions -p rge-editor-shell` -> exit 0.
- `cargo +nightly fmt --all -- --check` -> exit 0.
- `rg -n "editor_shell|editor-shell|cad_core|cad-core|cad_projection|cad-projection|CadGraph|CadProjection|cad_world|BRepHandle" crates/editor-actions/src crates/editor-actions/Cargo.toml` -> exit 1, no matches as expected.
- `rg -n "CommandBus|Action|submit\(|undo_command|redo_command|mark_saved_command|add_cad_cuboid_to_empty_scene" crates/editor-actions/src crates/editor-shell/src/lifecycle/commands.rs` -> exit 0; recorded the generic context and shell routing shape.
- `rg -n "^160\.|^161\.|^162\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md` -> exit 0; task 160 and one task 161 heading present, no task 162 heading.
- `git diff --name-only` -> exit 0; changed paths are within MAY edit plus current-dispatch artifacts.
- `git diff --check` -> exit 0. It printed only the existing CRLF warning for `.ai/dispatch.tasks.md`.

## Deviations from Task Packet

None - execution stayed strictly within the Task Packet scope.

## Open Questions for Reviewer

- Please review the context-family design in `rge-editor-actions`: it preserves default World-only `Box<dyn Action>` while letting editor-shell own a borrowed richer context without storing self-borrowed lifetimes in the bus.
- Please review the redo guard in `AddCadCuboidToEmptyScene`: it follows existing inspection semantics by treating a stale graph root id with no live root node as empty after `restore_to(pre_add_head)`.

## Worktree State

- Tracked files: modified but uncommitted:
  - `.ai/dispatch.tasks.md`
  - `crates/editor-actions/src/action.rs`
  - `crates/editor-actions/src/bus.rs`
  - `crates/editor-actions/src/lib.rs`
  - `crates/editor-actions/src/undo_stack.rs`
  - `crates/editor-actions/tests/test_actions.rs`
  - `crates/editor-shell/src/lifecycle/commands.rs`
  - `crates/editor-shell/src/lifecycle/mod.rs`
  - `crates/editor-shell/src/lifecycle/tests.rs`
- Untracked items:
  - `ai_handoffs/ISSUE-417_EXEC_2026-06-18_11-54-27+0300.md`
  - `ai_handoffs/ISSUE-417_EXEC_2026-06-18_11-54-27+0300.meta.json` (created by finalize after this packet was filled)
  - `ai_handoffs/ISSUE-417_TASK_2026-06-18_11-14-23+0300.md`
  - `ai_handoffs/ISSUE-417_TASK_2026-06-18_11-14-23+0300.meta.json`
  - `ai_handoffs/claims/ISSUE-417_2026-06-18_11-14-21-0500011+0300_claim.json`
- Branch: `ai-dispatch/ISSUE-417`
- Last commit: `a228bec` Re-arm dispatch with approved task 160 (generic CommandBus action-context)

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-417
AUTHOR: Executor / Codex
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---

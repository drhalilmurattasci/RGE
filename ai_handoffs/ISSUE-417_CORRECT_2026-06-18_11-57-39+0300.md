# Correction Packet

DISPATCH_ID: ISSUE-417
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-18_11-57-39+0300
RELATED_FILES:
- crates/editor-shell/src/lifecycle/commands.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-417_TASK_2026-06-18_11-14-23+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-417_EXEC_2026-06-18_11-54-27+0300.md`
- Latest Review Report: none; the orchestrator verification gate failed before Codex control review.
- Prior Correction Packets: none.
- Verification failure: `cargo run -q -p rge-tool-architecture-lints -- all` failed `split-exemption` because `crates\editor-shell\src\lifecycle\commands.rs` is 1044 lines and lacks a `// SPLIT-EXEMPTION:` annotation.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly this correction and nothing more.

1. **Satisfy split-exemption lint for commands.rs** - Canonical verification gate finding `[split-exemption] FAIL`.
   Required change: in `crates/editor-shell/src/lifecycle/commands.rs`, add the smallest scope-preserving `// SPLIT-EXEMPTION:` annotation with a concrete reason for this cohesive lifecycle command module exceeding 1000 lines after the ISSUE-417 CAD CommandBus routing work. Do not change architecture-lint rules, do not add new files, do not move unrelated code, and do not edit any path outside the Task Packet `MAY edit` envelope.
   Acceptance: `cargo run -q -p rge-tool-architecture-lints -- all` exits 0, and the full canonical `.ai/dispatch.verify.ps1` gate exits 0.

## Deferred Findings (NOT Approved for This Round)

None. The only observed blocker is the split-exemption verification failure above.

## Updated Acceptance Criteria

Unchanged from Task Packet, with this additional correction-round requirement: `crates/editor-shell/src/lifecycle/commands.rs` must satisfy the repository `split-exemption` architecture lint without broadening ISSUE-417 behavior or edit scope.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `cargo run -q -p rge-tool-architecture-lints -- all` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0.

If the canonical verification gate reports a new failure unrelated to `crates/editor-shell/src/lifecycle/commands.rs` and fixing it would require a forbidden path or expanded scope, halt and report the blocker instead of widening this correction.

## Halt Conditions (Updated if Any)

Unchanged from Task Packet, plus: halt if satisfying the split-exemption lint would require editing architecture-lint tooling, Cargo metadata, scripts, schemas, workflows, docs, CAD/render/UI files, or any other Task Packet `MUST NOT edit` path.

## Planner Notes

This correction is intentionally limited to the single verification blocker. The prior execution passed formatting and all pre-verify implementation gates it reported; the orchestrator failed only because `commands.rs` crossed the line-count threshold without the required split-exemption annotation. Do not revisit the CommandBus design except as needed to keep this one-file correction compiling and formatted.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-417
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Task Packet

DISPATCH_ID: ISSUE-118
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-23_12-04-16+0300
RELATED_FILES:
- plans/BASELINE.md
- Status.md
- HANDOFF.md
- change.md
- ai_handoffs/POSTV0-EDITOR-SHELL-PERF-HARNESS-001_EXEC_2026-05-14_21-51-40+0300.md
STATUS: OPEN

## Goal

Reconcile stale editor-shell perf-harness deferral text that still describes the
non-winit editor-shell render-frame perf harness as outstanding, even though the
POSTV0 editor-shell perf-harness dispatch landed it at commit `f8b8ed4` via
`crates/editor-shell/src/render_frame_e2e_perf.rs`. This is a documentation-only
history reconciliation for GitHub issue #118: preserve the old dated records as
history, add a current dated record that supersedes the stale deferral wording,
and cross-reference
`ai_handoffs/POSTV0-EDITOR-SHELL-PERF-HARNESS-001_EXEC_2026-05-14_21-51-40+0300.md`.

## Scope

### MAY edit
- plans/BASELINE.md
- Status.md
- HANDOFF.md
- change.md

### MUST NOT edit
- plans/IMPLEMENTATION.md
- crates/editor-shell/**
- Any other `crates/**` source, test, bench, fixture, or docs path
- Any `editor/**`, `kernel/**`, `runtime/**`, `tools/**`, `docs/**`, or `plans/**`
  path other than `plans/BASELINE.md`
- Cargo.toml
- Cargo.lock
- .gitignore
- AI_DISPATCH_AUTOMATION.md
- ai_handoffs/AI_HANDOFF_PROTOCOL.md
- ai_handoffs/templates/**
- Any existing ai_handoffs packet, including the POSTV0 packet referenced above

### MAY add new files
- ai_handoffs/ISSUE-118_EXEC_*.md, the execution report for this dispatch only
  if the executor is operating through the handoff protocol. Do not hand-create
  sidecars; if the orchestration layer finalizes the EXEC packet, its generated
  `.meta.json` sidecar is automation output rather than task content.

### MUST NOT add new files
- New baseline measurement artifacts
- New ADRs, doctrine docs, plans, scripts, schemas, lints, tests, benches, or
  fixtures
- New source files under any crate or application directory
- New Cargo manifests or lockfiles
- New ai_handoffs packets other than this dispatch's own EXEC packet

## Deliverables

- `plans/BASELINE.md` is revised only enough to stop presenting the non-winit
  editor-shell perf harness as currently deferred. It must record that the
  harness landed post-v0 at commit `f8b8ed4` in
  `crates/editor-shell/src/render_frame_e2e_perf.rs` and must cross-reference
  the POSTV0 EXEC packet.
- `Status.md` gets a new top prepended snapshot dated 2026-05-23 for ISSUE-118.
  Older snapshots must remain in place as historical records.
- `HANDOFF.md` gets a new top prepended snapshot dated 2026-05-23 for ISSUE-118.
  Older snapshots must remain in place as historical records.
- `change.md` gets exactly one new 2026-05-23 entry appended to the existing
  chronological history. Do not rewrite old entries.
- The docs clearly distinguish the unchanged v0 certification at commit
  `6aaf7f1` from the later post-v0 harness landing at commit `f8b8ed4`.

## Acceptance Criteria

- Only the four allowed documentation files are modified, apart from this
  dispatch's own EXEC packet.
- `plans/IMPLEMENTATION.md` is untouched.
- No source, test, bench, fixture, Cargo, schema, lint, automation, or protocol
  file is touched.
- `plans/BASELINE.md` no longer has current-state wording that says the
  non-winit editor-shell perf harness remains blocked or deferred. If older
  dated context is retained, it must be explicitly framed as historical and
  superseded by `f8b8ed4`.
- `Status.md` and `HANDOFF.md` preserve existing dated snapshots by prepending
  the new snapshot above them. Do not edit old snapshots merely to make them
  read current.
- `change.md` preserves existing dated entries by appending one new entry. Do
  not rewrite the 2026-05-14 v0 certification entry or any other old entry.
- The new current records include all three references:
  `f8b8ed4`, `crates/editor-shell/src/render_frame_e2e_perf.rs`, and
  `ai_handoffs/POSTV0-EDITOR-SHELL-PERF-HARNESS-001_EXEC_2026-05-14_21-51-40+0300.md`.
- No new `plans/BASELINE.md` measurement row is added for the editor-shell
  harness.
- No recorder-host P95/P50/worst-sample measurement numbers are copied from the
  POSTV0 EXEC packet into `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, or
  `change.md`.
- No new hard P95, worst-sample, or similar performance threshold is chosen for
  the editor-shell harness. Threshold pinning remains a future certification
  scope unless a separate task explicitly authorizes it.

## Constraints / Non-Goals

- This is documentation reconciliation only. It does not execute, modify, tune,
  or re-measure the editor-shell perf harness.
- Do not run the release-only editor-shell perf harness.
- Do not run fresh recorder-host measurements.
- Do not run Cargo commands of any kind for this dispatch.
- Do not change `plans/IMPLEMENTATION.md`, even though stale references may be
  mentioned there. The issue scope explicitly excludes it.
- Do not claim the post-v0 harness changes the old v0 release certification
  commit. The correct framing is: v0 certification remains at `6aaf7f1`; the
  editor-shell perf-harness deferral was later resolved by POSTV0 at `f8b8ed4`.
- Do not import the POSTV0 measurement table into the baseline docs. Record
  existence, path, commit, and packet provenance only.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`. These gates are intentionally docs-only and use no Cargo
commands:

- `git diff --check -- plans/BASELINE.md Status.md HANDOFF.md change.md`
  -> expected exit 0.
- `git diff --name-only -- plans/BASELINE.md Status.md HANDOFF.md change.md`
  -> expected exactly the four allowed docs when tracked changes are inspected.
- `git diff -- plans/IMPLEMENTATION.md`
  -> expected empty diff.
- `git status --short --untracked-files=all`
  -> expected no modified tracked files outside `plans/BASELINE.md`,
  `Status.md`, `HANDOFF.md`, and `change.md`, with only this dispatch's own
  ai_handoffs packet(s) allowed as untracked additions.
- Manual diff review of the four docs confirms: prepended snapshots in
  `Status.md` and `HANDOFF.md`, one appended `change.md` entry, no baseline
  measurement row, no copied POSTV0 P95/P50/worst-sample numbers, and no new
  editor-shell hard performance threshold.

## Halt Conditions

The Executor MUST halt without committing and write an EXEC packet with
`STATUS: NEEDS_HUMAN`, `HANDOFF_STATUS: NEEDS_HUMAN`, and
`NEXT_ROLE: HUMAN_ARBITER` if any of the following occur:

- Reconciliation appears to require editing `plans/IMPLEMENTATION.md`.
- Reconciliation appears to require editing `crates/editor-shell/**` or any
  other source, test, bench, fixture, Cargo, schema, lint, automation, or
  protocol file.
- The Executor believes a new measurement row, copied recorder-host numbers, or
  new hard editor-shell thresholds are necessary to make the docs accurate.
- The Executor believes any Cargo command, release-only perf harness run, or
  fresh recorder-host measurement is needed.
- The four allowed docs contain conflicting history that cannot be reconciled by
  adding a current dated snapshot/entry and narrow `plans/BASELINE.md` wording.

## Planner Notes

Relevant prior record:
`ai_handoffs/POSTV0-EDITOR-SHELL-PERF-HARNESS-001_EXEC_2026-05-14_21-51-40+0300.md`
states that the POSTV0 editor-shell perf harness dispatch amended the local
commit to `f8b8ed4` and that the changed files included
`crates/editor-shell/src/render_frame_e2e_perf.rs`. It also states that hard P95
threshold pinning was deferred. For ISSUE-118, use that packet as provenance for
existence and commit/path only. Do not treat its measurement table as a new
`plans/BASELINE.md` baseline row.

Known stale current-state wording to reconcile includes the v0 release snapshots
that list "editor-shell mock-event-loop perf harness" as a known deferral and
the `plans/BASELINE.md` Gate A note that describes the non-winit editor-shell
perf harness as still deferred. Preserve those old dated records as history in
`Status.md`, `HANDOFF.md`, and `change.md`; add current records that supersede
them.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-118
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Correction Packet

DISPATCH_ID: ISSUE-46
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_13-52-40+0300
RELATED_FILES:
- crates/material-graph/src/lib.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-46_TASK_2026-05-19_13-45-24+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-46_EXEC_2026-05-19_13-51-48+0300.md`
- Latest Review Report: none; the post-execution verification gate failed before Codex control review.
- Prior CORRECT packets in this dispatch: none.
- Verification failure source: `.ai/dispatch.verify.ps1` step `[1] cargo +nightly fmt --check`, where `cargo +nightly fmt --all -- --check` reported incorrect newline style in `crates/material-graph/src/lib.rs`.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly this correction and nothing more.

1. **Normalize rustfmt newline style in material graph** - Verification gate finding: `cargo +nightly fmt --all -- --check` reported incorrect newline style in `crates/material-graph/src/lib.rs`. Required change: apply only the line-ending/newline-style normalization required by rustfmt for `crates/material-graph/src/lib.rs`. Do not change Rust tokens, public API, tests, behavior, comments, Cargo files, the anim-graph implementation, scripts, docs, or any other handoff packet. Acceptance: `cargo +nightly fmt --all -- --check` exits 0 and no longer reports incorrect newline style for `crates/material-graph/src/lib.rs`.

## Deferred Findings (NOT Approved for This Round)

None. No review or control findings exist for this round because verification failed before Codex control review. The only approved correction is the newline-style fix above.

## Updated Acceptance Criteria

Unchanged from Task Packet, with one correction-round clarification: the only additional allowed project-file change is newline-style-only normalization of `crates/material-graph/src/lib.rs` needed for the canonical formatting gate.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from Task Packet, with these correction-round clarifications:

- If passing formatting requires semantic Rust changes, changing any file other than `crates/material-graph/src/lib.rs`, or changing anything in `crates/material-graph/src/lib.rs` beyond newline style, halt and report the exact required diff.
- If `.ai/dispatch.verify.ps1` still fails after the newline-style correction, halt and document the failing command, exit code, and log tail in the next execution report.

## Planner Notes

The ISSUE-46 implementation reached post-execution verification, but the canonical gate stopped at the first formatting step before any control review could run. This correction is intentionally mechanical: normalize the rustfmt newline style for the existing material-graph reference file, then rerun the full gate. Do not revisit the anim-graph design, add behavior, change dependency choices, or make unrelated cleanup in this round.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-46
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Correction Packet

DISPATCH_ID: ISSUE-57
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_02-08-50+0300
RELATED_FILES:
- crates/material-graph/src/lib.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-57_TASK_2026-05-20_02-01-12+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-57_EXEC_2026-05-20_02-08-01+0300.md`
- Latest Review Report: none; the post-execution verification gate failed before Codex control review could pass.
- Prior Correction Packets: none.
- Verification Failure: `.ai/dispatch.verify.ps1` step 1, `cargo +nightly fmt --all -- --check`, reported incorrect newline style in `crates/material-graph/src/lib.rs`.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections, nothing more and nothing less.

1. **Normalize material graph newline style only** - Verification gate failure from `cargo +nightly fmt --all -- --check`. Required change: convert `crates/material-graph/src/lib.rs` to the newline style expected by rustfmt without changing Rust source text, tests, APIs, behavior, formatting beyond line endings, or any other file. Acceptance: `cargo +nightly fmt --all -- --check` exits 0 and no content changes appear in `crates/material-graph/src/lib.rs` other than newline-style normalization.

## Deferred Findings (NOT Approved for This Round)

None. No review findings exist for this correction round because verification failed before control review.

## Updated Acceptance Criteria

Unchanged from the Task Packet and dispatch automation. The ISSUE-57 animation-graph smoke test remains the only functional deliverable, and the canonical post-execution verification gate must pass before the dispatch can proceed.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet except for the single approved newline-style normalization in `crates/material-graph/src/lib.rs`. Halt and return to Planner if fixing the verification failure would require any Rust source-text change, any edit to files other than `crates/material-graph/src/lib.rs`, any change to the ISSUE-57 test behavior, or any broader workspace cleanup.

## Planner Notes

This correction is intentionally limited to the verification failure shown in the post-execution log. Do not revisit the animation-graph implementation unless verification proves it is directly responsible for a remaining failure. Do not run broad formatting commands that rewrite unrelated files; use the narrowest operation that normalizes `crates/material-graph/src/lib.rs` line endings, then verify with the exact gates above.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-57
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

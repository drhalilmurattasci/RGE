# Correction Packet

DISPATCH_ID: ISSUE-72
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_17-57-43+0300
RELATED_FILES:
- crates/editor-ui/tests/phase8_graph_diff_smoke.rs
- crates/script-graph/src/lib.rs
- crates/gfx/src/lib.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-72_TASK_2026-05-20_17-29-21+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-72_EXEC_2026-05-20_17-56-07+0300.md`
- Latest Review Report: none; the canonical post-execution verification gate failed before Codex control review.
- Prior CORRECT packets in this dispatch: `ai_handoffs/ISSUE-72_CORRECT_2026-05-20_17-50-41+0300.md`
- Verification failure: `.ai/dispatch.verify.ps1` step 5, `cargo test --workspace --all-targets --no-fail-fast`, failed because the `-p rge-gfx --lib` test binary exited abnormally with Windows `STATUS_ACCESS_VIOLATION` after printing passing test output.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections: nothing more, nothing less.

1. **Re-run the failing rge-gfx library target with diagnostics** - Verification gate finding: `cargo test --workspace --all-targets --no-fail-fast` failed only after the `rge-gfx` library test binary exited with `0xc0000005, STATUS_ACCESS_VIOLATION`; no assertion failure or ISSUE-72 test failure was reported in the canonical log. Required action: run `cargo test -p rge-gfx --lib -- --nocapture` to determine whether the access violation is transient or reproducible. Do not edit `crates/gfx/**`, manifests, scripts, docs, schemas, or any production source under this correction. Acceptance: the command exits 0, or the follow-up execution report records the reproduced access violation and marks the dispatch blocked as described below.
2. **Re-run the canonical verification gate if the rge-gfx target passes** - Verification gate finding: the dispatch cannot proceed until `.ai/dispatch.verify.ps1` exits 0. Required action: if `cargo test -p rge-gfx --lib -- --nocapture` exits 0 without source edits, re-run `.ai/dispatch.verify.ps1`. Acceptance: `.ai/dispatch.verify.ps1` exits 0 and the follow-up execution report accurately records that no additional source changes were made in this correction round.
3. **Block rather than expanding scope if the rge-gfx crash reproduces** - Verification gate finding: fixing a reproducible `rge-gfx` access violation would require investigation and edits outside the ISSUE-72 ScriptGraph GraphDiff smoke scope. Required action: if `cargo test -p rge-gfx --lib -- --nocapture` or the canonical verification gate reproduces the `rge-gfx` access violation, do not attempt a `rge-gfx` fix in this dispatch. Write the follow-up `EXECUTION_REPORT` with `STATUS: BLOCKED`, `HANDOFF_STATUS: BLOCKED`, `NEXT_ROLE: PLANNER_AI`, and include the exact failing command, exit code, and relevant output. Acceptance: no new source edits are made beyond the existing ISSUE-72 test/formatting changes and the protocol-required execution report packet.

## Deferred Findings (NOT Approved for This Round)

1. **Debug or repair rge-gfx access violation** - Deferred because it is outside the approved ISSUE-72 scope, which is limited to ScriptGraph coverage in `crates/editor-ui/tests/phase8_graph_diff_smoke.rs` plus the prior formatting-only newline normalization already performed in `crates/script-graph/src/lib.rs`. Future trigger / dispatch: open a separate rge-gfx stabilization dispatch if the access violation is reproducible outside this verification run.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with this correction-round gate added: the canonical dispatch verification gate must pass after a clean rerun, or the dispatch must be explicitly blocked with rge-gfx crash diagnostics instead of expanding the ISSUE-72 source scope.

## Re-Verification Gates

The Executor MUST re-run these gates after the corrections:

- `cargo test -p rge-gfx --lib -- --nocapture` -> expected exit 0 if the access violation was transient; otherwise write a blocked execution report.
- `.ai/dispatch.verify.ps1` -> expected exit 0 before the dispatch can proceed to Codex control review.
- `cargo test -p rge-editor-ui --test phase8_graph_diff_smoke -- --nocapture` -> expected exit 0 if any source file timestamp or formatting operation touches the ISSUE-72 test file again.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet. Additionally, halt and report `BLOCKED` if the `rge-gfx` access violation reproduces, if making verification pass appears to require any `crates/gfx/**` edit, or if any semantic source, manifest, documentation, script, schema, lockfile, or packet edit outside the protocol-required follow-up execution report appears necessary.

## Planner Notes

The latest execution report claims the ISSUE-72 ScriptGraph smoke and formatting corrections are complete, but the canonical verification log still failed before Codex control review because an unrelated `rge-gfx` library test binary exited with `STATUS_ACCESS_VIOLATION`. This correction does not authorize a rge-gfx repair or any broader source change. It authorizes only a diagnostic rerun and a canonical verification rerun if the crash clears; a reproducible crash must be surfaced as a blocked dispatch for planner or human arbitration.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-72
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Correction Packet

DISPATCH_ID: ISSUE-84
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-21_02-36-48+0300
RELATED_FILES:
- kernel/graph-foundation/tests/diff_correctness.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-84_TASK_2026-05-21_02-31-50+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-84_EXEC_2026-05-21_02-36-13+0300.md`
- Latest Review Report: none generated. The post-execution canonical verification gate failed before Codex control review.
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less.

1. **Normalize diff_correctness.rs newline style** - Verification gate finding:
   `cargo +nightly fmt --all -- --check` failed with
   `Incorrect newline style in \\?\A:\RCAD\RGE\kernel\graph-foundation\tests\diff_correctness.rs`.
   Required change: normalize only the line endings in
   `kernel/graph-foundation/tests/diff_correctness.rs` to the newline style
   accepted by `rustfmt`. Do not change Rust tokens, formatting, comments,
   imports, test assertions, helper usage, graph semantics, or any other file.
   Acceptance: `cargo +nightly fmt --all -- --check` exits 0, and a
   whitespace-insensitive diff of
   `kernel/graph-foundation/tests/diff_correctness.rs` shows no content change
   beyond newline normalization.

## Deferred Findings (NOT Approved for This Round)

None. No review findings were produced; the only known blocker is the canonical
format gate failure above.

## Updated Acceptance Criteria

Unchanged from the Task Packet except for this correction-specific allowance:
`kernel/graph-foundation/tests/diff_correctness.rs` may receive a
line-ending-only normalization so the canonical verification gate can pass. The
existing ISSUE-84 added-node payload coverage must not be expanded or otherwise
changed.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `cargo test -p rge-kernel-graph-foundation --test diff_correctness` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0.

If `.ai/dispatch.verify.ps1` fails after the format step for an unrelated
pre-existing issue, stop and report the exact failing step in the new
`EXECUTION_REPORT` without expanding scope.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, with this correction-round addition: halt and
route back to Planner if making `cargo +nightly fmt --all -- --check` pass
would require any non-newline content change in
`kernel/graph-foundation/tests/diff_correctness.rs`, any edit to another file,
or any semantic/runtime change.

## Planner Notes

This correction is deliberately narrow. The ISSUE-84 execution report states
that the requested added-isolated-node payload test landed and the focused
`diff_correctness` test gate passed. The orchestrator's canonical verification
failed before control review on a newline-style problem in the same allowed test
file. Normalize that file's newline style only, preserve the existing ISSUE-84
test content, and rerun the verification gates listed above.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-84
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Execution Report

DISPATCH_ID: ISSUE-120
AUTHOR: Executor / Claude
TIMESTAMP: 2026-05-23_12-47-55+0300
RELATED_FILES:
- ai_handoffs/ISSUE-120_TASK_2026-05-23_12-25-14+0300.md — TASK packet (re-read; no edits).
- ai_handoffs/ISSUE-120_EXEC_2026-05-23_12-33-35+0300.md — prior EXEC packet (re-read; not modified).
- ai_handoffs/ISSUE-120_EXEC_2026-05-23_12-33-35+0300.meta.json — prior EXEC sidecar (already present untracked; not modified).
- ai_handoffs/ISSUE-120_CORRECT_2026-05-23_12-43-49+0300.md — CORRECT packet consumed; no edits.
- crates/script-bench/BASELINE.md — read-only audit input; no edits.
- crates/script-bench/METHODOLOGY.md — read-only audit input; no edits.
- crates/script-bench/src/script_host.rs — read-only audit input; no edits.
- crates/script-bench/Cargo.toml — read-only audit input; no edits.
- plans/IMPLEMENTATION.md — read-only audit input; no edits.
- Status.md — read-only audit input; no edits.
- HANDOFF.md — read-only audit input; no edits.
- change.md — read-only audit input; no edits.
- ai_handoffs/ISSUE-120_EXEC_2026-05-23_12-47-55+0300.md — this corrected EXEC packet (added).
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-120_TASK_2026-05-23_12-25-14+0300.md`

## Pre-Execution Review Consumed

This dispatch consumed a Planner CORRECTION packet rather than a pre-execution
REVIEW packet:

- Correction: `ai_handoffs/ISSUE-120_CORRECT_2026-05-23_12-43-49+0300.md`
  (Planner / OpenAI Codex, `STATUS: CORRECTION_OPEN`,
  `HANDOFF_STATUS: COMPLETE`, `NEXT_ROLE: EXECUTOR_AI`). The Executor concurs
  with all four enumerated corrections and has acted on exactly those four,
  with no scope expansion. This corrected EXEC packet replaces the prior EXEC
  packet's publish-readiness; the prior EXEC packet itself is preserved
  on disk unmodified per the correction's "do not modify existing packets"
  rule.

The four approved corrections this packet acts on:

1. Reclassify `Status.md:7` and `HANDOFF.md:7` as dated 2026-05-14 historical
   snapshots (not live current-state surfaces) sitting below the newer
   2026-05-23 snapshot at `Status.md:5` / `HANDOFF.md:5`.
2. Rewrite Q5 as exactly one docs-only forward-reconciliation dispatch that
   preserves existing dated history in `Status.md`, `HANDOFF.md`, and
   `change.md`; updates `plans/IMPLEMENTATION.md:318` as needed; never
   rewrites existing dated 2026-05-12 / 2026-05-14 entries.
3. Provide a defensible classification of the `crates/script-bench/BASELINE.md`
   2026-05-16 future-facing wording at lines ~438–442. This packet classifies
   it as **STALE-CURRENT in the closing future-tense sentence** and folds a
   minimal forward cross-reference into Q5's single docs-only follow-up.
4. Update worktree and verification prose to acknowledge that the prior EXEC
   sidecar `ISSUE-120_EXEC_2026-05-23_12-33-35+0300.meta.json` is already
   present as an existing untracked dispatch artifact.

## What I Changed

### Source

- None. Read-only audit per TASK `MAY edit: None` and per CORRECT `MAY edit:
  one new EXEC report only`.

### Tests

- None. Audit-only correction round; no test changes permitted.

### Docs

- None. The single allowed artifact is this corrected EXEC packet (and any
  automation-generated `.meta.json` sidecar produced at finalize time).

## Per-File Summary

This dispatch (across the original EXEC and this correction EXEC) has made no
repository edits other than adding the two EXEC packets and their
automation-generated sidecars. All eight required inspection files were
re-read in place and re-grepped for this correction; no file was modified.

## Verification Results

This dispatch is intentionally cargo-free per TASK §"Verification Gates" and
CORRECT §"Re-Verification Gates". Only the read-only gates relevant to this
correction were re-run.

- `git status --short --untracked-files=no` (pre-correction) → empty (no
  tracked changes from this dispatch at start of the correction round).
- `git status --short --untracked-files=all` (pre-correction) → only this
  dispatch's own untracked artifacts present:
  ```
  ?? ai_handoffs/ISSUE-120_CORRECT_2026-05-23_12-43-49+0300.md
  ?? ai_handoffs/ISSUE-120_CORRECT_2026-05-23_12-43-49+0300.meta.json
  ?? ai_handoffs/ISSUE-120_EXEC_2026-05-23_12-33-35+0300.md
  ?? ai_handoffs/ISSUE-120_EXEC_2026-05-23_12-33-35+0300.meta.json
  ?? ai_handoffs/ISSUE-120_TASK_2026-05-23_12-25-14+0300.md
  ?? ai_handoffs/ISSUE-120_TASK_2026-05-23_12-25-14+0300.meta.json
  ```
  The CORRECT packet and its sidecar arrived from the Planner; the prior EXEC
  packet plus its already-generated `.meta.json` sidecar are also present
  untracked — this confirms correction #4 and means the corrected EXEC
  packet's worktree-state prose must account for the existing sidecar rather
  than calling it "to be produced at finalize time".
- Re-read TASK packet `ai_handoffs/ISSUE-120_TASK_2026-05-23_12-25-14+0300.md`
  in full.
- Re-read prior EXEC packet
  `ai_handoffs/ISSUE-120_EXEC_2026-05-23_12-33-35+0300.md` in full.
- Re-read CORRECT packet
  `ai_handoffs/ISSUE-120_CORRECT_2026-05-23_12-43-49+0300.md` in full.
- Re-inspected `crates/script-bench/BASELINE.md` line ranges 395–442
  (especially the 2026-05-16 harness-revision section that closes with
  future-facing wording at 438–442).
- Re-inspected `plans/IMPLEMENTATION.md` lines 310–324 (Phase 3.4 exit-criteria
  block, including line 318).
- Re-inspected `Status.md` line ranges 1–15, then specifically lines 5, 7, 11
  (snapshot block ordering and per-line semicolon-bounded fragments mentioning
  `peak_rss` / `vss_delta` / `soak`).
- Re-inspected `HANDOFF.md` line ranges 1–15, then specifically lines 5, 7, 11
  (same snapshot ordering and fragment search).
- Re-inspected `change.md` lines 1443, 1523, 1527, 1574, 1616, 1620 already
  catalogued by the prior EXEC.
- Read-only search terms re-applied where the correction needed them
  (`peak_rss`, `vss_delta`, `soak`, `2026-05-16`, `2026-05-17`, `2026-05-23`,
  `harness`, `ProcessMemoryMetrics`, `memory-stats`,
  `phase_3_memory_soak_one_hour`, `phase3_memory_soak`,
  `FORMAL_MEMORY_SOAK_DURATION`, `one-hour`, `1-hour`, `deferr`). Search tool
  used: the available local `Grep` (ripgrep-backed) and `awk` for line-by-line
  full-content extraction of the over-long Status.md / HANDOFF.md / change.md
  snapshot lines. `rg` is available; no fallback was needed for this
  correction.
- After writing this corrected EXEC packet,
  `git status --short --untracked-files=all` is expected to show no tracked
  modifications; expected untracked dispatch artifacts are this dispatch's
  TASK packet + sidecar, prior EXEC packet + sidecar, CORRECT packet + sidecar,
  and this corrected EXEC packet (and its `.meta.json` sidecar if the finalize
  step generates one).
- No `cargo` / `cargo test` / `cargo bench` / `cargo fmt` / `cargo-deny` /
  `.ai/dispatch.verify.ps1` / one-hour soak invocation occurred. Audit-only
  correction-round scope respected.

### Command log (read-only, correction round only)

```
git status --short --untracked-files=no                # empty
git status --short --untracked-files=all               # six untracked dispatch artifacts as listed above
Read ai_handoffs/ISSUE-120_CORRECT_2026-05-23_12-43-49+0300.md
Read ai_handoffs/ISSUE-120_TASK_2026-05-23_12-25-14+0300.md
Read ai_handoffs/ISSUE-120_EXEC_2026-05-23_12-33-35+0300.md
wc -l Status.md HANDOFF.md change.md                   # 381 / 1676 / 1632 lines respectively
Grep "2026-05-23" Status.md                            # line 5
Grep "2026-05-23" HANDOFF.md                           # line 5
awk 'NR>=1 && NR<=15 { print NR": "length($0)" chars: "substr($0,1,200) }' Status.md
awk 'NR>=1 && NR<=15 { print NR": "length($0)" chars: "substr($0,1,200) }' HANDOFF.md
awk 'NR==5' Status.md                                  # full line; ISSUE-118 2026-05-23 snapshot; no peak_rss/vss_delta/soak mention
awk 'NR==5' HANDOFF.md                                 # parallel ISSUE-118 2026-05-23 snapshot; no peak_rss/vss_delta/soak mention
awk 'NR==5' Status.md  | grep -oE "peak_rss[^.]*|vss_delta[^.]*|soak[^.]*"   # no matches
awk 'NR==7' Status.md  | tr ';' '\n' | grep -nE "peak_rss|vss_delta|soak"
awk 'NR==11' Status.md | tr ';' '\n' | grep -nE "peak_rss|vss_delta|soak"
awk 'NR==7' HANDOFF.md | tr ';' '\n' | grep -nE "peak_rss|vss_delta|soak"
awk 'NR==11' HANDOFF.md| tr ';' '\n' | grep -nE "peak_rss|vss_delta|soak"
Read crates/script-bench/BASELINE.md (offset 395, limit 55)
Read plans/IMPLEMENTATION.md (offset 310, limit 15)
new-handoff.ps1 -DispatchId ISSUE-120 -PacketType EXEC -Author Executor
  # scaffolded this corrected EXEC packet at ISSUE-120_EXEC_2026-05-23_12-47-55+0300.md
```

## Evidence Table (Updated for Correction)

This evidence table supersedes the prior EXEC packet's evidence table for the
items the correction addresses; rows 1–14 and 20–22 from the prior EXEC remain
materially correct and are retained for reference. Rows 15–19, 23, and 24 are
new or revised to implement corrections #1, #3, and #4.

All citations are against the working-tree state at commit `4bd7ecc` (no edits
introduced by this correction round).

| # | Finding | File | Line(s) |
|---|---|---|---|
| 1 | `ProcessMemoryMetrics` struct (start/end/peak RSS, start/end/delta VSS, sample count) committed | `crates/script-bench/src/script_host.rs` | 227–247 |
| 2 | `MemorySoakReport::process_memory: Option<ProcessMemoryMetrics>` field committed | `crates/script-bench/src/script_host.rs` | 251–267 |
| 3 | `ScriptHostBench::memory_soak` folds per-cycle samples into `peak_rss` + end footprint, produces `process_memory` payload | `crates/script-bench/src/script_host.rs` | 583–651 |
| 4 | `sample_process_memory` wraps `memory_stats::memory_stats()` (Some on supported platforms, None elsewhere — honest unavailability) | `crates/script-bench/src/script_host.rs` | 757–772 |
| 5 | `memory_delta_bytes` signed end-minus-start delta, saturating into i64 | `crates/script-bench/src/script_host.rs` | 774–785 |
| 6 | `phase_3_memory_soak_one_hour` test prints `phase3_memory_soak_memory: …` line under `--nocapture` with the eight ProcessMemoryMetrics fields | `crates/script-bench/src/script_host.rs` | 1077–1092 |
| 7 | Bounded `memory_soak_reports_process_memory_metrics` test exercises the same metrics path with a sub-second floor, asserts numeric metrics on Windows | `crates/script-bench/src/script_host.rs` | 975–1041 |
| 8 | `memory-stats = "1"` dependency added with rationale comment (2026-05-16) | `crates/script-bench/Cargo.toml` | 20–31 |
| 9 | Committed formal one-hour metrics-enabled RUN 2026-05-17 — invocation, toolchain, exit code, exact `--nocapture` stdout, full evidence table including `peak_rss_bytes=10510336`, `vss_delta_bytes=1368064`, `cycles=3736934`, `elapsed_s=3600.00`, `samples=3736936`, `final_counter_sum=1000499500`, `restored_components=3736934000` | `crates/script-bench/BASELINE.md` | 140–211 |
| 10 | Platform mapping documented honestly (Windows working set + commit charge; Linux RSS + VSZ; other platforms None) | `crates/script-bench/BASELINE.md` | 183–192 |
| 11 | Prior 2026-05-12 RUN preserved as historical record; the metrics-enabled 2026-05-17 RUN explicitly complements it without rewriting history | `crates/script-bench/BASELINE.md` | 120–138, 200–204 |
| 12 | "Memory-soak process-memory metrics — harness revision 2026-05-16" section opening + body describe the harness-only capability addition; scoped to "this revision" | `crates/script-bench/BASELINE.md` | 405–437 |
| 12a | **STALE-CURRENT (forward cross-reference recommended)**: closing paragraph 438–442 of the 2026-05-16 harness-revision section says "This revision adds the harness capability only. It does not re-run the formal one-hour soak and publishes no new one-hour memory baseline row …" and ends with "A future release-readiness one-hour soak invocation would be the natural producer of a formal one-hour `peak_rss` / `vss_delta` baseline." The "future release-readiness one-hour soak invocation" sentence is **future-tense relative to 2026-05-16** but is now factually superseded by the 2026-05-17 RUN section already in the same file at lines 140–211. The section is scoped to "this revision" so the wording is dated-historical when read with that scope, but the closing future-tense sentence reads as a live forward-looking claim and a present-day reader who jumps directly to this section without seeing the 2026-05-17 section above could be misled. Recommend a minimal one-line forward cross-reference (see Q5). The body of the section (capability description, platform mapping, dependency rationale) is CURRENT-AND-CORRECT and must not be rewritten. | `crates/script-bench/BASELINE.md` | 438–442 |
| 13 | METHODOLOGY.md W5 section describes the 2026-05-16 process-memory sampling, `ProcessMemoryMetrics`, platform mapping, and `memory-stats` dependency | `crates/script-bench/METHODOLOGY.md` | 142–169 |
| 14 | METHODOLOGY.md Change log entry "Memory-soak process-memory metrics (2026-05-16)" | `crates/script-bench/METHODOLOGY.md` | 294–300 |
| 15 | **STALE-CURRENT-STATE (present-tense, load-bearing)**: `plans/IMPLEMENTATION.md` Phase 3.4 exit criterion #3 is a `CLOSED 2026-05-12` marker but the parenthetical reads in present tense: "(no explicit `peak_rss` / `vss_delta` capture in the harness — harness limitation flagged for future improvement)". The dated CLOSED-on-2026-05-12 framing is historical and accurate; the present-tense parenthetical is contradicted by committed code (`script_host.rs:227–247`, `script_host.rs:583–651`, `Cargo.toml:20–31`) and by the committed 2026-05-17 RUN (`BASELINE.md:140–211`). This is the only present-tense stale claim across the four scoped status surfaces. Forward-reconciliation target — append a forward-current pointer rather than rewriting the 2026-05-12 CLOSED record. | `plans/IMPLEMENTATION.md` | 318 |
| 16 | **HISTORICAL-ONLY DATED SNAPSHOT (preserve as written, reconcile forward not in place)**: `Status.md:7` is a dated `Snapshot: 2026-05-14` v0-cert snapshot block sitting **below** the newer 2026-05-23 snapshot at `Status.md:5`. The `peak_rss` / `vss_delta` soak-harness deferral wording occurs inside that 2026-05-14 v0-cert snapshot (semicolon-bounded fragment ~26 of line 7). It was accurate at the 2026-05-14 v0 certification and must be preserved verbatim per the explicit "older dated snapshots retained verbatim as history" framing inside the 2026-05-23 ISSUE-118 snapshot at `Status.md:5`. NOT a live current-state surface to remove in place. | `Status.md` | 7 (long line; semicolon-bounded fragment) |
| 17 | **HISTORICAL-ONLY DATED SNAPSHOT (preserve as written, reconcile forward not in place)**: `Status.md:11` is a dated `Snapshot: 2026-05-14` MAIN-ORDERED Job-10 closeout snapshot, also below the newer 2026-05-23 snapshot. Same `peak_rss` / `vss_delta` "pressure-driven future dispatch candidate" wording (semicolon-bounded fragment ~20 of line 11). Historical at its own date; preserve verbatim. NOT a live current-state surface. | `Status.md` | 11 (long line; semicolon-bounded fragment) |
| 18 | **HISTORICAL-ONLY DATED SNAPSHOT (preserve as written, reconcile forward not in place)**: `HANDOFF.md:7` mirrors `Status.md:7` — same dated `Snapshot: 2026-05-14` v0-cert wording including the `peak_rss` / `vss_delta` deferral (semicolon-bounded fragment ~18 of line 7). Below the newer 2026-05-23 snapshot at `HANDOFF.md:5`. Preserve verbatim. | `HANDOFF.md` | 7 (long line; semicolon-bounded fragment) |
| 19 | **HISTORICAL-ONLY DATED SNAPSHOT (preserve as written, reconcile forward not in place)**: `HANDOFF.md:11` mirrors `Status.md:11` — same dated `Snapshot: 2026-05-14` Job-10 closeout wording (semicolon-bounded fragment ~17 of line 11). Preserve verbatim. | `HANDOFF.md` | 11 (long line; semicolon-bounded fragment) |
| 20 | **HISTORICAL (preserve)**: change.md 2026-05-12 entries for the soak gate closure record "Does NOT certify: explicit memory-growth metrics (no `peak_rss` / `vss_delta` …)" — accurate description of that 2026-05-12 RUN; preserve as written | `change.md` | 1443, 1523, 1527, 1574 |
| 21 | **HISTORICAL (preserve)**: change.md 2026-05-14 Job-10 closeout entry lists `peak_rss` / `vss_delta` soak-harness improvement as a "pressure-driven future dispatch candidate". Accurate at that date; preserve as dated history | `change.md` | 1616 |
| 22 | **HISTORICAL (preserve)**: change.md 2026-05-14 v0 release certification entry lists `peak_rss` / `vss_delta` soak-harness improvement under "Known v0 deferrals (NOT blockers)". Accurate at that date; preserve as dated history | `change.md` | 1620 |
| 23 | The newest snapshot at `Status.md:5` and `HANDOFF.md:5` is a 2026-05-23 ISSUE-118 docs-only editor-shell render-frame perf-harness reconciliation. It explicitly says "older dated snapshots retained verbatim below as history" and **does not mention** `peak_rss`, `vss_delta`, or `soak`. That confirms (a) the 2026-05-14 snapshot at line 7 is dated history under a current snapshot at line 5, and (b) no live current-state snapshot has yet reconciled the `peak_rss` / `vss_delta` deferral forward — which is the gap Q5's docs-only follow-up should close. | `Status.md`, `HANDOFF.md` | 5 (long lines; first-200-char preview + targeted `grep -oE` for the terms returned no matches) |
| 24 | **EXISTING DISPATCH ARTIFACT (correction #4 acknowledgement)**: the prior EXEC sidecar `ai_handoffs/ISSUE-120_EXEC_2026-05-23_12-33-35+0300.meta.json` is already present as an untracked file in `git status --short --untracked-files=all`. It is automation output from the prior EXEC packet's finalize step, not task content. The corrected EXEC packet's worktree-state prose (see "Worktree State" below) accounts for it explicitly rather than describing it as a sidecar yet-to-be-produced. | `ai_handoffs/ISSUE-120_EXEC_2026-05-23_12-33-35+0300.meta.json` | n/a (binary-text sidecar produced by handoff automation) |

## 5-Question Memory-Soak Reconciliation Answer Block

### Q1. Metrics Support

**Yes — `script-bench` harness code committed today supports
`peak_rss_bytes` / `vss_delta_bytes` process-memory metrics for the memory
soak.** Unchanged from the prior EXEC packet; the correction did not touch
this answer.

- `ProcessMemoryMetrics` carries `start_rss_bytes` / `end_rss_bytes` /
  `peak_rss_bytes` / `start_vss_bytes` / `end_vss_bytes` / `vss_delta_bytes`
  / `samples` at `crates/script-bench/src/script_host.rs:227–247`.
- `MemorySoakReport::process_memory: Option<ProcessMemoryMetrics>` is wired
  into the soak report at `crates/script-bench/src/script_host.rs:251–267`.
- `ScriptHostBench::memory_soak` calls `sample_process_memory()` at soak
  start, after each completed hot-reload cycle, and at soak end, folding the
  observed samples into `peak_rss` / the end footprint and producing the
  `Option<ProcessMemoryMetrics>` payload
  (`crates/script-bench/src/script_host.rs:583–651`).
- `sample_process_memory()` returns `Some((resident, virtual))` from
  `memory_stats::memory_stats()` on supported platforms and `None` elsewhere
  — honest unavailability, never a fabricated zero
  (`crates/script-bench/src/script_host.rs:757–772`).
- `phase_3_memory_soak_one_hour` prints a `phase3_memory_soak_memory:
  samples=… start_rss_bytes=… end_rss_bytes=… peak_rss_bytes=… start_vss_bytes=…
  end_vss_bytes=… vss_delta_bytes=…` line under `--nocapture`
  (`crates/script-bench/src/script_host.rs:1077–1092`).
- A bounded non-`#[ignore]`'d
  `memory_soak_reports_process_memory_metrics` test pins the wiring with a
  tiny scene and a sub-second floor; on Windows it asserts the metrics are
  populated (`crates/script-bench/src/script_host.rs:975–1041`).
- The required `memory-stats` dependency is committed at
  `crates/script-bench/Cargo.toml:20–31` (added 2026-05-16 with rationale
  about the workspace `unsafe_code = "forbid"` lint forcing the FFI into the
  dep crate).

This is committed harness support and bounded (sub-second) metric-path test
coverage — distinct from formal one-hour result evidence, which Q2 covers
separately.

### Q2. Formal One-Hour Evidence

**Yes — a committed formal one-hour `peak_rss_bytes` / `vss_delta_bytes`
result is in the repository.** Unchanged from the prior EXEC packet; the
correction did not touch this answer.

- **Recorded date**: 2026-05-17 (release-profile background test run on the
  Windows 11 / x86_64 recorder host; toolchain `cargo 1.92.0` / `rustc
  1.92.0` / `wasmtime 44.0.1`).
- **Invocation**:
  ```
  cargo test -p rge-script-bench --release --lib \
    script_host::tests::phase_3_memory_soak_one_hour \
    -- --ignored --nocapture
  ```
- **Exit code**: 0.
- **Cargo wall-clock**: 3600.46 s; test harness summary `ok. 1 passed; 0
  failed; 0 ignored; 0 measured; 28 filtered out; finished in 3600.00s`.
- **Cycle count (`cycles`)**: 3,736,934.
- **Sample count (`samples`)**: 3,736,936 (= start + one per completed cycle
  + end — matches the harness sampling cadence).
- **Elapsed seconds (`elapsed_s`)**: 3600.00.
- **`peak_rss_bytes`**: **10,510,336**.
- **`vss_delta_bytes`**: **1,368,064** (positive — commit charge grew on
  Windows; recorded verbatim and signed).
- **Companion endpoints**: `start_rss_bytes=8,859,648`,
  `end_rss_bytes=10,346,496`, `start_vss_bytes=1,327,104`,
  `end_vss_bytes=2,695,168`, `final_counter_sum=1,000,499,500`,
  `restored_components=3,736,934,000`
  (`restored_components == cycles * entity_count` invariant HELD).
- **Source of evidence**: `crates/script-bench/BASELINE.md:140–211` —
  "Formal 1-hour memory soak — RUN 2026-05-17 (process-memory metrics
  enabled)" section, including the exact `--nocapture` stdout block, the
  measurement table, the Windows working-set/commit-charge platform mapping,
  and a load-bearing scope-limitation note (CONSTRAINED-CERTIFIED on the
  recorder host only).

**Distinguishing harness-support from formal-result evidence**: Q1 establishes
that the harness can capture the metrics. Q2 establishes that a formal
one-hour recorder-host RUN with those metrics captured exists, dated
2026-05-17, with all eight ProcessMemoryMetrics fields recorded verbatim from
stdout. The 2026-05-12 RUN at `BASELINE.md:120–138` predates the metrics and
remains the pre-metrics historical record, unchanged.

### Q3. Deferral Text Status (CORRECTED)

This Q3 supersedes the prior EXEC's Q3 for `Status.md`, `HANDOFF.md`, and the
2026-05-16 closing-paragraph fragment in `BASELINE.md`, and re-confirms the
other classifications.

Per-file classification of every relevant deferral / capability statement
across the six required docs. The audit distinguishes
**CURRENT-AND-CORRECT** (matches committed evidence), **STALE-CURRENT-STATE**
(present-tense or live-forward wording the committed evidence contradicts),
and **HISTORICAL-ONLY** (dated record that was accurate when written;
preserve forward).

- `crates/script-bench/BASELINE.md`:
  - **140–211** (RUN 2026-05-17 section): **CURRENT-AND-CORRECT** — records
    the formal one-hour metrics-enabled run with exact stdout evidence and
    explicit "complements the 2026-05-12 row" framing that preserves history.
  - **120–138** (RUN 2026-05-12 section): **HISTORICAL-ONLY** — accurate
    description of the pre-metrics run. Preserve as-is.
  - **396–402** ("It does NOT certify … `peak_rss` / `vss_delta` for this
    2026-05-12 run …"): **HISTORICAL-ONLY** — scoped to the 2026-05-12 RUN
    by its own wording ("for this 2026-05-12 run") and explicitly notes "The
    harness now does capture them — see 'Memory-soak process-memory metrics'
    below". Preserve as-is.
  - **405–437** (opening + body of "Memory-soak process-memory metrics —
    harness revision 2026-05-16"): **CURRENT-AND-CORRECT** as a dated
    description of the 2026-05-16 harness capability addition (struct
    definition, platform mapping, `memory-stats` dependency rationale, bounded
    test). Preserve.
  - **438–442** (closing paragraph of the same section, including the
    sentence "A future release-readiness one-hour soak invocation would be
    the natural producer of a formal one-hour `peak_rss` / `vss_delta`
    baseline."): **STALE-CURRENT (forward cross-reference recommended)**.
    Scoped reading: the paragraph opens "This revision adds the harness
    capability only" — past-tense, scoped to the 2026-05-16 revision — and
    that opening is correct as a historical statement of that dispatch's
    self-bounded scope. Stale reading: the closing sentence is future-tense
    relative to 2026-05-16 ("a future release-readiness one-hour soak
    invocation would be the natural producer …"). The 2026-05-17 RUN at
    `BASELINE.md:140–211` IS that future invocation; it is committed in the
    same file. A present-day reader who jumps to the 2026-05-16 section
    without scrolling up to the 2026-05-17 RUN may misread the closing
    sentence as a live forward gap. **Defensible classification choice**: the
    paragraph is **historical when read as a 2026-05-16 dispatch self-scope
    description**, but the closing future-tense sentence is **stale-current
    when read as a live forward statement**. Both readings are defensible;
    the safest publish-ready outcome is to keep the section text intact (it
    is dated context for the 2026-05-16 revision) and **add a one-line
    forward cross-reference** at the end of the closing paragraph pointing
    forward to RUN 2026-05-17 above. That cross-reference is folded into
    Q5's single docs-only follow-up scope. No rewrite of the body, no
    deletion of any existing sentence.

- `crates/script-bench/METHODOLOGY.md`:
  - **142–169** (W5 methodology description): **CURRENT-AND-CORRECT** —
    describes `ProcessMemoryMetrics`, `peak_rss_bytes`, `vss_delta_bytes`,
    platform mapping, and the `memory-stats` crate.
  - **294–300** (Change log "Memory-soak process-memory metrics 2026-05-16"):
    **HISTORICAL-ONLY / CURRENT-AND-CORRECT** dated change-log entry. No
    change required.

- `plans/IMPLEMENTATION.md`:
  - **318** (Phase 3.4 exit criterion #3 CLOSED-marker, dated 2026-05-12):
    **MIXED — dated CLOSED record is HISTORICAL-ONLY; present-tense
    parenthetical is STALE-CURRENT-STATE.** The 2026-05-12 CLOSED framing
    ("[CLOSED 2026-05-12 on recorder host: …") and its measurement summary
    are historical and accurate; preserve verbatim. The embedded parenthetical
    "(no explicit `peak_rss` / `vss_delta` capture in the harness — harness
    limitation flagged for future improvement)" reads as a present-tense
    capability claim ("in the harness") and a present-tense limitation
    ("harness limitation flagged for future improvement"); both are
    contradicted by committed code (`script_host.rs:227–247`,
    `script_host.rs:583–651`, `Cargo.toml:20–31`) plus the committed
    2026-05-17 RUN evidence (`BASELINE.md:140–211`). Forward reconciliation
    target — append a forward-current pointer (e.g. "Re-validated 2026-05-17
    on recorder host with process-memory metrics captured — see
    `crates/script-bench/BASELINE.md` 'RUN 2026-05-17' section") rather than
    rewriting the 2026-05-12 CLOSED record. The bullet's other CLOSED
    markers (rows 316–317, 319) do not need adjustment.

- `Status.md`:
  - **5** (current `Snapshot: 2026-05-23` head — ISSUE-118 docs-only editor-shell
    render-frame perf-harness reconciliation): **CURRENT-AND-CORRECT** for
    its own scope. Does not mention `peak_rss` / `vss_delta` / `soak`
    (confirmed by targeted `grep -oE` on the line). This is the live
    current-state surface for the document; the 2026-05-14 snapshot at line
    7 sits below it as dated history.
  - **7** (dated `Snapshot: 2026-05-14` v0-cert snapshot, sitting **below**
    the newer 2026-05-23 snapshot): **HISTORICAL-ONLY**. Reclassified per
    correction #1: this is a dated v0-cert-time snapshot of that day's
    posture, preserved verbatim per the explicit "older dated snapshots
    retained verbatim below as history" framing in the ISSUE-118 2026-05-23
    snapshot at line 5. The `peak_rss` / `vss_delta` soak-harness deferral
    wording (semicolon-bounded fragment ~26 of line 7) was accurate at
    2026-05-14 v0 cert and must be preserved as written. **Not** a live
    current-state surface; **must not** be removed in place.
  - **11** (older `Snapshot: 2026-05-14` MAIN-ORDERED Job-10 closeout
    snapshot, below line 5): **HISTORICAL-ONLY**. Reclassified per
    correction #1: dated Job-10 closeout-time snapshot. The `peak_rss` /
    `vss_delta` "pressure-driven future dispatch candidate" wording
    (semicolon-bounded fragment ~20 of line 11) was accurate at that
    closeout date and must be preserved.

- `HANDOFF.md`:
  - **5** (current `Snapshot: 2026-05-23` head — parallel ISSUE-118 snapshot):
    **CURRENT-AND-CORRECT**. No mention of `peak_rss` / `vss_delta` / `soak`.
  - **7** (dated `Snapshot: 2026-05-14` v0-cert snapshot): **HISTORICAL-ONLY**.
    Reclassified per correction #1 — same reasoning as `Status.md:7`.
  - **11** (dated `Snapshot: 2026-05-14` Job-10 closeout): **HISTORICAL-ONLY**.
    Reclassified per correction #1 — same reasoning as `Status.md:11`.

- `change.md`:
  - **1443, 1523, 1527, 1574**: **HISTORICAL-ONLY** — dated 2026-05-12
    entries describing the 2026-05-12 soak-gate-closure dispatch and its
    "Does NOT certify: explicit memory-growth metrics" wording. Accurate at
    that date; preserve as written.
  - **1616**: **HISTORICAL-ONLY** — dated 2026-05-14 Job-10 closeout entry
    listing `peak_rss` / `vss_delta` soak-harness improvement as a
    "pressure-driven future dispatch candidate". Accurate at that date;
    preserve.
  - **1620**: **HISTORICAL-ONLY** — dated 2026-05-14 v0 release
    certification entry listing the same under "Known v0 deferrals (NOT
    blockers)". Accurate at that date; preserve.

**Cross-cutting confirmation**:

- A grep for `2026-05-16` / `2026-05-17` / `memory-stats` / `ProcessMemoryMetrics`
  in `Status.md`, `HANDOFF.md`, `change.md` returns **no matches** — Status /
  HANDOFF / change.md have no forward-current reconciliation of the
  `peak_rss` / `vss_delta` deferral yet.
- A grep for `2026-05-17` / `2026-05-16` in `plans/IMPLEMENTATION.md` returns
  **no matches** — Phase 3.4 exit criterion #3 has no forward-current pointer
  at line 318 yet.
- A grep for `peak_rss` / `vss_delta` / `soak` against the over-long
  `Status.md:5` and `HANDOFF.md:5` lines (the newest 2026-05-23 snapshot)
  returns **no matches** — the existing 2026-05-23 snapshot is scoped to
  ISSUE-118's editor-shell perf-harness reconciliation and does not yet
  cover the memory-soak deferral.

Together these confirm that the only remaining gap is **docs-only forward
reconciliation**, and that the reconciliation must add forward-current text
without rewriting any dated 2026-05-12 / 2026-05-14 historical entry.

### Q4. Remaining Source/Harness Need

**No. No source or harness change is required before docs can be reconciled.**
Unchanged from the prior EXEC packet; the correction did not touch this
answer.

The committed evidence already satisfies what the stale present-state wording
calls "future improvement":

- The harness has `ProcessMemoryMetrics`, samples at start / per-cycle / end,
  exposes the metrics through `MemorySoakReport::process_memory`, and prints
  them under `--nocapture` (rows 1–7 of the evidence table).
- The `memory-stats` Cargo dependency is committed (Cargo.toml:20–31).
- A bounded non-`#[ignore]`'d test exercises the metrics path on every
  workspace test invocation (script_host.rs:975–1041).
- A formal one-hour metrics-enabled RUN on the recorder host (2026-05-17) is
  committed with full stdout evidence (BASELINE.md:140–211).

The only outstanding work the audit can identify is **forward-current
documentation reconciliation** in `plans/IMPLEMENTATION.md`, `Status.md`,
`HANDOFF.md`, and (as a minimal one-line cross-reference) `BASELINE.md`. No
`script-bench` source, test, fixture, Cargo, or workflow change is justified
by the audit evidence.

### Q5. Smallest Follow-Up (CORRECTED)

**Exactly one smallest follow-up: a single docs-only forward-reconciliation
dispatch that ADDS forward-current text to the live current-state surfaces,
PREPENDING / APPENDING rather than rewriting; preserves every dated
2026-05-12 / 2026-05-14 historical entry verbatim in `Status.md`,
`HANDOFF.md`, and `change.md`; and folds a minimal one-line forward
cross-reference into `BASELINE.md`'s 2026-05-16 harness-revision section to
close the stale-current closing-paragraph wording.**

Concrete scope of that single follow-up (forward-reconciliation only, not
in-place rewriting of dated history):

1. **`plans/IMPLEMENTATION.md:318`** — the document has no dated-snapshot
   layering, so the existing `[CLOSED 2026-05-12 …]` text is the load-bearing
   live entry. **Add** a forward-current pointer to the 2026-05-17 RUN, for
   example by appending a `+ re-validated 2026-05-17` clause or a trailing
   note like "Re-validated 2026-05-17 on recorder host with process-memory
   metrics captured — see `crates/script-bench/BASELINE.md` 'RUN 2026-05-17'
   section" inside the same bullet. The dated 2026-05-12 CLOSED record and
   its measurement summary must be preserved; only the present-tense
   parenthetical "(no explicit `peak_rss` / `vss_delta` capture in the
   harness — harness limitation flagged for future improvement)" needs to
   be reconciled forward (either updated in place to past tense scoped to
   2026-05-12, or supplemented with the trailing forward-current pointer
   above).

2. **`Status.md`** — **prepend a new dated forward-current snapshot at the
   top** (above the existing 2026-05-23 snapshot at line 5, or insert
   directly above it as a new sibling snapshot), modeled on the ISSUE-118
   pattern already present at `Status.md:5`: a new `> Snapshot: 2026-05-XX
   (ISSUE-120 docs-only memory-soak process-memory-metrics history
   reconciliation, IMPLEMENTED via the ai_handoffs/ dispatch protocol). …`
   block that (a) identifies the resolved deferral, (b) cites the committed
   harness evidence (`script_host.rs` + `Cargo.toml`) and the committed
   2026-05-17 formal RUN (`BASELINE.md:140–211`), (c) explicitly says "older
   dated snapshots below retained verbatim as history (2026-05-14 v0-cert
   snapshot at original line 7 and 2026-05-14 Job-10 closeout snapshot at
   original line 11 stay as written; the `peak_rss` / `vss_delta`
   soak-harness deferral wording inside those dated snapshots reflects that
   day's posture and is preserved)", and (d) does **not** introduce a new
   measurement table or copy the formal numbers into Status.md. Lines 7 and
   11 must remain unchanged.

3. **`HANDOFF.md`** — **prepend a parallel forward-current snapshot at the
   top**, mirroring (2) above, modeled on the ISSUE-118 pattern already
   present at `HANDOFF.md:5`. Lines 7 and 11 must remain unchanged.

4. **`change.md`** — **append a new dated `2026-05-XX` entry** (date of the
   follow-up dispatch) summarizing the docs-only reconciliation. DO NOT
   modify any existing 2026-05-12 or 2026-05-14 entry; the historical "Does
   NOT certify: explicit memory-growth metrics" wording at lines 1443, 1523,
   1527, 1574, the "pressure-driven future dispatch candidate" wording at
   line 1616, and the "Known v0 deferrals (NOT blockers)" wording at line
   1620 must be preserved as dated history.

5. **`crates/script-bench/BASELINE.md`** — at the **end of the existing
   2026-05-16 harness-revision section's closing paragraph (line 442)**,
   append a one-line forward cross-reference of the form "Superseded forward
   by RUN 2026-05-17 above (see "Formal 1-hour memory soak — RUN 2026-05-17"
   section) — the future release-readiness one-hour soak invocation referenced
   here is the committed 2026-05-17 metrics-enabled RUN at lines 140–211 of
   this file." Do NOT rewrite or delete lines 405–442; the section body
   (capability description, platform mapping, dependency rationale) stays
   intact as dated 2026-05-16 context.

6. **`crates/script-bench/METHODOLOGY.md`** — **no edits required.** W5 at
   lines 142–169 and the change-log entry at 294–300 are already
   CURRENT-AND-CORRECT for the 2026-05-16 harness capability.

7. **No `crates/script-bench` source / test / bench / fixture / Cargo edits;
   no architectural-lint / workflow / schema changes; no fresh one-hour
   soak; no `.ai/dispatch.verify.ps1` invocation.**

**Why this is the smallest follow-up**:

- **No source/harness edit is justified** by the audit evidence (Q4) — source
  already implements what the stale text calls "future improvement".
- **No fresh one-hour soak is justified** — `BASELINE.md:140–211` already
  carries a committed metrics-enabled formal RUN on the recorder host with
  the full set of `peak_rss_bytes` / `vss_delta_bytes` numbers and a
  load-bearing scope-limitation note.
- **No dated 2026-05-12 / 2026-05-14 historical entry is rewritten** —
  `Status.md:7`, `Status.md:11`, `HANDOFF.md:7`, `HANDOFF.md:11`, and the
  `change.md` 2026-05-12 / 2026-05-14 entries stay verbatim. The
  reconciliation is forward-current text added at the top (Status.md /
  HANDOFF.md), appended in line (change.md), or appended as a one-line
  cross-reference (BASELINE.md).
- **The only live current-state surface that is rewritten** is the
  present-tense parenthetical in `plans/IMPLEMENTATION.md:318`, which has no
  dated-snapshot layering to defer to and is the load-bearing stale text;
  the surrounding `[CLOSED 2026-05-12 …]` framing is preserved.
- **Source and docs reconciliation are not combined** — per TASK Constraints
  and CORRECT acceptance criteria, this dispatch must not recommend combining
  source/harness changes with documentation reconciliation in a single
  follow-up. Since Q4 establishes no source/harness change is needed, the
  recommendation is purely docs-only and the constraint is satisfied without
  a halt.
- **No fresh recorder-host one-hour soak is required to justify the doc
  edits** — the 2026-05-17 RUN is the load-bearing evidence row. Therefore
  this packet does NOT halt with `STATUS: NEEDS_HUMAN`; it routes to
  `REVIEWER_AI` with `HANDOFF_STATUS: COMPLETE` per the success path.
- **The BASELINE.md cross-reference is folded into the same follow-up** —
  the correction's Q5 acceptance allows the smallest follow-up to include
  forward reconciliation of the BASELINE.md 2026-05-16 stale-current closing
  paragraph; doing so as a single appended one-line cross-reference keeps
  the follow-up scope tight and avoids carving a second dispatch.

This follow-up itself MUST NOT be created as a TASK packet by this dispatch;
naming it is the entire Q5 deliverable per TASK §"Constraints / Non-Goals"
("Q5 must name exactly one smallest follow-up. It must not create a follow-up
TASK packet, GitHub issue comment, PR comment, code edit, or documentation
edit.") and per CORRECT §"Updated Acceptance Criteria".

## Deviations from Task Packet

None — this correction round stayed strictly within the CORRECT packet's
enumerated scope and within the original TASK Packet scope. No file was
edited except the addition of this corrected EXEC packet; no Cargo / lint /
verify / soak command was run; no follow-up TASK / REVIEW / CORRECT /
CLOSEOUT packet was authored; no GitHub issue or PR was modified; no
existing packet (TASK, prior EXEC, prior EXEC sidecar, CORRECT, or CORRECT
sidecar) was modified.

The prior EXEC packet's misclassifications (Status.md:7 / HANDOFF.md:7 as
"live current-state lines to remove in place"; the 2026-05-16 closing
paragraph as "historical-only" without sufficient justification; the EXEC
sidecar as "to be produced at finalize time" when it was already present)
are not deviations from the original TASK Packet — they were misreadings
that the Planner caught at control review and corrected in this round. This
corrected EXEC packet supersedes those classifications without modifying the
prior EXEC packet on disk.

A practical note on the inspection technique used for the three large status
files: `Status.md` / `HANDOFF.md` / `change.md` could not be read whole-file
with the Read tool (file size + token limits) and contain many extremely
long lines that the Grep `content` mode could not display in full. Inspection
was completed by combining `Grep -o -n` (precise occurrence list per file
with line numbers), `awk 'NR==<line>'` for full-line extraction, and `tr ';'
'\n' | grep -nE` for semicolon-bounded fragment filtering. This is a
read-only inspection technique that introduces no edits and produced enough
context to classify each occurrence as current, stale, or historical.

## Open Questions for Reviewer

- The follow-up dispatch named in Q5 should be authorized as a single
  docs-only forward-reconciliation dispatch with the scope envelope listed
  in items 1–7 of Q5. The Reviewer / Planner should confirm whether the
  preferred shape of the new forward-current snapshot in `Status.md` /
  `HANDOFF.md` is (a) a new sibling snapshot prepended above the existing
  2026-05-23 ISSUE-118 snapshot at line 5, or (b) an extension of the
  existing 2026-05-23 snapshot with a new sub-section covering the
  memory-soak deferral. This packet recommends (a) for clarity and to keep
  the ISSUE-118 scope intact, but either is consistent with the "preserve
  dated history" requirement.
- The Reviewer should confirm whether the BASELINE.md §405–442 forward
  cross-reference (Q5 item 5) is appropriately folded into the same Q5
  follow-up. CORRECT §"Approved Corrections" item 3 acceptance reads "Either
  classify the future-facing wording as stale-current and include minimal
  forward reconciliation for it in Q5, or explicitly justify why it is
  historical-only because it is scoped to the 2026-05-16 harness revision
  and is superseded by the nearby 2026-05-17 run section." This packet takes
  the first option (stale-current closing sentence + minimal forward
  cross-reference in Q5 item 5), and the wording of the cross-reference is
  intentionally minimal to preserve the dated section body unchanged.

## Worktree State

- Tracked files: clean (no modifications introduced by this dispatch's
  original EXEC round or by this correction round).
- Untracked items (this dispatch's own artifacts only — six entries at start
  of this correction round, expected seven after this packet is written, and
  expected eight after its `.meta.json` sidecar is generated by finalize):
  - `ai_handoffs/ISSUE-120_TASK_2026-05-23_12-25-14+0300.md`
  - `ai_handoffs/ISSUE-120_TASK_2026-05-23_12-25-14+0300.meta.json`
  - `ai_handoffs/ISSUE-120_EXEC_2026-05-23_12-33-35+0300.md` (prior EXEC)
  - `ai_handoffs/ISSUE-120_EXEC_2026-05-23_12-33-35+0300.meta.json` (prior
    EXEC sidecar — already present per correction #4; not modified)
  - `ai_handoffs/ISSUE-120_CORRECT_2026-05-23_12-43-49+0300.md`
  - `ai_handoffs/ISSUE-120_CORRECT_2026-05-23_12-43-49+0300.meta.json`
  - `ai_handoffs/ISSUE-120_EXEC_2026-05-23_12-47-55+0300.md` (this corrected
    EXEC packet)
  - An `ISSUE-120_EXEC_2026-05-23_12-47-55+0300.meta.json` sidecar will be
    produced if/when `new-handoff.ps1 -Finalize` is invoked; that is
    automation output, not task content.
- Branch: `ai-dispatch/ISSUE-120`
- Last commit: `4bd7ecc` chore(dispatch): queue task #18 memory-soak metrics
  reconciliation audit

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-120
AUTHOR: Executor / Claude
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---

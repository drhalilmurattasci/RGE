# Task Packet

DISPATCH_ID: ISSUE-351
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-08_23-19-16+0300
RELATED_FILES:
- .ai/dispatch.tasks.md
- plans/BASELINE.md
- Status.md
- HANDOFF.md
- change.md
- crates/editor-shell/src/lifecycle/extension_command.rs
- crates/editor-shell/src/lifecycle/mod.rs
- crates/editor-shell/src/render_path.rs
- crates/editor-shell/src/lifecycle/tests.rs
- crates/editor-egui-host/src/lib.rs
- crates/editor-egui-host/src/handoff.rs
- crates/editor-egui-host/src/menu_tests.rs
- crates/editor-ui/src/menus/**
- crates/editor-actions/**
- crates/editor-state/**
- crates/cad-core/**
- crates/cad-projection/**
- crates/plugin-*
- kernel/ecs/**
- kernel/plugin-host/**
- plugins/**
- runtime/**
- editor/rge-editor/**
- ai_handoffs/ISSUE-351_*.md
- ai_handoffs/claims/ISSUE-351_*.json
- ai_dispatch_logs/log_2026-06-08_*.md
STATUS: OPEN

## Goal

Run task 103's read-only source/docs selection audit after the task 102
extension-command executor seam. Confirm the task 102 closure boundary from
current source, compare the remaining Phase 9/editor-usability candidate
classes with falsifying searches, and either append exactly one bounded
implementation follow-up as task 104 or record `NEEDS_HUMAN` with evidence.

## Scope

This dispatch is audit-only with respect to production code. The executor may
read/search source, docs, and generated state, but may only edit task/status
bookkeeping documents and this dispatch's own generated artifacts.

### MAY edit
- `.ai/dispatch.tasks.md`
- `plans/BASELINE.md`
- `Status.md`
- `HANDOFF.md`
- `change.md`

### MUST NOT edit
- `crates/**`
- `kernel/**`
- `editor/**`
- `runtime/**`
- `plugins/**`
- `Cargo.toml`
- `Cargo.lock`
- `**/Cargo.toml`
- `.github/workflows/**`
- `*.ps1`
- `.ai/*.schema.json`
- `.ai/dispatch.verify.ps1`
- `tools/architecture-lints/**`
- `schemas/**`
- `ai_handoffs/**` except this dispatch's own newly generated ISSUE-351 packet
  artifacts listed under `MAY add new files`
- `ai_dispatch_logs/**` except this dispatch's own queue audit log listed under
  `MAY add new files`
- Any Rust source or test file
- Any dispatch/scheduler script, schema, workflow, Cargo metadata, architecture
  lint rule/config, plugin runtime/discovery/loading code, existing
  handoff/log artifact from another dispatch, or unrelated generated scratch
  file

### MAY add new files
- `ai_handoffs/ISSUE-351_TASK_2026-06-08_23-19-16+0300.meta.json`
- `ai_handoffs/ISSUE-351_EXEC_*.md`
- `ai_handoffs/ISSUE-351_EXEC_*.meta.json`
- `ai_handoffs/claims/ISSUE-351_*.json`
- `ai_dispatch_logs/log_2026-06-08_*.md`

### MUST NOT add new files
- New Rust source or test files
- New Cargo manifests, lockfiles, crates, examples, fixtures, schemas,
  workflows, scripts, architecture-lint files, plugin runtime/discovery/loading
  files, ADRs, doctrine docs, or unrelated handoff/log/scratch artifacts
- Any task 104 implementation files; task 103 only selects and records the
  follow-up

### Read/search-only surface

The executor MAY read/search the following paths to gather current evidence:
`crates/editor-*`, `editor/`, `crates/plugin-*`, `plugins/`,
`kernel/plugin-host`, `runtime/`, `crates/cad-*`, `crates/kernel-*`,
`kernel/ecs`, `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, `change.md`,
`.ai/dispatch.tasks.md`, and other source paths needed to falsify a specific
candidate claim. Reads/searches may use read-only commands such as `git grep`,
`Select-String`, `Get-Content`, `git diff`, and `git status`. Do not run cargo
commands for this audit.

## Deliverables

- Update `.ai/dispatch.tasks.md` so task 103 records the completed audit result.
- Append exactly one bounded implementation follow-up as task 104, unless the
  evidence does not support a safe task. The task 104 entry must include:
  explicit `MAY edit`, `MUST NOT edit`, `MAY add new files` if applicable,
  done criteria, verification, and halt conditions.
- If no safe bounded task can be defended, append no task 104 and record
  `NEEDS_HUMAN` in `.ai/dispatch.tasks.md`, `Status.md`, `HANDOFF.md`, and the
  EXEC report with the specific missing decision.
- Record the audit outcome in `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`,
  and `change.md` as narrowly as the evidence supports.
- Write exactly one EXEC packet for ISSUE-351, plus the `.meta.json` sidecar if
  produced by `new-handoff.ps1 -Finalize`.
- The EXEC report must include the source/doc search commands and observed
  results that support the task 102 boundary confirmation, every negative
  current-state claim, the candidate comparison, and the selected task 104 or
  `NEEDS_HUMAN` decision.

## Acceptance Criteria

- Task 102's boundary is confirmed from current source and docs: extension
  commands have an injected editor-shell handler seam; editor-shell/rge-editor
  still do not wire real plugin runtime/discovery/loading; the host-to-shell
  menu FIFO remains the menu/palette activation handoff; and no generalized
  registry executor is wired. For keybinding/conflict, palette history,
  unsaved quit/close, clipboard, CAD mutation, and camera UI, the EXEC report
  must distinguish pre-existing substrate from remaining gaps instead of
  treating each class as wholly absent.
- The audit compares at least these candidate classes with current source
  evidence, not only historical docs:
  1. host-shell FIFO/menu-click replacement and generalized registry execution
  2. conflict resolution, keybinding editor, and fatal gating
  3. persistent command-palette history/favorites
  4. unsaved-changes prompt and graceful quit
  5. OS clipboard / typed clipboard
  6. authoritative CAD deletion/duplication/undo integration
  7. broader camera UI
  8. real plugin runtime/discovery/loading beyond the task-102 seam
- For each candidate class, the EXEC report states whether the current source
  makes it safe, unsafe, premature, or human-dependent as a bounded task 104,
  and cites the searches/reads that would falsify that classification.
- Exactly one outcome occurs:
  - task 104 is appended as one implementation task with a bounded edit surface,
    verification, done criteria, and halt conditions; or
  - no implementation task is appended, and `NEEDS_HUMAN` is recorded with the
    source/doc evidence and the exact arbiter decision needed.
- If task 104 is appended, it does not authorize task 104 implementation work
  during task 103 and does not authorize edits to Rust source/tests, Cargo,
  workflows, schemas, dispatch/scheduler scripts, architecture lints, plugin
  runtime/discovery/loading, or existing artifacts outside its own future
  dispatch scope.
- All edits in this dispatch are limited to `.ai/dispatch.tasks.md`,
  `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, `change.md`, and ISSUE-351
  generated artifacts.

## Current-State Claims: Falsification Required

Every negative or "unchanged" assertion in this packet and in the EXEC report
must carry its inverse search/read command and observed result.

- Claim: task 102 is recorded as done, and task 103 is the current post-seam
  selection audit.
  Falsifying search: `git grep -n -E "^102\\.|^103\\." -- .ai/dispatch.tasks.md`
  -> observed task 102 at `.ai/dispatch.tasks.md:8688` as DONE for the
  editor-shell extension-command executor seam, and task 103 at
  `.ai/dispatch.tasks.md:8802` as the Phase 9 next-task selection audit.

- Claim: no task 104 is currently present in `.ai/dispatch.tasks.md`.
  Falsifying search: `git grep -n -E "^104\\.\\s" -- .ai/dispatch.tasks.md`
  -> no matches; command exits 1.

- Claim: the task 102 brief/closure boundary itself says seam-only and excludes
  plugin runtime/discovery/loading plus adjacent implementation surfaces.
  Falsifying search: `git grep -n -E "editor-shell injected handler seam only|without wiring real plugin runtime/discovery/loading|generalized registry execution|keybinding editor|conflict fatality policy|OS clipboard|typed clipboard|CAD graph/projection mutation|broader camera UI" -- .ai/dispatch.tasks.md`
  -> matches task 102's DONE marker, required behavior, and halt conditions;
  task 103's purpose repeats the same post-seam audit boundary.

- Claim: task 102 added an editor-shell-owned injected extension-command seam
  for already captured `Command::Custom` / `Command::Plugin` activations.
  Falsifying search: `git grep -n -E "ExtensionCommandHandler|ExtensionCommandEvent|process_captured_extension_menu_commands|capture_extension_menu_command|drain_extension_menu_commands|Command::Custom|Command::Plugin" -- crates/editor-shell/src/lifecycle/extension_command.rs crates/editor-shell/src/render_path.rs crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/tests.rs`
  -> matches the `ExtensionCommandHandler` trait, `ExtensionCommandEvent`
  enum, capture/process/drain methods, `route_menu_command` extension arm, and
  focused extension-command tests.

- Claim: `editor-shell` and `rge-editor` still do not wire real plugin
  runtime/discovery/loading after task 102.
  Falsifying search: `git grep -n -E "PluginHost|PluginContext|runtime-wasmtime|plugin-discovery|rge_kernel_plugin_host|rge-runtime|load_plugin|discover_plugin|plugin_manifest|capability manifest|WASM|wasm" -- crates/editor-shell editor/rge-editor`
  -> no matches; command exits 1.

- Claim: the host-to-shell menu FIFO remains the current command handoff
  boundary, so host-shell FIFO/menu-click replacement and generalized registry
  execution remain a live candidate class, not something task 102 already
  solved.
  Falsifying search: `git grep -n -E "MenuCommandHandoff|menu_command_handoff|drain_and_route_menu_commands|register_menu_entry|register_plugin_menu_entry|toggle_command_palette" -- crates/editor-egui-host/src crates/editor-shell/src`
  -> matches `EguiHost::menu_command_handoff`, menu entry registration methods,
  command-palette activation pushing into the handoff, and
  `EditorShell::drain_and_route_menu_commands`.

- Claim: generalized registry execution is not wired as a separate executor
  beyond the existing menu registry resolving commands into the shell router.
  Falsifying search: `git grep -n -E "dispatch.*(menu|command).*registry|registry.*dispatch|execute.*registry|registry.*execute|run.*registry|CommandRegistry|ActionExecutor|ActionRegistry" -- crates/editor-egui-host/src crates/editor-shell/src crates/editor-actions/src crates/editor-ui/src/menus`
  -> no matches; command exits 1. Positive registry/menu-router searches still
  show `ResolveResult::enabled_command_for_shortcut` and
  `EditorShell::route_menu_command`, so the executor must classify this as a
  registry-execution gap, not an absent menu registry.

- Claim: the current source has shortcut substrate and conflict diagnostics,
  but no user-facing keybinding editor/custom-keymap surface and no fatal
  shortcut-conflict gate.
  Falsifying search A: `git grep -n -E "Keymap|keymap|KeyBindingEditor|keybinding editor|binding editor|shortcut editor|ShortcutEditor|custom shortcut|custom shortcuts|user shortcut|user shortcuts" -- crates/editor-egui-host/src crates/editor-shell/src crates/editor-ui/src crates/editor-actions/src editor/rge-editor`
  -> one non-editor comment in `crates/editor-shell/src/play_toolbar.rs` saying
  real keymap binds happen in `editor-ui/menus`; no keybinding editor/custom
  keymap surface is found.
  Falsifying search B: `git grep -n -E "conflict.*fatal|fatal.*conflict|halt.*conflict|reject.*conflict|Conflict.*Error|ShortcutConflict.*Error|ShortcutConflict.*Fatal|decides whether a conflict is fatal|Shortcut Conflicts" -- crates/editor-egui-host/src crates/editor-shell/src crates/editor-ui/src crates/editor-actions/src`
  -> matches host rendering of a `Shortcut Conflicts` diagnostic and
  `AcceleratorTable` docs saying the host decides whether a conflict is fatal;
  no fatal/rejecting conflict policy is located.

- Claim: the command palette currently has host-local in-memory recent-command
  promotion, but no persistent history/favorites surface.
  Falsifying search A: `git grep -n -E "command_palette_recent_command_ids|record_command_palette_recent_command" -- crates/editor-egui-host/src crates/editor-shell/src crates/editor-ui/src crates/editor-actions/src editor/rge-editor/src`
  -> matches `EguiHost::command_palette_recent_command_ids` and
  `record_command_palette_recent_command`.
  Falsifying search B: `git grep -n -E "favorite|favorites|Favourite|favourite|persist.*command_palette|command_palette.*persist|recent_command_ids.*(load|save|serde|ron|json|config|file)|COMMAND_PALETTE_RECENT.*(load|save|serde|ron|json|config|file)" -- crates/editor-egui-host/src crates/editor-shell/src crates/editor-ui/src crates/editor-actions/src editor/rge-editor/src`
  -> no matches; command exits 1.

- Claim: dirty state is surfaced, but Close/Quit do not prompt for unsaved
  changes before clearing the document or exiting.
  Falsifying search: `git grep -n -E "unsaved.*prompt|prompt.*unsaved|confirm.*(close|quit|exit)|dirty.*(close|quit|exit)|close.*dirty|quit.*dirty|exit.*dirty|request_exit|exit_requested|quit_requested|application_exit_requested|WindowEvent::CloseRequested" -- crates/editor-egui-host/src crates/editor-shell/src crates/editor-ui/src editor/rge-editor/src/main.rs`
  -> matches dirty/save-status surfaces, `quit_requested`, comments in
  `handle_close_file_request` / `handle_quit_request` saying they do not prompt
  for unsaved changes, and `WindowEvent::CloseRequested` exiting the event loop;
  no confirm/prompt implementation is located.

- Claim: the current clipboard behavior is shell-local legacy-blob entity
  copy/paste/cut, not OS clipboard or typed clipboard integration.
  Falsifying search: `git grep -n -E "arboard|copypasta|clipboard-win|TypedClipboard|TextClipboard|system clipboard|OS clipboard|operating system clipboard" -- crates/editor-egui-host/src crates/editor-shell/src crates/editor-ui/src crates/editor-actions/src editor/rge-editor/src`
  -> matches only comments in `crates/editor-shell/src/lifecycle/mod.rs` and
  tests stating the path is not OS clipboard / typed clipboard / authoritative
  CAD clone; no source use of an OS clipboard API is located.

- Claim: Delete/Duplicate/Cut/Copy/Paste are bounded wrapper-world operations
  and are not authoritative CAD graph/projection mutations or command-bus
  dirty/undo actions.
  Falsifying search: `git grep -n -E "submit_action.*(delete|duplicate|cut|paste)|Delete.*submit_action|Duplicate.*submit_action|Cut.*submit_action|Paste.*submit_action|CommandBus.*(delete|duplicate|cut|paste)|delete_selected_entities|duplicate_selected_entities|copy_selected_entities|paste_copied_entities|cut_selected_entities|does not mutate CAD|CAD graph/projection|dirty/undo" -- crates/editor-shell/src crates/editor-actions/src`
  -> matches the shell-local entity operation methods, route-menu arms, and
  comments saying they do not mutate CAD graph/projection data or dirty/undo
  state; no submit-action/command-bus CAD delete/duplicate integration is
  located.

- Claim: the current camera UI is limited to View menu reset/frame-scene plus
  zoom-in/zoom-out commands; broader orbit/pan/view-cube/camera-control UI is
  not wired.
  Falsifying search: `git grep -n -E "pub fn reset_camera|pub fn zoom_camera_in|pub fn zoom_camera_out|No orbit / pan / zoom controls|Command::ResetCamera|Command::ZoomIn|Command::ZoomOut|CameraControls|camera panel|view cube|ViewCube|arcball|rotate_camera|pan_camera" -- crates/editor-shell/src crates/editor-egui-host/src crates/editor-ui/src/menus`
  -> matches `reset_camera`, `zoom_camera_in`, `zoom_camera_out`, their menu
  commands/tests, and `EditorCameraState` docs saying no orbit/pan controls;
  no camera panel/view-cube/arcball/rotate/pan control surface is located.

- Claim: existing docs/status already frame task 103 as a source-read selection
  audit, not an implementation dispatch.
  Falsifying search: `git grep -n -E "task 103|post-extension-seam next-task selection|append exactly one bounded implementation follow-up as task 104" -- .ai/dispatch.tasks.md Status.md HANDOFF.md change.md`
  -> matches task/status entries that require comparison of candidate classes
  and task 104 selection or `NEEDS_HUMAN`.

## Constraints / Non-Goals

- Do not implement task 104 during this dispatch.
- Do not edit Rust source/tests, Cargo manifests/lockfiles, workflows,
  dispatch/scheduler scripts, schemas, architecture-lint rule/config,
  plugin runtime/discovery/loading code, or existing artifacts from another
  dispatch.
- Do not introduce a real plugin host, plugin discovery, plugin loading, WASM
  runtime, capability manifest, async executor, sandbox integration, or
  generalized registry execution as part of task 103.
- Do not run cargo build/test/check/fmt, architecture lints, `.ai/dispatch.verify.ps1`,
  or recorder/benchmark/performance commands manually. This task is a
  docs/source-read audit plus task selection only.
- Do not infer absence from file listings. Use `git grep`, file reads, and
  explicit falsifying searches for negative claims.
- Do not choose the strong suggested prior automatically. It is a prior only;
  the source evidence may select another candidate or require `NEEDS_HUMAN`.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- Re-run and record the `git grep` searches listed under
  `Current-State Claims: Falsification Required`.
- Run additional `git grep` / file-read searches sufficient to classify all
  eight candidate classes and support the selected task 104 or `NEEDS_HUMAN`.
- `git diff --check` -> expected exit 0.
- `git status --short --untracked-files=all` -> record final output, including
  this dispatch's own generated artifacts and any pre-existing unrelated
  untracked artifacts.

No Rust build/test/format/cargo/architecture-lint command is expected from the
executor for this audit. The orchestrator may run its canonical verification
gate after execution; if that gate or a correction request asks for work outside
this packet's edit scope, the executor must halt under the scope-preserving halt
condition below.

## Halt Conditions

The Executor MUST halt without implementing task 104 and request fresh guidance
if any of the following occur:

- Confirming task 102's closure boundary requires editing source, tests, Cargo,
  workflows, scripts, schemas, architecture lints, plugin runtime/discovery/
  loading code, or existing artifacts.
- The current source contradicts the task 102 boundary strongly enough that the
  post-seam candidate list cannot be trusted.
- The audit cannot choose exactly one bounded task 104 from current evidence.
- Two or more candidates appear equally safe and the tie cannot be resolved by
  current source/docs evidence alone.
- The selected task 104 would need implementation edits during task 103.
- The selected task 104 would require touching forbidden surfaces such as Rust
  source/tests during task 103, Cargo metadata, workflows, dispatch/scheduler
  scripts, schemas, architecture lints, plugin runtime/discovery/loading code,
  or existing artifacts.
- A required negative claim cannot be falsified cheaply with a source/doc search
  or file read; record it as an open question instead of asserting it.
- Any tracked file outside `.ai/dispatch.tasks.md`, `plans/BASELINE.md`,
  `Status.md`, `HANDOFF.md`, `change.md`, and ISSUE-351 generated artifacts is
  already dirty in a way that makes the audit ambiguous.
- `git diff --check` fails because of changes outside this dispatch's allowed
  edit surface.
- A post-execution orchestrator verify/correction request asks the executor to
  fix anything outside this packet's allowed edit surface. In that case write an
  EXEC report with `STATUS: NEEDS_HUMAN`, `EXEC_STATUS: blocked`, and do not
  perform the out-of-scope correction.

## Planner Notes

This is the GitHub issue #351 plan packet for task 103 in
`.ai/dispatch.tasks.md`. Per the task 102/task 103 source-doc searches above,
task 102 (ISSUE-349) deliberately stopped at an editor-shell injected handler
seam for captured extension commands. The next dispatch should be chosen from
current source evidence after that seam, not from stale Phase 9 history or the
file listing alone.

The strongest prior is host-shell FIFO/menu-click replacement and generalized
registry execution because `editor-egui-host` still projects commands through a
host-to-shell FIFO, `editor-shell` drains them through
`EditorShell::drain_and_route_menu_commands`, and the generalized-registry
executor search above found no separate executor. Treat that only as a prior:
the executor must compare all eight required candidate classes before selecting
task 104.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-351
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Task Packet

DISPATCH_ID: ISSUE-415
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-16_11-44-43+0300
RELATED_FILES:
- crates/editor-shell/src/lifecycle/commands.rs
- crates/editor-shell/src/lifecycle/tests.rs
- .ai/dispatch.tasks.md
- ai_handoffs/ISSUE-414_TASK_2026-06-16_10-33-14+0300.md
- ai_handoffs/ISSUE-414_EXEC_2026-06-16_11-01-09+0300.md
- AI_DISPATCH_AUTOMATION.md
- ai_handoffs/AI_HANDOFF_PROTOCOL.md
STATUS: OPEN

## Goal

Perform a source/docs read-only audit of the post-stale-id first-cuboid
preflight-normalization boundary after task 158 / ISSUE-414. Inspect the local
source and task provenance around `add_cad_cuboid_to_empty_scene`,
`clear_stale_tracked_cad_entity`, `CadSceneInspection`, tracked-CAD liveness,
render-mesh inspection, and first-cuboid rejection behavior. The audit must stop
at a human decision point: edit `.ai/dispatch.tasks.md` only to record one
`NEEDS_HUMAN_RECORDED` marker and an immediately following recommendation block.
Do not append task 160 or any feature task.

## Scope

### MAY edit
- `.ai/dispatch.tasks.md` only for the final `NEEDS_HUMAN_RECORDED` marker line
  and immediately following `Recommendation for human approval` block.
- `ai_handoffs/ISSUE-415_EXEC_*.md` as the current-dispatch execution report.
- `ai_handoffs/ISSUE-415_EXEC_*.meta.json` if packet finalization creates or
  refreshes the current-dispatch EXEC sidecar.
- `.ai/dispatch-ISSUE-415/**` for current-dispatch scratch, verification, and
  control artifacts generated by the orchestrator.
- `ai_dispatch_logs/log_2026-06-16_*.md` only if generated as the queue audit log
  for this ISSUE-415 dispatch.
- `ai_handoffs/claims/ISSUE-415_*.json` only for current-dispatch claim/release
  artifacts generated by the queue.

### MUST NOT edit
- `crates/**`
- `editor/**`
- `runtime/**`
- `kernel/**`
- `tools/**`
- `docs/**`
- `Status.md`
- `HANDOFF.md`
- `plans/BASELINE.md`
- `change.md`
- `Cargo.toml`
- `Cargo.lock`
- `crates/**/Cargo.toml`
- `.github/**`
- `Invoke-*.ps1`
- `Register-AiDispatchSchedule.ps1`
- `Watch-*.ps1`
- `Wait-GitHubActions.ps1`
- `Get-AiDispatchHealth.ps1`
- `Get-AiDispatchTrends.ps1`
- `.ai/dispatch.verify.ps1`
- `.ai/*.schema.json`
- `AI_DISPATCH_AUTOMATION.md`
- `AI_DISPATCH_PARALLEL.md`
- `ai_handoffs/templates/**`
- `ai_handoffs/AI_HANDOFF_PROTOCOL.md`
- `ai_handoffs/ISSUE-414_*`
- `ai_handoffs/ISSUE-415_TASK_2026-06-16_11-44-43+0300.md` after Planner
  finalization.
- Any unrelated existing handoff, claim, audit-log, run-dir, or dispatch
  artifact.
- Any runtime behavior: render architecture, render-path behavior, mesh upload,
  GPU resource lifetime, CAD-core behavior, cad-projection behavior,
  CommandBus/action routing, undo/redo, dirty/save-mark behavior,
  save/load/open/close, camera/navigation, camera math, viewport hit testing,
  face-pick policy, UI/host behavior, plugin routing, shortcut routing, or
  command-palette behavior.

### MAY add new files
- `ai_handoffs/ISSUE-415_EXEC_*.md`
- `ai_handoffs/ISSUE-415_EXEC_*.meta.json`
- `.ai/dispatch-ISSUE-415/**`
- `ai_dispatch_logs/log_2026-06-16_*.md`
- `ai_handoffs/claims/ISSUE-415_*.json`

### MUST NOT add new files
- No source files, test files, docs, ADRs, schemas, scripts, workflows, crates,
  Cargo metadata, architecture-lint rules/config, packet templates, task 160,
  feature/implementation task, fixtures, or golden files.
- No handoff, claim, run-dir, or audit-log artifact for any dispatch other than
  ISSUE-415.

### Advisory machine-readable scope envelope

No `handoff:envelope` block is included; this absence is backed by the
structural search recorded under `Current-State Claims: Falsification Required`.
The positive scope combines one existing tracked task-brief file with generated
current-dispatch artifacts and date-scoped queue logs, so prose scope is
authoritative and advisory validation may remain unchecked rather than
over-broad.

## Deliverables

- An EXEC audit report that records the source/docs reads performed before any
  recommendation is written, including the post-stale-id first-cuboid boundary
  in `crates/editor-shell/src/lifecycle/commands.rs` and the focused lifecycle
  tests in `crates/editor-shell/src/lifecycle/tests.rs`.
- A source-grounded conclusion about the next human decision point after
  task 158, with negative or unchanged claims backed by explicit `rg` or read
  commands and observed results.
- `.ai/dispatch.tasks.md` updated only by appending exactly one marker line in
  this exact shape, using the actual ISO date:
  `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - source-grounded reason`
- The marker must be immediately followed by a `Recommendation for human
  approval` block covering: proposed next feature, exact edit surface, risks,
  verification, and why that proposal is the smallest coherent next step.
- The EXEC packet must carry this same gated-audit instruction: do not append
  task 160, do not append a feature task, and leave the autonomous driver paused
  for operator decision.

## Acceptance Criteria

- Source/docs are read-only: no Rust source, tests, docs/status files, Cargo
  metadata, workflows, scripts, schemas, packet templates, or runtime behavior
  are modified.
- Queue-state and already-filed-autonomous-task claims cite only the
  dispatcher-provided GitHub-state snapshot in this TASK packet or an exact
  local issue-body artifact. The executor must not call `gh`, use a browser,
  use network access, call the GitHub API, or perform any live remote lookup.
- The audit covers `EditorShell::add_cad_cuboid_to_empty_scene`,
  `EditorShell::clear_stale_tracked_cad_entity`, `CadSceneInspection`,
  tracked-CAD liveness, tracked-CAD render-mesh inspection, current first-cuboid
  rejection behavior, and adjacent task provenance from task 158 / ISSUE-414.
- The pre-edit backlog check proves task 159 exists, no task 160 heading exists,
  and no unresolved anchored `NEEDS_HUMAN_RECORDED:` marker line exists before
  the final marker edit.
- The final `.ai/dispatch.tasks.md` diff contains exactly one new anchored
  `NEEDS_HUMAN_RECORDED:` marker line and the immediately following
  recommendation block. It does not append task 160 or any feature task.
- `git diff --name-only` shows only `.ai/dispatch.tasks.md` as a tracked edit,
  aside from generated current-dispatch artifacts.
- The current EXEC packet passes `new-handoff.ps1 -Finalize -DryRun`.

## Dispatcher GitHub Snapshot Evidence

Use the dispatcher-provided snapshot below for all queue/already-filed GitHub
state claims. Do not replace it with live GitHub evidence.

- Snapshot generated by `Invoke-AiDispatchAuto.ps1` at
  `2026-06-16T11:44:18.3059776+03:00`, before GitHub issue #415 was created.
- Repo: `RustCADs/RGE`.
- Open `ai-dispatch` issues before #415 was created: none.
- Open failed autonomous issues labelled `ai-auto` plus
  `ai-dispatch-failed`: none.
- Relevant already-filed autonomous issues from the snapshot include:
  #403 `Audit CAD/CommandBus first mutation boundary` closed; #405 `Add one
  CAD cuboid primitive to an empty CAD scene` closed; #406 `Audit first CAD
  cuboid add aftermath` closed; #407 `Add headless CAD scene inspection
  snapshot` closed; #408 `Audit post-inspection CAD/editor-shell feature
  boundary` closed; #409 `Add tracked-CAD liveness to CadSceneInspection`
  closed; #410 `Audit post-tracked-CAD-liveness feature boundary` closed; #411
  `Clear stale tracked-CAD entity ids` closed; #412 `Record post-CAD-cleanup
  human decision point` closed; and #414 `Wire stale CAD cleanup into
  cuboid-add preflight` closed.
- The current issue #415 was filed after this snapshot from the task 159
  backlog entry. Do not infer any newer GitHub state unless it is present in an
  exact local issue-body artifact.

## Current-State Claims: Falsification Required

Every negative or unchanged assertion below includes the inverse read/search
used during planning. The Executor must re-run equivalent local reads before
relying on these claims in the EXEC report.

- Claim: `clear_stale_tracked_cad_entity` has exactly one production call site
  in `commands.rs`, from `add_cad_cuboid_to_empty_scene`, plus the helper
  definition.
  Falsifying search:
  `rg -n "clear_stale_tracked_cad_entity\\(" crates/editor-shell/src/lifecycle/commands.rs`
  -> matches at lines 275 (helper definition) and 379 (call from
  `add_cad_cuboid_to_empty_scene`).
- Claim: task 158's first-cuboid add path normalizes stale tracked ids before
  the existing CAD-state and render-content rejection guards.
  Falsifying search:
  `rg -n "self\\.clear_stale_tracked_cad_entity\\(\\)|self\\.cad_world\\.is_some\\(\\)|self\\.projection\\.is_some\\(\\)|self\\.cad_graph\\.is_some\\(\\)|self\\.cad_entity\\.is_some\\(\\)|prebuilt_render_meshes|meshes\\.is_empty\\(" crates/editor-shell/src/lifecycle/commands.rs`
  -> matches include line 379 cleanup call, lines 380-383 CAD
  world/projection/graph/entity guard, and line 389 prebuilt/uploaded render
  content guard.
- Claim: `CadSceneInspection` separates tracked-CAD liveness from
  render-mesh availability and reports render-only/uploaded mesh counts.
  Falsifying search:
  `rg -n "CadSceneInspection|tracked_cad_entity_live|tracked_cad_entity_render_mesh_present|projection_render_mesh_count|prebuilt_render_mesh_count|uploaded_mesh_count" crates/editor-shell/src/lifecycle/commands.rs`
  -> expected to show the struct fields and the inspection construction in
  `commands.rs`.
- Claim: before the final marker edit, the task brief has no task 160 heading
  and no unresolved anchored `NEEDS_HUMAN_RECORDED:` marker line.
  Falsifying search:
  `rg -n "^160\\.|^NEEDS_HUMAN_RECORDED:" .ai/dispatch.tasks.md`
  -> no matches, exit 1.
- Claim: current task provenance already contains task 159 as the gated audit
  selected for ISSUE-415.
  Falsifying search:
  `rg -n "^158\\.|^159\\.|^160\\.|^NEEDS_HUMAN_RECORDED:" .ai/dispatch.tasks.md`
  -> expected to show task 158 and task 159, no task 160, and no anchored
  unresolved marker before the final edit.
- Claim: this TASK packet does not include an ADR-121
  `handoff:envelope` advisory machine-readable scope block.
  Falsifying search:
  ``rg -n '^(?:`{3}handoff:envelope|handoff:envelope:)' ai_handoffs/ISSUE-415_TASK_2026-06-16_11-44-43+0300.md``
  -> no matches, exit 1.

## Constraints / Non-Goals

- This is not a feature dispatch. Do not implement the recommendation.
- Do not append task 160 and do not append any feature or implementation task.
- Do not modify source, tests, docs/status files, Cargo metadata, workflows,
  dispatch automation, scripts, schemas, architecture-lint rules/config, packet
  templates, or runtime behavior.
- Do not use live GitHub state. Do not call `gh`, browser tools, network tools,
  GitHub APIs, or any remote lookup for queue/already-filed evidence.
- Do not expand the audit beyond the smallest boundary needed to explain the
  post-stale-id first-cuboid preflight-normalization decision.
- Do not convert render-mesh absence, projection cleanup, graph restore,
  CommandBus authority, dirty/save, UI/host routing, or camera/viewport concerns
  into implementation work.

## Verification Gates

The Executor MUST run and document the result of each gate in its
`EXECUTION_REPORT`:

- `rg -n "^158\\.|^159\\.|^160\\.|^NEEDS_HUMAN_RECORDED:" .ai/dispatch.tasks.md`
  before edits and after edits. Before edits: task 158 and task 159 are present,
  task 160 and anchored marker are absent. After edits: exactly one anchored
  marker line has been added, the recommendation block immediately follows, and
  task 160 remains absent.
- `rg -n "add_cad_cuboid_to_empty_scene|clear_stale_tracked_cad_entity|cad_entity_has_live_brep_handle|tracked_cad_entity_live|tracked_cad_entity_render_mesh_present|CadSceneInspection|cad_scene_inspection|SceneNotEmpty" crates/editor-shell/src/lifecycle`
  expected to show the audited boundary in lifecycle source/tests.
- `rg -n "render_mesh_for|projection_render_mesh_count|prebuilt_render_mesh_count|uploaded_mesh_count" crates/editor-shell/src/lifecycle/commands.rs crates/editor-shell/src/lifecycle/tests.rs`
  expected to show render-mesh inspection evidence only, not an edit.
- `rg -n "2026-06-16T11:44:18\\.3059776\\+03:00|Open .* before #415 was created|Wire stale CAD cleanup into cuboid-add preflight" ai_handoffs/ISSUE-415_TASK_2026-06-16_11-44-43+0300.md`
  expected to confirm the dispatcher snapshot evidence is local.
- `git diff --name-only` expected tracked output limited to
  `.ai/dispatch.tasks.md`.
- `git status --porcelain=v1 --untracked-files=all` expected no untracked files
  except current-dispatch ISSUE-415 generated artifacts already allowed by this
  packet.
- `git diff --check` expected exit 0.
- `Get-ChildItem ai_handoffs\ISSUE-415_EXEC_*.md | Sort-Object LastWriteTime -Descending | Select-Object -First 1 | ForEach-Object { .\new-handoff.ps1 -Finalize -PacketPath $_.FullName -DryRun }`
  expected exit 0 for the current ISSUE-415 EXEC packet.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of
the following occur:

- The dispatcher-provided GitHub snapshot cannot be cited from this TASK packet
  or an exact local issue-body artifact without live `gh`, browser, network, or
  GitHub API access.
- The audit would require editing any source, test, docs/status, Cargo,
  workflow, script, schema, packet-template, automation, runtime-behavior, or
  unrelated artifact path.
- Current source/provenance contradicts the premise enough that one
  source-grounded human recommendation cannot be made without selecting between
  multiple coherent feature boundaries.
- A recommendation would require implementing code, adding tests, editing docs,
  changing task provenance beyond the required marker/block, or appending
  task 160 / a feature task.
- `.ai/dispatch.tasks.md` already contains an anchored unresolved
  `NEEDS_HUMAN_RECORDED:` marker or a task 160 heading before the final marker
  edit.
- Appending exactly one marker plus the required recommendation block would
  disturb existing task provenance or require rewriting prior task text.
- Verification finds any tracked diff outside `.ai/dispatch.tasks.md`.

## Planner Notes

This dispatch intentionally pauses the autonomous chain after task 158. The
source already shows the stale-id cleanup call sits immediately before the
first-cuboid non-empty guards, and task 159 already exists as a gated audit. The
executor's job is to audit that boundary, record the human decision marker, and
recommend the smallest coherent next feature for a human to approve later.

The recommendation may name a next feature and exact edit surface, but it must
not create that task or implement it. This keeps the autonomous selector from
turning a human decision point into another automatic feature dispatch.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-415
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Correction Packet

DISPATCH_ID: ISSUE-53
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_19-30-05+0300
RELATED_FILES:
- crates/cad-core/src/graph/operator_graph.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-53_TASK_2026-05-19_19-19-21+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-53_EXEC_2026-05-19_19-29-01+0300.md`
- Verification Gate Failure: post-execution `.ai/dispatch.verify.ps1` run started 2026-05-19T19:29:58.1191791+03:00 failed at `cargo run -q -p rge-tool-architecture-lints -- all` with `[split-exemption] FAIL`: `crates\cad-core\src\graph\operator_graph.rs:1001: file is 1107 lines (>1000) without // SPLIT-EXEMPTION: annotation`.
- Prior Correction Packets: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections and nothing more.

1. **Add split-exemption annotation** - Verification gate failure in the architecture lint `[split-exemption]`. Required change: add a concise `// SPLIT-EXEMPTION:` annotation to `crates/cad-core/src/graph/operator_graph.rs`, near the top-level module documentation or immediately before the first code item, explaining why this file is temporarily allowed to exceed 1000 lines after the in-scope `VizAdapter` implementation and tests. Do not split the file, move code, refactor existing code, change Rust semantics, alter tests, modify architecture-lint tooling, or edit any other file. Acceptance: `cargo run -q -p rge-tool-architecture-lints -- all` exits 0 for `[split-exemption]`, and the only source change made in this correction round is the exemption comment in `crates/cad-core/src/graph/operator_graph.rs`.

## Deferred Findings (NOT Approved for This Round)

None. The post-execution verification failure supplied for this correction round contains only the split-exemption architecture-lint failure above. Existing warning-level `package.metadata.rge.formats` messages for `io-*` crates and warning-level snapshot participation output are not approved for action in this dispatch.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with this additional correction-round requirement: the canonical post-execution verification gate must no longer fail the architecture-lint `[split-exemption]` check for `crates/cad-core/src/graph/operator_graph.rs`.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo run -q -p rge-tool-architecture-lints -- all` -> expected exit 0.
- `.\.ai\dispatch.verify.ps1` -> expected exit 0.

If `cargo` is not on `PATH`, the Executor may use the repository's established Rust toolchain location `A:\RustCache\cargo\bin\cargo.exe`, matching `.ai/dispatch.verify.ps1`.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, plus these correction-specific halt conditions:

- Halt if fixing the split-exemption failure requires edits outside `crates/cad-core/src/graph/operator_graph.rs`.
- Halt if the correction would require splitting the file, moving code into new modules, changing public API, changing adapter behavior, changing tests, modifying architecture-lint policy/tooling, editing manifests or lockfiles, or adding new source files.
- Halt if `cargo run -q -p rge-tool-architecture-lints -- all` still fails after adding the exemption annotation for any reason other than warning-level messages already shown in the failed verification log.
- Halt if `.\.ai\dispatch.verify.ps1` fails after the correction for any reason not directly caused by this correction.

## Planner Notes

This correction is limited to unblocking the canonical verification gate. The original ISSUE-53 implementation scope already permitted editing only `crates/cad-core/src/graph/operator_graph.rs`; this packet authorizes only the minimal `// SPLIT-EXEMPTION:` annotation needed by the architecture lint after the Executor's in-scope adapter and tests pushed the file above 1000 lines. Do not broaden the dispatch into architecture policy cleanup, file splitting, unrelated warnings, manifest metadata, docs, or workspace cleanup.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-53
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

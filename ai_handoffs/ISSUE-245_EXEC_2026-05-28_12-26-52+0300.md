# Execution Report

DISPATCH_ID: ISSUE-245
AUTHOR: Executor / Claude
TIMESTAMP: 2026-05-28_12-26-52+0300
RELATED_FILES:
- .github/workflows/bench.yml — appended new `script-bench-cold-start-observational` job alongside the byte-identical existing `script-bench-compile` job
- Status.md — prepended 2026-05-28 ISSUE-245 snapshot above the existing 2026-05-28 ISSUE-243 snapshot
- HANDOFF.md — prepended parallel 2026-05-28 ISSUE-245 snapshot above the existing 2026-05-28 ISSUE-243 snapshot
- change.md — appended 2026-05-28 ISSUE-245 entry between the existing ISSUE-243 entry and the 2026-05-23 15:20 REPO TRANSFER entry
- crates/script-bench/src/wasmtime_cranelift.rs — referenced unchanged (byte-identical to commit 97fda49, verified by empty `git diff 97fda49 -- …`)
- ai_handoffs/ISSUE-245_TASK_2026-05-28_12-14-18+0300.md — TASK packet executed by this report
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-245_TASK_2026-05-28_12-14-18+0300.md`

## Pre-Execution Review Consumed

No pre-execution review issued for this dispatch (orchestrator dispatch
semantics per `AI_DISPATCH_AUTOMATION.md`: TASK + EXEC packets only, with
the Codex control review flowing through the gitignored
`.ai/dispatch-<DispatchId>/codex.control.round0.json` run-dir JSON rather
than a `_REVIEW_` packet).

## What I Changed

### Source

- None. The TASK packet's "MUST NOT edit" envelope forbids any Rust
  source / test / bench / fixture / Cargo metadata edit. The W04 focused
  release test
  `crates/script-bench/src/wasmtime_cranelift.rs::tests::w04_cold_start_wasmtime_cranelift`
  is referenced from CI unchanged and is byte-identical to the file at
  commit `97fda49` (verified — `git diff 97fda49 -- crates/script-bench/src/wasmtime_cranelift.rs`
  emitted no output).

### Tests

- None added. The existing focused test is wired from the new CI job and
  is unchanged. No new threshold environment variable, no comparison-
  against-baseline logic, no helper script, no new bench harness.

### Docs

- `.github/workflows/bench.yml`: appended a new
  `script-bench-cold-start-observational` job after the existing
  `script-bench-compile` job. The existing job's stanza is preserved
  byte-identical to `git show 97fda49:.github/workflows/bench.yml`.
  The new job runs on `ubuntu-latest`, uses the existing workflow's
  `push` / `pull_request` triggers on `main`, installs the pinned
  toolchain via the same `dtolnay/rust-toolchain@stable` +
  `Swatinem/rust-cache@v2` pattern (with
  `shared-key: script-bench-cold-start-observational` so it does not
  collide with the existing job's cache), and in a single `bash` step
  runs
  `cargo test -p rge-script-bench --release --lib wasmtime_cranelift::tests::w04_cold_start_wasmtime_cranelift -- --nocapture`
  three times in succession, captures each run's `duration_ns` from
  stdout via `grep -oE 'duration_ns=[0-9]+'`, and computes the min-of-3
  via pure bash arithmetic
  (`min=$(( d1 < d2 ? d1 : d2 )); min=$(( min < d3 ? min : d3 ))`). The
  step then emits a Markdown table to `$GITHUB_STEP_SUMMARY` containing
  the three per-run `duration_ns` values, the min-of-3 `duration_ns`,
  `cargo --version`, `rustc --version`, the `wasmtime` version read
  from `Cargo.lock` (`awk '/^name = "wasmtime"$/{getline; gsub(/"/, "", $3); print $3; exit}' Cargo.lock`),
  and three verbatim caveat lines: "Observational only. No new timing
  threshold; inherits the harness's `< 2s` ceiling assertion.";
  "Runner is GitHub-hosted ubuntu-latest, NOT the DENTAL recorder
  host."; and "Numbers are NOT comparable to the
  `crates/script-bench/BASELINE.md` recorder-host row." The step uses
  `set -euo pipefail` so a missing `duration_ns` capture fails the job
  rather than emitting a "0" sample; default cargo-test failure
  semantics handle compile errors and test panics, and the harness's
  existing `assert!(duration < Duration::from_secs(2))` ceiling is the
  only timing-related failure path.
- `Status.md`: prepended a new 2026-05-28 ISSUE-245 snapshot above the
  existing 2026-05-28 ISSUE-243 snapshot. The snapshot states the
  headline fact, scope envelope, hard non-goals, and three required
  references; older dated snapshots and the dated "Validation gates
  met" table are retained verbatim.
- `HANDOFF.md`: prepended a parallel new 2026-05-28 ISSUE-245 snapshot
  above the existing 2026-05-28 ISSUE-243 snapshot. Older dated
  snapshots and the dated "Persistent gaps" section are retained
  verbatim.
- `change.md`: appended a 2026-05-28 ISSUE-245 entry between the
  existing 2026-05-28 ISSUE-243 entry and the 2026-05-23 15:20 REPO
  TRANSFER entry (the same insertion location used by the prior
  ISSUE-243 dispatch). The entry records the headline fact, hard
  non-goals, the §Verification-Gates outcomes (including the local
  single-sample stdout), acceptance-criteria status, files modified,
  and the three required references.

## Per-File Summary

- `.github/workflows/bench.yml`: pure post-line-34 append of the new
  job; `git diff HEAD -- .github/workflows/bench.yml` shows only added
  lines (no deletion, no modification of the existing
  `script-bench-compile` stanza). The new job's YAML `name:` is
  `script-bench-cold-start-observational`, satisfying the no-gate-
  legibility criterion (substring `observational` present in the
  check name).
- `Status.md`, `HANDOFF.md`, `change.md`: forward-only updates only;
  no historical snapshots / dated rows rewritten.

## Verification Results

- TASK packet inspection — read end-to-end before scaffolding any
  edit; scope envelope (MAY-edit / MUST NOT edit / MAY add / MUST NOT
  add), acceptance criteria (no-gate legibility, emission-surface lock,
  caveat strings, byte-identical `script-bench-compile`, no Rust /
  Cargo / `.github/`-outside-`bench.yml` edits), and halt conditions
  internalized.
- Harness inspection — `git diff 97fda49 -- crates/script-bench/src/wasmtime_cranelift.rs`
  → empty (no output); harness is byte-identical to the post-#244
  `main` HEAD at commit `97fda49`. PASS.
- YAML inspection — `git diff HEAD -- .github/workflows/bench.yml`
  shows pure addition after line 34; the existing
  `script-bench-compile` job stanza (lines 1-34 of the new file) is
  byte-identical to the pre-edit version. Python parse via
  `python -c "import yaml; d = yaml.safe_load(open('.github/workflows/bench.yml', encoding='utf-8'))"`
  emits no exception; `d['jobs']` keys are exactly
  `['script-bench-compile', 'script-bench-cold-start-observational']`;
  the `runs-on` of the new job is `ubuntu-latest`; the
  workflow-level `on` triggers (PyYAML 1.1 quirk — `on` is read as the
  Python boolean `True`) resolve to
  `{'push': {'branches': ['main']}, 'pull_request': {'branches': ['main']}}`,
  matching the existing triggers. The new job has 4 steps:
  `actions/checkout@v4`, `dtolnay/rust-toolchain@stable`,
  `Swatinem/rust-cache@v2`, and the bash run step. PASS.
- Local test run — `cargo test -p rge-script-bench --release --lib wasmtime_cranelift::tests::w04_cold_start_wasmtime_cranelift -- --nocapture`
  emitted `w04_cold_start_wasmtime_cranelift: duration_ns=276700 duration_ms=0.277`;
  `test wasmtime_cranelift::tests::w04_cold_start_wasmtime_cranelift ... ok`;
  final tally `1 passed; 0 failed; 0 ignored; 0 measured; 28 filtered out`;
  exit code 0. PASS. Recorded `duration_ns = 276 700` /
  `duration_ms = 0.277` here per the TASK packet's "Local test run"
  gate; explicitly NOT appended to `crates/script-bench/BASELINE.md`.
- Workflow syntax check (`actionlint`) — `actionlint` is NOT installed
  on this workstation (`Get-Command actionlint -ErrorAction SilentlyContinue`
  returned no result). Per the TASK packet's verification-gates
  guidance, the syntax check is **deferred to the first CI run on this
  dispatch's PR**. No halt was triggered because the YAML parses
  cleanly via the standalone Python parse above.
- `git diff --check` → exit 0, no whitespace errors. (Git emitted an
  informational "LF will be replaced by CRLF the next time Git touches
  it" warning for `.github/workflows/bench.yml`, which reflects the
  Windows checkout's autocrlf behaviour — not a tree corruption.)
- `git status --short --branch` → on branch `ai-dispatch/ISSUE-245`
  with four tracked-modified paths (`.github/workflows/bench.yml`,
  `HANDOFF.md`, `Status.md`, `change.md`) plus this dispatch's own
  untracked `ai_handoffs/ISSUE-245_TASK_2026-05-28_12-14-18+0300.md`
  and its `.meta.json` sidecar (the TASK packet itself is finalized
  upstream, not edited by this dispatch). After this EXEC report's
  `-Finalize` run a third untracked pair will appear, namely this EXEC
  packet and its `.meta.json` sidecar. All four tracked-modified paths
  are inside the TASK packet's MAY-edit envelope; no path outside it
  was modified.
- Substrate gates (workspace fmt / build / test / arch-lints) — NOT
  RUN. The TASK packet's verification-gates clause "Do not run broad
  workspace tests or the canonical dispatch gate unless a specific
  reason is documented. This is a workflow-config plus docs dispatch;
  the substrate is unchanged." authorizes the skip; no Rust source /
  test / bench / Cargo metadata edit was made, so the substrate is
  byte-identical to commit `97fda49` (the post-#244 `main` HEAD).

## Deviations from Task Packet

None — execution stayed strictly within the Task Packet scope. All
edits are inside the MAY-edit envelope; no Rust / Cargo / non-
`bench.yml` workflow / plan / ADR / automation file was touched; no
new file was added under `.github/`; `$GITHUB_STEP_SUMMARY` is the
sole emission surface; the new job's YAML `name:` contains the
required `observational` substring; the three caveat strings appear
verbatim in the summary-emit step; `actionlint` non-availability is
deferred to the first CI run per the gate's documented fallback; no
threshold environment variable, comparison-against-baseline logic, or
helper script was introduced.

## Open Questions for Reviewer

- Cache key choice for the new job — I used a distinct
  `shared-key: script-bench-cold-start-observational` so the new job
  does not race with the existing `script-bench-compile` job's
  `shared-key: script-bench-compile` cache key. Both jobs run on the
  same `ubuntu-latest` runner class and target the same Cargo
  workspace, so a shared cache would also be workable; I optioned for
  isolation. Confirm the isolation is preferable.
- Wasmtime version extraction — the summary reads the `wasmtime`
  version from `Cargo.lock` via
  `awk '/^name = "wasmtime"$/{getline; gsub(/"/, "", $3); print $3; exit}' Cargo.lock`,
  which assumes the canonical `[[package]]` ordering of `name` then
  `version` and quotes-delimited values. The standard Cargo.lock
  format guarantees this, and the local file is currently consistent
  (`name = "wasmtime"` at line 6299, `version = "44.0.1"` at line 6300).
  If Cargo's lockfile format changes the field ordering, the awk would
  need updating — flag if you prefer a more defensive extraction
  (e.g. `cargo metadata` parse) even though that adds latency and
  toolchain coupling to a no-gate observational job.
- `BASELINE.md` carry-over chip — per the TASK packet's Planner Notes
  the CI-host baseline row in `crates/script-bench/BASELINE.md` is an
  explicitly-deferred follow-up dispatch, not in scope here. This
  report records that the local single-sample (`duration_ns = 276 700`)
  was NOT appended to `BASELINE.md` per scope. Confirm the deferral is
  the right call rather than a one-line CI-host row appended now.

## Worktree State

- Tracked files: 4 modified — `.github/workflows/bench.yml`,
  `HANDOFF.md`, `Status.md`, `change.md`. No other tracked path
  modified; no tracked path deleted; no tracked path added.
- Untracked items: `ai_handoffs/ISSUE-245_TASK_2026-05-28_12-14-18+0300.md`
  and its `.meta.json` sidecar (TASK packet inherited from upstream;
  not edited by this dispatch); after finalize-time this EXEC packet
  (`ai_handoffs/ISSUE-245_EXEC_2026-05-28_12-26-52+0300.md`) and its
  `.meta.json` sidecar will also appear. Repo-root precedent files
  (`OPENAItoCLAUDE_*.md`, `CLAUDEtoOPENAI_*.md`,
  `CLAUDE_SUB_EPSILON_REVIEW.md`) are unchanged.
- Branch: `ai-dispatch/ISSUE-245`
- Last commit: `97fda49` Merge pull request #244 from RustCADs/ai-dispatch/ISSUE-243

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-245
AUTHOR: Executor / Claude
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---

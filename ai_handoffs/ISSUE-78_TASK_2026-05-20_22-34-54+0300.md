# Task Packet

DISPATCH_ID: ISSUE-78
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_22-34-54+0300
RELATED_FILES:
- docs/adr/ADR-097-cad-projection-module-split.md
- plans/PLAN.md
- docs/§18/CAD_PROJECTION.md
- docs/§18/ARCHITECTURE_LINTS.md
- crates/cad-projection/src/lib.rs
- crates/cad-projection/src/projection_structural/mod.rs
- crates/cad-projection/src/projection_geometry/mod.rs
- crates/cad-projection/src/projection_semantic/mod.rs
- crates/cad-projection/src/projection_runtime/mod.rs
- crates/cad-projection/src/projection_editor/mod.rs
- crates/cad-projection/src/projection_cache/mod.rs
STATUS: OPEN

## Goal

Implement GitHub issue #78 by backfilling the missing accepted ADR for ADR-097 at `docs/adr/ADR-097-cad-projection-module-split.md`. The ADR must document the already-accepted six-module `cad-projection` internal split from `plans/PLAN.md` and the current `crates/cad-projection` layout, without introducing new architecture, source changes, Cargo changes, or new doctrine outside this ADR.

## Scope

### MAY edit
- `docs/adr/ADR-097-cad-projection-module-split.md`, only if it already exists as an incomplete or stale local artifact.

### MUST NOT edit
- Any existing file other than `docs/adr/ADR-097-cad-projection-module-split.md`.
- `plans/PLAN.md`.
- Any file under `docs/§18/`.
- Any file under `crates/`, including `crates/cad-projection/**`.
- Any `Cargo.toml`, `Cargo.lock`, source file, test file, schema, script, CI file, `.gitignore`, `Status.md`, `HANDOFF.md`, `change.md`, or existing handoff packet.

### MAY add new files
- `docs/adr/ADR-097-cad-projection-module-split.md`, if absent.
- One protocol-required execution report matching `ai_handoffs/ISSUE-78_EXEC_*.md`.
- The matching execution-report `.meta.json` sidecar if generated by `new-handoff.ps1 -Finalize`.

### MUST NOT add new files
- No files other than the ADR-097 file and the protocol-required execution report packet plus optional sidecar.
- No new ADRs other than ADR-097.
- No source files, test files, fixtures, docs under `docs/§18/`, Cargo entries, architecture-lint files, generated artifacts, or automation files.

## Deliverables

- Add `docs/adr/ADR-097-cad-projection-module-split.md`.
- Use the existing ADR style in `docs/adr/`, with clear status metadata plus sections covering context, decision, consequences, alternatives not chosen, implementation guidance, followups or open questions, and references.
- Record that ADR-097 accepts one crate with six internal modules, not a new crate split:
  - `projection_structural`
  - `projection_geometry`
  - `projection_semantic`
  - `projection_runtime`
  - `projection_editor`
  - `projection_cache`
- Mirror the ownership model from `plans/PLAN.md` section 1.5.4.5 and `docs/§18/CAD_PROJECTION.md`: structural mapping and entity existence, geometric tessellation projection, semantic material and selection metadata, runtime collision or visibility feeders, editor gizmo or picking surfaces, and cache invalidation or dirty tracking.
- Explain the guardrails already accepted in PLAN: `projection_structural` must not import `projection_runtime` or `projection_editor`, each module should document triggers and emitted outputs, and adding a seventh category requires a future ADR.
- Cross-link the relevant PLAN sections and §18 docs from inside ADR-097, at minimum `plans/PLAN.md` section 1.5.4.5, `docs/§18/CAD_PROJECTION.md`, and `docs/§18/ARCHITECTURE_LINTS.md`.
- Add a completed execution report packet for this dispatch.

## Acceptance Criteria

- `docs/adr/ADR-097-cad-projection-module-split.md` exists and is the only repository content file added or modified by the dispatch.
- The ADR is a documentation backfill for an accepted decision; it does not propose a new architecture, a new crate, a module rename, a seventh projection category, a new lint, or any implementation work.
- The ADR's six-module list and module responsibilities agree with `plans/PLAN.md` section 1.5.4.5 and the current `crates/cad-projection/src/` layout.
- The ADR references current §18 documentation accurately, especially `docs/§18/CAD_PROJECTION.md` and `docs/§18/ARCHITECTURE_LINTS.md`.
- Source files and Cargo files are untouched.
- Existing ADRs, PLAN, §18 docs, architecture lint docs, and handoff packets other than the new execution report are untouched.
- `cargo test -p rge-cad-projection --no-fail-fast` exits 0.

## Constraints / Non-Goals

- This is a doc-only ADR backfill.
- Do not implement or modify any `cad-projection` Rust code.
- Do not update `plans/PLAN.md`; treat it as source material only.
- Do not update `docs/§18/` docs; treat them as source material only.
- Do not change `Cargo.toml`, `Cargo.lock`, dependencies, features, tests, architecture lints, or CI.
- Do not broaden ADR-097 into cad-core topology lineage, plugin-host owned handoff, PIE snapshot design, face-label propagation, or later ADR topics except as references when useful.
- Do not commit or push.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `if (-not (Test-Path -LiteralPath 'docs/adr/ADR-097-cad-projection-module-split.md')) { throw 'missing ADR-097' }` -> expected exit 0.
- `Select-String -LiteralPath 'docs/adr/ADR-097-cad-projection-module-split.md' -Pattern 'projection_structural','projection_geometry','projection_semantic','projection_runtime','projection_editor','projection_cache'` -> expected all six module names are present.
- `git status --short -- docs/adr/ADR-097-cad-projection-module-split.md` -> expected exactly one status line for `docs/adr/ADR-097-cad-projection-module-split.md`.
- `git status --short -- crates Cargo.toml Cargo.lock` -> expected no output.
- `cargo test -p rge-cad-projection --no-fail-fast` -> expected exit 0.
- `git status --short` -> expected repository changes are limited to `docs/adr/ADR-097-cad-projection-module-split.md`, the protocol-required `ai_handoffs/ISSUE-78_EXEC_*.md`, and the optional finalize-generated execution-report sidecar.

If a required command cannot be run in the environment, the Executor MUST document the exact command, the failure reason, and any partial verification that was completed.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- Completing ADR-097 appears to require editing any file other than `docs/adr/ADR-097-cad-projection-module-split.md` and the protocol-required execution report.
- The current `crates/cad-projection/src/` layout no longer has the six accepted modules named in PLAN section 1.5.4.5.
- PLAN and the current crate layout materially disagree in a way that would force the ADR to choose new architecture.
- Writing the ADR would require adding a seventh projection category, renaming modules, adding a crate, changing an architecture lint, or changing any source or Cargo file.
- `cargo test -p rge-cad-projection --no-fail-fast` fails for reasons that cannot be attributed to the doc-only ADR addition.
- Any pre-existing unrelated worktree change would make the "source/Cargo files untouched" acceptance criterion ambiguous.

## Planner Notes

The user goal's damaged placeholder refers to the repository's §18 docs. For this dispatch, the relevant cross-link targets are `docs/§18/CAD_PROJECTION.md` and `docs/§18/ARCHITECTURE_LINTS.md`; use `docs/§18/CAD_CORE_MODEL.md`, `docs/§18/PIE_SNAPSHOT.md`, `docs/§18/PLUGIN_API.md`, or `docs/§18/PLUGIN_HOST_PATTERNS.md` only if the ADR text explicitly mentions those adjacent substrates. ADR-097 should preserve the already-accepted decision captured by PLAN section 1.5.4.5 and the live `cad-projection` six-module layout. Revision number: 0. No prior Claude gate result.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-78
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

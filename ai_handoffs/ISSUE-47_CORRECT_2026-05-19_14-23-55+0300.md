# Correction Packet

DISPATCH_ID: ISSUE-47
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_14-23-55+0300
RELATED_FILES:
- crates/anim-graph/src/lib.rs
- crates/material-graph/src/lib.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-47_TASK_2026-05-19_14-15-56+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-47_EXEC_2026-05-19_14-22-41+0300.md`
- Latest Review Report: none; the post-execution verification gate failed before a control review packet was written.
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections and nothing more. Each
correction references the failed post-execution verification gate.

1. **Normalize anim-graph newline style** - Verification gate `[1] cargo +nightly fmt --check` failed with `Incorrect newline style in crates/anim-graph/src/lib.rs`. Required change: normalize `crates/anim-graph/src/lib.rs` to the newline style accepted by nightly rustfmt, with no Rust token, API, comment-content, or behavior changes. Acceptance: `cargo +nightly fmt --all -- --check` no longer reports this file.
2. **Apply rustfmt layout to material-graph tests** - Verification gate `[1] cargo +nightly fmt --check` reported rustfmt diffs in `crates/material-graph/src/lib.rs` around the adapter edge-view test expected vector and `for` loop. Required change: apply rustfmt's formatting to `crates/material-graph/src/lib.rs` only; do not change the `VizAdapter` implementation semantics, edge labels, test assertions, public APIs, or payload shapes. Acceptance: `cargo +nightly fmt --all -- --check` exits 0 and the material-graph diff is formatting-only relative to the executed implementation.

## Deferred Findings (NOT Approved for This Round)

None. No code-review findings are deferred; this correction round is limited to
the formatting and newline-style failures reported by the canonical
post-execution verification gate.

## Updated Acceptance Criteria

Unchanged from the Task Packet, except this correction round additionally
permits a format-only newline-style edit to `crates/anim-graph/src/lib.rs`
solely to satisfy the workspace rustfmt gate. `crates/material-graph/src/lib.rs`
remains the only file that may receive substantive implementation changes, and
this correction round approves no substantive implementation changes.

## Re-Verification Gates

The Executor MUST re-run these gates after the corrections:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.\.ai\dispatch.verify.ps1` -> expected exit 0.

If either command cannot be run, the Executor MUST document the exact command,
the failure reason, and any partial verification completed in the follow-up
execution report.

## Halt Conditions (Updated if Any)

- Halt and write a follow-up execution report if `cargo +nightly fmt --all`
  would change any tracked source file other than
  `crates/anim-graph/src/lib.rs` or `crates/material-graph/src/lib.rs`.
- Halt and request fresh Planner guidance if passing verification appears to
  require any semantic Rust change, Cargo/dependency change, graph-foundation
  change, runtime/editor/shader work, or edits to docs, schemas, scripts, or
  unrelated packets.
- Halt and report the remaining failure if the canonical verification script
  still fails after the approved formatting corrections.

## Planner Notes

The dispatch implementation is not reopened for redesign. The verification log
shows a workspace rustfmt failure: one newline-style issue in
`crates/anim-graph/src/lib.rs` and rustfmt layout changes in
`crates/material-graph/src/lib.rs`. The Executor should make only those
formatting corrections, then confirm the failing fmt gate and the canonical
dispatch verification gate both pass.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-47
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Task Packet

DISPATCH_ID: ISSUE-84
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-21_02-31-50+0300
RELATED_FILES:
- kernel/graph-foundation/tests/diff_correctness.rs
STATUS: OPEN

## Goal

Add focused integration-test coverage for GitHub issue #84: `GraphDiff::between` must preserve the payload for a newly added isolated node in `added_nodes`. This should complement the existing removed-node and edge payload coverage in `kernel/graph-foundation/tests/diff_correctness.rs` without changing production code.

## Scope

### MAY edit
- kernel/graph-foundation/tests/diff_correctness.rs

### MUST NOT edit
- Any production source file, including `kernel/graph-foundation/src/**`
- Any Cargo manifest or lockfile
- Any documentation file
- Any script, schema, or repository configuration file
- Any existing handoff packet other than the Executor's own execution report

### MAY add new files
- ai_handoffs/ISSUE-84_EXEC_*.md, only as the required execution report

### MUST NOT add new files
- Any new source, test, fixture, documentation, script, schema, crate, or Cargo-related file
- Any handoff packet other than the required `ISSUE-84_EXEC_*.md` execution report

## Deliverables

- One new focused `#[test]` in `kernel/graph-foundation/tests/diff_correctness.rs` covering an added isolated node.
- The test must create a first snapshot before the new node exists, add one isolated node with a concrete payload, create a second snapshot, and call `GraphDiff::between(&snap1, &snap2)`.
- The test must assert that `added_nodes` contains exactly the new node and that the stored payload equals the payload inserted into the second graph state.
- The test must assert no removed nodes, no changed nodes, and no edge-side diff noise.
- The test must assert `node_change_count() == 1` and `edge_change_count() == 0`.
- An `ISSUE-84_EXEC_*.md` execution report documenting the exact change and verification result.

## Acceptance Criteria

- `kernel/graph-foundation/tests/diff_correctness.rs` contains a dedicated added-isolated-node payload preservation test, preferably near the existing removed-node payload test.
- The new test fails if `GraphDiff::between` reports only the new node id without preserving the new node payload.
- The new test verifies all quiet sides explicitly: `removed_nodes`, `changed_nodes`, `added_edges`, `removed_edges`, and `changed_edges` are empty.
- The new test verifies derived counts: one node change and zero edge changes.
- No production code, API, manifest, documentation, script, schema, or unrelated test behavior is changed.
- `cargo test -p rge-kernel-graph-foundation --test diff_correctness` exits 0.

## Constraints / Non-Goals

- This is a coverage-only dispatch. Do not fix or refactor `GraphDiff` unless the requested test cannot compile against the current public API; halt instead if that occurs.
- Do not add a broader diff test matrix or property tests.
- Do not alter existing tests except for minimal local placement adjustments needed to add the new test cleanly.
- Do not change graph IDs, helper functions, or shared setup in ways that make existing assertions less direct.
- Do not run or require broad workspace validation beyond the focused gate unless needed to diagnose the focused gate.

## Verification Gates

The Executor MUST run and document the result of this gate in their `EXECUTION_REPORT`:

- `cargo test -p rge-kernel-graph-foundation --test diff_correctness` -> expected exit 0

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- The requested added-node payload test requires changes outside `kernel/graph-foundation/tests/diff_correctness.rs`.
- `GraphDiff::between` cannot expose the added node payload through the current public test-visible API.
- The focused cargo test gate fails for a reason that cannot be resolved by editing only the allowed test file.
- The executor determines that adding the test would require changing graph semantics, IDs, edge behavior, or existing assertions beyond this issue's scope.
- Any repository state or tooling problem would require editing files outside the allowed scope.

## Planner Notes

This task is the added-node counterpart to the existing payload preservation tests for removed nodes and edges. Keep the new graph isolated from edges so the assertions can prove that the node-side diff does not create edge-side noise.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-84
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

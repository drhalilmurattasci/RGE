# Task Packet

DISPATCH_ID: ISSUE-10
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18_01-17-26+0300
RELATED_FILES:
- AI_DISPATCH_AUTOMATION.md
- Invoke-AiDispatchQueue.ps1
- Invoke-AiDispatchAuto.ps1
STATUS: OPEN

## Goal

Document the queue-runner correction-round defaults requested by GitHub issue #10. The dispatch should update only `AI_DISPATCH_AUTOMATION.md` so Section 8 explains that the queue and autonomous runners intentionally use a larger correction-round default than the inner dispatch loop.

## Scope

### MAY edit
- AI_DISPATCH_AUTOMATION.md

### MUST NOT edit
- Invoke-AiDispatchLoop.ps1
- Invoke-AiDispatchQueue.ps1
- Invoke-AiDispatchAuto.ps1
- new-handoff.ps1
- .ai/dispatch.verify.ps1
- .gitignore
- ai_handoffs/AI_HANDOFF_PROTOCOL.md
- ai_handoffs/templates/*
- Any source file, schema, build file, status document, log, or packet other than the protocol-required execution report

### MAY add new files
- One protocol-required execution report matching `ai_handoffs/ISSUE-10_EXEC_*.md`

### MUST NOT add new files
- Any new source, docs, schema, script, test, log, or packet file other than the protocol-required execution report

## Deliverables

- A short note in `AI_DISPATCH_AUTOMATION.md` Section 8 placed immediately below the `Invoke-AiDispatchLoop.ps1` parameter defaults table and before the "Parameter sets" paragraph.
- The note states that `Invoke-AiDispatchQueue.ps1` and `Invoke-AiDispatchAuto.ps1` default `-MaxCorrectionRounds` to `2` because coding tasks more often reach `needs_changes`.
- The note states that both `Invoke-AiDispatchQueue.ps1` and `Invoke-AiDispatchAuto.ps1` pass `-MaxPlanRevisions` and `-MaxCorrectionRounds` through to `Invoke-AiDispatchLoop.ps1`.
- A completed execution report packet for this dispatch.

## Acceptance Criteria

- `AI_DISPATCH_AUTOMATION.md` is the only tracked project file changed by the implementation.
- Section 8 keeps the existing `Invoke-AiDispatchLoop.ps1` parameter defaults table intact, including the loop default of `1` for `-MaxCorrectionRounds`.
- The new note is concise and directly below the parameter defaults table.
- The new note names both `Invoke-AiDispatchQueue.ps1` and `Invoke-AiDispatchAuto.ps1`.
- The new note accurately distinguishes the queue/autonomous default of `-MaxCorrectionRounds 2` from the loop default in the table.
- The new note explicitly says both queue-layer scripts pass `-MaxPlanRevisions` and `-MaxCorrectionRounds` through to the loop.
- No scripts, schemas, source files, templates, status files, or unrelated documentation are changed.

## Constraints / Non-Goals

- Do not change runtime behavior, PowerShell parameters, defaults, CLI examples, schemas, tests, or automation scripts.
- Do not rewrite or reorganize Section 8 beyond inserting the requested note.
- Do not add a new top-level section or change the table of contents.
- Do not update GitHub issue metadata, labels, comments, branches, commits, or remotes.
- Do not run broad cargo or CI gates for this documentation-only change unless the local dispatch tooling requires them separately.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `git diff -- AI_DISPATCH_AUTOMATION.md`: expected diff shows only the requested Section 8 note.
- `git diff --check -- AI_DISPATCH_AUTOMATION.md`: expected exit 0.
- `git status --short`: expected tracked project-file change is limited to `AI_DISPATCH_AUTOMATION.md`, plus the protocol-required execution report packet.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- The requested note cannot be placed directly below the Section 8 parameter defaults table without a larger documentation rewrite.
- Accurate documentation appears to require changing any script, schema, source file, template, or file other than `AI_DISPATCH_AUTOMATION.md`.
- The existing scripts do not currently default `-MaxCorrectionRounds` to `2` or do not pass both `-MaxPlanRevisions` and `-MaxCorrectionRounds` through to the loop.
- Verification shows any tracked project-file change outside `AI_DISPATCH_AUTOMATION.md`.
- The executor believes the loop defaults table itself must be changed rather than supplemented by a note below it.

## Planner Notes

This task was filed automatically by `Invoke-AiDispatchAuto.ps1` from `.ai/dispatch.tasks.md` for GitHub issue #10. The task is intentionally documentation-only: the queue-layer scripts are relevant context, but they are not in edit scope.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-10
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

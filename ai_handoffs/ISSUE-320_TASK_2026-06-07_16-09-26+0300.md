# Task Packet

DISPATCH_ID: ISSUE-320
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-07_16-09-26+0300
RELATED_FILES:
- Status.md
- HANDOFF.md
- change.md
- plans/BASELINE.md
- ai_handoffs/ISSUE-320_TASK_2026-06-07_16-09-26+0300.md
STATUS: OPEN

## Goal

Reconcile the first full-automation batch readiness docs after tasks 74-80.
This is GitHub issue #320, a docs-only dispatch: make the readiness/status
docs say that the command-palette keyboard navigation batch is complete on
`main`, tasks 74-80 are complete on `main` including filter-edit selection
reset, GitHub issue #319 was manually salvaged and closed so it is no longer
an open autonomous failure blocker, and the next batch is exactly one
docs-only delegated-human `PublishMode main` task. Do not arm, register,
authorize, or reconfigure automation.

## Scope

This dispatch is docs-only. The Executor may reconcile wording in the four
allowed readiness docs and may produce only normal generated ISSUE-320 dispatch
artifacts.

### MAY edit
- `Status.md`
- `HANDOFF.md`
- `change.md`
- `plans/BASELINE.md`

### MUST NOT edit
- Any file not listed under `### MAY edit`, except generated ISSUE-320
  dispatch artifacts listed under `### MAY add new files`.
- Rust source, Rust tests, examples, or workspace code under `crates/**`,
  `kernel/**`, `runtime/**`, `editor/**`, `tests/**`, or `examples/**`.
- Cargo, lint, deny, formatting, toolchain, package, or workspace metadata:
  `Cargo.toml`, `Cargo.lock`, `**/Cargo.toml`, `clippy.toml`, `deny.toml`,
  `rustfmt.toml`, `rust-toolchain*`, or package-manager lock/config files.
- ADRs, doctrine docs, workflows, schemas, or general governance docs:
  `docs/**`, `.github/**`, `AI_DISPATCH_AUTOMATION.md`,
  `AI_DISPATCH_PARALLEL.md`, `AUTONOMOUS_WATCH.md`, `AGENTS.md`, or
  `CLAUDE.md`.
- Dispatch automation, guard, queue, scheduler, watcher, verification, or
  task-brief files, including `.ai/dispatch.tasks.md`,
  `.ai/dispatch.verify.ps1`, `Invoke-AiDispatch*.ps1`,
  `Register-AiDispatchSchedule.ps1`, `Get-AiDispatchHealth.ps1`,
  `Get-AiDispatchTrends.ps1`, `Watch-AiDispatch.ps1`,
  `Watch-DispatchStages.ps1`, `Wait-GitHubActions.ps1`, `.mcp.json`,
  `tools/**`, or `scripts/**`.
- Existing dispatch artifacts under `ai_handoffs/`, `ai_dispatch_logs/`, or
  `.ai/dispatch-*`; do not rewrite older TASK, EXEC, REVIEW, CORRECT,
  CLOSEOUT, sidecar, run-dir, or log artifacts.
- `.gitignore`, scheduler configuration, local machine scheduled-task state,
  or registered Windows Scheduled Tasks.

### MAY add new files
- `ai_handoffs/ISSUE-320_TASK_2026-06-07_16-09-26+0300.meta.json`
- `ai_handoffs/ISSUE-320_EXEC_*.md`
- `ai_handoffs/ISSUE-320_EXEC_*.meta.json`
- `ai_handoffs/ISSUE-320_CORRECT_*.md`
- `ai_handoffs/ISSUE-320_CORRECT_*.meta.json`
- `ai_dispatch_logs/log_*.md`
- `.ai/dispatch-ISSUE-320/**`

### MUST NOT add new files
- New source, test, Cargo, lint, ADR, workflow, schema, automation, scheduler,
  task-brief, or tooling files.
- New docs or plan files outside the four allowed existing docs.
- New GitHub issue state files, scheduler configuration files, or autonomous
  backlog files.
- New `REVIEW` or `CLOSEOUT` packets for this orchestrator dispatch. Missing
  `_REVIEW_` and `_CLOSEOUT_` packets are normal for orchestrator dispatches.
- Any generated artifact outside the ISSUE-320 patterns named under
  `### MAY add new files`.

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - Status.md
  - HANDOFF.md
  - change.md
  - plans/BASELINE.md
  - ai_handoffs/ISSUE-320_TASK_2026-06-07_16-09-26+0300.meta.json
  - ai_handoffs/ISSUE-320_EXEC_*.md
  - ai_handoffs/ISSUE-320_EXEC_*.meta.json
  - ai_handoffs/ISSUE-320_CORRECT_*.md
  - ai_handoffs/ISSUE-320_CORRECT_*.meta.json
  - ai_dispatch_logs/log_*.md
  - .ai/dispatch-ISSUE-320/**
MUST_NOT_EDIT:
  - crates/**
  - kernel/**
  - runtime/**
  - editor/**
  - tests/**
  - examples/**
  - Cargo.toml
  - Cargo.lock
  - **/Cargo.toml
  - clippy.toml
  - deny.toml
  - rustfmt.toml
  - rust-toolchain*
  - docs/**
  - .github/**
  - AI_DISPATCH_AUTOMATION.md
  - AI_DISPATCH_PARALLEL.md
  - AUTONOMOUS_WATCH.md
  - AGENTS.md
  - CLAUDE.md
  - .ai/dispatch.tasks.md
  - .ai/dispatch.verify.ps1
  - Invoke-AiDispatch*.ps1
  - Register-AiDispatchSchedule.ps1
  - Get-AiDispatchHealth.ps1
  - Get-AiDispatchTrends.ps1
  - Watch-AiDispatch.ps1
  - Watch-DispatchStages.ps1
  - Wait-GitHubActions.ps1
  - .mcp.json
  - tools/**
  - scripts/**
  - .gitignore
  - ai_handoffs/templates/**
  - ai_handoffs/AI_HANDOFF_PROTOCOL.md
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Deliverables

- Reconciled readiness wording in the allowed docs, edited only where needed.
- `Status.md`, `HANDOFF.md`, and `change.md` clearly state that tasks 74-80
  are complete on `main`, with command-palette keyboard navigation complete
  including filter-edit selection reset.
- The allowed docs clearly state that GitHub issue #319 was manually salvaged
  and closed, so it is no longer an open autonomous failure blocker.
- The allowed docs clearly state the next batch is exactly one docs-only
  delegated-human `PublishMode main` task.
- The allowed docs preserve scheduler registration and standing
  `PublishMode main` authorization as non-goals, not completed work and not
  implied authorization.
- One EXEC packet documenting the exact files changed, static inspection,
  verification gate results, and any residual risks.

## Acceptance Criteria

- The diff contains only the four allowed docs plus generated ISSUE-320
  dispatch artifacts.
- No Rust source, tests, Cargo files, lints, ADRs, workflows, automation
  scripts, scheduler config, `.ai/dispatch.tasks.md`, existing dispatch
  artifacts, or registered Windows Scheduled Tasks are modified.
- The docs explicitly say tasks 74-80 are complete on `main`, including
  filter-edit selection reset.
- The docs explicitly say the command-palette keyboard navigation work is
  complete on `main`; no surviving current-readiness text frames richer
  command-palette keyboard navigation as still open.
- The docs explicitly say issue #319 was manually salvaged and closed and is
  no longer an open autonomous failure blocker.
- The docs explicitly say the next batch is one docs-only delegated-human
  `PublishMode main` task.
- The docs continue to frame scheduler registration and any standing
  `PublishMode main` authorization as non-goals / not armed / not changed.
- `git diff --check` exits 0.

## Current-State Claims: Falsification Required

Every negative / "unchanged" assertion this packet makes about current source
MUST list its inverse-search command and result. A negative claim without a
falsifying search is not a valid premise (Protocol Rule 8).

- Claim: At planning time, the readiness docs do not yet contain an explicit
  `tasks 74-80` / `74-80` batch-completion statement.
  Falsifying search: `Select-String -Path 'Status.md','HANDOFF.md','change.md','plans/BASELINE.md' -Pattern 'tasks 74-80','Tasks 74-80','task 74-80','Task 74-80','74-80'` -> no matches.
- Claim: At planning time, issue #319 salvage is recorded in `change.md` but
  is not yet reconciled across the readiness docs.
  Falsifying search: `Select-String -Path 'Status.md','HANDOFF.md','change.md','plans/BASELINE.md' -Pattern 'Issue #319','issue #319','ISSUE-319','GitHub issue #319'` -> one match in `change.md` only.
- Claim: This dispatch does not require source, Cargo, workflow, automation,
  scheduler, or task-backlog changes.
  Falsifying search: `git diff --name-only -- crates kernel runtime editor tests Cargo.toml Cargo.lock .github .ai/dispatch.tasks.md Invoke-AiDispatch*.ps1 Register-AiDispatchSchedule.ps1 .ai/dispatch.verify.ps1` at planning time -> no output; Executor must rerun after edits and expect no output.

## Constraints / Non-Goals

- Do not reopen, re-run, or reinterpret tasks 74-80; only reconcile docs about
  their completed-on-main state.
- Do not perform GitHub label changes, issue closure, or PR work. Issue #319
  salvage/closure is historical state to document, not work to redo.
- Do not register, unregister, query-modify, or otherwise touch Windows
  Scheduled Tasks.
- Do not change scheduler registration, guard policy, queue policy,
  autonomous task selection, or `.ai/dispatch.tasks.md`.
- Do not create a standing `PublishMode main` authorization. The docs must say
  the next batch is one delegated-human docs-only `PublishMode main` task, not
  broad permanent authorization.
- Do not add code, tests, lints, ADRs, workflow changes, automation changes,
  schemas, or new documentation surfaces.
- Do not run cargo build/test/fmt/lint gates unless needed only for local
  confidence; they are not required for this docs-only dispatch.
- Do not commit or push manually from the Executor role; the queue runner owns
  publish behavior after control review.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- `git diff --check` -> expected exit 0.
- `git diff --name-only` -> expected only `Status.md`, `HANDOFF.md`,
  `change.md`, `plans/BASELINE.md`, and generated ISSUE-320 dispatch
  artifacts.
- `git diff --name-only -- crates kernel runtime editor tests Cargo.toml Cargo.lock .github .ai/dispatch.tasks.md Invoke-AiDispatch*.ps1 Register-AiDispatchSchedule.ps1 .ai/dispatch.verify.ps1` -> expected no output.
- `Select-String -Path 'Status.md','HANDOFF.md','change.md','plans/BASELINE.md' -Pattern '74-80','tasks 74-80','Tasks 74-80','filter-edit selection reset','filter-change reset','search-filter edits restart selection'` -> expected evidence for tasks 74-80 completion and the filter-edit selection reset.
- `Select-String -Path 'Status.md','HANDOFF.md','change.md','plans/BASELINE.md' -Pattern 'Issue #319','issue #319','ISSUE-319','salvaged','closed','no longer an open autonomous failure blocker'` -> expected evidence that issue #319 is no longer an open autonomous failure blocker.
- `Select-String -Path 'Status.md','HANDOFF.md','change.md','plans/BASELINE.md' -Pattern 'delegated-human','delegated human','PublishMode main','docs-only'` -> expected evidence that the next batch is one docs-only delegated-human `PublishMode main` task.
- Static inspection of the edited prose -> expected scheduler registration and
  standing `PublishMode main` authorization remain non-goals / not armed /
  not changed.

No cargo build, test, fmt, or architecture-lint gate is required for this
docs-only reconciliation.

## Halt Conditions

The Executor MUST halt (without committing) and request fresh guidance if any
of the following occur:

- Reconciling the docs appears to require editing any file outside the four
  allowed docs or the generated ISSUE-320 dispatch artifacts.
- Any source, test, Cargo, lint, ADR, workflow, automation, scheduler config,
  `.ai/dispatch.tasks.md`, existing dispatch artifact, or registered Windows
  Scheduled Task change appears necessary.
- Static inspection contradicts the required facts: tasks 74-80 complete on
  `main`, command-palette keyboard navigation complete including filter-edit
  selection reset, or issue #319 manually salvaged/closed.
- The docs cannot state the next batch as exactly one docs-only
  delegated-human `PublishMode main` task without implying standing main-push
  authorization or scheduler arming.
- The working tree contains tracked modifications outside the allowed docs
  that would make the final diff ambiguous.
- `git diff --check` fails and cannot be fixed within the allowed docs.

## Planner Notes

AGENTS.md explicitly distinguishes orchestrator dispatches from the manual
`TASK -> EXEC -> REVIEW -> CORRECT -> CLOSEOUT` protocol. This is an
orchestrator dispatch, so the Executor should not create `_REVIEW_` or
`_CLOSEOUT_` packets; missing review/closeout packets are expected.

The important boundary is the "first full-automation batch docs-only" promise:
the reconciliation may record completed facts and the next delegated-human
docs-only batch, but it must not arm the scheduler, change queue policy, edit
the autonomous backlog, or grant broad standing `PublishMode main`
authorization.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-320
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

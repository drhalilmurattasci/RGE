# Task Packet

DISPATCH_ID: ISSUE-372
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-13_16-02-48+0300
RELATED_FILES:
- ai_handoffs/ISSUE-372_TASK_2026-06-13_16-02-48+0300.md
- .ai/dispatch.tasks.md
- Status.md
- HANDOFF.md
- plans/BASELINE.md
- change.md
- crates/editor-shell/src/**
- crates/editor-egui-host/src/**
- crates/editor-ui/src/**
- crates/editor-actions/src/**
- crates/cad-core/src/**
- crates/cad-projection/src/**
- editor/rge-editor/src/**
- runtime/**
- docs/EXTERNAL_ENGINE_LESSONS.md
STATUS: OPEN

## Goal

Perform task 118 as a source-read audit after viewport mouse-wheel zoom and
right-button orbit. Re-grep current source, compare the remaining Phase 9
editor-usability candidate classes, and select exactly one smallest safe
implementation follow-up as task 119, or record NEEDS_HUMAN with evidence.
This dispatch selects work only; it must not implement task 119 or change
source, Cargo, workflow, automation, schema, script, or packet-template files.

## Scope

This is source-read audit-only. The writable surface below exists only for the
audit result, task-list/status bookkeeping, and this dispatch's generated
artifacts; it does not authorize implementation edits.

### MAY edit
- `.ai/dispatch.tasks.md`
- `Status.md`
- `HANDOFF.md`
- `plans/BASELINE.md`
- `change.md`

### MUST NOT edit
- Rust source or tests under `crates/**`, `kernel/**`, `runtime/**`, or `editor/**`
- Cargo manifests or lockfiles, including `Cargo.toml`, `Cargo.lock`, and `**/Cargo.toml`
- GitHub workflows under `.github/workflows/**`
- Dispatch automation, guard, queue, scheduler, verification, or handoff scripts
- Schemas under `.ai/*.schema.json`
- Architecture-lint rules/config under `tools/architecture-lints/**`
- ADR files under `docs/adr/**`
- Packet templates under `ai_handoffs/templates/**`
- Existing handoff/log/audit artifacts from other dispatches
- Plugin runtime, discovery, or loading implementation code
- `docs/EXTERNAL_ENGINE_LESSONS.md`

### MAY add new files
- `ai_handoffs/ISSUE-372_EXEC_*.md`
- `ai_handoffs/ISSUE-372_EXEC_*.meta.json`
- `ai_handoffs/claims/ISSUE-372_*.json`
- `.ai/dispatch-ISSUE-372/**`
- `ai_dispatch_logs/log_*ISSUE-372*.md`

### MUST NOT add new files
- New Rust source or tests under `crates/**`, `kernel/**`, `runtime/**`, or `editor/**`
- New Cargo manifests, lockfiles, dependencies, or workspace metadata
- New workflows, scripts, schemas, ADRs, architecture-lint files, packet templates, or doctrine docs
- New plugin runtime, discovery, loading, or external-engine-lesson artifacts
- New handoff/log/audit artifacts for any dispatch other than ISSUE-372

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - .ai/dispatch.tasks.md
  - Status.md
  - HANDOFF.md
  - plans/BASELINE.md
  - change.md
  - ai_handoffs/ISSUE-372_EXEC_*.md
  - ai_handoffs/ISSUE-372_EXEC_*.meta.json
  - ai_handoffs/claims/ISSUE-372_*.json
  - .ai/dispatch-ISSUE-372/**
  - ai_dispatch_logs/log_*ISSUE-372*.md
MUST_NOT_EDIT:
  - crates/**
  - kernel/**
  - runtime/**
  - editor/**
  - Cargo.toml
  - Cargo.lock
  - **/Cargo.toml
  - .github/workflows/**
  - Invoke-*.ps1
  - Register-AiDispatchSchedule.ps1
  - Get-AiDispatchHealth.ps1
  - Watch-AiDispatch.ps1
  - Wait-GitHubActions.ps1
  - new-handoff.ps1
  - .ai/*.schema.json
  - tools/architecture-lints/**
  - docs/adr/**
  - ai_handoffs/templates/**
  - docs/EXTERNAL_ENGINE_LESSONS.md
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Deliverables

- Re-run and summarize the required source searches in the EXECUTION_REPORT
  before selecting a follow-up.
- Compare the listed candidate classes from current source: next
  camera/navigation slice after wheel zoom and right-button orbit; host-shell
  FIFO/generalized command execution; extension/plugin command execution beyond
  the injected seam; keybinding/conflict-resolution or shortcut remapping;
  OS/typed clipboard; and authoritative CAD/editor mutation through CommandBus.
- Justify why the selected follow-up is the smallest safe implementation task
  compared with the deferred candidates, or explain why no bounded candidate is
  safe without human product or architecture policy.
- If a safe bounded implementation follow-up exists, mark task 118 complete and
  append exactly one task 119 to `.ai/dispatch.tasks.md` with explicit MAY,
  MUST-NOT, Done, Verification, and Halt sections.
- If no safe bounded implementation follow-up exists, record NEEDS_HUMAN in
  `.ai/dispatch.tasks.md` and the status/history docs with the concrete
  blocking decision; do not append task 119 in that case.
- Update `Status.md`, `HANDOFF.md`, `plans/BASELINE.md`, and `change.md` only
  as needed to reflect the audit result and the fact that no implementation
  work was done.

## Acceptance Criteria

- The EXECUTION_REPORT records the required searches and summarizes the
  candidate comparison from current source, not stale roadmap prose.
- Selection path: `.ai/dispatch.tasks.md` contains exactly one new task 119
  entry, and that entry is a single bounded Phase 9 editor-usability
  implementation task, not another audit batch or broad roadmap item.
- NEEDS_HUMAN path: the docs name the blocking product or architecture decision
  and do not append task 119.
- Any task 119 entry includes explicit MAY, MUST-NOT, Done, Verification, and
  Halt sections; every path token listed under its MAY edit / MAY add sections
  is wrapped in Markdown backticks.
- Task 119 forbids implementation of deferred alternatives outside its selected
  slice, including command-route replacement, plugin runtime/discovery/loading,
  generalized execution, shortcut remap policy, OS/typed clipboard,
  authoritative CAD mutation, or camera/navigation work beyond the selected
  slice unless that exact item is the selected bounded task.
- `git diff --check` exits 0.
- `git diff --name-only` lists only `.ai/dispatch.tasks.md`, `Status.md`,
  `HANDOFF.md`, `plans/BASELINE.md`, `change.md`, and ISSUE-372 generated
  artifacts.
- No source, Cargo, workflow, automation, schema, script, packet-template, or
  external-engine-lesson file is changed.

## Current-State Claims: Falsification Required

Every current-state claim below must be re-checked by the Executor and recorded
in the EXECUTION_REPORT. The results here are planning-time observations only.

- Claim: task 118 is the current open task-selection audit and task 119 has not
  already been appended.
  Falsifying search: `rg -n "^118\.|^119\." .ai/dispatch.tasks.md` -> one
  planning-time match at `.ai/dispatch.tasks.md:10404` for task 118 and no task
  119 match.

- Claim: task 117's viewport-only right-button orbit is present in current
  `editor-shell`, so task 119 must not reselect right-button orbit.
  Falsifying search: `git grep -n -E "ViewportOrbitDrag|viewport_orbit_drag|orbit_around_target|MouseButton::Right|start_viewport_orbit_drag|update_viewport_orbit_drag|stop_viewport_orbit_drag" -- crates/editor-shell/src/camera.rs crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/viewport_navigation.rs crates/editor-shell/src/lifecycle/tests.rs` -> planning-time matches include `orbit_around_target` in `camera.rs`, `ViewportOrbitDrag` in `lifecycle/viewport_navigation.rs`, the `viewport_orbit_drag` field and start/update/stop helpers in `lifecycle/mod.rs`, right-button `MouseInput` arms in `lifecycle/mod.rs`, and focused tests in `lifecycle/tests.rs`.

- Claim: task 115's viewport mouse-wheel zoom is still present, so task 119
  must not reselect wheel zoom.
  Falsifying search: `git grep -n -E "MouseWheel|MouseScrollDelta|zoom_camera_for_viewport_mouse_wheel|viewport_mouse_wheel_zoom_direction|is_pointer_over_viewport_tab|should_fire_face_pick|CursorMoved|MouseInput" -- crates/editor-shell/src crates/editor-egui-host/src` -> planning-time matches include `MouseScrollDelta` import, `zoom_camera_for_viewport_mouse_wheel`, `viewport_mouse_wheel_zoom_direction`, `WindowEvent::MouseWheel`, cursor tracking, `MouseInput`, `is_pointer_over_viewport_tab`, and `should_fire_face_pick` in `crates/editor-shell/src/lifecycle/mod.rs`.

- Claim: menu, palette, and accelerator activations still route through the
  host-to-shell `MenuCommandHandoff` / `EditorShell::route_menu_command` path;
  replacing that route is broader than this audit may select unless the
  executor finds a genuinely isolated follow-up in current source.
  Falsifying search: `git grep -n -E "MenuCommandHandoff|drain_and_route_menu_commands|route_menu_command|Command::ResetCamera|Command::ZoomIn|Command::ZoomOut|command_palette_window|keycode_to_shortcut" -- crates/editor-egui-host/src crates/editor-shell/src crates/editor-ui/src` -> planning-time matches include `MenuCommandHandoff` in `crates/editor-egui-host/src/handoff.rs`, palette enqueueing in `crates/editor-egui-host/src/lib.rs`, `keycode_to_shortcut` in `crates/editor-shell/src/lifecycle/accelerator.rs`, `drain_and_route_menu_commands` and `route_menu_command` in `crates/editor-shell/src/render_path.rs`, and View command registry/tests in `crates/editor-ui/src`.

- Claim: extension/plugin commands still stop at the injected handler seam, and
  no real plugin runtime/discovery/loading route is wired through the
  editor-shell, editor-egui-host, or binary command-route owners.
  Falsifying searches:
  `git grep -n -E "ExtensionCommandHandler|Command::Custom|Command::Plugin" -- crates/editor-shell/src crates/editor-egui-host/src crates/editor-ui/src` -> planning-time matches include `ExtensionCommandHandler` in `crates/editor-shell/src/lifecycle/extension_command.rs`, command capture in `crates/editor-shell/src/render_path.rs`, and host/menu tests for `Command::Custom` / `Command::Plugin`.
  `git grep -n -E "PluginHost|PluginContext|plugin-discovery|runtime-wasmtime" -- crates/editor-shell/src crates/editor-egui-host/src editor/rge-editor/src` -> planning-time exit 1, no matches in the editor command-route owners.

- Claim: the clipboard surface is shell-local entity clipboard behavior, not an
  OS or typed clipboard integration.
  Falsifying search: `git grep -n -E "has_clipboard_entities|clipboard|Command::(Cut|Copy|Paste|Duplicate|Delete)" -- crates/editor-shell/src crates/editor-egui-host/src crates/editor-ui/src crates/editor-actions/src` -> planning-time matches include the shell-local `entity_clipboard` field and comments excluding OS clipboard/typed kernel/CAD behavior in `crates/editor-shell/src/lifecycle/mod.rs`, Edit command routing in `crates/editor-shell/src/render_path.rs`, and menu/predicate tests in host/UI crates.

- Claim: keybinding/conflict work remains a diagnostic/projection surface plus
  shortcut-to-command translation unless current source proves a smaller remap
  slice.
  Falsifying search: `git grep -n -E "ShortcutConflict|shortcut_conflict|keybinding|keycode_to_shortcut|remap|remapping" -- crates/editor-shell/src crates/editor-egui-host/src crates/editor-ui/src crates/editor-actions/src` -> planning-time matches include `shortcut_conflicts` host diagnostics, `ShortcutConflict` data in `editor-ui`, and `keycode_to_shortcut` in `editor-shell`; remap matches are face-ID remapping comments, not shortcut-remap implementation.

- Claim: authoritative editor mutation through `CommandBus` exists, but moving
  CAD/projection/editor operations through it is a broader mutation/undo-policy
  candidate that must be compared rather than assumed safe.
  Falsifying search: `git grep -n -E "CommandBus|\bAction\b|dispatch\(|apply_action|undo|redo" -- crates/editor-shell/src crates/editor-actions/src crates/cad-core/src crates/cad-projection/src` -> planning-time matches include `rge_editor_actions::CommandBus`, `Action`, undo/redo plumbing, `SetTimeScale`, and shell comments that wrapper-world copy/paste does not mutate CAD graphs, projection caches, or dirty/undo state.

## Constraints / Non-Goals

- Do not implement task 119 in this dispatch.
- Do not edit Rust source/tests, Cargo metadata, workflows, scripts, schemas,
  ADRs, packet templates, plugin runtime/discovery/loading implementation, or
  `docs/EXTERNAL_ENGINE_LESSONS.md`.
- Do not add a command, accelerator, menu item, command route, command-palette
  behavior, shortcut remapping/conflict policy, host-shell FIFO replacement,
  plugin runtime/discovery/loading, OS/typed clipboard, authoritative CAD
  mutation, undo/dirty policy, camera controller framework, or generalized input
  router as part of this audit.
- Do not select more than one follow-up. If the next step is a product or
  architecture policy choice, record NEEDS_HUMAN instead of manufacturing a
  task.
- `docs/EXTERNAL_ENGINE_LESSONS.md` may be read only to tag a selected
  candidate by pressure category if useful; it is not implementation authority
  and must not be edited.

## Verification Gates

The Executor MUST run and document the result of each gate in the
EXECUTION_REPORT:

- `rg -n "^118\.|^119\." .ai/dispatch.tasks.md` before and after editing.
- `git grep -n -E "ViewportOrbitDrag|viewport_orbit_drag|orbit_around_target|MouseButton::Right|start_viewport_orbit_drag|update_viewport_orbit_drag|stop_viewport_orbit_drag" -- crates/editor-shell/src/camera.rs crates/editor-shell/src/lifecycle/mod.rs crates/editor-shell/src/lifecycle/viewport_navigation.rs crates/editor-shell/src/lifecycle/tests.rs`
- `git grep -n -E "MouseWheel|MouseScrollDelta|zoom_camera_for_viewport_mouse_wheel|viewport_mouse_wheel_zoom_direction|is_pointer_over_viewport_tab|should_fire_face_pick|CursorMoved|MouseInput" -- crates/editor-shell/src crates/editor-egui-host/src`
- `git grep -n -E "MenuCommandHandoff|drain_and_route_menu_commands|route_menu_command|Command::ResetCamera|Command::ZoomIn|Command::ZoomOut|command_palette_window|keycode_to_shortcut" -- crates/editor-egui-host/src crates/editor-shell/src crates/editor-ui/src`
- `git grep -n -E "ExtensionCommandHandler|Command::Custom|Command::Plugin" -- crates/editor-shell/src crates/editor-egui-host/src crates/editor-ui/src`
- `git grep -n -E "PluginHost|PluginContext|plugin-discovery|runtime-wasmtime" -- crates/editor-shell/src crates/editor-egui-host/src editor/rge-editor/src` with expected exit 1 unless current source has changed.
- `git grep -n -E "has_clipboard_entities|clipboard|Command::(Cut|Copy|Paste|Duplicate|Delete)" -- crates/editor-shell/src crates/editor-egui-host/src crates/editor-ui/src crates/editor-actions/src`
- `git grep -n -E "ShortcutConflict|shortcut_conflict|keybinding|keycode_to_shortcut|remap|remapping" -- crates/editor-shell/src crates/editor-egui-host/src crates/editor-ui/src crates/editor-actions/src`
- `git grep -n -E "CommandBus|\bAction\b|dispatch\(|apply_action|undo|redo" -- crates/editor-shell/src crates/editor-actions/src crates/cad-core/src crates/cad-projection/src`
- `git diff --check`
- `git diff --name-only`

No cargo build/test/fmt gate is required for this audit because source and Cargo
files are forbidden to change. If any source or Cargo file changes, halt rather
than running implementation verification.

## Halt Conditions

The Executor MUST halt without source changes and request fresh guidance if any
of the following occur:

- Any step would require editing a MUST NOT path.
- The audit begins implementing task 119 instead of only selecting and
  documenting it.
- Task 119 already exists before this dispatch edits `.ai/dispatch.tasks.md`.
- The required source searches cannot be run, or their results contradict a
  required premise enough that the docs-only selection cannot be defended.
- No single bounded source-safe implementation follow-up can be selected.
- The best candidate requires product policy, architecture policy, plugin
  runtime/discovery/loading work, command-route replacement, keybinding/remap
  policy, OS/typed clipboard policy, CAD authority/undo policy, or broad
  camera/navigation design beyond a one-dispatch slice.
- Describing task 119 would require editing any MUST NOT path.
- `git diff --name-only` shows any path outside the MAY edit / MAY add envelope.

## Planner Notes

This is the successor to ISSUE-369's post-wheel-zoom audit. ISSUE-371 added the
right-button orbit slice, so this audit must not reselect wheel zoom or
right-button orbit. The likely safe shape, if source still supports it, is one
small usability slice that stays inside an already-established boundary. If the
remaining candidates require command-route architecture, plugin runtime,
shortcut policy, OS clipboard semantics, or CAD authority/undo design, record
NEEDS_HUMAN instead.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-372
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

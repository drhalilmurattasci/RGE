# Execution Report

DISPATCH_ID: ISSUE-427
AUTHOR: Executor / Codex
TIMESTAMP: 2026-06-20_12-29-33+0300
RELATED_FILES:
- crates/editor-ui/src/menus/command.rs - added core `Command::DeleteCurrentCadCuboid` and diagnostic id.
- crates/editor-ui/src/menus/predicate.rs - added `PredicateContext::has_current_cad_cuboid_selection`, default false.
- crates/editor-ui/src/menus/default_menu.rs - registered the no-shortcut Edit menu entry and enablement predicate.
- crates/editor-ui/tests/menus_ordering.rs - added default-menu coverage for the no-shortcut CAD delete entry.
- crates/editor-shell/src/render_path.rs - added dedicated command routing and made the exact-selection helper `pub(crate)`.
- crates/editor-shell/src/lifecycle/mod.rs - published the new predicate bit from the shared helper.
- crates/editor-shell/src/lifecycle/tests.rs - added predicate and routing coverage for the dedicated command.
- crates/editor-egui-host/src/menu_tests.rs - updated projected menu, handoff, palette, enablement, and shortcut-display fixtures.
- crates/editor-egui-host/src/shortcut_help.rs - updated shortcut-help fixture expectations for the no-shortcut row.
- .ai/dispatch.tasks.md - appended gated source/docs-read-only audit task 167 only.
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-427_TASK_2026-06-20_12-13-45+0300.md`

## Pre-Execution Review Consumed

No pre-execution review issued for this dispatch.

## Current-State Claims: Falsification Re-run

- Claim: the tracked-CAD selection helper was private before this task.
  Re-run before edits: `git grep -nE "pub(\(crate\))? fn delete_menu_selection_is_exact_tracked_cad_entity" -- crates/editor-shell/src/render_path.rs` -> exit 1, no matches.
  Confirming re-run before edits: `git grep -n "fn delete_menu_selection_is_exact_tracked_cad_entity" -- crates/editor-shell/src/render_path.rs` -> `crates/editor-shell/src/render_path.rs:491` showed the bare private declaration.
- Claim: `editor-ui::menus::Command` had no `DeleteCurrentCadCuboid` variant.
  Re-run before edits: `git grep -n "DeleteCurrentCadCuboid" -- crates/editor-ui/src/menus/command.rs` -> exit 1, no matches.
- Claim: `PredicateContext` had no `has_current_cad_cuboid_selection` field.
  Re-run before edits: `git grep -n "has_current_cad_cuboid_selection" -- crates/editor-ui/src/menus/predicate.rs` -> exit 1, no matches.
- Claim: `EditorShell::predicate_context()` was already public.
  Re-run: `git grep -n "pub fn predicate_context" -- crates/editor-shell/src/lifecycle/mod.rs` -> `crates/editor-shell/src/lifecycle/mod.rs:1928`.
- Claim: editor-egui-host menu and shortcut-help tests resolve the real `default_editor_menu()`.
  Re-run: `git grep -n "default_editor_menu" -- crates/editor-egui-host/src/menu_tests.rs crates/editor-egui-host/src/shortcut_help.rs` -> exit 0, multiple matches in both files, including `menu_tests.rs:74` and `shortcut_help.rs:216`.
- Claim: `editor-actions` has no CAD/editor-shell coupling and must remain generic.
  Re-run: `rg -n "editor_shell|editor-shell|cad_core|cad-core|cad_projection|cad-projection|CadGraph|CadProjection|cad_world" crates/editor-actions/src crates/editor-actions/Cargo.toml` -> exit 1, no matches.

## What I Changed

### Source

- `crates/editor-ui/src/menus/command.rs`: added `Command::DeleteCurrentCadCuboid` after `Duplicate` and diagnostic id `delete_current_cad_cuboid`.
- `crates/editor-ui/src/menus/predicate.rs`: added `has_current_cad_cuboid_selection` to `PredicateContext`.
- `crates/editor-ui/src/menus/default_menu.rs`: registered `edit.delete_current_cad_cuboid` as "Delete Current CAD Cuboid" after the existing shortcut-backed Edit entries, with no shortcut or shortcut hint and enablement `c.is_editing && c.has_current_cad_cuboid_selection`.
- `crates/editor-shell/src/render_path.rs`: added a separate `Command::DeleteCurrentCadCuboid` route that warn-and-swallows false guard and entry-point rejection; it does not fall back to `delete_selected_entities`. Existing `Command::Delete` and `Command::Cut` arms were left intact.
- `crates/editor-shell/src/lifecycle/mod.rs`: set `ctx.has_current_cad_cuboid_selection` from `delete_menu_selection_is_exact_tracked_cad_entity()`.

### Tests

- `crates/editor-ui/src/menus/default_menu.rs::edit_delete_current_cad_cuboid_entry_has_no_shortcut`: pins id, label, no shortcut, no shortcut hint, and Edit ordering.
- `crates/editor-ui/src/menus/default_menu.rs::enablement_predicates_track_context`: extended to cover the new Edit enablement in Editing and PIE states.
- `crates/editor-ui/tests/menus_ordering.rs::default_edit_menu_contains_no_shortcut_current_cad_cuboid_delete`: covers integration-level default menu resolution and disabled-visible behavior.
- `crates/editor-shell/src/lifecycle/tests.rs::predicate_context_reports_current_cad_cuboid_selection_only_for_exact_tracked_entity`: covers empty, exact CAD, non-CAD, mixed, and none-tracked stale states.
- `crates/editor-shell/src/lifecycle/tests.rs` menu routing tests: added explicit `DeleteCurrentCadCuboid` success, undo/redo fresh-entity, false-guard no-fallback, and stale rejection no-fallback coverage.
- `crates/editor-egui-host/src/menu_tests.rs`: updated expected Edit menu order, FIFO handoff order, command palette inclusion, enablement, and shortcut-display rows.
- `crates/editor-egui-host/src/shortcut_help.rs`: updated shortcut-help group order and no-shortcut/disabled-row expectations.

### Docs

- `.ai/dispatch.tasks.md`: appended exactly one gated source/docs-read-only audit task 167 carrying the required self-rearm instruction; no task 168 was appended.

## Per-File Summary

The implementation is intentionally split at existing ownership boundaries: `editor-ui` owns command/menu data and predicates, `editor-shell` owns live predicate publication and routing, and `editor-egui-host` changes are limited to tests that project the real default menu generically.

## Verification Results

- `cargo test -p rge-editor-ui` -> exit 0; all editor-ui unit, integration, and doc tests passed, including the updated default-menu and `menus_ordering` coverage.
- `cargo test -p rge-editor-egui-host` -> exit 0; all egui-host unit, integration, and doc tests passed, including menu, command palette, handoff, and shortcut-help fixtures.
- `cargo test -p rge-editor-shell --lib -- route_menu_command` -> exit 0; 13 passed / 0 failed.
- `cargo test -p rge-editor-shell --lib -- predicate_context` -> exit 0; 4 passed / 0 failed.
- `cargo test -p rge-editor-shell --lib -- delete_current_cad_cuboid` -> exit 0; 11 passed / 0 failed.
- `cargo check -p rge-editor-ui -p rge-editor-shell -p rge-editor-egui-host` -> exit 0.
- `cargo +nightly fmt --all -- --check` -> exit 0.
- `rg -n "editor_shell|editor-shell|cad_core|cad-core|cad_projection|cad-projection|CadGraph|CadProjection|cad_world" crates/editor-actions/src crates/editor-actions/Cargo.toml` -> exit 1, no matches as expected.
- Diff review: `crates/editor-shell/src/lifecycle/commands.rs` has no diff; existing `Command::Delete` fallback behavior, `"edit.delete"` `Shortcut::plain(Key::Delete)`, and Cut paths are unchanged except for new tests asserting existing behavior.
- `git diff --name-only` -> exit 0; modified tracked files are only the allowed source/test/task-brief files.
- `git diff --check` -> exit 0; PowerShell reported the existing LF-to-CRLF warning for `.ai/dispatch.tasks.md`.
- `rg -n "^166\.|^167\.|^168\.|NEEDS_HUMAN_RECORDED" .ai/dispatch.tasks.md` -> exit 0; one task 166 heading, one task 167 heading, no task 168 heading. Historical and carried-forward `NEEDS_HUMAN_RECORDED` references remain provenance/instruction text; no task-166 direct marker was appended.

The orchestrator canonical `.ai/dispatch.verify.ps1` gate was not run manually in this executor session.

## Deviations from Task Packet

None - execution stayed strictly within the Task Packet scope. The EXEC sidecar is allowed by the active packet, so this report will be finalized.

## Open Questions for Reviewer

None. Suggested review focus: confirm the new command is strictly additive and that the generic Delete/Cut behavior did not drift.

## Worktree State

- Tracked files: modified but uncommitted:
  - `.ai/dispatch.tasks.md`
  - `crates/editor-egui-host/src/menu_tests.rs`
  - `crates/editor-egui-host/src/shortcut_help.rs`
  - `crates/editor-shell/src/lifecycle/mod.rs`
  - `crates/editor-shell/src/lifecycle/tests.rs`
  - `crates/editor-shell/src/render_path.rs`
  - `crates/editor-ui/src/menus/command.rs`
  - `crates/editor-ui/src/menus/default_menu.rs`
  - `crates/editor-ui/src/menus/predicate.rs`
  - `crates/editor-ui/tests/menus_ordering.rs`
- Untracked items:
  - `ai_handoffs/ISSUE-427_EXEC_2026-06-20_12-29-33+0300.md` (this report; `.meta.json` generated on finalize)
  - `ai_handoffs/ISSUE-427_TASK_2026-06-20_12-13-45+0300.md`
  - `ai_handoffs/ISSUE-427_TASK_2026-06-20_12-13-45+0300.meta.json`
  - `ai_handoffs/claims/ISSUE-427_2026-06-20_12-13-43-6404894+0300_claim.json`
- Branch: `ai-dispatch/ISSUE-427`
- Last commit: `4c9da19` Amend task 166 after plan-gate failure: Rule-8 falsifying searches + egui-host test-fixture carve-out

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-427
AUTHOR: Executor / Codex
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---

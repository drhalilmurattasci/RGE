# Task Packet

DISPATCH_ID: ISSUE-116
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-23_11-24-27+0300
RELATED_FILES:
- crates/editor-shell/src/render_frame_e2e_perf.rs
- crates/editor-shell/src/lib.rs
- crates/editor-shell/src/render_path.rs
- crates/editor-shell/src/lifecycle/**
- crates/editor-shell/tests/**
- plans/BASELINE.md
- plans/IMPLEMENTATION.md
- change.md
- Status.md
- HANDOFF.md
- ai_handoffs/ISSUE-116_EXEC_*.md
STATUS: OPEN

## Goal

Run the GitHub issue #116 read-only audit for the `crates/editor-shell` render-frame perf harness reconciliation. The Executor must compare the committed `render_frame_e2e_perf` harness and its gated crate exposure against the V0 and post-V0 deferral record, then produce one evidence-backed EXEC report answering whether the deferral text is stale, still current, or narrower than the current harness and whether the repository has committed recorder-host result evidence.

## Scope

### MAY edit
- None.

### MUST NOT edit
- `crates/editor-shell/**`
- `plans/BASELINE.md`
- `plans/IMPLEMENTATION.md`
- `change.md`
- `Status.md`
- `HANDOFF.md`
- Any other source file.
- Any test file.
- Any documentation file.
- `Cargo.toml`
- `Cargo.lock`
- Workspace dependency declarations, feature flags, lint configuration, CI/workflow files, scripts, schemas, lint files, `.gitignore`, ADRs, or status documents.
- Any existing handoff packet, including this TASK packet.
- Any handoff packet other than the single required `ai_handoffs/ISSUE-116_EXEC_*.md` execution report.

### MAY add new files
- Exactly one execution report matching `ai_handoffs/ISSUE-116_EXEC_*.md`.
- The matching `ai_handoffs/ISSUE-116_EXEC_*.meta.json` sidecar only if produced by `new-handoff.ps1` or the orchestrator while finalizing that EXEC report.

### MUST NOT add new files
- New source files.
- New test files.
- New docs.
- New fixtures, generated artifacts, screenshots, benchmark outputs, perf logs, Cargo entries, new crates, architecture lints, doctrine docs, ADRs, schemas, scripts, workflow/config files, TASK packets, REVIEW packets, CORRECT packets, or CLOSEOUT packets.

## Deliverables

- One `EXECUTION_REPORT` packet under `ai_handoffs/` and no repository changes other than that report and its optional sidecar.
- A read-only inventory of the committed render-frame perf harness in `crates/editor-shell/src/render_frame_e2e_perf.rs`, including how it is gated or exposed from `crates/editor-shell/src/lib.rs`.
- A read-only path analysis of the measured render-frame code using `crates/editor-shell/src/render_path.rs` and `crates/editor-shell/src/lifecycle/**`, with specific attention to `acquire_depth_view`, `render_frame_to_target`, encoder creation/submission, and whether winit surface acquire/present is included.
- A read-only reconciliation of V0 and post-V0 deferral text from `plans/BASELINE.md`, `plans/IMPLEMENTATION.md`, `change.md`, `Status.md`, and `HANDOFF.md`.
- A recorded-evidence check that distinguishes committed harness code from committed recorder-host result evidence. The Executor must not infer a perf result merely from the harness existing.
- A command log in the EXEC report documenting every grep/search, git history, and file-read command used for the audit, including any fallback if `rg` is unavailable.
- The exact answer section shown below, with all five headings present in this order:

```markdown
## 5-Question Render-Frame Perf Reconciliation Answer Block

Q1. What render-frame perf harness exists today, and what exact production path does it measure?

Q2. Which V0 or post-V0 deferral text is now stale, still accurate, or narrower than the current harness?

Q3. Is there recorded recorder-host evidence for the current harness, or only harness code?

Q4. What invocation, hardware assumptions, and gates should future maintainers use for this harness?

Q5. What is the smallest safe follow-up dispatch: docs-only reconciliation, recorder-host rerun, harness change, or NEEDS_HUMAN?
```

- In Q5, exactly one smallest safe follow-up shape, with proposed allowed files, must-not-touch surfaces, verification gates, and halt conditions, unless the correct result is `NEEDS_HUMAN`.

## Acceptance Criteria

- MUST be a read-only render-frame perf-harness reconciliation audit; do not edit source, tests, docs, Cargo.toml, Cargo.lock, workflows, scripts, or existing packets
- MUST produce a 5-question render-frame perf reconciliation answer block covering existing harness scope, stale-or-current deferral text, recorded evidence, invocation/gates, and smallest follow-up
- MUST inspect crates/editor-shell/src/render_frame_e2e_perf.rs, crates/editor-shell/src/lib.rs, crates/editor-shell/src/render_path.rs, crates/editor-shell/src/lifecycle/**, plans/BASELINE.md, plans/IMPLEMENTATION.md, change.md, Status.md, and HANDOFF.md
- MUST NOT run cargo commands, tests, formatters, architecture lints, .ai/dispatch.verify.ps1, or the release-only perf harness
- MUST distinguish committed harness code from committed recorder-host result evidence
- MUST halt with NEEDS_HUMAN if the smallest follow-up requires a recorder-host measurement run before any repository edit is justified
- MUST halt rather than combine harness source changes and documentation reconciliation in one follow-up dispatch
- Exactly one new EXEC packet is produced for this dispatch.
- The EXEC packet footer contains exactly one line-anchored `HANDOFF_STATUS: COMPLETE` marker and routes `NEXT_ROLE: REVIEWER_AI` when the audit succeeds, unless a halt condition requires `HANDOFF_STATUS: BLOCKED` or `NEEDS_HUMAN`.
- The EXEC packet includes the exact `## 5-Question Render-Frame Perf Reconciliation Answer Block` section and the exact Q1-Q5 headings listed in Deliverables.
- Q1 cites the harness file and names the measured path in terms of `acquire_depth_view`, `render_frame_to_target`, encoder/submit, and explicitly states whether winit surface acquire/present is in or out of scope.
- Q2 cites the stale-or-current baseline, implementation, status, handoff, or change-log text by file and line context, and classifies each as stale, still accurate, or requiring narrower wording.
- Q3 distinguishes committed harness code from committed recorder-host result evidence. The Executor must not infer a result from the harness's existence.
- Q4 records the exact documented invocation, release/ignored-test constraints, variance gate, soft or hard P95 target semantics, and hardware limits of the evidence.
- Q5 names exactly one smallest safe follow-up with proposed allowed files, must-not-touch surfaces, verification gates, and halt conditions, unless the correct outcome is `NEEDS_HUMAN`.
- No cargo build, cargo test, cargo fmt, architecture-lint run, `.ai/dispatch.verify.ps1`, source generation, docs generation, or release-only perf harness run is required or permitted for this read-only reconciliation audit.
- `git status --short --untracked-files=no` is clean before and after writing the EXEC report.

## Constraints / Non-Goals

- Do not run or update the render-frame perf harness.
- Do not run any cargo command, test command, formatter, architecture lint, canonical dispatch verification script, release-only perf harness, or benchmark command.
- Do not edit the harness, render path, lifecycle code, crate exports, tests, docs, plans, status files, manifests, lockfile, workflows, scripts, schemas, lints, or existing packets.
- Do not create a follow-up TASK packet, REVIEW packet, CORRECT packet, CLOSEOUT packet, issue note, design doc, ADR, code comment, benchmark artifact, or measurement log.
- Do not treat comments, harness constants, or expected thresholds as recorder-host result evidence unless a committed artifact records a concrete run result.
- Do not combine harness source changes and documentation reconciliation into one recommended follow-up dispatch.
- Do not commit or push.

## Verification Gates

The Executor MUST run and document each applicable read-only gate in the `EXECUTION_REPORT`:

- `git status --short --untracked-files=no` before audit work, expected empty output for tracked files. If it shows tracked changes, document them and halt rather than fixing them.
- Confirm the mandatory files and directories exist and are read: `crates/editor-shell/src/render_frame_e2e_perf.rs`, `crates/editor-shell/src/lib.rs`, `crates/editor-shell/src/render_path.rs`, `crates/editor-shell/src/lifecycle/**`, `plans/BASELINE.md`, `plans/IMPLEMENTATION.md`, `change.md`, `Status.md`, and `HANDOFF.md`.
- Inventory the scoped editor-shell files. Use `rg --files crates/editor-shell/src crates/editor-shell/tests` if `rg` is available; otherwise use the PowerShell equivalent `Get-ChildItem -Recurse -File -LiteralPath crates/editor-shell/src,crates/editor-shell/tests`.
- Search only the allowed read-only scope for render-frame perf and deferral evidence. Suggested patterns include `render_frame_e2e_perf`, `render_frame`, `render_frame_to_target`, `acquire_depth_view`, `encoder`, `submit`, `present`, `winit`, `perf`, `P95`, `p95`, `recorder`, `baseline`, `defer`, `deferred`, `release`, and `ignored`.
- Use read-only `git log` and `git show` only if needed to determine whether committed recorder-host result evidence exists. Record the exact commands and distinguish source history from result evidence.
- Read only files necessary to answer Q1-Q5 from the allowed scope. Prior `ai_handoffs/` packets may be read only if directly referenced by the harness comments or baseline notes.
- Record every grep/search, git-history, and file-read command used in the EXEC report.
- `git status --short --untracked-files=no` after writing the EXEC packet, expected empty output for tracked files.

No cargo build, cargo test, cargo fmt, architecture-lint run, `.ai/dispatch.verify.ps1`, source generation, docs generation, manual benchmark, release-only perf harness run, or recorder-host measurement run is allowed for this audit-only dispatch.

## Halt Conditions

The Executor MUST halt and write an EXEC packet rather than expanding scope if any of the following occur:

- Answering Q1-Q5 requires running the release-only perf harness, any cargo command, tests, formatters, architecture lints, `.ai/dispatch.verify.ps1`, a benchmark, or a recorder-host measurement run.
- The audit discovers the harness is absent, unreachable, or impossible to understand without source edits. Halt with `NEEDS_HUMAN` rather than implementing or repairing it.
- The smallest follow-up would require a recorder-host measurement run on specific hardware before any repository edit is justified. Halt with `NEEDS_HUMAN`; do not fake, extrapolate, or infer perf evidence.
- Q5 would require changing harness source and documentation in the same follow-up dispatch. Halt rather than combining those work types; source-harness changes and documentation reconciliation must stay separable unless a human explicitly widens scope.
- A truthful Q1-Q5 answer requires editing any source, test, doc, manifest, script, schema, status file, workflow, lint file, existing packet, or non-EXEC handoff file.
- The audit cannot distinguish committed harness code from committed recorder-host result evidence using the allowed read-only evidence.
- Any tracked file is already dirty in a way that makes the read-only audit ambiguous. Do not repair it; report the state and halt.
- Verification or status output reveals a failure or tracked modification outside this dispatch's allowed EXEC packet. Do not fix it; report it and halt.
- A correction round is requested for a verify failure outside this audit scope. The Executor MUST NOT fix the failure, MUST NOT edit unrelated files, and MUST write an EXEC report with `EXEC_STATUS: blocked`, header `STATUS: NEEDS_HUMAN`, footer `HANDOFF_STATUS: NEEDS_HUMAN`, `NEXT_ROLE: HUMAN_ARBITER`, and the exact out-of-scope failure.
- More than one EXEC report would be required to explain the result.

When a `NEEDS_HUMAN` halt condition fires after enough read-only evidence has been gathered, the EXEC report should still include the required five-question block and explain the exact human decision or recorder-host run needed. Use header `STATUS: NEEDS_HUMAN`, footer `HANDOFF_STATUS: NEEDS_HUMAN`, `NEXT_ROLE: HUMAN_ARBITER`, and non-zero `EXIT_CODE`.

## Planner Notes

This task reconciles an apparent mismatch between older V0 or baseline deferral text that says a non-winit editor-shell `render_frame` perf harness remains deferred and the current source tree, which contains `crates/editor-shell/src/render_frame_e2e_perf.rs` gated from `crates/editor-shell/src/lib.rs`. The answer must be documentary, not experimental: classify the committed code and committed evidence that already exist.

If the current harness measures only a narrower offscreen path, the EXEC report should say so directly rather than forcing docs or source changes into the same follow-up. If the repository has harness code but lacks recorder-host result evidence, Q3 and Q5 must keep those facts separate.

Under the orchestrator protocol, no `_REVIEW_` or `_CLOSEOUT_` packet is expected from this TASK. The only new packet produced by execution should be the `ISSUE-116_EXEC_*.md` report and its optional sidecar.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-116
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

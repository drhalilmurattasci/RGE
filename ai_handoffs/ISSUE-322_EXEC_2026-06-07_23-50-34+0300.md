# Execution Report

DISPATCH_ID: ISSUE-322
AUTHOR: Executor / Claude
TIMESTAMP: 2026-06-07_23-50-34+0300
RELATED_FILES:
- plans/BASELINE.md — added §13.3 "2026-06-07 — clean release build hotspot attribution (ISSUE-322)" subsection with ranked cost-driver + remediation tables
- Status.md — added top ISSUE-322 attribution snapshot
- HANDOFF.md — added matching top ISSUE-322 attribution snapshot
- change.md — appended dated ISSUE-322 audit entry
- .ai/dispatch.tasks.md — marked task 86 DONE (heading only)
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-322_TASK_2026-06-07_23-34-23+0300.md`

## Pre-Execution Review Consumed

No pre-execution review issued for this dispatch.

## Current-State Claims: Falsification Re-run

- Claim: Task 86 was the open backlog item for clean release build hotspot attribution, not already DONE.
  Re-run: `Select-String -Path '.ai/dispatch.tasks.md' -Pattern '86. **Phase 9 clean release build hotspot attribution.' -SimpleMatch` -> matched at `.ai/dispatch.tasks.md:7999` with **no `[DONE ...]` marker** before this dispatch. (Now marked DONE by this dispatch.)
- Claim: The current certified clean release miss was 156.591s and remained open for remediation.
  Re-run: `Select-String -Path 'plans/BASELINE.md','Status.md','HANDOFF.md','change.md' -Pattern '156.591' -SimpleMatch` -> matched in the Phase 9 baseline/status/handoff/change records (incl. `plans/BASELINE.md:296`, `:317`), all describing it as a MISS with remediation open. Confirmed before measuring.
- Claim: The brief forbids `cargo clean` and shared `A:\RustCache\target` deletion.
  Re-run: confirmed in TASK §Constraints/Non-Goals and `.ai/dispatch.tasks.md` task-86 allowed-surface block; honored — neither was run.
- Claim: `tools/compile-timing.ps1` is the repeatable non-destructive timing harness.
  Re-run: `Get-Content tools/compile-timing.ps1` -> script exists, supports `-Mode/-Iterations/-Release/-TimeoutSeconds/-JsonPath`, warm-cache/no-`cargo clean` policy, and **exposes no `--timings`/per-unit path** — which is why Cargo `--timings` was used directly for per-unit attribution (explicitly permitted by the TASK Planner Notes).

## What I Changed

### Source

- None. No Rust source, tests, benches, fixtures, Cargo manifests/lock, tooling, workflows, schemas, scheduler config, or dispatch automation were edited or added. This was an attribution/report dispatch only.

### Tests

- None (no source change to test; this is a docs/task-record audit).

### Docs

- `plans/BASELINE.md`: new §13.3 forward-only subsection recording the attribution method + exact commands, the fresh isolated-target clean-release result (147.82s wall / 2m 27s, exit 0, MISS), explicit supersession of 156.591s, a ranked cost-driver table, the critical-path / "bounded by one serial unit" finding, the `cranelift-codegen` ← `wasmtime` ← `rge-expr-wasm`/`rge-runtime-wasmtime-engine` provenance, the ~110s estimated post-remediation floor, and three intentionally-small remediation candidates with risk + automation suitability.
- `Status.md`: top snapshot summarizing the attribution result and next remediation options.
- `HANDOFF.md`: matching top snapshot with the same current-state conclusion and next handoff options.
- `change.md`: dated entry recording the audit, measurement/provenance commands, non-changes, and verification.
- `.ai/dispatch.tasks.md`: task 86 heading marked `[DONE ...]`; no other task entry touched.

## Per-File Summary

The whole tracked diff is documentation/task-record. The measurement evidence (Cargo `--timings` `UNIT_DATA` JSON, the `cargo-timing.html` report, and the build log) is retained under the gitignored `.ai/dispatch-ISSUE-322/` run dir, so it does not enter the tracked diff.

## Measurement Commands Run (with exit codes / artifacts)

Environment for all commands: `CARGO_HOME=A:\RustCache\cargo`, `RUSTUP_HOME=A:\RustCache\rustup` (already-downloaded registry + toolchain reused); `cargo 1.92.0`, `rustc 1.92.0`.

1. `cargo build --workspace --release --timings` with `CARGO_TARGET_DIR=B:\sdk\rge-clean-hotspots-ISSUE-322-20260607-234243` (fresh empty isolated target)
   → **exit 0**; wall **147.82s**; cargo `Finished ... in 2m 27s`; `--timings` Total 147.7s; max concurrency 16 jobs / 16 cores; 685 compile units; 1517.6 CPU-compile-seconds.
   → Artifacts: `cargo-timings/cargo-timing.html` (preserved to `.ai/dispatch-ISSUE-322/cargo-timing-clean-release.html`), build log `.ai/dispatch-ISSUE-322/clean-release-timings-20260607-234243.log`, parsed units `.ai/dispatch-ISSUE-322/unit-data.json`.
2. `cargo tree -i cranelift-codegen -e normal` → **exit 0**; confirms `cranelift-codegen` ← `cranelift-frontend`/`cranelift-native` ← `wasmtime-internal-cranelift` ← `wasmtime` 44.0.1 ← `rge-expr-wasm` + `rge-runtime-wasmtime-engine`.
3. `cargo tree -i wasmtime -e normal` → **exit 0**; confirms `wasmtime` 44.0.1 is pulled by exactly those two workspace crates (+ transitively `rge-script-host` / `rge-script-bench`).

Key attribution numbers: `cranelift-codegen` 0.131.1 single unit = **125.64s** (85% of the 147.66s critical wall), on the critical path 22.02s→147.66s (= build end); wasm/wasmtime/cranelift family = 340.8s CPU over 54 units; next-latest non-WASM tail = `rge-editor` bin link (ends 109.88s) and `rge-tool-architecture-lints` bin link (ends 109.90s) ⇒ estimated post-remediation floor ~110s.

## Verification Results

- `cargo build --workspace --release --timings` (isolated target) → exit 0
- `cargo tree -i cranelift-codegen -e normal` → exit 0
- `cargo tree -i wasmtime -e normal` → exit 0
- `git diff --check` → exit 0
- `git diff --name-only` → exactly `.ai/dispatch.tasks.md`, `HANDOFF.md`, `Status.md`, `change.md`, `plans/BASELINE.md` (confined to the MAY-edit docs surface)
- Isolated scratch-target safety gate → resolved path `B:\sdk\rge-clean-hotspots-ISSUE-322-20260607-234243` verified to start with `B:\sdk\rge-clean-hotspots-ISSUE-322-` **before** deletion; removed after the run (2.28 GB freed); `Test-Path` afterwards = false.
- The full workspace test/bench gate was intentionally NOT added (per TASK Verification Gates — this is a docs/task-record audit, not a source change).

## Deviations from Task Packet

None — execution stayed strictly within the Task Packet scope. Per the Planner Notes, Cargo's `--timings` was used directly for per-unit attribution because `tools/compile-timing.ps1` exposes no per-unit path; this is the explicitly-permitted fallback and used a fresh isolated `CARGO_TARGET_DIR` under `B:\sdk\rge-clean-hotspots-ISSUE-322-*`, never the shared `A:\RustCache\target` and never `cargo clean`.

One discretionary call worth flagging for the Reviewer: a fresh isolated-target clean release build *was* run (exit 0, 147.82s), so under the acceptance criteria it is eligible to supersede 156.591s. I recorded it as **superseding** 156.591s as the *current* clean-release figure (more recent, same recorder host + method, plus attribution), while keeping the historical 156.591s rows intact and noting both runs confirm the MISS and the ~8.8s delta is run-to-run variance. The gate is **not** claimed closed (no ≤120s result).

## Open Questions for Reviewer

- Supersession vs. corroboration: I let the fresh 147.82s become the current clean-release reference (both numbers agree on MISS). If you prefer the 156.591s stay the single "certified" number with 147.82s logged only as corroboration, that is a one-line framing change in the BASELINE/Status/HANDOFF snapshots — no remeasurement needed.
- Remediation ordering: I ranked candidate A (lower `opt-level` for `cranelift-codegen` via a `[profile.release.package]` override) first as the smallest honest slice, on the basis that the build is bounded by that one serial unit and the estimated post-fix floor (~110s) crosses the budget. Please sanity-check that the runtime wasm-JIT-throughput regression risk of lowering Cranelift's own `opt-level` is acceptable enough to file A before B/C — this audit did not benchmark that runtime cost.

## Worktree State

- Tracked files: modified-but-uncommitted — `.ai/dispatch.tasks.md`, `HANDOFF.md`, `Status.md`, `change.md`, `plans/BASELINE.md` (docs/task-record only; not committed, per protocol).
- Untracked items: `ai_handoffs/ISSUE-322_TASK_2026-06-07_23-34-23+0300.md` (+ `.meta.json`), `ai_handoffs/claims/ISSUE-322_2026-06-07_23-34-17-5254300+0300_claim.json` (pre-existing dispatch artifacts), this EXEC report (+ sidecar on finalize), and the gitignored `.ai/dispatch-ISSUE-322/` attribution artifacts. The isolated `B:\sdk\rge-clean-hotspots-ISSUE-322-20260607-234243` scratch target was removed.
- Branch: `ai-dispatch/ISSUE-322`
- Last commit: `b97eeb7` ai-dispatch ISSUE-321: Measure Phase 9 incremental p95 build time

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-322
AUTHOR: Executor / Claude
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---

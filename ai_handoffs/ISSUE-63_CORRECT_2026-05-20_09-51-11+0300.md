# Correction Packet

DISPATCH_ID: ISSUE-63
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_09-51-11+0300
RELATED_FILES:
- crates/editor-ui/tests/node_graph_operator_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-63_TASK_2026-05-20_09-42-26+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-63_EXEC_2026-05-20_09-50-18+0300.md`
- Latest Review Report: none; the orchestrator's canonical post-execution verification gate failed before Codex control review.
- Prior CORRECT packets in this dispatch: none.
- Verification failure: `.ai/dispatch.verify.ps1` step `[1] cargo +nightly fmt --check`, command `cargo +nightly fmt --all -- --check`, rejected `crates/editor-ui/tests/node_graph_operator_smoke.rs` for incorrect newline style.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing less.

1. **Normalize rustfmt newline style** - Post-execution verification gate failure, step `[1] cargo +nightly fmt --check`. Required change: in `crates/editor-ui/tests/node_graph_operator_smoke.rs`, normalize only the file newline style to the form accepted by `cargo +nightly fmt --all -- --check`. Do not change Rust tokens, assertions, imports, comments, test behavior, or any other file. Acceptance: `cargo +nightly fmt --all -- --check` exits 0, and the full canonical verification gate exits 0.

## Deferred Findings (NOT Approved for This Round)

None. No Codex control review findings exist because verification failed before control review.

## Updated Acceptance Criteria

Unchanged from the Task Packet except for this correction-only allowance: `crates/editor-ui/tests/node_graph_operator_smoke.rs` may be touched solely to normalize newline style, with no semantic or textual content change beyond line endings. The dispatch is not acceptable until the canonical verification gate exits 0.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `cargo test -p rge-editor-ui --test node_graph_phase8_three_domain_smoke` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0.

## Halt Conditions (Updated if Any)

- Halt if fixing the newline-style failure would require any edit other than line-ending normalization in `crates/editor-ui/tests/node_graph_operator_smoke.rs`.
- Halt if `cargo +nightly fmt --all -- --check` reports additional files or requires semantic formatting changes outside this approved correction.
- Halt if the full canonical verification gate fails after the newline normalization for any reason not directly caused by this approved correction; report the exact failing step in the next execution report instead of broadening the fix.

## Planner Notes

This correction is intentionally limited to the verification blocker reported by the orchestrator. The original ISSUE-63 deliverable remains the new three-domain editor-ui smoke; no production code, manifests, documentation, scripts, schemas, or additional packets are approved for modification in this correction round.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-63
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Correction Packet

DISPATCH_ID: ISSUE-78
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_22-46-18+0300
RELATED_FILES:
- kernel/graph-foundation/tests/diff_correctness.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-78_TASK_2026-05-20_22-34-54+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-78_EXEC_2026-05-20_22-44-50+0300.md`
- Latest Review Report: none; the post-execution verification gate failed before Codex control review.
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections, nothing more, nothing
less. Each correction references the specific verification finding it
addresses.

1. **Normalize newline style in `diff_correctness.rs`** - Post-execution
   verification finding: `cargo +nightly fmt --all -- --check` failed with
   `Incorrect newline style in \\?\A:\RCAD\RGE\kernel\graph-foundation\tests\diff_correctness.rs`.
   Required change: convert only `kernel/graph-foundation/tests/diff_correctness.rs`
   to the newline style accepted by `rustfmt`, preserving the file's Rust
   content and behavior. Do not edit `docs/adr/ADR-097-cad-projection-module-split.md`,
   any `cad-projection` source, Cargo files, docs, schemas, scripts, or any
   other handoff packet as part of this correction. Acceptance:
   `cargo +nightly fmt --all -- --check` exits 0, and any diff for
   `diff_correctness.rs` is line-ending-only.

## Deferred Findings (NOT Approved for This Round)

The following findings from the Review Report are NOT approved for this
correction round. They are recorded here as latent risks to be carried
forward in `FINAL_CLOSEOUT`.

1. None.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with one correction-only allowance: the
Executor may normalize newline style in
`kernel/graph-foundation/tests/diff_correctness.rs` solely to satisfy the
canonical formatter gate. No content, behavior, test semantics, production
code, manifest, schema, script, documentation, ADR, or additional handoff
packet changes are approved.

## Re-Verification Gates

The Executor MUST re-run these gates after the corrections:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, plus halt if satisfying the formatter failure
requires any edit other than newline-style normalization in
`kernel/graph-foundation/tests/diff_correctness.rs`, or if the canonical
verification gate still fails after that normalization.

## Planner Notes

The dispatch implementation itself is not being expanded. The only blocker
reported by the post-execution verification gate is a repository formatting
failure caused by newline style in `diff_correctness.rs`. Keep the correction
mechanical and line-ending-only, then rerun the canonical gate so the dispatch
can proceed to control review.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-78
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Correction Packet

DISPATCH_ID: ISSUE-25
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18T19:01:56+03:00
RELATED_FILES:
- crates/cad-core/tests/round_fillet_brep_chain_resolution_smoke.rs
- crates/cad-projection/tests/round_fillet_brep_face_id_lookup_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-25_TASK_2026-05-18_18-53-26+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-25_EXEC_2026-05-18_19-01-15+0300.md`
- Latest Review Report: none; post-execution canonical verification failed before control review.
- Prior Correction Packets: none.
- Verification Failure: `.ai/dispatch-ISSUE-25/verification.round0.log`, step `[1] cargo +nightly fmt --check`, command `cargo +nightly fmt --all -- --check`.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. Each correction references the specific verification failure it addresses.

1. **Apply rustfmt layout to the new ISSUE-25 smoke** - Verification gate
   failure: `cargo +nightly fmt --all -- --check` reported formatting diffs
   in `crates/cad-core/tests/round_fillet_brep_chain_resolution_smoke.rs`
   around lines 94 and 115. Required change: edit only this file to apply the
   exact rustfmt-compatible line wrapping shown by the verification log:
   keep the `brep_edge_ids_for_node(...).expect(...)` expression on the
   rustfmt-preferred two-line form and collapse the `resolve_round_fillet_chain`
   call in the radius loop to rustfmt's preferred wrapping. Do not change test
   assertions, selected edges, radii, imports, helper behavior, public API
   usage, or semantic code. Acceptance: `cargo +nightly fmt --all -- --check`
   reports no diff for this file and the ISSUE-25 targeted smoke still exits 0.
2. **Normalize newline style in the prior cad-projection smoke** - Verification
   gate failure: `cargo +nightly fmt --all -- --check` reported `Incorrect
   newline style in \\?\A:\RCAD\RGE\crates\cad-projection\tests\round_fillet_brep_face_id_lookup_smoke.rs`.
   Required change: normalize only the line endings/newline style of
   `crates/cad-projection/tests/round_fillet_brep_face_id_lookup_smoke.rs` to
   the rustfmt-accepted repository style. Do not change Rust tokens, imports,
   comments, assertions, test structure, behavior, Cargo metadata, scripts,
   docs, or any other file. Acceptance: `cargo +nightly fmt --all -- --check`
   exits 0 and any diff for this file is limited to newline-style
   normalization.

## Deferred Findings (NOT Approved for This Round)

None. The post-execution verification log reported only the rustfmt layout
diffs in the new ISSUE-25 smoke and the rustfmt newline-style blocker in the
prior cad-projection smoke. No semantic, architecture, supply-chain, workspace
test, or doctest finding is approved for correction in this round.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with this verification-only addition: the
repository rustfmt gate and full canonical dispatch verification must pass after
the two format-only fixes listed above.

## Re-Verification Gates

The Executor MUST re-run these gates after the corrections:

- `cargo +nightly fmt --all -- --check` -> expected exit 0
- `cargo test -p rge-cad-core --test round_fillet_brep_chain_resolution_smoke -- --nocapture` -> expected exit 0 with 2 passed / 0 failed / 0 ignored
- `.\.ai\dispatch.verify.ps1` -> expected exit 0
- `git diff -- crates/cad-core/tests/round_fillet_brep_chain_resolution_smoke.rs crates/cad-projection/tests/round_fillet_brep_face_id_lookup_smoke.rs` -> expected only the approved rustfmt layout and newline-style changes above

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, plus:

- If passing rustfmt requires any semantic source, test assertion, fixture,
  selected-edge, radius, import, Cargo metadata, script, schema, or
  documentation change, halt and route back to Planner.
- If the newline-style correction requires changing Rust tokens in
  `crates/cad-projection/tests/round_fillet_brep_face_id_lookup_smoke.rs`,
  halt and route back to Planner.
- If the canonical verification gate exposes a new, distinct blocking failure
  after these two format-only fixes, halt and preserve the failing output.

## Planner Notes

The ISSUE-25 implementation itself passed the targeted smoke before the
canonical gate stopped at rustfmt. This correction does not reopen RoundFillet
identity behavior, resolver behavior, cad-projection semantics, or test
coverage design. The only approved work is to make the working tree satisfy the
repository format gate: apply rustfmt's reported wrapping in the new ISSUE-25
cad-core smoke and normalize the newline style of the cad-projection smoke
named by the same fmt check.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-25
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

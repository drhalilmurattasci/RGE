# Correction Packet

DISPATCH_ID: ISSUE-21
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18_15-29-08+0300
RELATED_FILES:
- crates/cad-projection/tests/sweep_brep_face_id_lookup_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-21_TASK_2026-05-18_15-20-49+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-21_EXEC_2026-05-18_15-28-21+0300.md`
- Latest Review Report: none. The orchestrator's post-execution verification gate failed before Codex control review.
- Prior CORRECT packets in this dispatch: none.
- Verification failure: `.ai/dispatch.verify.ps1` failed at `cargo +nightly fmt --all -- --check` with rustfmt diffs in `crates/cad-projection/tests/sweep_brep_face_id_lookup_smoke.rs`.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. Each correction references the verification finding it addresses.

1. **Apply rustfmt to the new sweep projection smoke test** - Verification
   gate finding: `cargo +nightly fmt --all -- --check` reported formatting
   drift in `crates/cad-projection/tests/sweep_brep_face_id_lookup_smoke.rs`.
   Required change: reformat only this test file to the exact style produced by
   `cargo +nightly fmt --all`, including the line wrap around the
   `direct_pairs` assignment near line 104 and the `direct_ids` method chain
   near line 134. Do not change assertions, helper logic, test names,
   production code, manifests, docs, scripts, or any other file. Acceptance:
   `cargo +nightly fmt --all -- --check` exits 0 and the test behavior remains
   unchanged.

## Deferred Findings (NOT Approved for This Round)

The following findings from the verification result are NOT approved for this
correction round. They are recorded here as latent risks to be carried forward
in `FINAL_CLOSEOUT`.

1. None. No other review, control, or verification findings are approved for
   this correction round.

## Updated Acceptance Criteria

Unchanged from Task Packet. This correction adds only the requirement that the
new test file be rustfmt-compliant so the canonical verification gate can pass.

## Re-Verification Gates

The Executor MUST re-run these gates after the corrections:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `cargo test -p rge-cad-projection --test sweep_brep_face_id_lookup_smoke` -> expected exit 0.
- `.\.ai\dispatch.verify.ps1` -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from Task Packet. Additionally, halt and report back if making the
formatting gate pass requires any edit outside
`crates/cad-projection/tests/sweep_brep_face_id_lookup_smoke.rs`.

## Planner Notes

The first failing canonical verification step is purely formatting. The diff in
the verification log shows rustfmt-only wrapping changes, so the approved
correction is limited to formatting the single test file introduced by this
dispatch. Do not broaden the correction into test redesign or production
changes.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-21
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

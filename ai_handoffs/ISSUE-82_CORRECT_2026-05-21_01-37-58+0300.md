# Correction Packet

DISPATCH_ID: ISSUE-82
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-21_01-37-58+0300
RELATED_FILES:
- kernel/graph-foundation/tests/diff_correctness.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-82_TASK_2026-05-21_01-31-13+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-82_EXEC_2026-05-21_01-37-00+0300.md`
- Latest Review Report: none generated. The post-execution canonical verification gate failed before Codex control review.
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. Each correction references the failed post-execution verification gate.

1. **Normalize diff_correctness.rs newline style** - Verification gate finding:
   `cargo +nightly fmt --all -- --check` failed with
   `Incorrect newline style in \\?\A:\RCAD\RGE\kernel\graph-foundation\tests\diff_correctness.rs`.
   Required change: normalize only the line endings in
   `kernel/graph-foundation/tests/diff_correctness.rs` to the newline style
   accepted by `rustfmt`. Do not change Rust tokens, formatting, comments,
   imports, helper code, test behavior, or any other file. Acceptance:
   `cargo +nightly fmt --all -- --check` exits 0, and
   `git diff --ignore-space-at-eol -- kernel/graph-foundation/tests/diff_correctness.rs`
   shows no content diff beyond newline normalization.

## Deferred Findings (NOT Approved for This Round)

None. No Codex control review ran because the canonical verification gate failed
first, and no review findings were produced.

## Updated Acceptance Criteria

Unchanged from the Task Packet except for this correction-specific allowance:
`kernel/graph-foundation/tests/diff_correctness.rs` may receive a
line-ending-only normalization so the canonical verification gate can pass. The
existing ISSUE-82 test addition in `crates/script-graph/src/lib.rs` must not be
expanded, rewritten, or otherwise changed.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0
- `git diff --ignore-space-at-eol -- kernel/graph-foundation/tests/diff_correctness.rs` -> expected no content diff beyond newline normalization
- `cargo test -p rge-script-graph` -> expected exit 0
- `.\.ai\dispatch.verify.ps1` -> expected exit 0

If `.\.ai\dispatch.verify.ps1` fails after the format step for an unrelated
pre-existing issue, stop and report the exact failing step in the new
`EXECUTION_REPORT` without expanding scope.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, plus:

- Halt if making the format gate pass requires semantic Rust changes,
  implementation changes, test assertion changes, Cargo manifest changes, or
  edits outside `kernel/graph-foundation/tests/diff_correctness.rs`.
- Halt if the newline-style correction cannot be confined to
  `kernel/graph-foundation/tests/diff_correctness.rs`.

## Planner Notes

This correction is deliberately narrow. The ISSUE-82 execution report states
that the requested `ScriptGraph` removed-edge record preservation test was
added in `crates/script-graph/src/lib.rs` and the scoped diff checks passed.
The only known blocker is the canonical format gate reporting an incorrect
newline style in `kernel/graph-foundation/tests/diff_correctness.rs`.
Normalize that file's newline style only, preserve the existing ISSUE-82 test
content, and rerun the canonical verification gate.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-82
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

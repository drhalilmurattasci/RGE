# Correction Packet

DISPATCH_ID: ISSUE-72
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_17-50-41+0300
RELATED_FILES:
- crates/editor-ui/tests/phase8_graph_diff_smoke.rs
- crates/script-graph/src/lib.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-72_TASK_2026-05-20_17-29-21+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-72_EXEC_2026-05-20_17-49-27+0300.md`
- Latest Review Report: none; the canonical post-execution verification gate failed before Codex control review.
- Prior CORRECT packets in this dispatch: none.
- Verification failure: `.ai/dispatch.verify.ps1` step 1, `cargo +nightly fmt --check`, reported a rustfmt diff in `crates/editor-ui/tests/phase8_graph_diff_smoke.rs` and incorrect newline style in `crates/script-graph/src/lib.rs`.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections: nothing more, nothing less.

1. **Apply rustfmt to the Phase 8 GraphDiff smoke test** - Verification gate finding `cargo +nightly fmt --check` failed with a diff in `crates/editor-ui/tests/phase8_graph_diff_smoke.rs` around the `diff.removed_nodes.is_empty()` and `diff.removed_edges.is_empty()` assertions. Required change: format `crates/editor-ui/tests/phase8_graph_diff_smoke.rs` so `cargo +nightly fmt --all -- --check` accepts it, preserving the existing ScriptGraph test behavior and assertions. Acceptance: the rustfmt diff for this file disappears and `cargo +nightly fmt --all -- --check` exits 0.
2. **Normalize ScriptGraph library newline style** - Verification gate finding `cargo +nightly fmt --check` reported `Incorrect newline style in \\?\A:\RCAD\RGE\crates\script-graph\src\lib.rs`. Required change: normalize `crates/script-graph/src/lib.rs` to the newline style expected by `cargo +nightly fmt --all -- --check`, using `cargo +nightly fmt --all` or an equivalent formatting-only rewrite. Do not change ScriptGraph behavior, public API, tests, manifests, docs, scripts, schemas, or any file outside the two files listed in `RELATED_FILES`, except for the protocol-required follow-up execution report. Acceptance: the newline-style error disappears and `cargo +nightly fmt --all -- --check` exits 0.

## Deferred Findings (NOT Approved for This Round)

None. No Codex control review findings exist because the canonical verification gate failed before control review.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with this correction-round gate added: the canonical dispatch verification gate must pass after the formatting and newline-style normalization.

## Re-Verification Gates

The Executor MUST re-run these gates after the corrections:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `cargo test -p rge-editor-ui --test phase8_graph_diff_smoke -- --nocapture` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet. Additionally, halt and report `BLOCKED` if making `cargo +nightly fmt --all -- --check` pass requires any semantic source change, production API change, manifest edit, documentation edit, script edit, schema edit, packet edit other than the Executor's own execution report, or any file edit outside `crates/editor-ui/tests/phase8_graph_diff_smoke.rs` and `crates/script-graph/src/lib.rs`.

## Planner Notes

The executed test change appears to have reached the required behavior, but the dispatch cannot proceed while the canonical post-execution verification gate fails. This correction approves only formatting and newline-style repairs required by the failed fmt gate. It does not authorize any broader ScriptGraph, GraphDiff, editor-ui, manifest, documentation, architecture, or test-behavior changes.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-72
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

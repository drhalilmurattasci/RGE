# Correction Packet

DISPATCH_ID: ISSUE-254
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-28_23-30-28+0300
RELATED_FILES:
- ai_handoffs/ISSUE-254_TASK_2026-05-28_22-57-20+0300.md
- ai_handoffs/ISSUE-254_EXEC_2026-05-28_23-06-49+0300.md
- ai_handoffs/ISSUE-254_CORRECT_2026-05-28_23-30-28+0300.md
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-254_TASK_2026-05-28_22-57-20+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-254_EXEC_2026-05-28_23-06-49+0300.md`
- Control Review: `.ai/dispatch-ISSUE-254/codex.control.round0.json`
- Prior CORRECT packets in this dispatch: none

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections, nothing more and nothing less. This is still an audit-only correction round.

1. **Correct `MenuRegistry` search evidence** - Control review finding: the EXEC report claims zero `MenuRegistry` matches across editor-shell/editor surfaces, but current source has a doc-comment hit at `crates/editor-shell/src/play_toolbar.rs:12`. Required change: write a corrected execution report that does not make a broad zero-match claim for literal `MenuRegistry`. If retaining the conclusion that menu registry execution is absent from the editor runtime surface, narrow the claim to functional construction/resolution evidence and explicitly cite the doc-only hit. Acceptance: the corrected report notes the `play_toolbar.rs:12` doc-comment mention separately, and supports the functional negative claim with narrower searches such as `MenuRegistry::`, `ResolvedEntry`, `menus::Command`, and `resolve(` in `crates/editor-shell/src/**/*.rs`, `editor/rge-editor/**/*.rs`, and `crates/editor-egui-host/**/*.rs`.

2. **Correct save-direction search evidence** - Control review finding: the EXEC report says `fs::write` has zero matches under `editor/rge-editor/src/` and `crates/editor-shell/src/`, but `editor/rge-editor/src/main.rs` contains multiple `std::fs::write(...)` test/helper fixture writes. Required change: write a corrected execution report that does not use a literal zero `fs::write` claim to support runtime save-direction absence. Re-scope the negative conclusion to runtime project/scene save or RON serialization symbols, and cite the fixture/helper `std::fs::write(...)` hits as non-semantic for live editor save behavior. Acceptance: the corrected report distinguishes test/helper fixture writes from runtime save code, and supports the save-direction conclusion with exact scoped searches for symbols such as `ron::ser`, `ron::to_string`, `save_project`, `save_scene`, `save_to`, and `write_to_file`.

3. **Recheck other broad negative claims for doc/test/helper hits** - Control review finding: the same pattern may exist elsewhere in the audit. Required change: review all other negative current-state claims in the corrected report and keep them only when the exact command, scope, and result support the wording. Do not report broad zero-result patterns when current source has doc-comment, test, or helper hits; qualify those hits separately. Acceptance: every retained zero-result or negative current-state claim in the corrected report is scoped precisely enough to remain true against current source, including any exclusions for doc comments, tests, helper fixtures, string-literal IDs, or unrelated API-internal consumers.

## Deferred Findings (NOT Approved for This Round)

1. **Source, docs, scripts, schemas, and prior packet edits** - Not approved. Deferred because the control review only requires correcting the audit deliverable. Future trigger / dispatch: a separate Planner packet may authorize source or docs reconciliation after this audit passes review.

## Updated Acceptance Criteria

Unchanged from Task Packet, with one clarification: the deliverable must be a corrected EXECUTION_REPORT that preserves the audit-only scope and corrects or qualifies the inaccurate negative search claims identified above.

## Re-Verification Gates

The Executor MUST re-run these gates after the corrections:

- `git status --short --branch -uall` -> expected exit 0; tracked files remain unmodified, with only dispatch-owned handoff/log artifacts untracked.
- `git diff --stat` and `git diff --name-only` -> expected exit 0; no tracked source/docs/script/schema diff.
- `git diff --check` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0 with `VERIFY OK`.
- Targeted evidence checks sufficient to support the corrected report wording:
  - literal `MenuRegistry` search must report the `crates/editor-shell/src/play_toolbar.rs:12` doc-comment hit;
  - functional menu searches such as `MenuRegistry::`, `ResolvedEntry`, `menus::Command`, and `resolve(` must be scoped and reported exactly;
  - literal `fs::write` search must report the `editor/rge-editor/src/main.rs` test/helper fixture hits;
  - runtime save / RON serialization searches such as `ron::ser`, `ron::to_string`, `save_project`, `save_scene`, `save_to`, and `write_to_file` must be scoped and reported exactly.

## Halt Conditions (Updated if Any)

Unchanged from Task Packet. Additionally, halt with `NEEDS_HUMAN` if satisfying these corrections would require editing production source, existing docs, scripts, schemas, prior packets, or any file outside the corrected EXEC report and normal dispatch-owned sidecars/log artifacts.

## Planner Notes

The audit conclusions may still stand after narrowing the evidence. The issue is not that `MenuRegistry` is functionally wired or that runtime project/scene save exists; the issue is that the EXEC report overstates several search results as literal zero matches when current source contains doc-comment and test/helper hits. Correct the report by preserving only claims backed by exact scoped commands and by separating non-runtime hits from runtime behavior.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-254
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Task Packet

DISPATCH_ID: ISSUE-13
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18_08-10-13+0300
RELATED_FILES:
- crates/audio/src/falloff.rs
STATUS: OPEN

## Goal

Implement GitHub issue #13 by adding focused test-only coverage for `AudioFalloff` in `crates/audio/src/falloff.rs`. The dispatch should cover every `AudioFalloff::to_kira_easing` variant mapping, including the currently missing `Logarithmic` and `Custom` cases, and prove that `AudioFalloff::Custom` with a negative exponent produces a finite, non-negative amplitude without changing runtime behavior.

## Scope

### MAY edit
- `crates/audio/src/falloff.rs`, only inside the existing `#[cfg(test)] mod tests`.

### MUST NOT edit
- Any file other than `crates/audio/src/falloff.rs`.
- Any production/runtime code in `crates/audio/src/falloff.rs`, including `AudioFalloff`, `AudioFalloff::amplitude`, and `AudioFalloff::to_kira_easing`.
- Cargo manifests, lockfiles, schemas, scripts, docs, CI files, `.gitignore`, or existing handoff packets.

### MAY add new files
- One protocol-required execution report matching `ai_handoffs/ISSUE-13_EXEC_*.md`.
- The matching execution-report `.meta.json` sidecar if generated by `new-handoff.ps1 -Finalize`.

### MUST NOT add new files
- No new source files, integration tests, fixtures, docs, schemas, scripts, Cargo entries, or architecture-lint files.
- No handoff packets other than the protocol-required `ISSUE-13_EXEC` report and its finalize-generated sidecar.

## Deliverables

- Extend the existing `easing_map_is_total` test, or the equivalent existing `AudioFalloff::to_kira_easing` assertions, so all four variants are covered: `Linear`, `Logarithmic`, `InverseSquare`, and `Custom`.
- Add coverage asserting the exact Kira easing family and payload expected for `Logarithmic` and `Custom`, using the existing local assertion style.
- Add one unit test for `AudioFalloff::Custom` with a negative exponent at an in-range distance that asserts the returned amplitude is finite and non-negative.
- A completed execution report packet for this dispatch.

## Acceptance Criteria

- The only implementation file changed is `crates/audio/src/falloff.rs`.
- All implementation changes are test-only and remain inside the existing `#[cfg(test)] mod tests`.
- `easing_map_is_total` or its replacement asserts all four `to_kira_easing` mappings:
  - `AudioFalloff::Linear` maps to `kira::Easing::Linear`.
  - `AudioFalloff::Logarithmic` maps to `kira::Easing::OutPowf(2.5)`.
  - `AudioFalloff::InverseSquare` maps to `kira::Easing::OutPowi(2)`.
  - `AudioFalloff::Custom(exp)` maps to `kira::Easing::OutPowf(exp as f64)` for a positive test exponent.
- A negative-exponent `Custom` amplitude test exists and asserts at minimum `amp.is_finite()` and `amp >= 0.0`.
- No runtime behavior, public API, serialization, documentation, dependency, or Cargo configuration changes are made.
- Existing `falloff` tests continue to pass.

## Constraints / Non-Goals

- This is a test-only dispatch. Do not modify `AudioFalloff` runtime semantics to satisfy the new tests.
- Do not change how negative custom exponents are clamped or converted; the tests should document existing behavior.
- Do not add new dependencies, fixtures, integration tests, or helper modules.
- Do not broaden the task into audio engine behavior, Kira spatializer behavior, distance units, or falloff formula changes.
- Do not rewrite unrelated tests in `falloff.rs` except for the minimal adjustment needed to extend the existing easing assertions.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `cargo +nightly fmt --check -p rge-audio` -> expected exit 0.
- `cargo test -p rge-audio --lib falloff --no-fail-fast` -> expected exit 0, including the expanded easing coverage and the negative-exponent `Custom` amplitude test.
- `cargo test -p rge-audio --lib --no-fail-fast` -> expected exit 0.
- `git diff -- crates/audio/src/falloff.rs` -> expected diff is limited to test code inside `#[cfg(test)] mod tests`.
- `git diff --check -- crates/audio/src/falloff.rs` -> expected exit 0.

If a required command cannot be run in the environment, the Executor MUST document the exact command, the failure reason, and any partial verification that was completed.

## Halt Conditions

The Executor MUST halt without modifying runtime behavior and request fresh guidance if any of the following occur:

- The requested tests fail against the existing production implementation and passing them would require changing `AudioFalloff` runtime code.
- Any required edit outside `crates/audio/src/falloff.rs` appears necessary.
- Any edit outside the existing `#[cfg(test)] mod tests` in `crates/audio/src/falloff.rs` appears necessary.
- Any new dependency, fixture, integration test file, Cargo manifest change, or generated implementation artifact appears necessary.
- The Kira `Easing` variant names or payload types differ from the current assumptions and cannot be asserted cleanly with test-only changes.
- Formatting or tests fail for reasons unrelated to the test-only changes and cannot be isolated within the allowed scope.

## Planner Notes

This task was filed automatically by `Invoke-AiDispatchAuto.ps1` from `.ai/dispatch.tasks.md` for GitHub issue #13. Revision number: 0. There is no prior Claude gate result. Keep the implementation narrow: this dispatch adds tests for existing `AudioFalloff` behavior only.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-13
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

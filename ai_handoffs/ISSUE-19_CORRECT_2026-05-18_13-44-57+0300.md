# Correction Packet

DISPATCH_ID: ISSUE-19
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18T13:44:57+03:00
RELATED_FILES:
- crates/cad-core/src/operators/sweep.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-19_TASK_2026-05-18_13-21-39+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-19_EXEC_2026-05-18_13-43-54+0300.md`
- Latest Review Report: none; post-execution canonical verification failed before control review.
- Prior Correction Packets: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. Each correction references the specific verification failure it
addresses.

1. **Add `sweep.rs` split exemption** - Verification gate failure:
   `cargo run -q -p rge-tool-architecture-lints -- all` failed
   `[split-exemption]` because `crates/cad-core/src/operators/sweep.rs` is
   over 1000 lines and has no `// SPLIT-EXEMPTION:` annotation. Required
   change: edit only `crates/cad-core/src/operators/sweep.rs` to add a
   concise top-of-file `// SPLIT-EXEMPTION:` annotation, matching the style
   used by neighboring cohesive operator files, explaining why the Sweep
   operator implementation, face provider, new edge provider, and local tests
   remain one cohesive substrate after ISSUE-19. Do not change Rust APIs,
   geometry, validation, face-label order, face-ID derivation, edge-ID
   derivation, resolver behavior, tests, architecture-lint code, or any other
   file. Acceptance: the split-exemption lint reports 0 violations and the
   canonical dispatch verification gate exits 0.

## Deferred Findings (NOT Approved for This Round)

None. The post-execution verification log reported one blocking failure, the
`sweep.rs` split-exemption annotation gap above. No other correction is
approved for this round.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with this verification-only addition: the
repository architecture lint suite must pass after adding the required
split-exemption annotation to `crates/cad-core/src/operators/sweep.rs`.

## Re-Verification Gates

The Executor MUST re-run these gates after the corrections:

- `cargo run -q -p rge-tool-architecture-lints -- all` -> expected exit 0
- `.\.ai\dispatch.verify.ps1` -> expected exit 0

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, plus:

- If passing the split-exemption lint requires editing any file other than
  `crates/cad-core/src/operators/sweep.rs`, halt and route back to Planner.
- If the correction requires semantic Rust changes, resolver changes, test
  changes, architecture-lint changes, or generated artifact changes rather
  than the annotation-only fix above, halt and route back to Planner.
- If the canonical verification gate exposes a new, distinct blocking failure
  after this annotation is added, halt and preserve the failing output.

## Planner Notes

The canonical verification gate already passed rustfmt before failing in
architecture lints. The blocking issue is narrow: the ISSUE-19 Sweep edge-ID
work kept the operator implementation, direct face provider, direct edge
provider, and local tests together in `sweep.rs`, pushing the file over the
split-exemption threshold. This packet does not approve splitting the module,
changing Sweep behavior, changing tests, or touching architecture-lint
implementation.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-19
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Task Packet

DISPATCH_ID: ISSUE-82
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-21_01-31-13+0300
RELATED_FILES:
- crates/script-graph/src/lib.rs
STATUS: OPEN

## Goal

Add focused unit-test coverage for GitHub issue #82: the `ScriptGraph` wrapper must preserve full old edge records when graph-foundation diffs report a removed edge. The new smoke test should exercise the existing private substrate access available inside `crates/script-graph/src/lib.rs`'s `cfg(test)` module and prove that `GraphDiff::between` reports the removed script edge with its old `src`, `dst`, and `ScriptEdge` payload intact, without unrelated added or changed diff noise.

## Scope

### MAY edit
- `crates/script-graph/src/lib.rs`

### MUST NOT edit
- Any file outside `crates/script-graph/src/lib.rs`, except for the protocol-required execution report packet and its finalize-generated sidecar.
- Production code outside the existing `#[cfg(test)] mod tests` in `crates/script-graph/src/lib.rs`.
- `kernel/graph-foundation/**`, any graph-foundation implementation, or any other domain graph crate.
- Cargo manifests, lockfiles, workspace configuration, schemas, scripts, docs, CI files, `.gitignore`, generated files, or any existing handoff packet.

### MAY add new files
- One protocol-required execution report matching `ai_handoffs/ISSUE-82_EXEC_*.md`.
- The matching execution-report `.meta.json` sidecar if generated by `new-handoff.ps1 -Finalize`.

### MUST NOT add new files
- No new Rust source files, integration test files, fixtures, snapshots, docs, schemas, scripts, Cargo entries, or architecture-lint files.
- No handoff packets other than the protocol-required `ISSUE-82_EXEC` report and its finalize-generated sidecar.

## Deliverables

- Add exactly one new additive `#[test]` function inside the existing `#[cfg(test)] mod tests` in `crates/script-graph/src/lib.rs`.
- The test builds a `ScriptGraph` with exactly two script nodes and one `ScriptEdge`.
- The test captures the old state with `GraphSnapshot::from_graph(&g.graph)`.
- The test removes only that edge through the private substrate, using the edge id returned by `ScriptGraph::connect` and `g.graph.remove_edge(edge_id)`.
- The test captures the new state and computes `GraphDiff::between(&old_snapshot, &new_snapshot)`.
- The test asserts `diff.removed_edges` contains the old `EdgeRecord<ScriptEdge>` with the original `src`, `dst`, and `ScriptEdge` payload preserved.
- The test asserts there is no added-edge, changed-edge, added-node, removed-node, or changed-node noise.
- A completed execution report packet for this dispatch.

## Acceptance Criteria

- The only implementation file changed is `crates/script-graph/src/lib.rs`.
- The new regression is one new `#[test]` function; it may reuse existing imports already present in the test module.
- The graph under test starts with exactly two nodes and one edge, then removes exactly that edge without removing or mutating either node.
- The removed edge assertion checks:
  - `diff.removed_edges.len() == 1`
  - `diff.removed_edges` contains the removed `EdgeId`
  - the removed `EdgeRecord` has the expected old `src`
  - the removed `EdgeRecord` has the expected old `dst`
  - the removed `EdgeRecord` has the expected old `data`, including the `ScriptEdge` key payload
- The no-noise assertions check:
  - `diff.added_edges.is_empty()`
  - `diff.changed_edges.is_empty()`
  - `diff.added_nodes.is_empty()`
  - `diff.removed_nodes.is_empty()`
  - `diff.changed_nodes.is_empty()`
- No `ScriptGraph`, `ScriptNode`, `ScriptEdge`, graph-foundation API, production diff implementation, dependency, Cargo configuration, documentation, or architecture policy is changed.
- `cargo test -p rge-script-graph` exits 0.

## Constraints / Non-Goals

- This is a test-only dispatch. Do not modify runtime behavior, public APIs, substrate code, snapshot code, diff code, ID derivation, visualization, invalidation, or serialization behavior.
- Do not add a public `ScriptGraph::remove_edge` wrapper; this smoke test must use the existing private substrate access available inside the unit-test module.
- Do not add coverage for added edges, changed edges, node removals, snapshot RON round trips, invalidation propagation, editor UI, material graphs, animation graphs, CAD graphs, or graph-foundation integration tests.
- Do not add dependencies, fixtures, helper modules, new test files, Cargo manifest entries, docs, scripts, or snapshots.
- Do not rewrite or restructure unrelated tests in `crates/script-graph/src/lib.rs` except for minimal local placement of the new test.
- Do not commit, stage, push, or create any packet other than the protocol-required execution report and optional EXEC sidecar.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `cargo test -p rge-script-graph` -> expected exit 0.
- `git diff -- crates/script-graph/src/lib.rs` -> expected diff is limited to exactly one additive unit test for removed `ScriptEdge` record preservation.
- `git diff --check -- crates/script-graph/src/lib.rs` -> expected exit 0.

If a required command cannot be run in the environment, the Executor MUST document the exact command, the failure reason, and any partial verification that was completed.

## Halt Conditions

The Executor MUST halt without modifying runtime behavior and request fresh guidance if any of the following occur:

- The requested removed-edge regression cannot pass without changing `ScriptGraph`, `Graph`, `GraphDiff`, `GraphSnapshot`, `EdgeRecord`, ID derivation, or any other runtime source.
- Any required edit outside `crates/script-graph/src/lib.rs` appears necessary.
- Any new file other than the protocol-required execution report and its finalize-generated sidecar appears necessary.
- The existing private substrate access inside the unit-test module cannot express the edge removal without adding or changing a public API.
- Implementing the requested coverage appears to require changing added-edge, changed-edge, node-removal, serialization, invalidation, visualization, or cross-crate graph behavior.
- Formatting or tests fail for reasons unrelated to the requested regression and cannot be isolated within the allowed scope.

## Planner Notes

This task was filed automatically by `Invoke-AiDispatchAuto.ps1` from `.ai/dispatch.tasks.md` for GitHub issue #82. Revision number: 0. There is no prior Claude gate result. Keep the implementation narrow: add only the `ScriptGraph` wrapper removed-edge diff smoke in the existing unit-test module, mirroring the local style of `structural_diff_reports_added_script_node_and_edge` but using `g.graph.remove_edge(edge_id)` to produce the old-to-new removed-edge diff.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-82
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

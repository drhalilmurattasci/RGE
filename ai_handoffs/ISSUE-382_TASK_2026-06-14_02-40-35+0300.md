# Task Packet

DISPATCH_ID: ISSUE-382
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-14_02-40-35+0300
RELATED_FILES:
- .ai/dispatch.tasks.md
- Status.md
- HANDOFF.md
- plans/BASELINE.md
- change.md
- .ai/dispatch-ISSUE-382/codex.plan.rev0.log
- crates/editor-egui-host/src/shortcut_help.rs
- crates/editor-egui-host/src/shortcut_conflicts.rs
- crates/editor-egui-host/src/menu.rs
- crates/editor-ui/src/menus/registry.rs
- crates/editor-ui/src/menus/command.rs
- crates/editor-shell/src/lifecycle/commands.rs
- crates/editor-shell/src/lifecycle/extension_command.rs
- crates/editor-shell/src/lifecycle/viewport_navigation.rs
- crates/editor-shell/src/camera.rs
- crates/editor-actions/src/bus.rs
STATUS: OPEN

## Goal

Perform GitHub issue #382 / autonomous task 128 as a docs/source-read-only
Phase 9 editor-usability audit after ISSUE-381. Use only the dispatcher-provided
GitHub state snapshot and local repository reads to compare the remaining
keybinding/remap, host-shell command execution, plugin execution, OS/typed
clipboard, CommandBus/CAD mutation, and camera follow-up surfaces. Append
exactly one source-grounded, bounded implementation task 129, or record a
source-grounded `NEEDS_HUMAN` result if no single safe implementation slice
exists. Do not implement task 129.

## Scope

### Scope note

This is an audit/selection task. It may edit planning and status documents to
record the audit and append task 129 or `NEEDS_HUMAN`, but it must not edit
Rust source, tests, Cargo metadata, workflows, automation, schemas, ADRs,
packet templates, or any implementation surface. "Read-only" below means
source-read-only and implementation-read-only.

### MAY edit
- `.ai/dispatch.tasks.md`
- `Status.md`
- `HANDOFF.md`
- `plans/BASELINE.md`
- `change.md`

### MUST NOT edit
- `crates/**`
- `editor/**`
- `kernel/**`
- `runtime/**`
- `tools/**`
- `plugins/**`
- `Cargo.toml`
- `Cargo.lock`
- `**/Cargo.toml`
- `.github/**`
- `.ai/dispatch.verify.ps1`
- `.ai/*.schema.json`
- `Invoke-AiDispatchAuto.ps1`
- `Invoke-AiDispatchLoop.ps1`
- `Invoke-AiDispatchQueue.ps1`
- `Invoke-AiDispatchGuard.ps1`
- `Register-AiDispatchSchedule.ps1`
- `Watch-AiDispatch.ps1`
- `Watch-DispatchStages.ps1`
- `Wait-GitHubActions.ps1`
- `Get-AiDispatchHealth.ps1`
- `Get-AiDispatchTrends.ps1`
- `new-handoff.ps1`
- `Test-HandoffPacket.ps1`
- `ai_handoffs/AI_HANDOFF_PROTOCOL.md`
- `ai_handoffs/templates/**`
- This finalized TASK packet, except for orchestrator-owned finalization outside
  the executor step.
- Existing unrelated handoff packets, dispatch-run scratch, or audit logs.
- Plugin runtime/discovery/loading implementation.
- `MenuCommandHandoff` storage semantics, host-shell FIFO replacement, or
  `EditorShell::route_menu_command`.
- Command-palette activation semantics, accelerator execution semantics, or
  menu-click behavior.
- Shortcut remapping UI, user preferences, persistence, or fatal startup policy
  as implementation.
- OS/typed clipboard behavior.
- CAD graph/projection mutation, `CommandBus` action signatures,
  undo/dirty/save-load authority, or save/load behavior.
- Camera/navigation behavior.

### MAY add new files
- `ai_handoffs/ISSUE-382_EXEC_*.md`
- `ai_handoffs/ISSUE-382_EXEC_*.meta.json`
- `.ai/dispatch-ISSUE-382/**`
- `ai_dispatch_logs/log_*ISSUE-382*.md`

### MUST NOT add new files
- New Rust source files or tests.
- New crates, Cargo manifests, lockfiles, workflows, schemas, ADRs,
  architecture-lint rules/config, automation scripts, packet templates,
  fixtures, generated assets, plugin runtime/discovery/loading files, or
  implementation documentation outside the MAY-edit planning/status documents.
- Any new handoff packet other than the allowed `ISSUE-382` EXEC packet and
  matching sidecar.
- Any task 130 or later entry.

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - .ai/dispatch.tasks.md
  - Status.md
  - HANDOFF.md
  - plans/BASELINE.md
  - change.md
  - ai_handoffs/ISSUE-382_EXEC_*.md
  - ai_handoffs/ISSUE-382_EXEC_*.meta.json
  - .ai/dispatch-ISSUE-382/**
  - ai_dispatch_logs/log_*ISSUE-382*.md
MUST_NOT_EDIT:
  - crates/**
  - editor/**
  - kernel/**
  - runtime/**
  - tools/**
  - plugins/**
  - Cargo.toml
  - Cargo.lock
  - **/Cargo.toml
  - .github/**
  - .ai/dispatch.verify.ps1
  - .ai/*.schema.json
  - Invoke-AiDispatchAuto.ps1
  - Invoke-AiDispatchLoop.ps1
  - Invoke-AiDispatchQueue.ps1
  - Invoke-AiDispatchGuard.ps1
  - Register-AiDispatchSchedule.ps1
  - Watch-AiDispatch.ps1
  - Watch-DispatchStages.ps1
  - Wait-GitHubActions.ps1
  - Get-AiDispatchHealth.ps1
  - Get-AiDispatchTrends.ps1
  - new-handoff.ps1
  - Test-HandoffPacket.ps1
  - ai_handoffs/AI_HANDOFF_PROTOCOL.md
  - ai_handoffs/templates/**
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Dispatcher Snapshot Evidence

Use this exact local artifact/read path for the raw dispatcher GitHub state
snapshot copied into the planner revision-0 log:

- `Get-Content -LiteralPath '.ai\dispatch-ISSUE-382\codex.plan.rev0.log' | Select-Object -Skip 40 -First 205`

Observed locator checks during planning:

- `rg -a -n "Dispatcher GitHub state snapshot|Generated by Invoke-AiDispatchAuto|Executor instruction:|#381 \[CLOSED\]|^Rules:" .ai/dispatch-ISSUE-382/codex.plan.rev0.log` -> line 42 is `Dispatcher GitHub state snapshot`; line 44 is the generated timestamp; line 238 is `#381 [CLOSED] Expose shortcut-help conflict peer entry IDs`; line 240 is the executor instruction that says to use the dispatcher-provided snapshot and not call `gh` or the network for GitHub queue evidence; line 252 is the first planner `Rules:` heading after the snapshot.
- The exact read command above returns the dispatcher snapshot from line 42
  through the executor instruction and revision-0 context, including `(none)`
  for both open queue categories and the already-filed autonomous issue list.

The executor MUST use that local planner-log artifact as the only evidence for
GitHub queue state and already-filed autonomous tasks. Do not call `gh`, browse,
or use network access for that confirmation. Repository/source evidence must
come from local reads and searches in this worktree.

## Deliverables

- Record the dispatcher snapshot evidence from
  `.ai/dispatch-ISSUE-382/codex.plan.rev0.log`, including the generated timestamp,
  no-open-queue facts, and already-filed autonomous issue range relevant to
  deciding whether task 129 has already been filed.
- Before editing, run and record
  `rg -n "^126\.|^127\.|^128\.|^129\." .ai/dispatch.tasks.md`.
- Audit the current local source/docs for all six candidate classes:
  keybinding/remap, host-shell command execution, plugin execution, OS/typed
  clipboard, CommandBus/CAD mutation, and camera follow-up surfaces.
- For each candidate class, record source-grounded evidence and at least one
  falsifying grep/read where practical. Negative claims such as "no route",
  "not implemented", "unchanged", "no task", or "no call site" must include the
  inverse search/read and observed result.
- Append exactly one bounded implementation task 129 to `.ai/dispatch.tasks.md`
  with explicit `MAY edit`, `MUST NOT edit`, `Done criteria`, `Verification`,
  and `Halt conditions` sections; or mark task 128 `NEEDS_HUMAN` with concrete
  source evidence explaining why no bounded task can be safely specified.
- If using the standing delegated Human=Codex policy to choose a boundary, state
  that policy explicitly in the audit result and in task 129, and keep it to
  the smallest source-safe boundary.
- Update `Status.md`, `HANDOFF.md`, `plans/BASELINE.md`, and `change.md` as
  needed to reflect the audit result and selected task 129 or `NEEDS_HUMAN`.
- Write the EXEC report for this dispatch.

## Acceptance Criteria

- The EXEC report and edited docs cite
  `.ai/dispatch-ISSUE-382/codex.plan.rev0.log` with the exact read command above as
  the GitHub snapshot evidence, and no `gh` or network command is used.
- The audit records the pre-edit task-heading check. Planning observed:
  `rg -n "^126\.|^127\.|^128\.|^129\." .ai/dispatch.tasks.md` -> lines 11244
  task 126 done, 11405 task 127 done, 11504 task 128 open, and no `^129.`
  match.
- The audit compares all six required candidate classes from current source:
  keybinding/remap, host-shell command execution, plugin execution, OS/typed
  clipboard, CommandBus/CAD mutation, and camera follow-up.
- Exactly one of these outcomes is present:
  task 129 is appended as a bounded implementation task with the required
  `MAY edit`, `MUST NOT edit`, `Done criteria`, `Verification`, and
  `Halt conditions` sections; or task 128 is marked `NEEDS_HUMAN` with concrete
  source evidence and no task 129 is appended.
- No implementation work for task 129 is performed.
- No task 130 or later entry is appended.
- `git diff --name-only` contains only MAY-edit docs plus this dispatch's
  generated EXEC/run/log artifacts.
- `git diff --check` is clean.

## Current-State Claims: Falsification Required

Every negative / "unchanged" assertion this packet makes about current source
or dispatch state must list its inverse-search command and result. A negative
claim without a falsifying search is not a valid premise under Protocol Rule 8.

- Claim: The local planner-log artifact contains the raw GitHub state
  snapshot and the executor instruction to avoid live `gh`/network queue
  checks.
  Falsifying read: `Get-Content -LiteralPath '.ai\dispatch-ISSUE-382\codex.plan.rev0.log' | Select-Object -Skip 40 -First 205` -> includes `Dispatcher GitHub state snapshot`, `Generated by Invoke-AiDispatchAuto.ps1 at 2026-06-14T02:40:09.4240537+03:00 before this issue was created.`, `(none)` under open `ai-dispatch`, `(none)` under open failed autonomous issues, closed autonomous issues through `#381 [CLOSED] Expose shortcut-help conflict peer entry IDs`, and the executor instruction not to call `gh` or the network for that confirmation.
- Claim: At planning time, the local task brief had task 128 open and no task
  129 heading.
  Falsifying search: `rg -n "^126\.|^127\.|^128\.|^129\." .ai/dispatch.tasks.md` -> `11244:126. **~~Post-shortcut-help Phase 9 next-task audit.~~ DONE 2026-06-14 (manual salvage after ISSUE-380 plan-gate failure).**`, `11405:127. **[DONE 2026-06-14 via ISSUE-381] Expose shortcut-help conflict peer entry IDs.**`, `11504:128. **Post-shortcut-conflict-help Phase 9 next-task audit.**`, and no `^129.` match.
- Claim: This TASK packet does not authorize source, Cargo, workflow,
  automation, schema, ADR, packet-template, plugin-runtime, clipboard, CAD, or
  camera implementation edits.
  Falsifying read: `rg -n "^### MAY edit$|^### MUST NOT edit$|^### MAY add new files$|^### MUST NOT add new files$|\.ai/dispatch\.tasks\.md|Status\.md|HANDOFF\.md|plans/BASELINE\.md|change\.md|crates/\*\*|Cargo\.toml|Cargo\.lock|\.github/\*\*|\.ai/\*\.schema\.json|ai_handoffs/templates/\*\*|Plugin runtime/discovery/loading implementation|OS/typed clipboard behavior|CAD graph/projection mutation|Camera/navigation behavior" ai_handoffs/ISSUE-382_TASK_2026-06-14_02-40-35+0300.md` -> only the five planning/status docs are listed as MAY-edit tracked files, generated ISSUE-382 artifacts are listed as MAY-add files, and the implementation surfaces named in the claim are forbidden in the MUST-NOT sections.

## Constraints / Non-Goals

- Do not implement task 129.
- Do not write Rust source or tests.
- Do not edit Cargo manifests, `Cargo.lock`, workflows, dispatch automation,
  schemas, ADRs, packet templates, or existing unrelated generated artifacts.
- Do not call `gh`, browse, or use network access for GitHub queue or
  already-filed-task evidence.
- Do not replace or alter `MenuCommandHandoff`,
  `EditorShell::route_menu_command`, command-palette activation, accelerator
  execution, menu-click behavior, shortcut remapping/persistence/fatal policy,
  OS/typed clipboard behavior, CAD/projection mutation, `CommandBus` action
  signatures, undo/dirty/save-load authority, save/load behavior, or
  camera/navigation behavior.
- Do not append more than one follow-up task. If a bounded task 129 cannot be
  stated safely from source evidence, record `NEEDS_HUMAN` for task 128 instead
  of inventing an unsafe implementation task.
- Do not append task 130.

## Candidate Surfaces To Compare

The executor must re-read current source before deciding; the bullets below are
audit targets to verify, not current-state assertions and not permission to
implement anything in this dispatch.

- Keybinding/remap policy after tasks 123, 125, and 127: inspect the current
  shortcut execution, conflict diagnostics, Keyboard Shortcuts help, peer-id
  detail, remap, preference, persistence, and fatal-policy surfaces. Determine
  whether any narrower diagnostic or policy follow-up remains source-safe.
- Host-shell command execution: inspect current menu, command-palette, and
  accelerator activation routes, including `MenuCommandHandoff` and
  `EditorShell::route_menu_command`. Determine whether a small host-owned
  source-safe slice exists without replacing the route.
- Real plugin command execution: inspect the current extension-command seam,
  plugin command shapes, runtime/discovery/loading crates, and route ownership.
  Determine whether a bounded seam-only follow-up exists without implementing
  runtime/discovery/loading or route-owner changes.
- OS/typed clipboard: inspect current Edit Cut/Copy/Paste behavior and any
  shell-local, OS clipboard, typed component, CAD identity, or cross-process
  clipboard surfaces. Determine whether a narrow policy/documented substrate
  slice exists without changing clipboard behavior in this audit.
- CAD/editor mutation through CommandBus: inspect current `CommandBus`,
  editor-action, CAD/projection, undo/dirty, and save/load mutation surfaces.
  Determine whether a bounded implementation slice exists without changing
  authoritative CAD/projection mutation, action signatures, undo/dirty
  authority, or save/load behavior in this audit.
- Camera/navigation follow-up after viewport wheel zoom, right-button orbit,
  middle-button pan, and left-double-click frame-all: inspect current camera
  input, frame/reset, bounds, pointer-capture/window-grab, persistence, and
  controller-policy surfaces. Determine whether a bounded follow-up remains
  without changing camera/navigation behavior in this audit.

## Suggested Source Greps

Run whatever reads are necessary, but the EXEC report must include comparable
source evidence for each class. These commands are acceptable starting points:

- `rg -n "enabled_command_for_shortcut|command_for_shortcut|AcceleratorTable|ProjectedShortcutConflict|shortcut_help_rows|shortcut_conflict_rows|remap|preferences|persist|fatal" crates/editor-ui/src/menus crates/editor-egui-host/src`
- `rg -n "MenuCommandHandoff|route_menu_command|command_palette|accelerator|drain|Command::" crates/editor-egui-host/src crates/editor-shell/src crates/editor-ui/src/menus`
- `rg -n "ExtensionCommandHandler|extension_command|plugin command|PluginCommand|Command::Plugin|plugin runtime|plugin-discovery" crates/editor-shell/src crates/editor-ui/src crates/plugin-discovery/src crates/runtime-wasmtime/src crates/runtime-wasmtime-engine/src`
- `rg -n "clipboard|Cut|Copy|Paste|Duplicate|Delete|legacy|entity" crates/editor-shell/src crates/editor-actions/src crates/editor-egui-host/src crates/editor-ui/src`
- `rg -n "CommandBus|Action|Cad|cad_|projection|dirty|undo|save|load" crates/editor-shell/src crates/editor-actions/src crates/cad-core/src crates/cad-projection/src`
- `rg -n "MouseWheel|right|middle|double|frame|reset_camera|is_pointer_over_viewport_tab|pointer capture|camera|viewport_navigation|current_scene_bounds" crates/editor-shell/src`

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- `rg -n "^126\.|^127\.|^128\.|^129\." .ai/dispatch.tasks.md` -> expected to
  show task 126 and task 127 done, task 128 updated by this dispatch, and
  either exactly one task 129 heading or no task 129 if task 128 records
  `NEEDS_HUMAN`.
- The six candidate source-grep/read groups listed above, or stricter
  equivalent searches, with observed results recorded in the audit.
- `rg -n "^130\." .ai/dispatch.tasks.md` -> expected no matches.
- `git diff --name-only` -> expected only MAY-edit docs plus this dispatch's
  generated EXEC/run/log artifacts.
- `git diff --check` -> expected exit 0.
- `new-handoff.ps1 -Finalize -PacketPath ai_handoffs/ISSUE-382_EXEC_*.md -DryRun`
  using the actual execution report path -> expected exit 0 after the EXEC
  packet exists.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of
the following occur:

- The raw dispatcher snapshot cannot be read from
  `.ai/dispatch-ISSUE-382/codex.plan.rev0.log` with the exact read command in
  `## Dispatcher Snapshot Evidence`, or the executor would need live
  `gh`/network evidence to decide queue/already-filed-task state.
- Any required edit would touch a path outside the MAY-edit/MAY-add lists.
- The audit starts implementing task 129, writing Rust, editing Cargo, changing
  workflows, changing automation, changing schemas, changing ADRs, or changing
  packet templates.
- A candidate task requires changing shortcut execution, menu-click behavior,
  command-palette activation, accelerator routing, remapping/persistence/fatal
  policy, plugin runtime/discovery/loading, OS clipboard behavior, CAD graph or
  projection mutation, `CommandBus` action signatures, undo/dirty/save-load
  authority, save/load behavior, or camera/navigation behavior during this
  audit.
- More than one task 129 candidate appears necessary. Record the comparison and
  choose exactly one only if a single source-grounded bounded slice is clear;
  otherwise mark task 128 `NEEDS_HUMAN`.
- The canonical verification gate or an auto-routed correction asks for a
  source fix outside this audit scope. In that case, preserve the audit scope
  and report `NEEDS_HUMAN`; do not execute the source fix.
- Verification fails, or `git diff --name-only` shows any MUST-NOT path.

## Planner Notes

ISSUE-380 failed because its task packet did not expose enough raw
dispatcher-snapshot and task-heading falsification evidence for the sandboxed
executor to verify negative premises without `gh`. This packet intentionally
cites the local planner revision-0 log artifact and the exact pre-edit
task-heading search to avoid repeating that failure mode.

Selection should be conservative. A good task 129 is one small implementation
slice with a narrow MAY-edit surface, clear tests, and explicit boundaries. If
all remaining candidates require a product or architecture decision, mark task
128 `NEEDS_HUMAN` in the allowed docs rather than manufacturing a task.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-382
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

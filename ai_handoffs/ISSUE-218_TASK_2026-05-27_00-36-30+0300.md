# Task Packet

DISPATCH_ID: ISSUE-218
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-27_00-36-30+0300
REVISION: 0
RELATED_FILES:
- GitHub issue #218
- GitHub Actions runs/logs for the current repository
- AI_DISPATCH_AUTOMATION.md
- ai_handoffs/AI_HANDOFF_PROTOCOL.md
- .github/workflows/
- .ai/dispatch.verify.ps1
- .ai/dispatch-ISSUE-*/
- ai_dispatch_logs/
- plans/PLAN.md
- plans/IMPLEMENTATION.md
- plans/BASELINE.md
- Status.md
- HANDOFF.md
- change.md
- Cargo.toml
- Cargo.lock
- crates/
- kernel/
- runtime/
- editor/
- tools/
- ai_handoffs/ISSUE-218_EXEC_*.md
- ai_handoffs/ISSUE-218_EXEC_*.meta.json
STATUS: OPEN

## Goal

Perform a read-only Phase 9 compile-time pressure audit for GitHub issue
#218. Use current CI logs, local dispatch verification logs, crate and test
fanout, workflow definitions, verification-gate behavior, and recent Cargo
dependency churn to decide whether compile-time pressure has a bounded
autonomous follow-up. The EXEC report must name exactly one smallest safe
follow-up or `NEEDS_HUMAN`, with line-cited evidence and an explicit
branch-mode requirement for any implementation follow-up. This dispatch must
not change tracked source, tests, documentation, Cargo files, workflows,
schemas, automation scripts, scheduler configuration, or `.ai/dispatch.tasks.md`.

## Scope

### MAY edit
- None. This is an audit-only dispatch. Do not modify tracked source, tests,
  documentation, Cargo files, workflows, schemas, automation scripts,
  scheduler configuration, task brief files, existing handoff packets, existing
  logs, fixtures, generated assets, or prior artifacts.

### MUST NOT edit
- `plans/PLAN.md`
- `plans/IMPLEMENTATION.md`
- `plans/BASELINE.md`
- `Status.md`
- `HANDOFF.md`
- `change.md`
- `Cargo.toml`
- `Cargo.lock`
- `**/Cargo.toml`
- `.github/workflows/**`
- `.ai/dispatch.tasks.md`
- `.ai/dispatch.verify.ps1`
- `.ai/*.schema.json`
- `Invoke-AiDispatchAuto.ps1`
- `Invoke-AiDispatchQueue.ps1`
- `Invoke-AiDispatchLoop.ps1`
- `Register-AiDispatchSchedule.ps1`
- `Get-AiDispatchHealth.ps1`
- `Get-AiDispatchTrends.ps1`
- `Watch-AiDispatch.ps1`
- `Wait-GitHubActions.ps1`
- `new-handoff.ps1`
- `ai_handoffs/AI_HANDOFF_PROTOCOL.md`
- `ai_handoffs/templates/**`
- This task packet,
  `ai_handoffs/ISSUE-218_TASK_2026-05-27_00-36-30+0300.md`, after the
  executor starts.
- Any existing handoff packet or sidecar.
- Any existing `ai_dispatch_logs/**` artifact.
- `crates/**`
- `kernel/**`
- `editor/**`
- `runtime/**`
- `tools/**`
- `tests/**`
- `docs/**`
- `tasks/**`
- `plugins/**`
- `files/**`
- `golden-projects/**`
- `OLD/**`
- `.claude/worktrees/**`
- Any tracked source file, Rust test, documentation file, Cargo metadata file,
  workflow, schema, automation script, scheduler config, task brief, existing
  handoff artifact, existing dispatch log, generated fixture, sandbox
  worktree, or unrelated local scratch file.

### MAY add new files
- `ai_handoffs/ISSUE-218_EXEC_*.md`
- `ai_handoffs/ISSUE-218_EXEC_*.meta.json`
- `ai_dispatch_logs/log_*.md`
- `.ai/dispatch-ISSUE-218/**`
- `.ai/dispatch-trace/Invoke-AiDispatchAuto-*.jsonl`
- `.ai/dispatch-trace/Invoke-AiDispatchQueue-*.jsonl`

### MUST NOT add new files
- New source, test, documentation, schema, Cargo, workflow, task brief,
  automation, scheduler, fixture, generated asset, ADR, plan, status,
  architecture-lint, benchmark, screenshot, ZIP, copied evidence file, or
  persistent scratch file.
- New handoff packets or sidecars for any dispatch other than `ISSUE-218`.
- `REVIEW`, `CORRECT`, or `CLOSEOUT` packets for this orchestrator dispatch.
- Replacement or edited copies of prior `ISSUE-218` execution artifacts. If a
  prior failed EXEC exists, treat it as read-only context and write a new
  timestamped `ISSUE-218_EXEC_*.md` instead.
- Temporary source-layout inventories, extracted logs, generated reports,
  copied CI logs, or measurement outputs inside the repository. If scratch is
  required to inspect an archive or command result, place it outside the repo
  and remove it before writing the EXEC report.

## Deliverables

- One new `ISSUE-218` EXEC report containing the read-only audit.
- A decision block with exactly these fields:
  `COMPILE_TIME_PRESSURE_VERDICT`, `BOUNDED_AUTONOMOUS_FOLLOW_UP`,
  `SMALLEST_SAFE_FOLLOW_UP`, and `FOLLOW_UP_BRANCH_MODE`.
- `COMPILE_TIME_PRESSURE_VERDICT` must use exactly one of:
  `pressure_deferred`, `bounded_autonomous_follow_up`, or `NEEDS_HUMAN`.
- `BOUNDED_AUTONOMOUS_FOLLOW_UP` must be exactly `yes`, `no`, or
  `NEEDS_HUMAN`.
- `SMALLEST_SAFE_FOLLOW_UP` must name exactly one action or be exactly
  `NEEDS_HUMAN`. It must not become a backlog list.
- `FOLLOW_UP_BRANCH_MODE` must be exactly
  `PublishMode branch only unless later authorized by a human` for any
  follow-up that would implement source, test, script, Cargo, workflow,
  schema, scheduler, automation-behavior, or production-doc changes. If the
  selected follow-up is audit-only or `NEEDS_HUMAN`, state exactly
  `not_applicable`.
- A CI evidence section covering every current workflow under
  `.github/workflows/`, including the latest available GitHub Actions run/log
  evidence for format, architecture, deny, workspace tests, and script bench
  compile. The report must distinguish runner infrastructure failures from
  actual Rust compile, test, dependency, deny, formatting, or lint failures.
- A local dispatch verification evidence section covering current
  `.ai/dispatch.verify.ps1` behavior and recent local dispatch logs under
  `.ai/dispatch-ISSUE-*` and `ai_dispatch_logs/`. It must report observed
  durations or failure patterns where the logs expose them, and it must label
  historical logs as historical.
- A crate/test fanout section covering current workspace package/manifests,
  workflow comments about all-targets/doc/bench coverage, test file inventory,
  and any local verify log counts or durations. Inventory-derived claims must
  include the exact read-only command and scope.
- A recent dependency churn section covering Cargo manifest and lockfile churn
  from recent commits, current workspace dependency surface, and whether churn
  plausibly affects compile-time pressure. Current Cargo-file claims require
  line citations; git-history claims require exact read-only commands and
  commit IDs.
- A candidate comparison section explaining why the selected follow-up is the
  smallest safe compile-time-pressure follow-up, or why `NEEDS_HUMAN` is
  required. Compare CI evidence, local verify evidence, crate/test fanout, and
  dependency churn rather than defaulting to the first visible signal.
- A Phase 9 context section citing `plans/PLAN.md`,
  `plans/IMPLEMENTATION.md`, `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`,
  recent `change.md`, and relevant existing dispatch artifacts. It must
  separate current compile-time pressure from historical release, CI, or
  dispatch-doctrine evidence.
- A final verification section reporting `git diff --check`,
  `git status --short --untracked-files=all`, and static inspection proving no
  tracked production/doc/script/Cargo/workflow/schema/task-brief files changed.

## Acceptance Criteria

- The EXEC report is audit-only and does not modify tracked source, tests,
  docs, Cargo metadata, workflows, schemas, scripts, scheduler configuration,
  fixtures, or `.ai/dispatch.tasks.md`.
- The report classifies compile-time pressure using exactly one
  `COMPILE_TIME_PRESSURE_VERDICT` value and exactly one
  `BOUNDED_AUTONOMOUS_FOLLOW_UP` value from the deliverables section.
- The report names exactly one smallest safe follow-up or exactly
  `NEEDS_HUMAN`; it does not produce a backlog list.
- The report includes the branch-mode requirement for any implementation
  follow-up exactly as specified in `FOLLOW_UP_BRANCH_MODE`.
- Every substantive claim about current code, docs, scripts, Cargo metadata,
  workflows, CI logs, local dispatch logs, or dispatch artifacts is backed by
  `path:line` evidence when the evidence lives in a line-bearing file. If a
  claim comes from command output, GitHub Actions metadata, git history, or
  inventory rather than a line-bearing file, the report includes the exact
  read-only command, scope, and observed result.
- The audit reads and line-cites `AI_DISPATCH_AUTOMATION.md` and
  `ai_handoffs/AI_HANDOFF_PROTOCOL.md` before judging dispatch state, and it
  does not treat missing orchestrator `_REVIEW_` or `_CLOSEOUT_` packets as
  evidence of an unfinished dispatch.
- The audit reads and line-cites `plans/PLAN.md`, `plans/IMPLEMENTATION.md`,
  `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, and recent `change.md`
  entries to establish Phase 9 context and current release/dispatch posture.
- The audit inspects and line-cites `.github/workflows/fmt.yml`,
  `.github/workflows/architecture.yml`, `.github/workflows/deny.yml`,
  `.github/workflows/tests.yml`, and `.github/workflows/bench.yml`.
- The audit inspects and line-cites `.ai/dispatch.verify.ps1`, including its
  workflow mirror claim, serialized workspace test behavior, and bench compile
  step.
- The audit inspects current GitHub Actions evidence read-only. It must use
  `gh run list`, `gh run view`, `gh api`, GitHub MCP data, or an equivalent
  read-only source. If current run logs are unavailable, it must state the
  exact command/source attempted, the failure, and whether classification is
  still possible without guessing.
- The audit inspects recent local dispatch evidence read-only from
  `ai_dispatch_logs/log_*.md` and available `.ai/dispatch-ISSUE-*/` verify
  outputs. It must cite current file lines for local log claims and must not
  rewrite, copy, or normalize those logs.
- The audit inventories crate and test fanout read-only using current
  repository state, for example `git ls-files`, `git grep`, `Select-String`,
  or static Cargo file inspection. It must not run tests, builds, benches,
  profilers, or `.ai/dispatch.verify.ps1` manually for the audit.
- The audit inspects recent dependency churn read-only using current Cargo
  files and git history for `Cargo.toml`, `Cargo.lock`, and `**/Cargo.toml`.
  It must separate actual dependency additions/removals from ordinary source
  or test churn.
- The report distinguishes compile-time pressure from runtime performance
  pressure, GPU frame pressure, invalidation-economics pressure, reflection
  pressure, runner infrastructure failures, cache misses, stale CI logs,
  one-off local machine noise, and ordinary expected cost of a large workspace.
- The report uses `pressure_deferred` only if current evidence does not show a
  compile-time-pressure follow-up that is both concrete and bounded.
- The report uses `bounded_autonomous_follow_up` only if current evidence
  supports one specific, small follow-up with clear allowed files,
  verification gates, halt conditions, and branch-mode requirement.
- The report uses `NEEDS_HUMAN` only if current evidence is unavailable,
  contradictory, or points to an unbounded policy/architecture/infrastructure
  decision rather than a safe autonomous follow-up.
- The report does not run new benchmarks, profilers, cargo builds, cargo
  tests, cargo fmt, cargo deny, full repository verification, the autonomous
  queue, or a nested dispatch as part of this audit.
- `git diff --check` exits 0.
- `git status --short --untracked-files=all` shows no tracked
  production/doc/script/Cargo/workflow/schema/task-brief changes. Any new
  files must be limited to this dispatch's own EXEC report, optional matching
  sidecar/log artifacts, and ignored `.ai` dispatch scratch.

## Constraints / Non-Goals

- Do not implement a fix.
- Do not edit source, tests, docs, Cargo files, workflows, schemas, scripts,
  scheduler config, task brief files, fixtures, prior handoff packets, prior
  logs, or existing dispatch artifacts.
- Do not update `plans/PLAN.md`, `plans/IMPLEMENTATION.md`,
  `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, `change.md`, or any other
  documentation.
- Do not add or run new benchmarks, profilers, generated reports,
  screenshots, ZIPs, fixtures, or persistent scratch files in the repository.
- Do not run publish, push, merge, branch-delete, label-edit, GitHub issue
  mutation, scheduler registration, or scheduler modification commands.
- Do not create a branch, commit, PR, issue comment, label change, or close
  the GitHub issue.
- Do not run the autonomous queue or a nested dispatch as verification.
- Do not run full repository verification manually for this audit. The
  task-specific gates are read-only inspection plus diff/status checks; the
  orchestrator may still run its canonical gate automatically after execution.
- Do not infer compile-time pressure from file count, crate count, or
  dependency churn alone. Use CI logs, local verify logs, workflow/gate
  behavior, and current Cargo/test fanout together.
- Do not treat a GitHub Actions hosted-runner startup or log-retention problem
  as a Rust compile-time regression unless step-level Rust command output
  proves it.
- Do not recommend broad workspace restructuring, dependency policy changes,
  CI matrix rewrites, cache strategy rewrites, or architecture changes unless
  the report returns `NEEDS_HUMAN`.
- Treat unrelated dirty worktree state as out of scope. Do not clean, revert,
  or modify changes you did not make; report them if they affect confidence.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- `Get-Content -Raw AI_DISPATCH_AUTOMATION.md` and
  `Get-Content -Raw ai_handoffs/AI_HANDOFF_PROTOCOL.md` -> expected readable;
  use them to avoid mistaking orchestrator packet shape for dispatch
  incompleteness.
- `Get-Content -Raw plans/PLAN.md`,
  `Get-Content -Raw plans/IMPLEMENTATION.md`, and
  `Get-Content -Raw plans/BASELINE.md` -> expected readable; use them to
  establish Phase 9 compile-time pressure context and current revisit
  triggers.
- `Get-Content -Raw Status.md` and `Get-Content -Raw HANDOFF.md` -> expected
  readable; use them to establish current state, closed gates, known
  deferrals, and next-work posture.
- Inspect recent `change.md` entries with live line numbers immediately before
  writing the final EXEC report. Do not reuse copied line numbers from prior
  packets or logs.
- Inspect workflow definitions read-only:
  `.github/workflows/fmt.yml`, `.github/workflows/architecture.yml`,
  `.github/workflows/deny.yml`, `.github/workflows/tests.yml`, and
  `.github/workflows/bench.yml`.
- Inspect `.ai/dispatch.verify.ps1` read-only, including the format,
  architecture, deny, workspace all-targets, doctest, and bench compile steps.
- Inspect current GitHub Actions evidence read-only for each workflow. Record
  the exact `gh`, `gh api`, GitHub MCP, or equivalent command/source used; the
  run ID, job ID when available, branch, head SHA, conclusion, timestamp, and
  representative log or unavailable-log result.
- Inspect local dispatch evidence read-only from recent
  `ai_dispatch_logs/log_*.md` files and available `.ai/dispatch-ISSUE-*/`
  verify outputs. Prefer the most recent passed and failed runs where both are
  available; state if only one category exists.
- Inventory crate/test fanout read-only. Acceptable commands include
  `git ls-files '*Cargo.toml'`, `git ls-files '*.rs'`,
  `git grep -n "name =" -- Cargo.toml '**/Cargo.toml'`,
  `Select-String`, or equivalent read-only commands. Report exact command,
  scope, and observed counts.
- Inspect current Cargo metadata read-only from `Cargo.toml`, `Cargo.lock`,
  and `**/Cargo.toml`. Use line citations for current dependency claims.
- Inspect recent dependency churn read-only with `git log`, `git show`,
  `git diff --stat`, or equivalent commands scoped to `Cargo.toml`,
  `Cargo.lock`, and `**/Cargo.toml`. Report commit IDs and exact command
  scopes. Do not modify or regenerate `Cargo.lock`.
- Inspect relevant existing dispatch artifacts read-only, especially prior CI
  audit, verification-parity, Phase 9 pressure-audit, dependency-churn, and
  compile-heavy dispatch artifacts. Ignore `OLD/**`, `.claude/worktrees/**`,
  `.git/**`, and build output when assessing repository state unless a
  specific artifact is cited as provenance.
- `git diff --check` -> expected exit 0.
- `git status --short --untracked-files=all` -> expected no tracked
  production/doc/script/Cargo/workflow/schema/task-brief changes. Any new
  files must be limited to this dispatch's own handoff/log artifacts and
  optional ignored `.ai/dispatch-ISSUE-218` scratch.
- Static no-source-change inspection: run `git diff --name-only` and a
  targeted read-only inspection of protected tracked surfaces such as
  `crates`, `kernel`, `editor`, `runtime`, `tools`, `docs`, `plans`,
  `Status.md`, `HANDOFF.md`, `change.md`, `Cargo.toml`, `Cargo.lock`,
  `.github/workflows`, `.ai/dispatch.tasks.md`, `.ai/dispatch.verify.ps1`,
  `.ai/*.schema.json`, and root `*.ps1` automation scripts. Expected result:
  no tracked production/doc/script/Cargo/workflow/schema/task-brief diff.

## Halt Conditions

The Executor MUST halt and return `NEEDS_HUMAN` without editing any source,
docs, tests, scripts, schemas, workflows, Cargo files, scheduler config, task
briefs, fixtures, prior packets, or prior logs if any of the following occur:

- Required evidence files are missing or unreadable.
- Current GitHub Actions evidence cannot be retrieved or classified read-only,
  and local evidence is insufficient to avoid guessing.
- Required local dispatch logs or verify evidence cannot be inspected
  read-only.
- Required crate/test fanout or dependency-churn inventory cannot be completed
  read-only.
- The evidence cannot support exactly one `COMPILE_TIME_PRESSURE_VERDICT`
  value from the allowed set.
- The evidence cannot support exactly one
  `BOUNDED_AUTONOMOUS_FOLLOW_UP` value from the allowed set.
- The smallest safe follow-up cannot be narrowed to exactly one action.
- The smallest safe follow-up would require implementation changes but cannot
  be constrained to `PublishMode branch only unless later authorized by a
  human`.
- The audit needs an implementation, documentation edit, Cargo edit, workflow
  edit, schema edit, script edit, scheduler change, task-brief edit, prior
  artifact edit, benchmark run, profiler run, cargo build/test/fmt/deny run,
  or GitHub mutation to be truthful.
- Required line citations cannot be produced for document-, code-, Cargo-,
  workflow-, script-, or artifact-based claims.
- Current evidence points to a broad policy decision, CI infrastructure
  decision, workspace partitioning strategy, dependency governance decision,
  or architecture tradeoff rather than one bounded autonomous follow-up.
- Any GitHub mutation, push, merge, branch deletion, label change, issue
  comment, scheduler command, benchmark run, profiler run, full verification
  run, or nested dispatch seems necessary.
- `git diff --check` fails for a reason caused by this dispatch.
- `git status --short --untracked-files=all` shows new or modified files
  outside this dispatch's own handoff/log artifacts and optional ignored
  `.ai/dispatch-ISSUE-218` scratch, unless they are clearly pre-existing
  unrelated user changes.

If a halt condition fires, the Executor must still write the
`EXECUTION_REPORT` if possible, set header `STATUS: NEEDS_HUMAN`, include the
exact halt reason and current status output, and route the footer to
`NEXT_ROLE: HUMAN_ARBITER`.

## Planner Notes

This is delegated-main smoke batch task 6 of 10 from `.ai/dispatch.tasks.md`.
The desired outcome is not to create compile-time work by default; it is to
decide whether current CI/local-verify evidence, crate/test fanout, and recent
dependency churn show one bounded compile-time-pressure follow-up that is safe
for autonomous execution.

Use current evidence over prior summaries. Prior dispatch artifacts can be
used as provenance, but the report must label them as historical unless the
cited artifact itself proves current applicability. For CI logs, distinguish
step-level Rust command output from hosted-runner startup or log-retention
failures. For local verify logs, distinguish the orchestrator's automatic
canonical gate from commands the executor manually runs for this audit.

Under the orchestrator protocol, no `REVIEW` or `CLOSEOUT` packet is expected
from this TASK. The queue's Codex control review is stored in the run
directory, not as a handoff packet.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-218
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

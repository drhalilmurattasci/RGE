# Correction Packet

DISPATCH_ID: ISSUE-83
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-21_02-07-57+0300
RELATED_FILES:
- crates/script-graph/src/lib.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-83_TASK_2026-05-21_02-02-53+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-83_EXEC_2026-05-21_02-07-19+0300.md`
- Latest Review Report: none - post-execution verification failed before Codex control review, so no review packet exists.
- Prior Correction Packets: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less.

1. **Normalize rustfmt newline style** - Verification gate finding:
   `cargo +nightly fmt --all -- --check` failed with `Incorrect newline style`
   in `crates/script-graph/src/lib.rs`. Required change: normalize
   `crates/script-graph/src/lib.rs` to the newline style expected by rustfmt,
   without changing Rust tokens, behavior, APIs, comments, imports, or module
   structure. Acceptance: `cargo +nightly fmt --all -- --check` exits 0, and
   any resulting diff for `crates/script-graph/src/lib.rs` is limited to
   newline normalization.

## Deferred Findings (NOT Approved for This Round)

None. No Codex control-review findings were produced because the canonical
post-execution verification gate failed before control review.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with this correction-round addition:
the canonical dispatch verification gate must pass after the newline-style
normalization. No semantic changes are approved.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.\.ai\dispatch.verify.ps1` -> expected exit 0.
- `git diff -- crates/script-graph/src/lib.rs` -> expected no Rust content
  change beyond newline normalization.
- `cargo test -p rge-kernel-graph-foundation --test diff_correctness` ->
  expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, plus: halt and route back to Planner if making
the format gate pass requires any semantic Rust change, any Cargo configuration
change, any architecture-policy change, or edits outside
`crates/script-graph/src/lib.rs` beyond the existing ISSUE-83 test change and
required execution-report packet.

## Planner Notes

The only blocking issue reported by the verification gate is newline style in
`crates/script-graph/src/lib.rs`. This correction does not approve additional
test coverage, production behavior changes, dependency changes, or cleanup.
Keep the prior ISSUE-83 test addition intact and limit this round to making the
canonical verification gate pass.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-83
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

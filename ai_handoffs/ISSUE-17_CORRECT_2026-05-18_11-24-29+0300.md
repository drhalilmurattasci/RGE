# Correction Packet

DISPATCH_ID: ISSUE-17
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18T11:24:29+03:00
RELATED_FILES:
- crates/cad-core/src/topology/provider.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-17_TASK_2026-05-18_10-51-39+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-17_EXEC_2026-05-18_11-18-53+0300.md`
- Codex Control Review: `needs_changes` review result provided by Planner instruction for this correction round.
- Prior Correction Packet: `ai_handoffs/ISSUE-17_CORRECT_2026-05-18_11-13-45+0300.md`

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. Each correction references the specific control-review finding it
addresses.

1. **Refresh `BRepProvider` public docs** - Control review finding:
   `crates/cad-core/src/topology/provider.rs` has stale public docs around
   lines 11 and 43-47 saying only `CuboidOp` implements `BRepProvider` and
   that `SweepOp` deliberately does not implement it. Required change: edit
   only `crates/cad-core/src/topology/provider.rs` doc comments so they no
   longer claim the provider surface is Cuboid-only or that Sweep is excluded.
   The docs must reflect the current direct providers, including Sweep, without
   changing Rust APIs, provider behavior, resolver behavior, tests, or any
   other file. Acceptance: inspection of `topology/provider.rs` shows the docs
   accurately describe the current direct provider set and no stale
   `SweepOp`-does-not-implement wording remains; formatting and the canonical
   dispatch verification gate pass.

## Deferred Findings (NOT Approved for This Round)

None. The control review found implementation and verification otherwise
coherent, with the canonical dispatch verification log reporting all 6 steps
passed. No other correction is approved for this round.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with this documentation-accuracy addition:
`crates/cad-core/src/topology/provider.rs` public docs must match the current
direct `BRepProvider` implementors, including Sweep.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --check` -> expected exit 0
- `git diff --check` -> expected exit 0
- `.\.ai\dispatch.verify.ps1` -> expected exit 0

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, plus:

- If fixing the stale docs requires editing any file other than
  `crates/cad-core/src/topology/provider.rs`, halt and route back to Planner.
- If the correction requires semantic Rust changes, test changes, resolver
  changes, or generated artifact changes rather than documentation comments in
  `provider.rs`, halt and route back to Planner.
- If any re-verification gate fails after the documentation correction, halt
  and preserve the failing output.

## Planner Notes

This correction is intentionally narrow. The prior implementation, tests, and
canonical dispatch gate are accepted except for stale public docs in
`topology/provider.rs`. Do not expand this round into additional cleanup or
source changes.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-17
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

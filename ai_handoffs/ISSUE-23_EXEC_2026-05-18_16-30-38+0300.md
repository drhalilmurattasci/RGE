# Execution Report

DISPATCH_ID: ISSUE-23
AUTHOR: Executor / Claude
TIMESTAMP: 2026-05-18_16-30-38+0300
RELATED_FILES:
- crates/cad-projection/tests/transform_brep_face_lookup_smoke.rs — new focused cad-projection smoke test (1 test function)
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-23_TASK_2026-05-18_16-24-24+0300.md`

## Pre-Execution Review Consumed

No pre-execution review issued for this dispatch.

## What I Changed

### Source

- None. No production / `cad-core` / renderer / `Cargo` / docs / schema /
  script changes were made.

### Tests

- `crates/cad-projection/tests/transform_brep_face_lookup_smoke.rs::cuboid_through_transform_root_resolves_upstream_cuboid_face_ids`:
  one new integration test. It builds a `CadGraph` with
  `CuboidOp -> TransformOp` (Transform carries a non-identity translation
  `[3.0, -2.0, 1.0]`, default identity rotation + unit scale), connects the
  Cuboid into Transform input port 0, sets the Transform node as the graph
  root, and commits. It then spawns a `CadProjection` entity bound to the
  **Transform root** (not the upstream Cuboid node), sets
  `BRepHandle.brep_owner` to `TEST_OWNER` (`[0x23; 16]`), and ticks the
  projection. The proof: the projected mesh has 12 triangles and a `Some`
  `face_labels` (Transform preserved upstream labels through projection),
  and every triangle `0..12` resolves via
  `CadProjection::brep_face_id_for_triangle` to the upstream Cuboid's
  `BRepFaceId` minted by `CuboidOp::brep_face_ids(TEST_OWNER)` — in the
  canonical 2-triangles-per-face order (`direct_ids[tri / 2]`). The test
  fails if Transform projection drops face labels (every triangle would
  resolve to `None`) or if cad-projection lookup no longer resolves through
  the Transform root to the upstream Cuboid face IDs.

### Docs

- None.

## Per-File Summary

`crates/cad-projection/tests/transform_brep_face_lookup_smoke.rs` is the sole
new file — the single optional cad-projection test file explicitly permitted
by the Task Packet. It is modeled on the existing local patterns in
`sweep_brep_face_id_lookup_smoke.rs` and `brep_face_id_lookup_smoke.rs`
(graph build, `spawn_brep_entity`, post-spawn `brep_owner` set, `tick`,
direct-provider `BRepFaceId` comparison). It contains exactly one `#[test]`
function whose name contains `transform`. No identity-derivation, resolver,
`TransformOp`, `CuboidOp`, tessellation, or projection-cache behavior was
touched.

## Verification Results

- `cargo test -p rge-cad-projection transform -- --nocapture` → exit 0.
  The new test `cuboid_through_transform_root_resolves_upstream_cuboid_face_ids`
  was executed (the `transform` filter matched it because its name contains
  `transform`) and passed: `1 passed; 0 failed; 0 ignored`. All other
  cad-projection test binaries reported `0 passed` with their tests filtered
  out, confirming the new test is the only one the filtered command runs.

## Deviations from Task Packet

None — execution stayed strictly within the Task Packet scope. Exactly one
new test file was added (the permitted optional file); no MUST-NOT-edit path
was touched; no production code, cad-core identity derivation, renderer/gfx
code, Cargo metadata, docs, schemas, or scripts were changed.

## Open Questions for Reviewer

None. The test relies on `TransformOp` being topology-preserving (indices and
face labels pass through one-for-one per `crates/cad-core/src/operators/transform.rs`)
and on the resolver's identity-preserving `Transform` arm
(`crates/cad-core/src/topology/resolve.rs` recurses to the single input), so
the canonical `tri / 2` face mapping carries through the Transform root
unchanged. If a Reviewer prefers a looser membership assertion
(`direct_ids.contains(&resolved)`) over the exact canonical-order assertion,
note it — the current form is the stronger check and still a single test.

## Worktree State

- Tracked files: clean (no tracked file modified).
- Untracked items:
  - `crates/cad-projection/tests/transform_brep_face_lookup_smoke.rs` (new test)
  - `ai_handoffs/ISSUE-23_TASK_2026-05-18_16-24-24+0300.md` (+ `.meta.json`)
  - `ai_handoffs/ISSUE-23_EXEC_2026-05-18_16-30-38+0300.md` (this packet)
- Branch: ai-dispatch/ISSUE-23
- Last commit: `aabd87a` ai-dispatch ISSUE-22: [ai-dispatch] Broaden #18: TransformOp label preservation + stale-test reconciliation

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-23
AUTHOR: Executor / Claude
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---

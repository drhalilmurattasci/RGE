# Correction Packet

DISPATCH_ID: ISSUE-70
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20T16:34:59+03:00
RELATED_FILES:
- crates/editor-ui/tests/node_graph_script_graph_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-70_TASK_2026-05-20_16-28-42+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-70_EXEC_2026-05-20_16-34-19+0300.md`
- Latest Review Report: none; this correction is based on the post-execution verification gate failure.
- Prior Correction Packets: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections -- nothing more, nothing
less. Each correction references the specific verification failure it addresses.

1. **Normalize Rust test file newline style** -- Post-execution verification
   failed at `cargo +nightly fmt --all -- --check` with `Incorrect newline
   style in \\?\A:\RCAD\RGE\crates\editor-ui\tests\node_graph_script_graph_smoke.rs`.
   Required change: normalize `crates/editor-ui/tests/node_graph_script_graph_smoke.rs`
   to LF newlines accepted by rustfmt, preserving file content and avoiding
   unrelated formatting or semantic edits. Acceptance: `cargo +nightly
   fmt --all -- --check` exits 0, and the canonical dispatch verification gate
   exits 0.

## Deferred Findings (NOT Approved for This Round)

None. The only approved correction for this round is the failed rustfmt newline
style check.

## Updated Acceptance Criteria

Unchanged from the Task Packet.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.\.ai\dispatch.verify.ps1` -> expected exit 0 across the canonical format,
  architecture lint, supply-chain, workspace test, and doctest gates.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, plus halt and report if making the rustfmt gate
pass requires editing any file other than
`crates/editor-ui/tests/node_graph_script_graph_smoke.rs` or requires changing
that file beyond newline normalization.

## Planner Notes

The verification gate failed before later canonical checks could run. This
packet authorizes only the newline-style correction required to clear the
format gate, followed by the full canonical verification gate to prove the
dispatch is publishable.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-70
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

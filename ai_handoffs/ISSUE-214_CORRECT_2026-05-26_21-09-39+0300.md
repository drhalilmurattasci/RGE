# Correction Packet

DISPATCH_ID: ISSUE-214
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-26_21-09-39+0300
RELATED_FILES:
- ai_handoffs/ISSUE-214_TASK_2026-05-26_20-39-49+0300.md
- ai_handoffs/ISSUE-214_TASK_2026-05-26_20-39-49+0300.meta.json
- ai_handoffs/ISSUE-214_EXEC_2026-05-26_20-53-50+0300.md
- ai_handoffs/ISSUE-214_EXEC_2026-05-26_20-53-50+0300.meta.json
- ai_handoffs/ISSUE-214_EXEC_*.md
- ai_handoffs/ISSUE-214_EXEC_*.meta.json
- AGENTS.md
- AI_DISPATCH_AUTOMATION.md
- ai_handoffs/AI_HANDOFF_PROTOCOL.md
- tools/invalidation-profiler/src/main.rs
- crates/gfx/src/frame_graph/compile/mod.rs
- plans/BASELINE.md
- change.md
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-214_TASK_2026-05-26_20-39-49+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-214_EXEC_2026-05-26_20-53-50+0300.md`
- Codex Control Review Result: ISSUE-214 round0 JSON supplied to Planner, verdict `needs_changes`
- Prior Correction Packets: none

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections and nothing more. This is a report-correction round only: write one corrected follow-up `ISSUE-214` EXEC packet and its matching `.meta.json` sidecar. Do not edit existing packets, source, docs, Cargo files, workflows, schemas, scripts, scheduler configuration, task briefs, dispatch logs, or any other file.

1. **Replace unsupported orchestrator packet-shape citations** - Control review finding: the prior EXEC report repeatedly cites `AI_DISPATCH_AUTOMATION.md:1041-1062` as support for the claim that orchestrator dispatches emit no `_REVIEW_` or `_CLOSEOUT_` packets, but those lines cover the read-only audit halt lesson and do not support that claim. Required change: in the corrected follow-up EXEC packet, replace every packet-shape use of `AI_DISPATCH_AUTOMATION.md:1041-1062`. Use live supporting citations such as `AGENTS.md:26-36` for the exact no-REVIEW/no-CLOSEOUT orchestrator rule, `AI_DISPATCH_AUTOMATION.md:961-973` for run-directory control JSON and post-run inspection context, and `ai_handoffs/AI_HANDOFF_PROTOCOL.md:128-145` only when contrasting the manual protocol packet type set. Acceptance: no occurrence of `AI_DISPATCH_AUTOMATION.md:1041-1062` remains in the corrected EXEC report, and every statement about orchestrator packet shape is backed by a live, relevant line citation.

2. **Re-scan and correct stale/off-by-one line ranges** - Control review finding: the prior EXEC report contains stale or off-by-one citation ranges, including `tools/invalidation-profiler/src/main.rs:1-6` for a 5-line file and `crates/gfx/src/frame_graph/compile/mod.rs:1-44` for a 43-line file. Required change: re-scan every remaining `path:line` or `path:start-end` citation used in the corrected follow-up EXEC packet before finalizing it. Correct the confirmed examples to `tools/invalidation-profiler/src/main.rs:1-5` and `crates/gfx/src/frame_graph/compile/mod.rs:1-43`, and fix any other stale ranges found by the re-scan. Acceptance: cited line ranges do not exceed the current live file lengths, and substantive claims about current files are supported by current line evidence rather than copied stale ranges.

3. **Make the selected follow-up internally consistent** - Control review finding: the prior EXEC report's decision block and candidate comparison contradict each other by describing the selected follow-up as `single-file` while also naming `plans/BASELINE.md` plus `change.md`. Required change: in the corrected follow-up EXEC packet, reconcile the decision block, candidate comparison, and open-question/follow-up wording so they name exactly one follow-up shape without contradiction. If the selected follow-up needs the standard append-only `change.md` chronology entry in addition to `plans/BASELINE.md`, say so consistently and do not call it single-file. If it is truly single-file, remove `change.md` from that follow-up. Acceptance: `SMALLEST_SAFE_FOLLOW_UP` names exactly one action, the candidate-comparison winner uses the same file list and scope, and the wording does not create a backlog list or conflicting scope count.

4. **Preserve the audit-only correction envelope** - Control review finding: the dispatch work stayed audit-only, no tracked files changed, and the orchestrator verification gate passed; only the EXEC report evidence needs correction. Required change: do not rewrite the erroneous EXEC packet, do not edit the TASK packet or any prior packet, and do not edit source, docs, schemas, scripts, scheduler config, Cargo files, workflows, task briefs, or dispatch logs. Add only one corrected follow-up `ai_handoffs/ISSUE-214_EXEC_*.md` report plus its matching `.meta.json` sidecar. Acceptance: `git diff --stat` and `git diff --name-only` remain empty; `git status --short --branch --untracked-files=all` shows only ISSUE-214 handoff artifacts allowed by this correction round; the corrected EXEC report is ready for review.

## Deferred Findings (NOT Approved for This Round)

None. All control review findings required to make this dispatch pass review are approved above, and no extra cleanup, source change, documentation change, benchmark, measurement, or task expansion is approved.

## Updated Acceptance Criteria

The original TASK acceptance criteria remain in force, with this correction-specific addition: the Executor must produce one corrected follow-up EXEC report and matching sidecar that fixes the unsupported packet-shape citations, stale line ranges, and contradictory follow-up wording described above. The correction round does not authorize changes to tracked source, tests, documentation, Cargo metadata, workflows, schemas, automation scripts, scheduler configuration, `.ai/dispatch.tasks.md`, existing packets, existing sidecars, or dispatch logs.

## Re-Verification Gates

The Executor MUST run and document these gates after writing the corrected follow-up EXEC packet:

- `git status --short --branch --untracked-files=all` -> expected: current branch is `ai-dispatch/ISSUE-214`; no tracked protected files changed; untracked files are limited to ISSUE-214 handoff artifacts allowed by the TASK and this correction packet.
- `git diff --stat` -> expected empty output.
- `git diff --name-only` -> expected empty output.
- `git diff --check` -> expected exit 0.
- Re-read `AGENTS.md`, `AI_DISPATCH_AUTOMATION.md`, and `ai_handoffs/AI_HANDOFF_PROTOCOL.md` with line numbers for the packet-shape claims; expected: corrected EXEC uses relevant live citations, not `AI_DISPATCH_AUTOMATION.md:1041-1062`.
- Search the corrected EXEC report for `AI_DISPATCH_AUTOMATION.md:1041-1062`; expected: zero matches.
- Re-scan every cited source/document path line range used in the corrected EXEC report; expected: no stale or out-of-range citations, including `tools/invalidation-profiler/src/main.rs:1-5` and `crates/gfx/src/frame_graph/compile/mod.rs:1-43` for the confirmed examples.
- Inspect the decision block and candidate comparison in the corrected EXEC report; expected: `SMALLEST_SAFE_FOLLOW_UP` names exactly one follow-up and the same scope/file list is used consistently everywhere.
- Read `.ai/dispatch-ISSUE-214/verification.round0.log` if present; expected: the prior orchestrator canonical gate remains readable and ended with `VERIFY OK: all 7 verification step(s) passed.`

No cargo command, formatter, architecture lint, benchmark, profiler, source edit, documentation edit, schema edit, script edit, scheduler command, GitHub mutation, nested dispatch, publish, push, merge, or branch operation is authorized for this correction round.

## Halt Conditions (Updated if Any)

Unchanged from the TASK packet, plus:

- Halt with `NEEDS_HUMAN` if correcting the EXEC report truthfully requires editing any source, test, documentation, Cargo, workflow, schema, automation script, scheduler configuration, task brief, existing packet, existing sidecar, or dispatch log.
- Halt with `NEEDS_HUMAN` if more than one corrected follow-up EXEC report or more than one matching sidecar is required.
- Halt with `NEEDS_HUMAN` if tracked protected files are dirty for reasons attributable to this correction round.
- Halt with `NEEDS_HUMAN` if the evidence cannot support exactly one internally consistent `SMALLEST_SAFE_FOLLOW_UP`.
- Halt rather than running cargo, tests, formatters, architecture lints, benchmarks, profilers, `.ai/dispatch.verify.ps1`, autonomous queue commands, nested dispatch commands, publish, push, merge, branch-delete, GitHub issue mutation, or scheduler commands.

## Planner Notes

This correction is intentionally narrow. The control review found that the audit stayed read-only and the orchestrator verification gate passed; the blocker is the report's citation accuracy and follow-up wording. Preserve the chain of custody by leaving the prior EXEC packet untouched and writing a corrected follow-up EXEC packet that supersedes it for review.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-214
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

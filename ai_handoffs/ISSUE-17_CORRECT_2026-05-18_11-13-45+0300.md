# Correction Packet

DISPATCH_ID: ISSUE-17
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-18T11:13:45+03:00
RELATED_FILES:
- crates/cad-core/src/topology/face_tag.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-17_TASK_2026-05-18_10-51-39+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-17_EXEC_2026-05-18_11-12-25+0300.md`
- Latest Review Report: none; post-execution canonical verification failed before control review.
- Prior Correction Packets: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less. Each correction references the specific verification failure it addresses.

1. **Add `face_tag.rs` split exemption** - Verification gate failure:
   `cargo run -q -p rge-tool-architecture-lints -- all` failed
   `[split-exemption]` because `crates/cad-core/src/topology/face_tag.rs`
   is 1155 lines and has no `// SPLIT-EXEMPTION:` annotation. Required
   change: edit only `crates/cad-core/src/topology/face_tag.rs` to add a
   concise top-of-file `// SPLIT-EXEMPTION:` annotation explaining why the
   per-operator face-tag enums and their local serialization/discriminant
   tests remain cohesive after the ISSUE-17 Sweep tag addition. Do not
   change Rust APIs, enum variants, discriminants, serialization behavior,
   test assertions, formatting policy, architecture-lint code, or any other
   file. Acceptance: the split-exemption lint reports 0 violations and the
   canonical dispatch verification gate exits 0.

## Deferred Findings (NOT Approved for This Round)

None. The post-execution verification log reported one blocking failure, the
`face_tag.rs` split-exemption annotation gap above. No other correction is
approved for this round.

## Updated Acceptance Criteria

Unchanged from the Task Packet, with this verification-only addition: the
repository architecture lint suite must pass after adding the required
split-exemption annotation to `crates/cad-core/src/topology/face_tag.rs`.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo run -q -p rge-tool-architecture-lints -- all` -> expected exit 0
- `.\.ai\dispatch.verify.ps1` -> expected exit 0

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, plus:

- If passing the split-exemption lint requires editing any file other than
  `crates/cad-core/src/topology/face_tag.rs`, halt and route back to Planner.
- If the correction requires semantic source or test changes rather than the
  annotation-only fix above, halt and route back to Planner.
- If the canonical verification gate exposes a new, distinct blocking failure
  after this annotation is added, halt and preserve the failing output.

## Planner Notes

The canonical verification gate already passed rustfmt before failing in
architecture lints. The blocking issue is narrow: `face_tag.rs` crossed the
1000-line cap as part of the cohesive per-operator face-tag surface, and the
existing lint accepts a reasoned `// SPLIT-EXEMPTION:` marker. This packet does
not approve splitting topology modules, changing Sweep identity behavior, or
touching architecture-lint implementation.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-17
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Execution Report

DISPATCH_ID: ISSUE-254
AUTHOR: Executor / Claude
TIMESTAMP: 2026-05-28_23-49-37+0300
RELATED_FILES:
- ai_handoffs/ISSUE-254_TASK_2026-05-28_22-57-20+0300.md — original audit Task Packet (unchanged)
- ai_handoffs/ISSUE-254_CORRECT_2026-05-28_23-47-27+0300.md — round1 Planner correction packet driving this packet
- plans/BASELINE.md — read-only (Editor-usability preflight § + Live-inspector wiring preflight §)
- editor/rge-editor/src/main.rs — read-only audit (CLI parsing, `--glb` / `--scene` paths, glTF ingestion, watcher wrapper, `#[cfg(test)]` test helpers)
- editor/rge-editor/Cargo.toml — read-only audit (deps, `--scene` / `--glb` rge-data / rge-scene-loader / rge-io-gltf wiring)
- crates/editor-shell/src/lifecycle/commands.rs — read-only audit (`EditorKeyCommand`, `SetTimeScale` Action impl, shell-side CommandBus surface)
- crates/editor-shell/src/lifecycle/playback.rs — read-only audit (`EditorPlaybackCommand` for Space / Escape → PIE)
- crates/editor-shell/src/lifecycle/mod.rs — read-only audit (`window_event::KeyboardInput` + `ModifiersChanged` branches, egui-consumed gating)
- crates/editor-shell/src/render_path.rs — read-only audit (EguiHost construction, `render_frame_egui_only`)
- crates/editor-shell/src/play_toolbar.rs — read-only audit (W08 doc-comment narrative referencing `MenuRegistry`)
- crates/editor-ui/src/dock/spawner_registry.rs — read-only audit (`PlaceholderTabBody` registration still v0)
- crates/editor-egui-host/src/tabs.rs — read-only audit (`TabBody::Inspector` + `EditorTabViewer`)
- crates/editor-actions/src/action.rs — read-only audit (`Action::apply` World-only signature)
- crates/editor-actions/src/bus.rs — read-only audit (`CommandBus::submit` World-only signature)
STATUS: AWAITING_REVIEW

## Task Packet Reference

`ai_handoffs/ISSUE-254_TASK_2026-05-28_22-57-20+0300.md`

## Pre-Execution Review Consumed

No pre-execution review issued for this dispatch.

This packet is the **canonical, self-contained corrected EXECUTION_REPORT**
prepared in response to the round1 Planner CORRECTION packet
`ai_handoffs/ISSUE-254_CORRECT_2026-05-28_23-47-27+0300.md`. Per that
packet's two approved corrections, this report (1) carries the five
Task-required audit sections in full as a self-contained corrected answer
block — with the round0 `MenuRegistry` and `fs::write` evidence corrections
applied **inline**, not by reference — and (2) documents the canonical round1
verification substeps exactly as `.ai/dispatch-ISSUE-254/verification.round1.log`
and `.ai/dispatch.verify.ps1` recorded them.

This is still an audit-only correction round. No production source, docs,
scripts, schemas, prior packets, or files outside this new EXEC packet (and its
forthcoming sidecar / optional log) are touched. The audit's classifications
and recommendations are unchanged from the original EXEC report; this report
supersedes the two prior EXEC reports
(`ai_handoffs/ISSUE-254_EXEC_2026-05-28_23-06-49+0300.md` and
`ai_handoffs/ISSUE-254_EXEC_2026-05-28_23-36-58+0300.md`) and stands alone as
the audit deliverable.

## What I Changed

### Source

- None. This is an audit-only correction round; no existing tracked file was
  modified.

### Tests

- None. No new tests added.

### Docs

- None. `plans/BASELINE.md`, `Status.md`, `HANDOFF.md`, `change.md`, and every
  other existing Markdown / docs file are byte-identical pre- and
  post-correction. This packet is the only deliverable, plus the matching
  sidecar JSON and the dispatch log under `ai_dispatch_logs/` if emitted.

## Audit Answer Block (self-contained corrected answer)

This block contains the five Task-required deliverable sections in full. Every
positive and negative current-state claim is grounded against a current source
line range or a recorded targeted-search result. The two round0 evidence
corrections (the literal `MenuRegistry` doc-comment hit and the `fs::write`
test-fixture hits) are applied inline below; no incorporation-by-reference is
needed to understand the corrected answer. Negative claims inherited from the
2026-05-21 BASELINE preflight have all been re-verified against current source
— the ISSUE-253 control-block premise error (treating 2026-05-21 negative
claims as still authoritative without re-checking source) is explicitly avoided
here. All reads/searches were run on branch `ai-dispatch/ISSUE-254` at
`HEAD = 273d658`.

### 1. Current Source Evidence Read

#### Files inspected (read tool, current branch `ai-dispatch/ISSUE-254` at `HEAD = 273d658`)

| File | Line range read | Why |
|---|---|---|
| `plans/BASELINE.md` | 560-639 | Editor-usability preflight (Top 3 gaps at 586-592, revisit triggers at 615-620, methodology + status footer). |
| `plans/BASELINE.md` | 660-720 | 2026-05-28 Live-inspector wiring reconciliation (post-Dispatch-F + #249) — the only reconciliation that has already prepended above the 2026-05-21 egui-host snapshot. |
| `plans/BASELINE.md` | 705-796 | 2026-05-21 initial egui-host status snapshot + Notes/caveats (cross-references to "CommandBus integration design preflight"). |
| `editor/rge-editor/src/main.rs` | 1-1242 (then targeted reads beyond limit) | CLI parser (`Cli`, `parse_args`); `--glb <path>` (`load_all_glb_meshes` → `rge_io_gltf::import_glb`); `--scene <path>` (`load_scene_world_from_path` → `rge_data::Project` / `rge_data::Scene` RON parse → `rge_scene_loader::load_scene_into_world`); `build_cuboid_demo_shell` default branch; `EditorApp::window_event` `RedrawRequested` watcher drain (lines 877-907); `#[cfg(test)] mod tests` block opening at `:1043`. |
| `editor/rge-editor/Cargo.toml` | 1-71 | Confirms deps: `rge-editor-shell`, `rge-cad-core`, `rge-cad-projection`, `rge-kernel-ecs`, `rge-io-gltf`, `rge-brep-render`, `rge-data`, `rge-scene-loader`, `ron`, `glam`, `winit`, `tracing`, `tracing-subscriber`, `notify`. No `rge-io-image` dep; no `egui` dep (host comes in via `editor-shell → rge-editor-egui-host`). |
| `crates/editor-shell/src/lifecycle/commands.rs` | 1-409 (whole file) | `EditorKeyCommand::{Undo, Redo, MarkSaved, SetTimeScaleDoubleSpeed, ResetTimeScaleDefault, SetTimeScaleMaxFastForward}`; `from_key_press` mapping (Ctrl-without-Shift); `SetTimeScale` Action impl with coalesce-via-payload; shell methods `submit_action` / `undo_command` / `redo_command` / `mark_saved_command` / `command_bus` / `handle_key_command` / `set_time_scale`. |
| `crates/editor-shell/src/lifecycle/playback.rs` | 1-465 (whole file) | `EditorPlaybackCommand::{TogglePlay, Stop}`; `from_key_press` (modifiers-must-be-empty); `handle_playback_command` driving `ToolbarButtonId::Play|Pause|Stop`. Module docstring explicitly: "No CommandBus involvement; no new ECS state; no toolbar UI." |
| `crates/editor-shell/src/lifecycle/mod.rs` | 1-100, 1515-1741 | `window_event` arms: `CloseRequested`, `Resized` (with egui `host.resize`), `RedrawRequested`, `CursorMoved`, `MouseInput` (egui-consumed gate via `should_fire_face_pick`), `ModifiersChanged` (tracks `self.modifiers`), `KeyboardInput` (cascade `EditorKeyCommand` → `EditorPlaybackCommand` → plain-`R` asset reload). Catch-all `_ => {}` at line 1725. |
| `crates/editor-shell/src/render_path.rs` | targeted reads via grep (see below) — confirmed `gfx_ctx.queue().submit(...)` at lines 485, 607, 1039 around encoder.finish; egui pass references documented per the 2026-05-28 reconciliation. |
| `crates/editor-shell/src/play_toolbar.rs` | 1-16 | W08 `PlayToolbar` module doc-header narrative; the single literal `MenuRegistry` substring in the editor runtime surface lives here at `:12` as a forward-looking cross-reference (see §1 search 3 and Gap 3). |
| `crates/editor-ui/src/dock/spawner_registry.rs` | 120-210 | `PlaceholderTabBody { source: TabId, title: String }` is the only body the `register_default_spawners` convenience produces against the default-tab id set, including `"tab/inspector"`. The host-side `editor-egui-host::TabBody::Inspector` is a separate, host-internal enum (not registered through `editor-ui::SpawnerRegistry`). |
| `crates/editor-egui-host/src/tabs.rs` | 1-150 | Host-internal `TabBody::{Viewport, Inspector(InspectorTabBody), Placeholder{title}}` + `EditorTabViewer`. The Inspector body carries `Arc<InspectorHandoff>`, not a snapshot copy; the widget receives a freshly-acquired snapshot per frame. |
| `crates/editor-actions/src/action.rs` | 74-96 (grepped) | `pub trait Action: Send + Sync + 'static` with `apply(&self, world: &mut World)` and `revert(&self, world: &mut World)` — still World-only. |
| `crates/editor-actions/src/bus.rs` | 143 (grepped) | `submit(&mut self, action: Box<dyn Action>, world: &mut World)` — still World-only. |
| `docs/adr/` (directory listing) | 097..120 | No ADR named for `CommandBus` context or `EditorCommandCtx`. Grep for `CommandBus context|EditorCommandCtx|CommandBus.*CadGraph` returned no matches in `docs/adr/`. |

#### Targeted searches (commands run via Grep tool; ripgrep semantics; no result was inferred — every negative below was actually executed, and every doc-comment / test / helper hit is reported separately from the runtime-functional negative)

1. **`CommandBus|\.submit\(|editor_actions::`** across `crates/editor-shell/**/*.rs`, `crates/editor-ui/**/*.rs`, `editor/rge-editor/**/*.rs`, `crates/editor-egui-host/**/*.rs`
   - **Non-zero matches in editor-shell production:** `crates/editor-shell/src/lifecycle/commands.rs:43,44,280,291,302,309,318` (use+constructor of `CommandBus` + `submit` / `undo` / `redo` / `mark_saved`), `crates/editor-shell/src/lifecycle/mod.rs:185,492,507,609,693,941` (`use rge_editor_actions::CommandBus;` + field + three constructors `command_bus: CommandBus::new()`), and `crates/editor-shell/src/render_path.rs:485,607,1039` (these `.submit(...)` matches are `wgpu::Queue::submit`, not the bus — confirmed by surrounding `encoder.finish()` context).
   - **Non-zero matches in tests only:** `crates/editor-shell/tests/keyboard_command_bus_round_trip.rs`, `crates/editor-shell/tests/time_scale_test.rs`.
   - **Zero matches in `editor/rge-editor/**/*.rs`, `crates/editor-ui/**/*.rs`, `crates/editor-egui-host/**/*.rs`** (re-grepped `CommandBus` literally across each of these three scopes — zero matches in each; no doc-comment / test surprise hits). The bus is reached only via `editor-shell`'s shell-side surface; the binary, the UI crate, and the host crate do not name it.

2. **`KeyboardInput|key_code|modifiers`** within `crates/editor-shell/src/lifecycle/mod.rs`
   - Hits at `:1676` (`WindowEvent::KeyboardInput { event, .. }` match arm), `:1668` (`ModifiersChanged`), `:1702-1707` (`self.modifiers.control_key()` / `shift_key()` + `EditorKeyCommand::from_key_press` + `EditorPlaybackCommand::from_key_press`). The 2026-05-21 baseline claim that `lifecycle.rs:776` is a catch-all that swallows every keyboard event is **STALE** — the catch-all is now at `:1725` and the keyboard branch above it is real and routes through three handlers. (Positive-current-state claim, backed by concrete line citations.)

3. **`MenuRegistry` (literal token)** across `crates/editor-shell/src/**/*.rs`, `editor/rge-editor/**/*.rs`, `crates/editor-egui-host/**/*.rs`
   - **One literal match total:** `crates/editor-shell/src/play_toolbar.rs:12` — doc-comment line ``//! `MenuRegistry`. Extension-point IDs follow the dotted-path convention``. This is the W08 narrative cross-reference in the `PlayToolbar` module's doc header (see also `play_toolbar.rs:5-8`: "The real menu/toolbar registry lives in `editor-ui/menus` (W08); when W08 lands, this module's `PlayToolbar::register_into` will hand its `ToolbarButton` entries to the registry. Until W08, the registration is a **stub**"). It is a forward-looking doc reference, **not** a construction or invocation of `MenuRegistry`.
   - **Zero literal `MenuRegistry` matches** in `editor/rge-editor/**/*.rs`.
   - **Zero literal `MenuRegistry` matches** in `crates/editor-egui-host/**/*.rs`.
   - **Functional menu searches (narrower, run individually; every result reported):**
     - `MenuRegistry::` (associated-function call) across the same three scopes — **zero matches.** No `MenuRegistry::new(...)`, `MenuRegistry::default()`, `MenuRegistry::register(...)`, etc. exists in the editor runtime surface.
     - `ResolvedEntry` across the same three scopes — **zero matches.** No `ResolvedEntry` value is constructed, named, or pattern-matched in the editor runtime surface.
     - `menus::Command` across the same three scopes — **zero matches.** No `editor-ui::menus::Command` variant is named, constructed, or dispatched against in the editor runtime surface.
     - `\.resolve\(` (literal `.resolve(` method call) across the same three scopes — **zero matches.** No `.resolve(...)` call (the entry point for `MenuRegistry::resolve` on a `MenuRegistry` value, or any other resolver pattern) exists in the editor runtime surface.
   - **Conclusion (functional negative, source-grounded on the four narrower searches):** the editor runtime surface (`editor-shell` production code + `rge-editor` binary + `editor-egui-host` production code) does **not** construct a `MenuRegistry`, does **not** call any `MenuRegistry::*` associated function, does **not** call `.resolve(...)` on any value, does **not** name `ResolvedEntry`, and does **not** name `menus::Command` variants. The only `MenuRegistry::new` / `resolve` call sites in the entire workspace are inside `crates/editor-ui/src/menus/**` (internal tests + doc-comments) and `crates/editor-ui/tests/menus_ordering.rs`. Separately, the literal token `MenuRegistry` appears once in the editor-runtime scope at `crates/editor-shell/src/play_toolbar.rs:12` as a doc-comment cross-reference; that mention is non-functional (no code path executes from a doc comment) and does not contradict the functional negative claim.
   - **[round0 correction applied]** The original EXEC's wording "**Zero matches** in those scopes" for the literal `MenuRegistry` token was overbroad and is corrected here: the literal token has the one `play_toolbar.rs:12` doc-comment hit; the functional negative is carried by the four narrower searches above.

4. **`io_gltf::|io_image::|io_3mf::`** across the same scopes
   - **Non-zero matches:** `editor/rge-editor/src/main.rs` extensively imports and uses `rge_io_gltf::` (`import_glb`, `MemoryCache`, `Scene`, `EntityComponents`, `MaterialAsset`, `MeshAsset`, `Transform`, `PixelFormat`); `crates/editor-shell/src/lifecycle/asset_reload.rs:57` and `crates/editor-shell/src/lifecycle/mod.rs:153,428,726` mention `rge_io_gltf::MemoryCache` in doc-comments + `AssetReloadHook` trait paperwork (these are doc/paperwork mentions, **not** functional `io_gltf` consumption from editor-shell — the editor-shell crate stays glTF-agnostic; the binary owns the loader edge).
   - **Zero matches** for `io_image::` or `io_3mf::` anywhere in editor-shell / editor-ui / rge-editor / editor-egui-host (re-grepped `io_image` and `io_3mf` literally across all four scopes — zero matches in each; no doc-comment / test surprise hits). The 2026-05-21 baseline claim that `io-image::load_path` / `load_bytes` have zero editor-surface call sites is **CURRENT.**

5. **`load_path|load_bytes`** across the same scopes — **zero matches** in editor-shell, rge-editor, editor-egui-host, and editor-ui (re-grepped literally; no doc-comment / test surprise hits), confirming the io-image consumer count remains zero on the editor surface.

6. **`\.rge"|\.rgeproj"|\.scene"|\.project"|\.rge-project|\.rge-scene`** across the same scopes
   - **Zero matches in `crates/editor-shell/**/*.rs`.** The wrapper has no extension-aware code; the binary owns the parse.
   - The `editor/rge-editor/src/main.rs` matches concern the `--scene <path>` parser, at lines `:28, :29, :121, :122, :170, :213, :220, :225, :230, :232, :244, :251, :254, :258, :263, :271, :272, :284, :286, :287, :289, :291, :1132, :1133, :1139, :1140, :1151, :1158, :1179, :1188`. This inventory covers (i) the module-level doc-comment narrative explaining `--scene` (`:28-29`, `:121-122`, `:170`), (ii) the `SceneLoadCliError` enum doc comments and `Display` arms (`:213-263`), (iii) the load implementation including the `file_name == ".rge-project"` dispatch at `:289` and the `.rge-scene` suffix check at `:291` (`:271-291`), and (iv) `parse_args` tests (`:1132-1188`). The 2026-05-21 baseline claim of "zero `.rge` / `.rgeproj` / `.scene` / `.project` extension matches anywhere in `editor-shell` or `rge-editor`" is **STALE** for `rge-editor` because ISSUE-225 added the `--scene` CLI ingestion path. **Still CURRENT** for `editor-shell` proper.

7. **`open_file|load_project|save_project|save_scene|save_to_path`** across the same scopes
   - **Hits only in `crates/editor-ui/src/menus/command.rs:103`** (`Command::OpenFile.diagnostic_id() == "open_file"` — a string-literal id on the `Command` enum, not a function) and **`crates/editor-ui/src/menus/command.rs:154`** (a unit-test assertion in the same file — the diagnostic-id round-trip test, test-only, not a runtime dispatcher).
   - **Zero hits in `crates/editor-shell/**/*.rs`, `editor/rge-editor/**/*.rs`, and `crates/editor-egui-host/**/*.rs`.** No `load_project`, `save_project`, `save_scene`, or `save_to_path` symbol is defined or called anywhere in the editor surface. The `Command::OpenFile` variant exists and has a diagnostic id, but no production code in editor-shell / rge-editor / editor-egui-host constructs, names, dispatches against, or executes that variant; only `editor-ui` itself owns the enum, and its only reachable consumer is the unit test in the same file.

8. **`drag|drop|DragAndDrop|DroppedFile|HoveredFile`** (then a follow-up restricted to `DroppedFile|HoveredFile|HoveredFileCancelled` and `DragAndDrop`) across the same scopes
   - `DroppedFile|HoveredFile|HoveredFileCancelled` — **zero matches in every scope.**
   - `DragAndDrop` — **zero matches in every scope.**
   - The broad `drag|Drag|drop|Drop` match-set resolves entirely into non-winit-drag-and-drop narrative and Rust `Drop` semantics, reported per scope so the negative is precise:
     - `crates/editor-shell/**/*.rs`: (a) "slider drag" / "rapid drag" doc-comment narrative for the `SetTimeScale` Action's coalescing semantics (`commands.rs:28-31`, `:118`, `:146`, `:171`, `:200`, `:214`, `:224`, `:370-373`), (b) the same narrative in the `lifecycle/mod.rs` module doc-header (`:81`), and (c) the comment at `mod.rs:1644` "scroll / drag / hover are non-goals (later dispatches)." Tests in `crates/editor-shell/tests/time_scale_test.rs` echo the same slider-drag terminology.
     - `crates/editor-egui-host/**/*.rs`: egui's `drag-undock` / `drag-detach` dock-window UI narrative in doc comments (`lib.rs:275`, `:336`, `:415`, `:487`, `:536`), `Arc` / snapshot drop-old narrative (`handoff.rs:44`, `:106`, `:126`, `:236`; tests `inspector_handoff_smoke.rs:161-182`, `dock_layout_smoke.rs:19`), `host_scaffolding_smoke.rs:48` test comment, the encoder's "begin render pass + render + drop" comment (`lib.rs:582`), and the `lib.rs:7` "drop the bad frame, log, continue" doc-comment.
     - `editor/rge-editor/**/*.rs`: the explicit `drop(tracing_subscriber::fmt::try_init());` call at `main.rs:919` (Rust `Drop` semantics for the lint workaround), `drop(std::fs::remove_file(...))` cleanup calls in the `#[cfg(test)]` block (`:1756`, `:2425-2426`, `:2579-2580`, `:2731`), test-comment narrative around `EditorShell` lifetime drop at `:1807`, the watcher's "Errors from the notify callback are dropped" doc-comment narrative in `glb_watcher.rs:32-162`, and the `main.rs:65` "image bytes ... are dropped on the floor; io-gltf doesn't extract image bytes" doc-comment.
   - **Conclusion:** there is no winit drag-and-drop wiring in the editor surface; the broad `drag|drop` matches are doc-comment narrative, test-cleanup `drop(...)` calls, `Drop`-trait drops, and one negative-scope comment that explicitly defers drag/scroll/hover to "later dispatches."

9. **`LayoutService|workspace_layout`** within `crates/editor-shell/src/**/*.rs`, `editor/rge-editor/**/*.rs`, `crates/editor-egui-host/**/*.rs` — **zero matches in each** (re-grepped literally; no doc-comment / test surprise hits). `LayoutService::{save, load, load_or_migrate}` is exercised only by `crates/editor-ui/src/dock/layout_service.rs` itself and its own tests; the editor binary / shell / egui host do not consume it. The 2026-05-21 BASELINE record at `:580` that workspace-layout persistence "works" refers to the substrate; it is not bound to a user-input gesture.

10. **Save-direction symbols, searched individually (round0 correction applied; the original EXEC's single bundled alternation overstated the result)** across `editor/rge-editor/src/` and `crates/editor-shell/src/`:
    - `ron::ser` — **zero matches.**
    - `ron::to_string` — **zero matches.**
    - `save_project` — **zero matches.**
    - `save_scene` — **zero matches.**
    - `save_to` — **zero matches.**
    - `write_to_file` — **zero matches.**
    - **`fs::write` — six matches, all in `editor/rge-editor/src/main.rs`, all `std::fs::write(...)`, all inside the `#[cfg(test)] mod tests` block opening at `main.rs:1043`** (zero matches in `crates/editor-shell/src/`):
      - `:1740` — `std::fs::write(&path, &bytes).expect("write");` — writes an exported GLB byte-stream to `std::env::temp_dir()` so the surrounding test can re-load it via `load_all_glb_meshes`.
      - `:2289` — `std::fs::write(&malformed_path, b"not a glb").expect("write malformed bytes");` — writes a malformed-GLB fixture for the ISSUE-89 reload-error test.
      - `:2376` — `std::fs::write(&malformed_path, &valid_bytes).expect("overwrite with valid bytes");` — overwrites that fixture with valid bytes to drive the recovery half.
      - `:2489` — `std::fs::write(&staged_path, b"NOT-A-VALID-GLB").expect("write malformed");` — writes a malformed staging fixture for the watcher-debounce test.
      - `:2546` — `std::fs::write(&staged_path, &valid_bytes).expect("write valid bytes");` — completes the same watcher-debounce test's positive half.
      - `:2720` — `std::fs::write(&path, &bytes).expect("write");` — writes an exported GLB to `temp_dir` for the M3 no-normals flat-shading test.
    - **Conclusion:** the editor runtime surface has no `ron::ser` invocation, no `ron::to_string` invocation, and no `save_project` / `save_scene` / `save_to` / `write_to_file` symbol — so the editor cannot write a `.rge-project` or `.rge-scene` back to disk through any production code path. The six `fs::write` hits are **test-helper fixture writes** (they construct on-disk GLB binary input that a test feeds back into `load_all_glb_meshes` or the notify watcher to exercise the load / reload paths); they are non-semantic for live editor save behavior and do not establish a runtime save direction. The 2026-05-21 baseline claim that the editor never invokes the `rge-data` RON serializer remains **CURRENT** for the save direction.

11. **`impl Action for|impl rge_editor_actions::Action`** across `crates/` — production hits: `crates/editor-actions/src/compound.rs:51` (`CompoundAction`, a meta-wrapper provided by the bus crate itself), and `crates/editor-shell/src/lifecycle/commands.rs:193` (`SetTimeScale`). Every other hit is in `tests/` or `#[cfg(test)]` blocks. The editor surface has exactly one production `Action` impl, `SetTimeScale`, and it touches `World::insert_resource(TimeScale)` only. (Positive-current-state claim, backed by concrete line citations.)

12. **`CommandBus context|EditorCommandCtx|CommandBus.*CadGraph`** across `docs/adr/**` — **zero matches.** No ADR or design note records a decision on the bus context shape (no `EditorCommandCtx` aggregate, no `(&mut CadGraph, &mut CadProjection)` extension). (Zero-result claim narrowly scoped to ADR files.)

### 2. Gap 1 / 2 / 3 Current Status

For each `plans/BASELINE.md:586-592` gap, the source-grounded classification:

#### Gap 1 — "No scene/project persistence — zero file-I/O paths for actual scene state."

**Classification: PARTIALLY STALE.**

- **Load direction — STALE.** ISSUE-225 landed the `--scene <path>` CLI path. `editor/rge-editor/src/main.rs:912-968` parses `.rge-project` / `.rge-scene` files (via `load_scene_world_from_path` at `:280-339`), constructs an `EditorShell::with_world(shell_world)` from a `rge_kernel_ecs::World` populated by `rge_scene_loader::load_scene_into_world`, and runs the winit event loop. The dep wiring at `editor/rge-editor/Cargo.toml:36-38` (`rge-data`, `rge-scene-loader`, `ron`) is real. The negative claim "the editor never calls" the RON serializer is no longer true for the load direction.
- **Save direction — CURRENT.** Grep for the runtime save / RON serialization symbols `ron::ser`, `ron::to_string`, `save_project`, `save_scene`, `save_to`, and `write_to_file` individually across `editor/rge-editor/src/` and `crates/editor-shell/src/` returns zero matches each (§1 search 10). Literal `fs::write` has six matches in `editor/rge-editor/src/main.rs`, all inside `#[cfg(test)] mod tests` (test-helper fixture writes for the load / reload tests; not live editor save behavior). The editor cannot write a `.rge-project` or `.rge-scene` back to disk; there is no symmetric counterpart to `--scene <path>` for serializing the live world. `Ctrl+S` is bound, but to `EditorKeyCommand::MarkSaved` → `CommandBus::mark_saved()` (a dirty-flag operation on the in-memory bus), not to filesystem-write.
- **Workflow framing — PARTIALLY STALE.** "There is no loop to friction-test" is now half-true: the user can load a `.rge-project` via CLI (boot-time only — no in-app open dialog), but cannot save anything edited at runtime. The authoring loop is still structurally incomplete because the save direction is missing.

#### Gap 2 — "Command Bus is fully implemented in `editor-actions` but structurally unreachable from the editor UI."

**Classification: SUBSTANTIALLY CLOSED (with caveats below).**

- **Negative claim "Zero call sites from `editor-shell` / `rge-editor` dispatch any user-triggered command through the bus" — STALE.** `crates/editor-shell/src/lifecycle/commands.rs:280` (`self.command_bus.submit(...)`) is reached from `EditorShell::handle_key_command` via Ctrl+Z/Y/S/2/0/4 in the `KeyboardInput` branch at `crates/editor-shell/src/lifecycle/mod.rs:1700-1722`.
- **Negative claim "`lifecycle.rs::window_event()` ends with a catch-all `_ => {}` (line 776) that silently swallows every `KeyboardInput` event" — STALE.** The `KeyboardInput` arm is now an explicit branch at `crates/editor-shell/src/lifecycle/mod.rs:1676`, gated by `!egui_consumed`, with a triple cascade: `EditorKeyCommand::from_key_press` → `EditorPlaybackCommand::from_key_press` → plain-`R` asset reload. The catch-all `_ => {}` now lives at `:1725` and only catches the events not enumerated above.
- **Negative claim "The user cannot press Ctrl+Z, cannot trigger any command from the keyboard, cannot escape a tool" — STALE.** Ctrl+Z (`EditorKeyCommand::Undo`), Ctrl+Y (`Redo`), Ctrl+S (`MarkSaved`), Ctrl+0 / Ctrl+2 / Ctrl+4 (`TimeScale` presets), Space (`EditorPlaybackCommand::TogglePlay`), Escape (`Stop`), and plain-`R` (asset reload) are all routed.
- **Caveat — substrate-vs-coverage.** Even though the wiring is real, **only one production `Action` impl exists** (`SetTimeScale` at `commands.rs:193`, mutating `TimeScale` via `World::insert_resource`). No CAD-graph mutation flows through the bus; the bus signatures stayed World-only (`Action::apply(&self, world: &mut World)` at `crates/editor-actions/src/action.rs:87` — unchanged from the 2026-05-21 record at `bus.rs:143`). The 2026-05-21 record that the bus is World-only is **STILL CURRENT** at the trait-signature level; the gap that closed is the keyboard-to-bus wire, not the bus's domain reach.
- **Why not "CURRENT":** The 2026-05-21 framing was specifically "structurally unreachable from the editor UI." That framing is no longer true. The remaining limitation (Action context is World-only, so CAD mutations cannot flow through the bus) is a separate, narrower gap that the BASELINE entry itself enumerated as the rationale for rejecting F → `SpawnCuboidAt` — see Gap 9 in §4 below.

#### Gap 3 — "`MenuRegistry` + `io-*` asset loaders are both ready internally but never called from the editor."

**Classification: PARTIALLY STALE.**

- **`MenuRegistry` half — CURRENT.** §1 search 3 confirms zero `MenuRegistry::` (associated-function call), zero `ResolvedEntry`, zero `menus::Command`, and zero `.resolve(` (literal method call) references inside `crates/editor-shell/src/**/*.rs`, `editor/rge-editor/**/*.rs`, or `crates/editor-egui-host/**/*.rs`. Separately, the literal token `MenuRegistry` appears once in this scope at `crates/editor-shell/src/play_toolbar.rs:12` as a doc-comment cross-reference to the W08 menu/toolbar registry; that mention is forward-looking narrative, not a functional menu registry construction or resolution. The only functional consumers remain `editor-ui::menus::**` itself + `editor-ui/tests/menus_ordering.rs`. No `ResolvedEntry` is produced or acted upon by any production editor-shell / rge-editor / editor-egui-host code path; no menu bar paints; no `"tab/inspector"` spawner is upgraded — `crates/editor-ui/src/dock/spawner_registry.rs:165-168` still registers `PlaceholderTabBody::for_id` for the default tab id set. (Separately, the egui host crate's internal `TabBody::Inspector` does carry a real `Arc<InspectorHandoff>` and renders the inspector widget; that is the inspector wire-up but it does not flow through `MenuRegistry`.)
- **`io-gltf` half — STALE.** `editor/rge-editor/src/main.rs:612` (`import_glb(path, &mut cache)` inside `load_all_glb_meshes`) is a real call site, reached via `--glb <path>` CLI at `:971-1026`. The `--glb` flow imports a GLB, walks the scene, bakes TRS, resolves base colors + textures, and constructs an `EditorShell::with_render_meshes_and_base_colors_and_textures(...)`. ISSUE-85 added a `notify`-backed hot-reload watcher (`editor/rge-editor/src/glb_watcher.rs` + the `EditorApp::window_event` drain at `main.rs:886-907`) that re-invokes `load_all_glb_meshes` on file change and routes through `EditorShell::handle_asset_reload`. The 2026-05-21 claim that `io-gltf::import_glb` has "zero call sites from `editor-shell` or `rge-editor`" is **STALE** for the rge-editor binary; it is **CURRENT** for editor-shell (the editor-shell crate stays glTF-agnostic; the binary owns the loader edge, exposed to the shell as `AssetReloadHook`).
- **`io-image` half — CURRENT.** §1 searches 4 and 5 confirm zero `io_image::` / `load_path` / `load_bytes` references across `editor-shell`, `editor-ui`, `rge-editor`, and `editor-egui-host`. No `editor/rge-editor/Cargo.toml` line declares `rge-io-image`. Image bytes that flow through the glTF path arrive via `rge_io_gltf::MaterialAsset.base_color_image_handle` → `MemoryCache::get_image` (see `main.rs:517-540`), NOT through `io-image` directly.
- **Drag-drop half — CURRENT.** §1 search 8 confirms zero `DroppedFile` / `HoveredFile` / `HoveredFileCancelled` / `DragAndDrop` matches across the editor surface. No drag-drop ingestion path exists.
- **CLI vs. menu — note for next reconciliation.** The `--glb` and `--scene` flags reach the loader via **launch-time CLI arguments**, not through a `MenuRegistry` "File → Open" gesture. The 2026-05-21 framing "Users cannot 'File → Open' anything" remains technically correct in the menu-driven sense; what changed is that users now CAN load a glTF or `.rge-project` at startup. A reconciliation should distinguish the two.

### 3. Revisit Trigger Status

The two literal triggers from `plans/BASELINE.md:615-620` (Editor-usability preflight, 2026-05-21 snapshot):

#### Trigger 1 — CommandBus integration design dispatch lands a decision on signature shape

**Verdict: AMBIGUOUS (de facto decided by code, no documented decision artifact).**

- **What landed:** `EditorShell` wires the existing `CommandBus::submit(Box<dyn Action>, &mut World)` directly (`commands.rs:280`); the production Action impl `SetTimeScale` (`commands.rs:193`) honors the World-only signature. The trait surface at `crates/editor-actions/src/action.rs:87` is byte-identical to the 2026-05-21 record. Implicitly: the bus stayed World-only.
- **What did NOT land:** No ADR or `docs/adr/**` entry, no `EditorCommandCtx` aggregate, no `(&mut CadGraph, &mut CadProjection)` extension, no stub adapter for CAD-graph mutation. §1 search 12 confirms zero such ADRs. The 2026-05-21 BASELINE footnote and the 2026-05-28 live-inspector reconciliation both refer in passing to a "CommandBus integration design preflight (decided the bus stays World-only)" — but **no such standalone preflight section exists in `plans/BASELINE.md`** today (verified by re-reading the `^##` and `^###` heading inventory of BASELINE.md). The phrase appears only in the live-inspector preflight's narrative cross-reference, and the only BASELINE.md prose that decides "World-only" is the same Editor-usability preflight section we are auditing — i.e. the decision references itself.
- **Why ambiguous:** Under a strict reading of the trigger ("a CommandBus integration design dispatch lands a decision … The decision itself can be a docs-only ADR / design note plus a stub adapter"), no such ADR / design note exists; the trigger has NOT FIRED in its literal "docs-only ADR/design-note" sense. Under a behavioral reading ("the bus surface has been committed to as World-only for the foreseeable future via shipped code"), the trigger HAS FIRED via fait accompli — and the BASELINE.md narrative on 2026-05-28 already speaks of the decision as past tense.
- **Human decision needed:** Whether to (a) treat the existing Phase 9 keyboard wiring + `SetTimeScale` Action as the de facto decision and reconcile the Editor-usability preflight on that basis, or (b) require an explicit ADR / design note before declaring Trigger 1 fired. The next reconciliation TASK must NOT just assert "the bus stayed World-only" without resolving this ambiguity, because the original preflight required the decision to be a deliverable (docs artifact), not an inferred state.

#### Trigger 2 — A non-CAD user-input path lands first (Ctrl+S/Ctrl+O for workspace RON, or PIE Play/Stop bound to a keyboard shortcut)

**Verdict: FIRED.**

- **PIE Play/Stop via keyboard — FIRED.** `crates/editor-shell/src/lifecycle/playback.rs:88-198` ships `EditorPlaybackCommand::{TogglePlay, Stop}`; `Space` cycles Editing/Playing/Paused via `ToolbarButtonId::Play|Pause`, `Escape` calls `ToolbarButtonId::Stop` when `play_state().is_pie_active()`. Reached from `crates/editor-shell/src/lifecycle/mod.rs:1706-1709`. The 2026-05-21 example "PIE Play/Stop bound to a keyboard shortcut" is precisely what shipped.
- **Ctrl+S bound — PARTIALLY FIRED.** `EditorKeyCommand::MarkSaved` at `commands.rs:72` binds Ctrl+S, but the handler at `commands.rs:308` routes to `CommandBus::mark_saved()` — a dirty-flag mark, NOT a `LayoutService::save` to a RON file. The 2026-05-21 trigger example was specifically "workspace-layout RON load/save bound to `Ctrl+S` / `Ctrl+O`", and that exact wire is **NOT** present (`LayoutService` has zero call sites in editor-shell / rge-editor — §1 search 9). So the spirit of the trigger fired via the more general Ctrl+S → bus path, but the literal "workspace RON via Ctrl+S/Ctrl+O" wire described in the BASELINE example did not land.
- **Catch-all-at-`lifecycle.rs:776` consequence — FIRED.** The 2026-05-21 narrative ("would surface the `KeyboardInput` catch-all at `lifecycle.rs:776` and force a real `window_event` keyboard branch without first redesigning the bus") is exactly the outcome: a real `KeyboardInput` arm now sits at `mod.rs:1676`, the catch-all moved to `:1725`, and the bus signature stayed World-only.

#### Combined revisit-state interpretation

Trigger 2 has unambiguously fired and Trigger 1 has effectively-but-not-documentedly fired. The BASELINE.md text "Until **at least one** of those fires, **defer all user-facing editor wire-up dispatches**" is therefore satisfied; the deferral is no longer in force; a re-run of the Editor-usability preflight is justified by the source. The narrower question — exactly which prose to write — is the next docs reconciliation chip's responsibility, not this audit's.

### 4. Still-Open Usability Gaps (source-grounded)

After classifying Gaps 1/2/3, the following residual gaps are independently supported by current source. None of these should be asserted as "still unchanged" without re-grounding — each below is cited.

- **Gap 4 — Save direction has no path at all.** No `ron::ser` / `ron::to_string` / `save_project` / `save_scene` / `save_to` / `write_to_file` runtime symbol in `editor/rge-editor/src/` or `crates/editor-shell/src/` (§1 searches 7 and 10). The only `fs::write` hits are six `std::fs::write(...)` test-fixture writes under `#[cfg(test)]` in `editor/rge-editor/src/main.rs` (load/reload test setup, not a runtime save). Ctrl+S marks the bus saved-cursor; nothing is written to disk. `--scene` is load-only by design (`editor/rge-editor/src/main.rs:32-37` doc-comment: "load-only path with no CAD projection or operator graph").
- **Gap 5 — Menu command execution is still absent.** `MenuRegistry::resolve` is never called from production editor-shell / rge-editor / editor-egui-host (the four narrower functional searches `MenuRegistry::`, `ResolvedEntry`, `menus::Command`, `.resolve(` are individually zero — §1 search 3); `ResolvedEntry` values are never produced or acted upon there; no menu bar is painted; `crates/editor-ui/src/menus/command.rs:103` defines `Command::OpenFile.diagnostic_id() == "open_file"` but nothing in the editor runtime surface dispatches the variant (the only other hit, `:154`, is a unit-test assertion in the same file). The single literal `MenuRegistry` token in this scope is the non-functional `play_toolbar.rs:12` doc-comment. Even though the inspector tab renders via the host-internal `TabBody::Inspector`, that is NOT a menu resolution — it's a fixed dock layout shipped by `EguiHost`.
- **Gap 6 — Drag-and-drop ingestion is still absent.** Zero `DroppedFile|HoveredFile|HoveredFileCancelled|DragAndDrop` matches across the editor surface (§1 search 8). Files come in only via `--glb` / `--scene` at launch or via the ISSUE-85 notify watcher on a previously-`--glb`-bound path.
- **Gap 7 — `io-image` is not consumed by the editor surface.** Zero `io_image::|load_path|load_bytes` matches across editor-shell / editor-ui / rge-editor / editor-egui-host (§1 searches 4 and 5); no Cargo dep on `rge-io-image` in `editor/rge-editor/Cargo.toml`. Image data only reaches the editor through `rge_io_gltf::MaterialAsset.base_color_image_handle` (a co-bundled path), not through the `io-image` API directly.
- **Gap 8 — Non-CLI open/load UX is still absent.** Files arrive only via boot-time CLI args. There is no in-app "open" gesture, no file picker, no drag-drop (Gap 6). The 2026-05-21 narrative "Users cannot 'File → Open' anything" remains accurate when read literally — but should not be re-asserted in the load direction without distinguishing CLI ingestion from in-app gestures.
- **Gap 9 — CommandBus coverage is keyboard-shaped, not CAD-shaped.** `SetTimeScale` is the only production `Action` impl (§1 search 11). The bus reaches `TimeScale` (a `World` resource); it cannot reach `CadGraph` or `CadProjection` because the `Action::apply(&self, world: &mut World)` signature at `crates/editor-actions/src/action.rs:87` is unchanged. CAD-graph mutations (the F → `SpawnCuboidAt` family) remain blocked on the same World-only invariant the 2026-05-21 record described — except now the bus IS reachable from the keyboard, so the rejection rationale changes from "bus is unreachable" to "bus's Action context cannot mutate CAD state."
- **Gap 10 — Spawner registry still produces `PlaceholderTabBody` for default ids including `"tab/inspector"`.** `crates/editor-ui/src/dock/spawner_registry.rs:165-168` is unchanged from the 2026-05-21 record. The inspector is rendered today via a different surface — the egui host's internal `TabBody::Inspector` enum (`crates/editor-egui-host/src/tabs.rs`) — that does NOT flow through `editor-ui::SpawnerRegistry`. So the strict statement "the spawner registry registers `PlaceholderTabBody` for `tab/inspector`" is still CURRENT; what changed is that the *inspector* is no longer reached through the spawner registry path at all.

### 5. Recommended Next Reconciliation Scope

#### Recommended scope boundary

The smallest safe follow-up docs reconciliation should cover **`plans/BASELINE.md:586-620` as one cohesive forward-only prepend** (the Top 3 gaps + the "Cross-cutting pattern" + the "Rejected F → `SpawnCuboidAt`" framing + the Status + the Revisit Triggers + the closing "defer all user-facing editor wire-up dispatches" sentence). Reasons:

- The Top 3 gaps (586-592) and the revisit triggers (615-620) are textually entangled with the Status, the cross-cutting pattern, and the rejected F → `SpawnCuboidAt` analysis (596-613). Splitting the reconciliation across only 586-592 would leave the Status + revisit-trigger text stating "defer all user-facing editor wire-up dispatches" — which is the very deferral that has been overtaken by Phase 9 keyboard wiring + `SetTimeScale`. Reconciling only 586-592 would publish an internally contradictory section.
- The reconciliation should NOT expand beyond 620 — the "Notes / caveats" block at 622-639 (reproducer grep recipes, sample-pattern observations, complementary-baselines text) is still load-bearing as a methodology record and does not contain stale current-state claims that block the next chip.
- The reconciliation should NOT touch the 2026-05-21 initial egui-host status snapshot (705+), which already has its own 2026-05-28 forward-only reconciliation prepended at 662-702 by ISSUE-251. That section is independent.
- Forward-only pattern: prepend a new dated `### 2026-05-28 — Editor-usability preflight reconciliation` subsection above the existing `### 2026-05-21 — initial editor-usability adoption snapshot` (561), mirroring ISSUE-251's prepend pattern for the live-inspector wiring section. Do NOT edit the 2026-05-21 snapshot in place.

#### Exact claims the future reconciliation TASK must NOT make without fresh source reads

These are the booby traps that ISSUE-253 fell into. The future reconciliation MUST cite current source for each, not just inherit the 2026-05-21 wording:

1. **"Gap 2 is UNCHANGED."** Re-grounded above as SUBSTANTIALLY CLOSED. Asserting "unchanged" against `crates/editor-shell/src/lifecycle/commands.rs` (which exists with 409 lines of CommandBus wiring) is the literal premise error ISSUE-253 committed.
2. **"`lifecycle.rs:776` catch-all swallows every `KeyboardInput`."** The catch-all moved to `:1725` and is now downstream of a real `KeyboardInput` arm at `:1676`. Cite `mod.rs:1676` if making any claim about the keyboard branch.
3. **"Zero CommandBus call sites in editor-shell / rge-editor."** Wrong since Phase 9: cite `commands.rs:280` (`command_bus.submit`), `:291` (undo), `:302` (redo), `:309` (mark_saved).
4. **"No `--scene` / `.rge-project` ingestion."** Wrong since ISSUE-225: cite `editor/rge-editor/src/main.rs:912-968` and the deps in `Cargo.toml:36-38`.
5. **"`io-gltf::import_glb` has zero call sites in editor surface."** Wrong since dispatch G: cite `editor/rge-editor/src/main.rs:612` (and the ISSUE-85 watcher at `main.rs:877-907`).
6. **"`io-image` is used by the editor."** It is NOT — verified zero matches; that direction (asserting USE) would be the symmetric error. The CORRECT claim is still "zero io-image call sites in editor surface," and the reconciliation must explicitly cite the zero-match grep to keep it grounded.
7. **"The bus grew a CAD context."** It did NOT — the trait surface at `crates/editor-actions/src/action.rs:87` is World-only. Any claim about CAD-graph mutation through the bus must cite this still-unchanged signature.
8. **"A CommandBus integration design ADR exists."** §1 search 12 returns zero ADR hits. The decision is implicit-via-code, not documented as an ADR. The reconciliation TASK should resolve the Trigger-1 ambiguity flagged in §3 before publishing past-tense narrative about "the decision."
9. **"`MenuRegistry` is reached from the editor surface."** It is NOT — the reconciliation must distinguish a `MenuRegistry` *doc-comment cross-reference* (which exists at `crates/editor-shell/src/play_toolbar.rs:12`, narratively referencing the W08 future registry) from a *functional `MenuRegistry` construction or query* (which does NOT exist in the editor runtime surface; the four narrower searches `MenuRegistry::`, `ResolvedEntry`, `menus::Command`, `.resolve(` are individually zero in editor-shell / rge-editor / editor-egui-host). The reconciliation must not conflate the host-internal `EguiHost`-owned dock with menu-driven tab spawning.
10. **"The `"tab/inspector"` spawner was upgraded."** It was NOT — `crates/editor-ui/src/dock/spawner_registry.rs:165-168` still registers `PlaceholderTabBody`. The inspector wire-up flows through the host's `TabBody::Inspector` (`crates/editor-egui-host/src/tabs.rs`), which is a separate type from `editor-ui::PlaceholderTabBody`; see the doc-comment at `tabs.rs:5-16` explaining the coexistence.

#### What the future reconciliation TASK should explicitly leave OUT of scope

- Re-running the §6.3 Gate A perf baselines or any cargo gates beyond `.ai/dispatch.verify.ps1`.
- Editing the 2026-05-21 snapshot in place — the prepend pattern preserves it byte-identical.
- Reconciling the §1.1 reflection-scale, §1.10.4 invalidation-radius, or §13.3 compile-time preflights, which are independent.
- Reconciling the W03 / W04 / Phase 5.3 / Phase 3.2 / Phase 6 §6.3 / §13.2 entries, which are dated baseline rows with their own provenance.
- Deciding the next code dispatch shape — that is a separate planning artifact, not part of the docs reconciliation.

## Per-File Summary

This packet does not modify any tracked file. It only authors a new EXEC packet
under `ai_handoffs/` (this corrected, self-contained audit report).

## Verification Results

Per the round1 CORRECT packet's Re-Verification Gates. The canonical
verification gate `.ai/dispatch.verify.ps1` was run during round1 and its
output is recorded in `.ai/dispatch-ISSUE-254/verification.round1.log`; this
correction round adds only a new EXEC packet, so the tracked-source state is
byte-identical to round1 and the round1 gate evidence remains authoritative.

- `git status --short --branch -uall` (this correction round): exit 0. Branch
  `ai-dispatch/ISSUE-254`; no tracked modifications. Only this dispatch's own
  ISSUE-254 handoff packets/sidecars are untracked (TASK + `.meta.json`; the two
  prior EXEC reports + `.meta.json`; the round0 + round1 CORRECT packets +
  `.meta.json`; and this new EXEC packet + its sidecar after finalize).
- `git diff --stat` and `git diff --name-only`: empty (zero tracked
  source/docs/script/schema diffs). Exit 0.
- `git diff --check`: clean (no whitespace / merge-conflict errors). Exit 0.
- **Canonical gate `.ai/dispatch.verify.ps1` — round1 evidence in
  `.ai/dispatch-ISSUE-254/verification.round1.log`. The script runs exactly
  seven steps (it mirrors the five CI workflows: fmt, architecture, deny,
  tests, bench). All seven reported `--- ok:` and the log's final line is
  `VERIFY OK: all 7 verification step(s) passed.`** The canonical steps, taken
  verbatim from the round1 log and `.ai/dispatch.verify.ps1`, are:
  1. `cargo +nightly fmt --check` (`cargo +nightly fmt --all -- --check`) — ok (2s).
  2. architecture lints (`cargo run -q -p rge-tool-architecture-lints -- all`) — ok (8s).
  3. architecture-lint test suite (`cargo test -p rge-tool-architecture-lints --all-targets`) — ok (1s).
  4. `cargo deny check` — ok (2s).
  5. `cargo test --workspace --all-targets` (`cargo test --workspace --all-targets --no-fail-fast -j 1`) — ok (46s).
  6. `cargo test --workspace --doc` (`cargo test --workspace --doc --no-fail-fast -j 1`) — ok (20s).
  7. `cargo bench -p rge-script-bench --no-run` — ok (1s).
  **No standalone `cargo build --workspace --all-targets` step exists in the
  canonical gate or the round1 log** (a grep of the round1 log for `cargo build`
  returns zero hits). The build is performed implicitly as part of the
  `cargo test --workspace --all-targets` step (step 5), not as an independent
  verification step. This corrects the prior EXEC report
  (`ai_handoffs/ISSUE-254_EXEC_2026-05-28_23-36-58+0300.md`), which incorrectly
  listed step 4 as a "workspace build (`cargo build --workspace --all-targets`)"
  and reordered the deny/tests steps; the accurate ordering is the seven steps
  above.
- Targeted evidence checks for the corrected report wording (round1
  Re-Verification Gates):
  - Literal `MenuRegistry` search across `crates/editor-shell/src/**/*.rs`,
    `editor/rge-editor/**/*.rs`, and `crates/editor-egui-host/**/*.rs` reports
    the `crates/editor-shell/src/play_toolbar.rs:12` doc-comment hit (and only
    that hit). ✅
  - Functional menu searches `MenuRegistry::`, `ResolvedEntry`, `menus::Command`,
    and `\.resolve\(` are scoped and reported exactly. All four are zero in the
    named scopes. ✅
  - Literal `fs::write` search across `editor/rge-editor/src/` and
    `crates/editor-shell/src/` reports the six
    `editor/rge-editor/src/main.rs` `std::fs::write(...)` test/helper fixture
    hits at `:1740`, `:2289`, `:2376`, `:2489`, `:2546`, `:2720`, all inside
    `#[cfg(test)] mod tests` opening at `main.rs:1043`. ✅
  - Runtime save / RON serialization searches `ron::ser`, `ron::to_string`,
    `save_project`, `save_scene`, `save_to`, and `write_to_file` are scoped and
    reported exactly. All six are zero in `editor/rge-editor/src/` and
    `crates/editor-shell/src/`. ✅

## Deviations from Task Packet

None — execution stayed strictly within the Task Packet scope as narrowed by the
round1 CORRECT packet. No existing tracked file was edited; only the ISSUE-254
handoff/log artifacts authorized by `### MAY add new files` were created. This
new EXEC packet is the canonical, self-contained audit artifact: it carries the
five Task-required sections in full with the round0 evidence corrections applied
inline, and it documents the canonical verification substeps exactly as the
round1 log and `.ai/dispatch.verify.ps1` recorded them. The two prior EXEC
reports are preserved byte-identical; this report supersedes them.

The round1 CORRECT packet's §Deferred Findings is "None"; no source, docs,
schemas, scripts, or older packet files were touched.

## Open Questions for Reviewer

1. **Trigger 1 framing (carried over from prior EXEC).** §3 flags Trigger 1 as
   AMBIGUOUS because there is no ADR or design-note artifact, only
   implicit-via-code adoption. Should the next reconciliation TASK require the
   docs-only ADR before declaring Trigger 1 fired, or accept the de facto
   wiring as sufficient? This audit deliberately does not pre-decide that.
2. **Scope boundary on the next reconciliation.** §5 recommends `586-620` as the
   cohesive prepend boundary. Confirm or override before the next chip is
   drafted.
3. **`play_toolbar.rs:12` doc-comment framing.** §1 search 3 and Gap 3 / Gap 5
   classify the single literal `MenuRegistry` token in the editor-runtime scope
   as forward-looking W08 narrative (non-functional). The Reviewer should
   confirm this framing is acceptable; the functional menu negative is carried
   by the four narrower searches (`MenuRegistry::`, `ResolvedEntry`,
   `menus::Command`, `.resolve(`), all individually zero.

## Worktree State

- Tracked files: clean (no modifications to any existing tracked source / docs
  file).
- Untracked items: this dispatch's own artifacts only —
  `ai_handoffs/ISSUE-254_TASK_2026-05-28_22-57-20+0300.md` + `.meta.json`
  (Planner-authored), `ai_handoffs/ISSUE-254_EXEC_2026-05-28_23-06-49+0300.md`
  + `.meta.json` (round0 EXEC, preserved),
  `ai_handoffs/ISSUE-254_CORRECT_2026-05-28_23-30-28+0300.md` + `.meta.json`
  (round0 CORRECT), `ai_handoffs/ISSUE-254_EXEC_2026-05-28_23-36-58+0300.md` +
  `.meta.json` (round1 EXEC, superseded by this report),
  `ai_handoffs/ISSUE-254_CORRECT_2026-05-28_23-47-27+0300.md` + `.meta.json`
  (round1 CORRECT packet driving this round),
  `ai_handoffs/ISSUE-254_EXEC_2026-05-28_23-49-37+0300.md` (this corrected EXEC
  packet) + its sidecar after `new-handoff.ps1 -Finalize`, and the
  corresponding `ai_dispatch_logs/log_*ISSUE-254*.md` when the orchestrator
  emits one.
- Branch: `ai-dispatch/ISSUE-254`.
- Last commit: `273d658` Merge pull request #252 from
  RustCADs/ai-dispatch/ISSUE-251.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-254
AUTHOR: Executor / Claude
NEXT_ROLE: REVIEWER_AI
EXIT_CODE: 0

---

# Correction Packet

DISPATCH_ID: ISSUE-55
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_01-09-00+0300
RELATED_FILES:
- crates/cad-core/tests/operator_graph_snapshot_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-55_TASK_2026-05-20_01-01-33+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-55_EXEC_2026-05-20_01-08-03+0300.md`
- Latest Review Report: none; the orchestrator verification gate failed before Codex control review.
- Prior Correct Packets: none.
- Verification failure: `.ai/dispatch.verify.ps1` failed at `cargo +nightly fmt --all -- --check` with `Incorrect newline style in \\?\A:\RCAD\RGE\crates\cad-core\tests\operator_graph_snapshot_smoke.rs`.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing less.

1. **Normalize newline style in existing snapshot smoke test** - Verification gate finding from `cargo +nightly fmt --all -- --check`. Required change: edit only `crates/cad-core/tests/operator_graph_snapshot_smoke.rs` to normalize its file newline style to the rustfmt-accepted workspace style, without changing Rust code, assertions, comments, imports, test names, or behavior. Acceptance: `cargo +nightly fmt --all -- --check` exits 0.

## Deferred Findings (NOT Approved for This Round)

None. There are no other approved fixes, and no latent review findings are being deferred.

## Updated Acceptance Criteria

- The correction may edit `crates/cad-core/tests/operator_graph_snapshot_smoke.rs` only to normalize newline style.
- The correction must not change behavior in `crates/cad-core/tests/operator_graph_snapshot_smoke.rs`.
- The correction must not edit `crates/cad-core/tests/operator_graph_structural_diff_smoke.rs`; it is covered only by re-verification.
- Aside from the Executor's new execution report and optional sidecar, no production source, manifest, lockfile, docs, schemas, scripts, CI files, architecture-lint tooling, or unrelated tests may be changed.
- The canonical dispatch verification gate must pass.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0
- `cargo test -p rge-cad-core --test operator_graph_structural_diff_smoke` -> expected exit 0
- `.ai/dispatch.verify.ps1` -> expected exit 0

If `cargo` is not on PATH, use the established repository fallback toolchain path `A:\RustCache\cargo\bin\cargo.exe` where applicable, matching `.ai/dispatch.verify.ps1`.

## Halt Conditions (Updated if Any)

- Halt if normalizing `crates/cad-core/tests/operator_graph_snapshot_smoke.rs` would require changing Rust tokens or test behavior instead of newline style only.
- Halt if any verification failure remains after the newline-style correction; do not broaden the correction to other files without a new Planner packet.
- Halt if any manifest, lockfile, docs file, schema, script, CI file, architecture-lint tooling, production source file, or unrelated test file would need to change.
- All other halt conditions remain unchanged from the Task Packet.

## Planner Notes

The original execution stayed within the Task Packet, but the canonical post-execution gate is stricter than the scoped task gate and failed before Codex control review. This correction narrowly approves a formatting-only line-ending normalization in `crates/cad-core/tests/operator_graph_snapshot_smoke.rs` because that single rustfmt newline-style failure blocks dispatch publication. Do not use this correction round to revise the issue #55 test logic or broaden the dispatch.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-55
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

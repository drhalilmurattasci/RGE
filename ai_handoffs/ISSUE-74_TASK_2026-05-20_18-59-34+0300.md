# Task Packet

DISPATCH_ID: ISSUE-74
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_18-59-34+0300
RELATED_FILES:
- .github/workflows/bench.yml
- .github/workflows/tests.yml
- .github/workflows/architecture.yml
- rust-toolchain.toml
- crates/script-bench/Cargo.toml
STATUS: OPEN

## Goal

Add a narrow GitHub Actions workflow that closes the persistent script benchmark compile CI gap by checking that the `rge-script-bench` benchmark targets compile without running benchmark workloads. The workflow must follow the repository's existing Rust toolchain and cargo cache conventions and must not change Rust source, benchmark logic, manifests, scripts, or documentation.

## Scope

### MAY edit
- .github/workflows/bench.yml

### MUST NOT edit
- .github/workflows/fmt.yml
- .github/workflows/tests.yml
- .github/workflows/architecture.yml
- .github/workflows/deny.yml
- Any `.github/workflows/**` file other than `.github/workflows/bench.yml`
- Cargo.toml
- Cargo.lock
- crates/**/Cargo.toml
- crates/**/src/**
- crates/**/benches/**
- crates/script-bench/**
- kernel/**
- runtime/**
- editor/**
- rust-toolchain.toml
- .ai/**
- docs/**
- ai_dispatch_logs/**
- new-handoff.ps1
- Invoke-AiDispatch*.ps1
- Register-AiDispatchSchedule.ps1
- Get-AiDispatchHealth.ps1
- .gitignore
- Any Rust source, benchmark implementation, manifest, lockfile, script, schema, documentation, existing handoff packet, or generated file outside the MAY-edit file, except for the protocol-required execution report packet described below.

### MAY add new files
- .github/workflows/bench.yml, if it does not already exist.
- ai_handoffs/ISSUE-74_EXEC_*.md for the Executor's execution report only.
- The matching `ai_handoffs/ISSUE-74_EXEC_*.meta.json` sidecar if `new-handoff.ps1 -Finalize` writes one for that execution report.

### MUST NOT add new files
- Any new Rust source file, benchmark file, crate, fixture, snapshot, manifest, lockfile, documentation file, schema, script, or GitHub Actions workflow other than `.github/workflows/bench.yml`.
- Any additional TASK, REVIEW, CORRECT, CLOSEOUT, or unrelated EXEC packet.

## Deliverables

- Create or update `.github/workflows/bench.yml` as a standalone GitHub Actions workflow for script benchmark compilation only.
- Use the same trigger shape as the existing primary CI workflows: `push` and `pull_request` targeting `main`. Do not add a scheduled trigger.
- Use repository-standard environment defaults where relevant, including `CARGO_TERM_COLOR: always` and `RUST_BACKTRACE: 1`.
- Define one Ubuntu job whose purpose is compiling `rge-script-bench` benchmarks without executing them.
- Install the repository-pinned Rust toolchain using the existing workflow pattern: `dtolnay/rust-toolchain@stable` with `toolchain: stable`, relying on `rust-toolchain.toml` to pin Rust 1.92.0 and components.
- Use the existing cargo cache action pattern: `Swatinem/rust-cache@v2` with a workflow-specific `shared-key` for the script bench compile job.
- Run exactly `cargo bench -p rge-script-bench --no-run` as the benchmark compile command.

## Acceptance Criteria

- `.github/workflows/bench.yml` is valid GitHub Actions YAML.
- The workflow has one focused job for `cargo bench -p rge-script-bench --no-run`; it must not run benchmark workloads without `--no-run`.
- The workflow checks out the repository, installs the pinned Rust toolchain via the established `dtolnay/rust-toolchain@stable` pattern, uses the established `Swatinem/rust-cache@v2` cargo cache pattern, and then runs the bench compile command.
- No Rust source, benchmark logic, manifests, lockfiles, docs, scripts, schemas, or existing workflow files other than `.github/workflows/bench.yml` are changed.
- The local verification command `cargo bench -p rge-script-bench --no-run` exits 0.

## Constraints / Non-Goals

- Do not modify `crates/script-bench` or any benchmark implementation to make the command pass.
- Do not edit Cargo manifests, dependency pins, the lockfile, or `rust-toolchain.toml`.
- Do not broaden the workflow to workspace tests, doctests, formatting, architecture lints, cargo-deny, runtime benchmark execution, publishing, artifacts, or matrix coverage.
- Do not edit `.ai/dispatch.verify.ps1` in this dispatch even though it documents the current workflow mirror. Any dispatch-verify update is out of scope for ISSUE-74 and should be handled separately if desired.
- Do not commit, stage, push, or create any packet other than the protocol-required EXEC report and optional EXEC sidecar.

## Verification Gates

The Executor MUST run and document the result of this command in their `EXECUTION_REPORT`:

- `cargo bench -p rge-script-bench --no-run` -> expected exit 0

The Executor MUST also inspect `.github/workflows/bench.yml` before reporting success and document that the workflow YAML is syntactically valid and limited to the scoped job above. If a local GitHub Actions or YAML validator is already available, use it; do not install new tooling or use network access just to validate YAML.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- Making `cargo bench -p rge-script-bench --no-run` pass appears to require editing Rust source, benchmark code, manifests, the lockfile, or `rust-toolchain.toml`.
- The workflow cannot be implemented by editing only `.github/workflows/bench.yml` plus the protocol-required execution report packet.
- The existing Rust toolchain install or cargo cache workflow patterns cannot be reused without changing other CI files.
- The required local verification command fails for a reason that cannot be fixed inside `.github/workflows/bench.yml`.
- Satisfying the task would require adding a scheduled workflow, matrix, artifact upload, benchmark execution, new dependency, new script, or any broader CI behavior outside the requested bench compile check.

## Planner Notes

Use `.github/workflows/tests.yml` and `.github/workflows/architecture.yml` as the pattern source for checkout, Rust toolchain installation, cargo cache usage, CI triggers, environment variables, and step naming. Keep the workflow intentionally small: this issue is about compiling the script benchmark package in CI, not changing benchmark behavior or mirroring the new workflow in local dispatch verification.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-74
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

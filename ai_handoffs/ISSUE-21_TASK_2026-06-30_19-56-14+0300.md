# Task Packet

DISPATCH_ID: ISSUE-21
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-30_19-56-14+0300
RELATED_FILES:
- .ai/dispatch.tasks.md
- Status.md
- HANDOFF.md
- plans/BASELINE.md
- change.md
- crates/editor-ui/src/menus/registry.rs
- crates/editor-ui/tests/menus_ordering.rs
- crates/editor-ui/src/menus/default_menu.rs
- crates/editor-ui/src/menus/keybinding.rs
- crates/editor-ui/src/menus/mod.rs
- crates/editor-ui/src/menus/command.rs
- crates/editor-ui/src/menus/predicate.rs
- crates/editor-ui/src/menus/shortcut.rs
- crates/editor-shell/
- crates/editor-egui-host/
- Cargo.toml
- Cargo.lock
- .github/workflows/
- .ai/*.schema.json
- .ai/dispatch.verify.ps1
STATUS: OPEN

## Goal

Audit task 176, the reverse effective-binding accessors on
`ResolveResult`, for GitHub issue #21. This is a read-only audit over Rust
code, tests, Cargo metadata, workflows, schemas, automation, and
host/shell/runtime integration surfaces: confirm `shortcut_for_command` and
`bindings()` are read-only views over the existing `shortcut_commands` map, add
no field or secondary index, do not alter resolve behavior, expose
deterministic ordering, and have the required
bound/unbound/override/determinism coverage. The Executor may read repository
surfaces for evidence and may add only the current dispatch EXEC artifacts; it
MUST NOT edit `.ai/dispatch.tasks.md`, status/governance docs, source, tests,
Cargo, workflows, schemas, automation, or host/shell/runtime surfaces.

## Scope

This is an audit-only task over source, tests, Cargo, workflows, schemas,
automation, and host/shell/runtime integration surfaces. The Executor may read
those surfaces for evidence but MUST NOT modify any existing repository file.
The only writes allowed are new current-dispatch EXEC artifacts required by
the handoff protocol.

### MAY edit
- None. This audit must not modify existing repository files.

### MUST NOT edit
- Any existing repository file, including `.ai/dispatch.tasks.md`,
  `Status.md`, `HANDOFF.md`, `plans/BASELINE.md`, and `change.md`.
- Any Rust source or test file, including `crates/**`, `kernel/**`,
  `editor/**`, and `tools/**`.
- `crates/editor-ui/src/menus/registry.rs`.
- `crates/editor-ui/tests/menus_ordering.rs`.
- `crates/editor-ui/src/menus/keybinding.rs`.
- `crates/editor-ui/src/menus/mod.rs`.
- `crates/editor-ui/src/menus/default_menu.rs`.
- `crates/editor-ui/src/menus/command.rs`.
- `crates/editor-ui/src/menus/predicate.rs`.
- `crates/editor-ui/src/menus/shortcut.rs`.
- `crates/editor-shell/**`, `crates/editor-egui-host/**`,
  `crates/editor-actions/**`, `crates/editor-state/**`,
  `crates/plugin-host/**`, `crates/runtime-wasmtime/**`,
  `crates/cad-core/**`, `crates/cad-graph/**`, and
  `crates/cad-projection/**`.
- `Cargo.toml`, `Cargo.lock`, or any `**/Cargo.toml`.
- `.github/workflows/**`.
- `.ai/*.schema.json` and `.ai/dispatch.verify.ps1`.
- `Invoke-AiDispatchLoop.ps1`, `Invoke-AiDispatchQueue.ps1`,
  `Invoke-AiDispatchAuto.ps1`, `Register-AiDispatchSchedule.ps1`,
  `Get-AiDispatchHealth.ps1`, `Wait-GitHubActions.ps1`,
  `Watch-AiDispatch.ps1`, or `new-handoff.ps1`.
- `ai_handoffs/templates/**`, generated non-current-dispatch handoff
  artifacts, packet templates, schemas, scripts, local tooling, scheduler
  configuration, or any host/shell/runtime surface.

### MAY add new files
- `ai_handoffs/ISSUE-21_EXEC_*.md`
- `ai_handoffs/ISSUE-21_EXEC_*.meta.json`

### MUST NOT add new files
- Any Rust source, Rust test, Cargo, workflow, schema, automation, script,
  host/shell/runtime, docs, ADR, template, or tooling file.
- Any handoff packet or sidecar outside current-dispatch
  `ai_handoffs/ISSUE-21_EXEC_*` artifacts.
- Any new task file or task line. Do not append task 178 in this read-only
  audit; record any follow-up recommendation in the EXEC packet only.

### Advisory machine-readable scope envelope

This read-only dispatch intentionally omits an ADR-121
`handoff:envelope v1` block. The only permitted write artifacts are the
current-dispatch EXEC packet and its finalize sidecar; all existing repository
files are read-only for this task.

## Deliverables

- A source-grounded audit report in the current dispatch EXEC packet that
  cites the local source reads and commands used to verify each required
  invariant.
- An explicit statement in the EXEC packet that no existing repository files
  were edited and only current-dispatch EXEC artifacts were added.
- A final follow-up decision in the EXEC packet only: either a
  "Recommendation for human approval" block containing the next bounded
  FEATURE task candidate, or exactly one line matching
  `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - concrete reason` plus a
  "Recommendation for human approval" block explaining why no in-policy bounded
  task 178 should be appended automatically.
- Explicit verification that task 178 was not appended and that no completed
  dated `NEEDS_HUMAN_RECORDED` marker was written into `.ai/dispatch.tasks.md`
  by this read-only audit.

## Acceptance Criteria

- No existing repository file is modified. Only current-dispatch EXEC
  artifacts may be added.
- No Rust source, Rust tests, Cargo metadata, workflow, schema, automation,
  host/shell/runtime, governance/status file, or prohibited handoff/template
  surface is modified.
- `ResolveResult::shortcut_for_command` and `ResolveResult::bindings` are
  present as methods on the already-exported `ResolveResult`; no `mod.rs`
  export, public type, enum, serde, settings, persistence, host/shell, plugin
  runtime, CAD, Cargo, workflow, schema, or automation surface changed.
- Both accessors read the existing private `shortcut_commands` map at accessor
  call time; no new field, secondary index, cached vector, global state,
  resolve-time side table, or resolve behavior change was added.
- `bindings()` exposes deterministic ordering and does not leak raw `HashMap`
  iteration order.
- `shortcut_for_command(&Command)` uses the same stable order as `bindings()`
  and documents which shortcut wins when multiple shortcuts resolve to the same
  command.
- `resolve`, `resolve_with_keybinding_overrides`, override application,
  conflict detection/suppression, `keybinding_diagnostics`,
  `AcceleratorTable`, default menu entries, labels, order, predicates,
  enablement predicates, shortcut values, and passive shortcut hints remain
  unchanged.
- Default resolve is still pinned at 19 executable accelerators and zero
  conflicts.
- Required tests cover: a bound command, an unbound command, stable full
  enumeration, remap override reflection, unbind override reflection,
  repeated-resolve determinism, and the default 19-accelerator / zero-conflict
  invariant.
- Task 176 is confirmed to have changed only its allowed
  implementation/test/task-brief surfaces plus current dispatch artifacts.
- No task 178 is appended and no completed dated `NEEDS_HUMAN_RECORDED` marker
  is written to `.ai/dispatch.tasks.md` by this read-only audit. The follow-up
  decision is recorded in the EXEC packet only.

## Current-State Claims: Falsification Required

Every negative or unchanged assertion in the EXEC report MUST carry the
inverse search/read that would falsify it, plus the observed result. At minimum
the Executor must re-run and record these checks:

- Claim: there is no task line beginning `178.` before or after this
  read-only audit.
  Falsifying search: `rg -n "^176\\.|^177\\.|^178\\." .ai/dispatch.tasks.md`
  -> planning-time result matched exactly task 176 at line 1158 and task 177
  at line 1265; no line beginning `178.`.
- Claim: reverse-accessor references are confined to `editor-ui` registry
  implementation and `menus_ordering` tests.
  Falsifying search: `rg -n "shortcut_for_command|fn bindings|\\.bindings\\(|shortcut_commands" crates`
  -> planning-time result matched only
  `crates/editor-ui/src/menus/registry.rs` and
  `crates/editor-ui/tests/menus_ordering.rs`.
- Claim: prohibited host/shell/persistence/plugin/CAD/Cargo/schema/workflow
  terms in the accessor implementation/tests are only comments, docs, or
  test descriptions, not new implementation wiring.
  Falsifying search: `rg -n "host|shell|settings|persist|plugin|cad|Cargo|serde|workflow|schema|dispatch\\.verify" crates/editor-ui/src/menus/registry.rs crates/editor-ui/tests/menus_ordering.rs`
  -> planning-time result matched existing comments/test names/descriptions
  and CAD command names in tests; executor must inspect each match and record
  whether any is implementation wiring.
- Claim: broad prohibited implementation, runtime, automation, Cargo,
  workflow, schema, and host/shell surfaces are unchanged in this worktree
  before task execution and remain unchanged after execution.
  Falsifying tracked-diff search: `git diff --name-only -- crates kernel editor tools Cargo.toml Cargo.lock .github/workflows .ai/*.schema.json .ai/dispatch.verify.ps1 Invoke-AiDispatchLoop.ps1 Invoke-AiDispatchQueue.ps1 Invoke-AiDispatchAuto.ps1 Register-AiDispatchSchedule.ps1 Get-AiDispatchHealth.ps1 Wait-GitHubActions.ps1 Watch-AiDispatch.ps1 new-handoff.ps1`
  -> planning-time result was no output.
  Falsifying tracked/untracked status search: `git -c core.excludesfile= status --short -- crates kernel editor tools Cargo.toml Cargo.lock .github/workflows .ai/*.schema.json .ai/dispatch.verify.ps1 Invoke-AiDispatchLoop.ps1 Invoke-AiDispatchQueue.ps1 Invoke-AiDispatchAuto.ps1 Register-AiDispatchSchedule.ps1 Get-AiDispatchHealth.ps1 Wait-GitHubActions.ps1 Watch-AiDispatch.ps1 new-handoff.ps1`
  -> planning-time result was no output.
- Claim: read-only governance/status files are unchanged before task execution
  and remain unchanged after execution.
  Falsifying tracked/untracked status search: `git -c core.excludesfile= status --short -- .ai/dispatch.tasks.md Status.md HANDOFF.md plans/BASELINE.md change.md`
  -> planning-time result was no output.
- Claim: the prior executor gate has been incorporated, and this revision does
  not rely on a "no prior gate" premise.
  Falsifying search: `rg -n "GATE_VERDICT: needs_changes" .ai/dispatch-ISSUE-21`
  -> planning-time result found the prior gate output under
  `.ai/dispatch-ISSUE-21/` with `GATE_VERDICT: needs_changes`.

The dispatcher-provided GitHub snapshot in the issue body is external queue
evidence captured at 2026-06-30T19:55:42.2480663+03:00 before issue #21 was
created. Treat that snapshot, not a live query, as the evidence that there were
no open `ai-dispatch` issues, no open failed autonomous issues, and that the
already-filed autonomous issues were #6, #7, #10, #13, #16, and #18 at snapshot
time. Do not call `gh` or the network from the executor sandbox to reconfirm
that snapshot.

## Constraints / Non-Goals

- Do not fix, refactor, format, or otherwise edit Rust source or tests.
- Do not change `ResolveResult`, `shortcut_commands`, any resolver behavior,
  default menu data, keybinding diagnostics, accelerator conflict behavior, or
  test expectations.
- Do not add fields, secondary indexes, cached vectors, global state, serde
  surfaces, settings persistence, UI, plugin runtime integration, CAD
  integration, host/shell wiring, Cargo dependencies, workflow changes,
  schemas, automation, or scheduler behavior.
- Do not use `gh`, network access, or live GitHub state for task-selection or
  already-filed issue checks; use the dispatcher snapshot and local source
  reads.
- Do not append task 178 and do not write a completed
  `NEEDS_HUMAN_RECORDED` marker to `.ai/dispatch.tasks.md`; record the
  follow-up decision in the EXEC packet only.
- Do not commit, push, merge, create a PR, or alter labels.

## Verification Gates

The Executor MUST run and document the result of each gate in the
`EXECUTION_REPORT`:

- `git diff -- crates/editor-ui/src/menus/registry.rs crates/editor-ui/tests/menus_ordering.rs .ai/dispatch.tasks.md Status.md HANDOFF.md plans/BASELINE.md change.md`
  EXPECTING no changes from this read-only audit.
- `git diff --name-only -- crates kernel editor tools Cargo.toml Cargo.lock .github/workflows .ai/*.schema.json .ai/dispatch.verify.ps1 Invoke-AiDispatchLoop.ps1 Invoke-AiDispatchQueue.ps1 Invoke-AiDispatchAuto.ps1 Register-AiDispatchSchedule.ps1 Get-AiDispatchHealth.ps1 Wait-GitHubActions.ps1 Watch-AiDispatch.ps1 new-handoff.ps1`
  EXPECTING no changes.
- `git -c core.excludesfile= status --short -- crates kernel editor tools Cargo.toml Cargo.lock .github/workflows .ai/*.schema.json .ai/dispatch.verify.ps1 Invoke-AiDispatchLoop.ps1 Invoke-AiDispatchQueue.ps1 Invoke-AiDispatchAuto.ps1 Register-AiDispatchSchedule.ps1 Get-AiDispatchHealth.ps1 Wait-GitHubActions.ps1 Watch-AiDispatch.ps1 new-handoff.ps1`
  EXPECTING no tracked or untracked changes.
- `git -c core.excludesfile= status --short -- .ai/dispatch.tasks.md Status.md HANDOFF.md plans/BASELINE.md change.md`
  EXPECTING no tracked or untracked changes.
- `rg -n "shortcut_for_command|fn bindings|\\.bindings\\(|shortcut_commands" crates`
  EXPECTING matches only in `crates/editor-ui/src/menus/registry.rs` and
  `crates/editor-ui/tests/menus_ordering.rs`.
- `rg -n "host|shell|settings|persist|plugin|cad|Cargo|serde|workflow|schema|dispatch\\.verify" crates/editor-ui/src/menus/registry.rs crates/editor-ui/tests/menus_ordering.rs`
  EXPECTING matches only in comments, docs, test names/descriptions, or
  existing command names that preserve the no-host/no-persistence/no-Cargo
  boundary, not implementation wiring.
- `cargo test -p rge-editor-ui`
- `cargo test -p rge-editor-ui --test menus_ordering`
- `cargo check -p rge-editor-ui -p rge-editor-egui-host -p rge-editor-shell`
- `cargo run -q -p rge-tool-architecture-lints -- all`
- `cargo +nightly fmt --all -- --check`
- `git diff --check`
- `rg -n "^174\\.|^175\\.|^176\\.|^177\\.|^178\\.|^\\s*NEEDS_HUMAN_RECORDED: 20[0-9]{2}-[0-9]{2}-[0-9]{2} - " .ai/dispatch.tasks.md`
  EXPECTING exactly one task 176 and exactly one task 177; no task 178 and no
  completed dated `NEEDS_HUMAN_RECORDED` marker written by this read-only audit.

If an expensive verification gate fails solely because of an unrelated
out-of-scope dependency/security/workspace issue, the Executor MUST NOT repair
it in this dispatch. Record the failure, preserve scope, and halt with
`HANDOFF_STATUS: NEEDS_HUMAN` unless the failure can be resolved by rerunning
the same gate without edits.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of
the following occur:

- Completing the audit requires editing Rust source, tests, Cargo metadata,
  workflows, schemas, automation, packet templates, scripts, or host/shell/runtime
  surfaces.
- Any prohibited surface shows a new diff after the Executor starts.
- The accessors are not read-only over the existing `shortcut_commands` map, a
  new field/secondary index/cache/global state is present, deterministic
  ordering is absent, the many-shortcuts rule is undocumented, or resolve
  behavior changed.
- Required tests for bound, unbound, override-reflected, deterministic, or
  default 19-accelerator / zero-conflict behavior are missing or failing, and
  fixing them would require source/test edits.
- The follow-up recommendation cannot be expressed in the EXEC packet as a
  bounded FEATURE candidate for human approval or as a single
  `NEEDS_HUMAN_RECORDED: YYYY-MM-DD - concrete reason` marker plus
  recommendation block.
- Local source reads contradict the dispatcher snapshot in a way that affects
  task selection or already-filed autonomous-task status.
- Any verification gate fails because of an out-of-scope surface and rerunning
  the same gate cannot clear it without edits.

## Planner Notes

This packet intentionally carries task 177 from `.ai/dispatch.tasks.md` into a
bounded issue dispatch. The Executor should use local source reads for
implementation/test evidence and the dispatcher-provided GitHub state snapshot
for queue/issue evidence. Prior executor gate feedback for this dispatch
returned `GATE_VERDICT: needs_changes`; this revision incorporates it by
widening the prohibited-surface checks, adding untracked-file checks, grounding
the prior-gate state, and wording the GitHub snapshot as dispatcher-provided
evidence rather than a live current-state premise.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-21
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

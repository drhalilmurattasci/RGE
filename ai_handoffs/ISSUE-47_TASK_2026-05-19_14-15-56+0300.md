# Task Packet

DISPATCH_ID: ISSUE-47
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-19_14-15-56+0300
RELATED_FILES:
- crates/material-graph/src/lib.rs
- kernel/graph-foundation/src/viz_adapter.rs
STATUS: OPEN

## Goal

Implement GitHub issue #47 by adding `rge_kernel_graph_foundation::VizAdapter` support to `MaterialGraph`. The adapter should expose the existing material graph structure to graph-viewer consumers without changing material node/edge payload shapes, graph construction APIs, or adding any shader/runtime/editor integration.

## Scope

### MAY edit
- `crates/material-graph/src/lib.rs` only.

### MUST NOT edit
- Any file other than `crates/material-graph/src/lib.rs`.
- `kernel/graph-foundation/src/viz_adapter.rs` or any other graph-foundation substrate file.
- Cargo manifests, lockfiles, schemas, scripts, CI files, docs, `.gitignore`, or existing handoff packets.
- Any material runtime, material editor, gfx, renderer, shader, or asset-pipeline crate.

### MAY add new files
- One protocol-required execution report matching `ai_handoffs/ISSUE-47_EXEC_*.md`.
- The matching execution-report `.meta.json` sidecar if generated by `new-handoff.ps1 -Finalize`.

### MUST NOT add new files
- No new Rust source files, integration-test files, fixtures, docs, schemas, scripts, Cargo entries, WGSL files, renderer assets, or architecture-lint files.
- No handoff packets other than the protocol-required `ISSUE-47_EXEC` report and its finalize-generated sidecar.

## Deliverables

- Implement `rge_kernel_graph_foundation::VizAdapter` for `MaterialGraph` in `crates/material-graph/src/lib.rs`.
- Adapter `node_count` and `edge_count` must delegate to the existing `MaterialGraph`/substrate counts and must not introduce duplicate count state.
- Adapter `nodes()` must iterate existing material graph nodes via the existing substrate node records. Each `NodeView` must use:
  - `id`: the existing `NodeId`.
  - `display_name`: the existing material node key string.
  - `kind`: the static string `"MaterialNode"`.
- Adapter `edges()` must iterate existing graph edge records. Each `EdgeView` must use:
  - `id`: the existing `EdgeId`.
  - `src`: the edge record source node id.
  - `dst`: the edge record destination node id.
  - `label`: a deterministic static label derived from the existing `MaterialEdge` port payload.
- Add a private helper for material edge labels returning `&'static str`; labels must be lower-case source-to-destination strings using port names `scalar`, `vector`, `color`, and `texture` (for example, `scalar->color` and `texture->vector`). The helper must not allocate.
- Add focused unit tests for adapter counts, node view contents/order, and edge view contents/order.
- A completed execution report packet for this dispatch.

## Acceptance Criteria

- The only implementation file changed is `crates/material-graph/src/lib.rs`.
- `MaterialGraph` implements `VizAdapter` and is usable through `&dyn VizAdapter`.
- Existing public construction APIs remain unchanged: do not change `MaterialGraph::new`, `Default`, `add_node`, `connect`, `node_count`, `edge_count`, `PortType`, `MaterialEdge`, or their signatures.
- Existing node and edge payload shapes remain unchanged: do not add fields to `MaterialNode` or `MaterialEdge`, do not remove fields, and do not change `PortType` variants or discriminants.
- Node views are backed by existing material node keys: `display_name` is the node key, `kind` is `"MaterialNode"`, and node iteration order is the deterministic substrate order returned by `Graph::nodes()` rather than insertion order.
- Edge views are backed by existing graph edge records: ids, sources, and destinations come from `Graph::edges()` records, labels come only from the existing `MaterialEdge` port payload, and edge iteration order is the deterministic substrate order returned by `Graph::edges()` rather than insertion order.
- Unit tests include:
  - an adapter count test that observes the counts through the `VizAdapter` trait;
  - a node-view test that asserts ids, display names, kind values, and deterministic order;
  - an edge-view test that asserts edge ids, src/dst ids, static labels, and deterministic order.
- Existing material graph behavior remains intact, including stable node ids, duplicate rejection, differing-port edge distinctness, dangling-endpoint rejection, and empty graph counts.

## Constraints / Non-Goals

- Do not add WGSL generation, shader graph semantics, material evaluation, graph traversal, cycle detection, editor widgets, gfx integration, renderer integration, asset loading, or runtime material behavior.
- Do not make material nodes typed beyond the existing opaque key string; no new public node kind enum or public material node payload API.
- Do not change edge identity derivation, duplicate detection, or typed-port payload semantics.
- Do not add dependencies or feature flags.
- Do not refactor unrelated tests or move tests to a new file.
- Keep any helper functions private to `crates/material-graph/src/lib.rs` unless trait implementation requires otherwise.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `cargo test -p rge-material-graph` -> expected exit 0.
- `git diff -- crates/material-graph/src/lib.rs` -> expected diff is limited to the `VizAdapter` implementation, any private static-label helper, import/doc adjustments needed for that implementation, and focused unit tests.
- `git diff --check -- crates/material-graph/src/lib.rs` -> expected exit 0.

If a required command cannot be run in the environment, the Executor MUST document the exact command, the failure reason, and any partial verification that was completed.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- Implementing the adapter appears to require editing any file other than `crates/material-graph/src/lib.rs`.
- Implementing the adapter appears to require changing `kernel/graph-foundation` APIs or the `VizAdapter`, `NodeView`, or `EdgeView` definitions.
- Passing tests appears to require changing material node/edge payload shapes, `PortType` variants/discriminants, or public construction API signatures.
- A non-static or allocating edge-label strategy appears necessary because of the current `EdgeView` lifetime shape.
- Any WGSL generation, material evaluation, editor-widget work, gfx work, renderer work, dependency change, Cargo manifest change, or new source/test file appears necessary.
- `cargo test -p rge-material-graph` fails for reasons unrelated to the scoped adapter implementation and cannot be resolved within `crates/material-graph/src/lib.rs`.

## Planner Notes

This task was filed automatically by `Invoke-AiDispatchAuto.ps1` from `.ai/dispatch.tasks.md` for GitHub issue #47. Revision number: 0. There is no prior Claude gate result. The key implementation constraint is that `EdgeView::label` borrows a string slice, so material edge labels should be private, deterministic `&'static str` values derived from the existing `(src_port, dst_port)` pair instead of allocated strings.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-47
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

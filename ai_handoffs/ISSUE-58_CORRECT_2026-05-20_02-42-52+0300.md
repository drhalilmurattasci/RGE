# Correction Packet

DISPATCH_ID: ISSUE-58
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_02-42-52+0300
RELATED_FILES:
- crates/editor-ui/src/widgets/node_graph.rs
- crates/anim-graph/src/lib.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-58_TASK_2026-05-20_02-32-12+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-58_EXEC_2026-05-20_02-41-16+0300.md`
- Latest Review Report: none; the post-execution verification gate failed before Codex control review.
- Verification Log: `.ai/dispatch-ISSUE-58/verification.round0.log`
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections, nothing more and nothing less.
Each correction references the failed post-execution verification gate entry:
`.ai/dispatch-ISSUE-58/verification.round0.log`, step `[1] cargo +nightly fmt --check`.

1. **Rustfmt the new node-graph widget source** - Verification finding:
   `crates/editor-ui/src/widgets/node_graph.rs` has rustfmt diffs around
   `NodeView` and `EdgeView` closure destructuring. Required change: apply
   rustfmt-compatible formatting to this file only, preserving the implemented
   API, tests, borrowed-label behavior, and adapter-order behavior. Acceptance:
   `cargo +nightly fmt --all -- --check` reports no diff for
   `crates/editor-ui/src/widgets/node_graph.rs`.
2. **Normalize the reported anim-graph newline style** - Verification finding:
   `crates/anim-graph/src/lib.rs` has incorrect newline style under rustfmt.
   Required change: make the minimal rustfmt/newline-only normalization needed
   for this file. Do not make semantic edits, API edits, comment rewrites, or
   unrelated formatting churn. Acceptance: `cargo +nightly fmt --all -- --check`
   no longer reports incorrect newline style for `crates/anim-graph/src/lib.rs`.

## Deferred Findings (NOT Approved for This Round)

None. No Codex control-review findings exist because verification failed before
control review. Do not address any issue beyond the two formatting failures
enumerated above.

## Updated Acceptance Criteria

Unchanged from the Task Packet, plus the canonical post-execution verification
gate must pass after the approved formatting-only corrections.

## Re-Verification Gates

The Executor MUST re-run these gates after the corrections:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `.ai/dispatch.verify.ps1` -> expected exit 0, including format, architecture
  lints, supply-chain checks, workspace tests, and doctests.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, except this correction round authorizes the
formatting-only edit to `crates/anim-graph/src/lib.rs` listed above. Halt and
return an EXECUTION_REPORT with `STATUS: BLOCKED` if rustfmt requires changes to
any additional files or if either approved file appears to need a semantic
change.

## Planner Notes

This is a verification-gate correction only. The dispatch implementation should
not be redesigned, broadened, or otherwise revised in this round. Preserve all
ISSUE-58 behavior reported in the execution packet and limit changes to the
formatting failures that blocked the canonical gate.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-58
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

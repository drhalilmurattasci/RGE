# Task Packet

DISPATCH_ID: ISSUE-321
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-07_23-00-56+0300
RELATED_FILES:
- plans/BASELINE.md
- Status.md
- HANDOFF.md
- change.md
- .ai/dispatch.tasks.md
- tools/compile-timing.ps1
- crates/**/src/**/*.rs
- ai_handoffs/ISSUE-321_TASK_2026-06-07_23-00-56+0300.md
STATUS: OPEN

## Goal

Measure the Phase 9 PLAN section 13.3 one-line incremental p95 build time for
GitHub issue #321. Use `tools/compile-timing.ps1` around warm-cache
`cargo build --workspace` samples after repeated one-line touches to exactly
one low-risk leaf Rust source file. Do not run `cargo clean`, do not delete or
modify `A:\RustCache\target` outside normal cargo build writes, and do not
change production behavior. Record the p95 result in the baseline/status docs
and state clearly whether it passes or misses the <=10s incremental p95 budget.

## Scope

This is a measurement and documentation dispatch. Final tracked changes are
limited to the allowed docs/task-record files below plus normal generated
ISSUE-321 dispatch artifacts. A temporary source touch is allowed only for
measurement and must leave no final source diff.

### MAY edit

- `plans/BASELINE.md`
- `Status.md`
- `HANDOFF.md`
- `change.md`
- `.ai/dispatch.tasks.md`, only to mark the ISSUE-321 task done if a live
  matching task entry exists.

### MUST NOT edit

- Any file not listed under `### MAY edit`, except generated ISSUE-321
  dispatch artifacts listed under `### MAY add new files` and the temporary
  source-touch exception described below.
- Final Rust source, Rust tests, examples, generated source, or workspace code
  under `crates/**`, `kernel/**`, `runtime/**`, `editor/**`, `tests/**`, or
  `examples/**`.
- Cargo, lint, deny, formatting, toolchain, package, or workspace metadata:
  `Cargo.toml`, `Cargo.lock`, `**/Cargo.toml`, `clippy.toml`, `deny.toml`,
  `rustfmt.toml`, `rust-toolchain*`, or package-manager lock/config files.
- The measurement harness or any tooling/automation source, including
  `tools/compile-timing.ps1`, `tools/**`, `scripts/**`,
  `.ai/dispatch.verify.ps1`, `Invoke-AiDispatch*.ps1`,
  `Register-AiDispatchSchedule.ps1`, `Get-AiDispatchHealth.ps1`,
  `Get-AiDispatchTrends.ps1`, `Watch-AiDispatch.ps1`,
  `Watch-DispatchStages.ps1`, `Wait-GitHubActions.ps1`, and `.mcp.json`.
- ADRs, doctrine docs, workflows, schemas, or protocol docs, including
  `docs/**`, `.github/**`, `AI_DISPATCH_AUTOMATION.md`,
  `AI_DISPATCH_PARALLEL.md`, `AUTONOMOUS_WATCH.md`, `AGENTS.md`,
  `CLAUDE.md`, `ai_handoffs/templates/**`, and
  `ai_handoffs/AI_HANDOFF_PROTOCOL.md`.
- Existing dispatch artifacts for other dispatches under `ai_handoffs/`,
  `ai_dispatch_logs/`, or `.ai/dispatch-*`.
- `.gitignore`, scheduler configuration, local machine scheduled-task state,
  registered Windows Scheduled Tasks, or GitHub workflow state.
- `A:\RustCache\target`, except for ordinary non-destructive writes that
  `cargo build` performs while using the shared target cache.

### Temporary measurement-only source touch

- The Executor MAY temporarily toggle exactly one harmless line in exactly one
  low-risk leaf Rust source file for timing samples, for example a normal
  comment line in a small leaf crate under `crates/**/src/**/*.rs`.
- The same chosen file MUST be used for all incremental samples.
- The temporary edit MUST NOT change public API, generated code, semantics,
  tests, Cargo metadata, or production behavior.
- The source file MUST be restored to its original content before final
  verification. `git diff -- $selectedSourceFile` must show no output, and
  `git status --short --untracked-files=no` must show no `.rs` changes.
- If the Executor cannot confidently choose and restore one low-risk leaf
  source file, halt and request fresh guidance instead of broadening scope.

### MAY add new files

- `ai_handoffs/ISSUE-321_TASK_2026-06-07_23-00-56+0300.meta.json`
- `ai_handoffs/ISSUE-321_EXEC_*.md`
- `ai_handoffs/ISSUE-321_EXEC_*.meta.json`
- `ai_handoffs/ISSUE-321_CORRECT_*.md`
- `ai_handoffs/ISSUE-321_CORRECT_*.meta.json`
- `ai_dispatch_logs/log_*.md`
- `.ai/dispatch-ISSUE-321/**`

### MUST NOT add new files

- New source, test, fixture, benchmark, Cargo, lint, ADR, workflow, schema,
  scheduler, automation, or tooling files.
- New docs or plan files outside the existing allowed files.
- Timing JSON, scratch logs, helper scripts, or notes outside
  `.ai/dispatch-ISSUE-321/**` and normal generated dispatch artifacts.
- New `REVIEW` or `CLOSEOUT` packets for this orchestrator dispatch. Missing
  `_REVIEW_` and `_CLOSEOUT_` packets are normal for orchestrator dispatches.
- Any generated artifact outside the ISSUE-321 patterns named under
  `### MAY add new files`.

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - plans/BASELINE.md
  - Status.md
  - HANDOFF.md
  - change.md
  - .ai/dispatch.tasks.md
  - ai_handoffs/ISSUE-321_TASK_2026-06-07_23-00-56+0300.meta.json
  - ai_handoffs/ISSUE-321_EXEC_*.md
  - ai_handoffs/ISSUE-321_EXEC_*.meta.json
  - ai_handoffs/ISSUE-321_CORRECT_*.md
  - ai_handoffs/ISSUE-321_CORRECT_*.meta.json
  - ai_dispatch_logs/log_*.md
  - .ai/dispatch-ISSUE-321/**
MUST_NOT_EDIT:
  - crates/**
  - kernel/**
  - runtime/**
  - editor/**
  - tests/**
  - examples/**
  - Cargo.toml
  - Cargo.lock
  - **/Cargo.toml
  - clippy.toml
  - deny.toml
  - rustfmt.toml
  - rust-toolchain*
  - tools/**
  - scripts/**
  - .ai/dispatch.verify.ps1
  - Invoke-AiDispatch*.ps1
  - Register-AiDispatchSchedule.ps1
  - Get-AiDispatchHealth.ps1
  - Get-AiDispatchTrends.ps1
  - Watch-AiDispatch.ps1
  - Watch-DispatchStages.ps1
  - Wait-GitHubActions.ps1
  - .mcp.json
  - docs/**
  - .github/**
  - AI_DISPATCH_AUTOMATION.md
  - AI_DISPATCH_PARALLEL.md
  - AUTONOMOUS_WATCH.md
  - AGENTS.md
  - CLAUDE.md
  - ai_handoffs/templates/**
  - ai_handoffs/AI_HANDOFF_PROTOCOL.md
  - .gitignore
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Deliverables

- A new or revised `plans/BASELINE.md` Phase 9 section 13.3 entry recording
  the one-line incremental `cargo build --workspace` measurement methodology,
  chosen temporary source file, sample count, p50/p95 or equivalent summary,
  p95 calculation method, command shape, cache policy, and pass/miss verdict
  against the <=10s budget.
- `Status.md`, `HANDOFF.md`, and `change.md` updated only as needed to record
  the ISSUE-321 measurement result and current Phase 9 incremental-build
  status.
- `.ai/dispatch.tasks.md` updated only if there is a live ISSUE-321 task entry
  to mark done; otherwise leave it unchanged and note that in the EXEC packet.
- One EXEC packet documenting the selected source file, exact source-touch
  method, sample count, per-sample wall seconds, p95 computation, budget
  verdict, changed files, and verification gate results.

## Acceptance Criteria

- `tools/compile-timing.ps1` is used for every timed `cargo build --workspace`
  sample, and every counted measurement command exits 0.
- The Executor collects at least 20 successful one-line incremental build
  samples unless a smaller bounded sample is necessary; if fewer than 20
  samples are used, the EXEC packet and `plans/BASELINE.md` must state the
  exact reason and still compute/report p95 from the successful bounded sample.
- The p95 calculation is explicit. Preferred method: nearest-rank p95 over
  `wall_seconds` from successful incremental samples, using rank
  `ceil(0.95 * sample_count)` after sorting ascending.
- Warm-up/no-op builds, failed builds, timeout builds, and builds not preceded
  by a one-line source touch are not counted in the incremental p95 sample.
- The selected temporary source edit is restored before final verification;
  no final diff includes Rust source, Cargo metadata, tooling, automation,
  workflows, architecture lints, scheduler config, or production behavior.
- `plans/BASELINE.md` clearly says whether the measured p95 passes or misses
  the <=10s incremental p95 budget.
- The final tracked diff contains only `plans/BASELINE.md`, `Status.md`,
  `HANDOFF.md`, `change.md`, `.ai/dispatch.tasks.md` if needed, and normal
  generated ISSUE-321 dispatch artifacts.
- The dispatch does not run `cargo clean`, does not delete target directories,
  and does not remove or wipe `A:\RustCache\target`.

## Current-State Claims: Falsification Required

Every negative / "unchanged" assertion this packet makes about current source
MUST list its inverse-search command and result. A negative claim without a
falsifying search is not a valid premise (Protocol Rule 8).

- Claim: At planning time, `plans/BASELINE.md` still identifies the
  one-line-edit incremental p95 sample as an open follow-up rather than a
  completed measurement.
  Falsifying search: `Select-String -Path 'plans/BASELINE.md' -Pattern 'NOT a 1-line-edit incremental p95 measurement','1-line-edit incremental p95 sample'` -> matches in `plans/BASELINE.md` section 13.3.
- Claim: The timing harness is intended for non-destructive warm-cache timing,
  not clean-build target deletion.
  Falsifying search: `Select-String -Path 'tools/compile-timing.ps1' -Pattern 'cargo clean','target deletion','delete target','Remove-Item'` -> matches only the harness comments about no `cargo clean` / no target deletion plus temporary-file cleanup; no target-wipe mode is present.
- Claim: This task does not require final Rust source, Cargo, workflow,
  scheduler, or automation edits.
  Falsifying search: `git diff --name-only -- crates kernel runtime editor tests examples Cargo.toml Cargo.lock .github tools .ai/dispatch.verify.ps1 Invoke-AiDispatch*.ps1 Register-AiDispatchSchedule.ps1` at planning time -> no output.

## Constraints / Non-Goals

- Do not remediate compile time. This dispatch measures and records the
  current incremental p95 result only.
- Do not retarget PLAN thresholds, budgets, or Phase 9 policy.
- Do not run `cargo clean`, delete any target directory, remove
  `A:\RustCache\target`, or substitute a fresh clean target measurement for
  the incremental warm-cache measurement.
- Do not edit `tools/compile-timing.ps1` or add a new timing harness.
- Do not edit Cargo manifests, `Cargo.lock`, architecture lints, workflows,
  scheduler config, dispatch automation scripts, or production behavior.
- Do not leave the temporary source touch in the final diff, and do not use
  more than one source file for timing touches.
- Do not create broad performance dashboards, CI gates, benchmark crates, or
  regression tests.
- Do not commit or push manually from the Executor role; the queue runner owns
  publish behavior after control review.

## Verification Gates

The Executor MUST run and document the result of each of these in their
`EXECUTION_REPORT`:

- `git status --short --untracked-files=no` before measurement -> expected no
  unrelated tracked changes that would affect the measurement or final diff.
- Warm-up command, not counted in p95:
  `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tools\compile-timing.ps1 -Mode build -Iterations 1 -TimeoutSeconds 120 -JsonPath .ai\dispatch-ISSUE-321\compile-timing-warmup.json`
  -> expected exit 0.
- Incremental sample loop: before each counted sample, toggle one harmless line
  in the selected source file, then run
  `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tools\compile-timing.ps1 -Mode build -Iterations 1 -TimeoutSeconds 120 -JsonPath .ai\dispatch-ISSUE-321\compile-timing-incremental-NN.json`
  -> expected exit 0 for every counted sample.
- P95 computation over successful incremental sample JSON files -> expected a
  documented p95 and explicit PASS/MISS against <=10s.
- `git diff -- $selectedSourceFile` after restoring the temporary source
  edit -> expected no output.
- `git diff --check` -> expected exit 0.
- `git status --short --untracked-files=no` -> expected only intended
  docs/task-record changes among `plans/BASELINE.md`, `Status.md`,
  `HANDOFF.md`, `change.md`, and `.ai/dispatch.tasks.md`.
- `git diff --name-only -- crates kernel runtime editor tests examples Cargo.toml Cargo.lock .github tools .ai/dispatch.verify.ps1 Invoke-AiDispatch*.ps1 Register-AiDispatchSchedule.ps1` -> expected no output.
- Static inspection of `plans/BASELINE.md` -> expected methodology, sample
  count, p95 calculation, selected source file, cache policy, and <=10s
  PASS/MISS verdict are all recorded.

No fmt, unit-test, architecture-lint, or full workspace verification gate is
required because final source behavior is unchanged. The timed `cargo build`
commands above are the required cargo gates for this dispatch.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of
the following occur:

- Measuring incremental builds appears to require `cargo clean`, deleting or
  wiping `A:\RustCache\target`, changing Cargo metadata, or editing the timing
  harness.
- A low-risk leaf Rust source file cannot be chosen confidently, or the
  temporary source edit cannot be restored exactly.
- More than one Rust source file would need to be touched for measurement.
- Any timed build repeatedly exits nonzero or times out such that no credible
  bounded p95 can be reported.
- Reporting the result requires changing thresholds, adding lints, adding CI,
  or remediating performance rather than recording the measurement.
- Any final Rust source, Cargo, tooling, workflow, scheduler, automation, or
  production-behavior change appears necessary.
- The working tree contains unrelated tracked modifications in a file the
  Executor needs to edit and the safe merge path is unclear.
- `git diff --check` fails and cannot be fixed within the allowed docs/task
  record files.

## Planner Notes

AGENTS.md distinguishes orchestrator dispatches from the manual packet
protocol. This is an orchestrator dispatch, so the Executor should not create
`_REVIEW_` or `_CLOSEOUT_` packets; missing review/closeout packets are
expected. Revision 0 has no prior Claude gate.

The important measurement detail is that `tools/compile-timing.ps1 -Iterations
N` would only measure one incremental build followed by no-op builds if the
source is touched once before the command. For a valid p95, touch the same
source file before each counted `-Iterations 1` build and compute p95 over
those per-touch samples. Keep all timing JSON under `.ai/dispatch-ISSUE-321/`
so the final tracked diff remains documentation/task-record only.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-321
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

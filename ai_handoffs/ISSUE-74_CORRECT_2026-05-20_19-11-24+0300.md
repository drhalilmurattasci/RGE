# Correction Packet

DISPATCH_ID: ISSUE-74
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-20_19-11-24+0300
RELATED_FILES:
- crates/editor-ui/tests/node_graph_phase8_three_domain_smoke.rs
STATUS: CORRECTION_OPEN

## References

- Task Packet: `ai_handoffs/ISSUE-74_TASK_2026-05-20_18-59-34+0300.md`
- Latest Execution Report: `ai_handoffs/ISSUE-74_EXEC_2026-05-20_19-10-19+0300.md`
- Latest Review Report: none generated. The post-execution canonical verification gate failed before Codex control review.
- Prior CORRECT packets in this dispatch: none.

## Approved Corrections (Planner Sign-Off)

The Executor MUST act on exactly these corrections - nothing more, nothing
less.

1. **Normalize editor-ui smoke test newline style** - Verification gate
   finding: `cargo +nightly fmt --all -- --check` failed with
   `Incorrect newline style in \\?\A:\RCAD\RGE\crates\editor-ui\tests\node_graph_phase8_three_domain_smoke.rs`.
   Required change: normalize only the line endings in
   `crates/editor-ui/tests/node_graph_phase8_three_domain_smoke.rs` to the
   newline style accepted by `rustfmt`. Do not change Rust tokens, formatting,
   comments, imports, module structure, test behavior, `.github/workflows/bench.yml`,
   or any other file. Acceptance: `cargo +nightly fmt --all -- --check` exits 0,
   and a whitespace-insensitive diff of
   `crates/editor-ui/tests/node_graph_phase8_three_domain_smoke.rs` shows no
   content change beyond newline normalization.

## Deferred Findings (NOT Approved for This Round)

None. No review findings were produced; the only known blocker is the canonical
format gate failure above.

## Updated Acceptance Criteria

Unchanged from the Task Packet except for this correction-specific allowance:
`crates/editor-ui/tests/node_graph_phase8_three_domain_smoke.rs` may receive a
line-ending-only normalization so the canonical verification gate can pass. The
existing `.github/workflows/bench.yml` deliverable from the execution round must
not be expanded or otherwise changed.

## Re-Verification Gates

The Executor MUST re-run these gates after the correction:

- `cargo +nightly fmt --all -- --check` -> expected exit 0.
- `git diff --ignore-space-at-eol -- crates/editor-ui/tests/node_graph_phase8_three_domain_smoke.rs` -> expected no content diff beyond newline normalization.
- `.\.ai\dispatch.verify.ps1` -> expected exit 0.

## Halt Conditions (Updated if Any)

Unchanged from the Task Packet, with this correction-round addition: halt and
route back to Planner if making `cargo +nightly fmt --all -- --check` pass
would require any non-newline content change in
`crates/editor-ui/tests/node_graph_phase8_three_domain_smoke.rs`, any edit to
another file, any change to `.github/workflows/bench.yml`, or any
semantic/runtime change.

## Planner Notes

This correction is deliberately narrow. The ISSUE-74 execution report indicates
the requested script benchmark compile workflow landed and its local gate
passed, but the orchestrator's canonical verification failed before control
review on a newline-style problem in
`crates/editor-ui/tests/node_graph_phase8_three_domain_smoke.rs`. Normalize that
file's newline style only, preserve the existing ISSUE-74 workflow change, and
rerun the canonical verification gate.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-74
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

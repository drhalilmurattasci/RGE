# Task Packet

DISPATCH_ID: ISSUE-323
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-06-08_00-28-34+0300
RELATED_FILES:
- plans/BASELINE.md
- Status.md
- HANDOFF.md
- change.md
- .ai/dispatch.tasks.md
- crates/io-3mf/src/lib.rs
STATUS: OPEN

## Goal

Reconcile GitHub issue #323 by updating the scoped plan/status surfaces that still describe `io-3mf` as missing even though `crates/io-3mf` now exists. The final text must say the crate exists, remains a stub, and that real 3MF parser/exporter work is still deferred until format-handler implementation pressure appears.

## Scope

### MAY edit
- `plans/BASELINE.md`
- `Status.md`
- `HANDOFF.md`
- `change.md`
- `.ai/dispatch.tasks.md`
- `crates/io-3mf/src/lib.rs`

`crates/io-3mf/src/lib.rs` may be edited only for crate-level documentation or stub-boundary clarity. `.ai/dispatch.tasks.md` may be edited only to mark task 87 done.

### MUST NOT edit
- `Cargo.toml`
- `Cargo.lock`
- `crates/**/Cargo.toml`
- `.github/workflows/**`
- `tools/architecture-lints/**`
- `Invoke-AiDispatchLoop.ps1`
- `Invoke-AiDispatchQueue.ps1`
- `Invoke-AiDispatchAuto.ps1`
- `Register-AiDispatchSchedule.ps1`
- `Get-AiDispatchHealth.ps1`
- `Watch-AiDispatch.ps1`
- `Wait-GitHubActions.ps1`
- `new-handoff.ps1`
- `.ai/dispatch.verify.ps1`
- `.ai/*.schema.json`

Also do not edit scheduler configuration, dispatch automation behavior, architecture-lint policy, dependency declarations, lockfiles, or Rust source outside `crates/io-3mf/src/lib.rs`.

### MAY add new files
- None.

### MUST NOT add new files
- Do not add product files, docs, ADRs, tests, fixtures, Rust modules, manifests, workflows, scripts, schemas, sidecars, or logs as deliverables for this task. The Executor's required `EXEC` handoff packet is protocol output, not product scope.

### Advisory machine-readable scope envelope

<!-- handoff:envelope v1 -->
MAY_EDIT:
  - plans/BASELINE.md
  - Status.md
  - HANDOFF.md
  - change.md
  - .ai/dispatch.tasks.md
  - crates/io-3mf/src/lib.rs
MUST_NOT_EDIT:
  - Cargo.toml
  - Cargo.lock
  - crates/**/Cargo.toml
  - .github/workflows/**
  - tools/architecture-lints/**
  - Invoke-AiDispatchLoop.ps1
  - Invoke-AiDispatchQueue.ps1
  - Invoke-AiDispatchAuto.ps1
  - Register-AiDispatchSchedule.ps1
  - Get-AiDispatchHealth.ps1
  - Watch-AiDispatch.ps1
  - Wait-GitHubActions.ps1
  - new-handoff.ps1
  - .ai/dispatch.verify.ps1
  - .ai/*.schema.json
INCIDENTAL_OK: false
<!-- /handoff:envelope -->

## Deliverables

- `plans/BASELINE.md`, `Status.md`, and `HANDOFF.md` no longer leave active status readers with the false impression that `io-3mf` is entirely missing.
- `change.md` records the reconciliation in forward-only chronological style; old historical entries may remain historical if a current entry clearly supersedes the stale claim.
- `.ai/dispatch.tasks.md` task 87 is marked done using the repository's existing done-marker style, with `ISSUE-323` as the dispatch reference if PR or final commit data is not yet known.
- If `crates/io-3mf/src/lib.rs` is touched, the change is limited to documentation or stub-boundary wording and does not add parser/exporter API, modules, behavior, or dependencies.

## Acceptance Criteria

- The scoped docs state that `crates/io-3mf` exists in the workspace.
- The scoped docs state that `rge-io-3mf` is still a stub.
- The scoped docs state that real 3MF parser/exporter implementation remains deferred until format-handler pressure appears.
- Any stale "entirely missing" wording in active status surfaces is removed or explicitly superseded so it cannot be read as current status.
- No parser/exporter implementation, public API, dependency, Cargo manifest, workflow, architecture-lint, scheduler, or dispatch-automation change is introduced.
- The final product diff is limited to the files listed under `MAY edit`.

## Current-State Claims: Falsification Required

Every negative or unchanged assertion this packet makes about current source carries a search/read that can falsify it.

- Claim: The drift exists in the scoped status surfaces: current text still contains `io-3mf` missing-language while the crate exists.
  Falsifying search: `Select-String -Path Status.md,HANDOFF.md,change.md -Pattern 'io-3mf','missing'` plus `Get-ChildItem -Recurse crates/io-3mf` -> observed missing-language matches in `Status.md:314`, `HANDOFF.md:1697`, `change.md:280`, `change.md:385`, and `change.md:879`; observed crate files `crates/io-3mf/Cargo.toml` and `crates/io-3mf/src/lib.rs`.
- Claim: `crates/io-3mf/src/lib.rs` is currently documentation-only stub text, not a parser/exporter implementation.
  Falsifying search: `Select-String -Path crates/io-3mf/src/lib.rs -Pattern 'parse','parser','export','exporter','read','write'` -> no output; direct read shows only crate-level stub documentation.
- Claim: The workspace already declares the `rge-io-3mf` package and includes `crates/io-3mf`.
  Falsifying search: `Select-String -Path Cargo.toml,crates/*/Cargo.toml -Pattern 'rge-io-3mf','io-3mf'` -> observed `Cargo.toml:104: "crates/io-3mf"` and `crates/io-3mf/Cargo.toml:2: name = "rge-io-3mf"`.

## Constraints / Non-Goals

- Do not implement a 3MF parser, exporter, loader, serializer, adapter, or format handler.
- Do not add dependencies or edit any Cargo manifest or `Cargo.lock`.
- Do not add tests, fixtures, modules, ADRs, workflows, schemas, architecture lints, scheduler config, or dispatch automation changes.
- Do not claim 3MF import/export is supported. The correct state is: crate exists, stub remains, real implementation deferred until pressure appears.
- Preserve chronological meaning. Prefer forward-only superseding notes in historical logs over rewriting old dated history unless the surrounding document is clearly a current-status snapshot.

## Verification Gates

The Executor MUST run and document the result of each applicable gate in the `EXECUTION_REPORT`:

- `git diff --check` -> expected exit 0.
- If `crates/io-3mf/src/lib.rs` is changed: `cargo check -p rge-io-3mf` -> expected exit 0.
- If `crates/io-3mf/src/lib.rs` is not changed: document that `cargo check -p rge-io-3mf` was skipped because the dispatch was docs-only.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- Reconciling the docs appears to require implementing, designing, or exposing a real 3MF parser/exporter or format-handler API.
- The work appears to require Cargo manifest, dependency, workflow, architecture-lint, scheduler, or dispatch-automation edits.
- The executor's checkout does not contain `crates/io-3mf/Cargo.toml` and `crates/io-3mf/src/lib.rs`, or the package is no longer named `rge-io-3mf`.
- `git diff --check` fails and cannot be fixed within the allowed files.
- `cargo check -p rge-io-3mf` is required because Rust was changed, but it fails for a reason that cannot be fixed by documentation-only edits in `crates/io-3mf/src/lib.rs`.
- The final product diff would include any file outside the `MAY edit` list.

## Planner Notes

This is a reconciliation dispatch, not format support. The desired wording should make the boundary boring and explicit: `crates/io-3mf` exists as a workspace crate, it is intentionally still a stub, and parser/exporter work stays deferred until real format-handler pressure appears. Keep `change.md` chronological and avoid broad cleanup of unrelated historical wording.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-323
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

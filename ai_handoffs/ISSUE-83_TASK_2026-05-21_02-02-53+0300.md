# Task Packet

DISPATCH_ID: ISSUE-83
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-21_02-02-53+0300
RELATED_FILES:
- kernel/graph-foundation/tests/diff_correctness.rs
STATUS: OPEN

## Goal

Add focused integration-test coverage for GitHub issue #83: `GraphDiff::between` must report removed nodes with their old payloads. The new regression should remove exactly one isolated node between snapshots and prove that `removed_nodes` preserves that old node payload without added-node, changed-node, or edge-side diff noise.

## Scope

### MAY edit
- `kernel/graph-foundation/tests/diff_correctness.rs`

### MUST NOT edit
- Any file outside `kernel/graph-foundation/tests/diff_correctness.rs`, except for the protocol-required execution report packet described below.
- Any production/runtime source under `kernel/graph-foundation/src/**`, including `Graph`, `GraphDiff`, `GraphSnapshot`, `NodeId`, `EdgeId`, node storage, edge storage, snapshotting, or diffing.
- Any graph-foundation snapshot code, public APIs, domain graph crate, or graph consumer outside this integration test.
- Cargo manifests, lockfiles, workspace configuration, scripts, docs, schemas, CI files, `.gitignore`, generated files, or any existing handoff packet.

### MAY add new files
- One protocol-required execution report matching `ai_handoffs/ISSUE-83_EXEC_*.md`.
- The matching execution-report `.meta.json` sidecar if generated by `new-handoff.ps1 -Finalize`.

### MUST NOT add new files
- No new Rust source files, new integration test files, fixtures, snapshots, docs, schemas, scripts, Cargo entries, or architecture-lint files.
- No handoff packets other than the protocol-required `ISSUE-83_EXEC` report and its finalize-generated sidecar.

## Deliverables

- Add exactly one focused integration test in `kernel/graph-foundation/tests/diff_correctness.rs`, preferably named `diff_removed_node_preserves_old_node_payload`.
- The test must create graph snapshots where the only semantic difference is removal of exactly one isolated node.
- The test must compute `GraphDiff::between(&snap1, &snap2)` and assert `removed_nodes` contains that removed `NodeId` with the old node payload.
- The test must assert there is no unrelated diff noise: no added nodes, no changed nodes, no added edges, no removed edges, and no changed edges.
- The test must assert precise `node_change_count()` and `edge_change_count()` results.
- A completed execution report packet for this dispatch.

## Acceptance Criteria

- The only implementation file changed is `kernel/graph-foundation/tests/diff_correctness.rs`.
- The new regression is one new `#[test]` function; it may reuse the existing local `n(...)` and `e(...)` helpers.
- The graph under test removes exactly one isolated node after the old snapshot is captured; no edge is inserted, removed, or changed as part of this scenario.
- The test fails if `GraphDiff::between` records the removed node without the expected old payload.
- The test asserts:
  - `diff.removed_nodes.len() == 1`
  - `diff.removed_nodes` contains the removed `NodeId`
  - the removed-node entry equals the expected old payload
  - `diff.added_nodes.len() == 0`
  - `diff.changed_nodes.len() == 0`
  - `diff.added_edges.len() == 0`
  - `diff.removed_edges.len() == 0`
  - `diff.changed_edges.len() == 0`
  - `diff.node_change_count() == 1`
  - `diff.edge_change_count() == 0`
- No graph-foundation API, production diff implementation, snapshot implementation, domain graph crate, dependency, Cargo configuration, documentation, or architecture policy is changed.
- `cargo test -p rge-kernel-graph-foundation --test diff_correctness` exits 0.

## Constraints / Non-Goals

- This is a test-only dispatch. Do not modify `Graph`, `GraphDiff`, `GraphSnapshot`, graph storage, node removal, snapshotting, diffing, or any other runtime behavior to satisfy the new test.
- Do not add or change public APIs.
- Do not add coverage for added nodes, changed nodes, removed-edge payloads, added-edge payloads, changed-edge payloads, cascading node-removal edges, serialization, traversal, stable hashing, visualization, or domain graph behavior.
- Do not add dependencies, fixtures, helper modules, new test files, Cargo manifest entries, docs, scripts, or snapshots.
- Do not rewrite or restructure existing tests in `diff_correctness.rs` except for minimal local placement needed to add the requested regression cleanly.
- Do not commit, stage, push, or create any packet other than the protocol-required execution report and optional EXEC sidecar.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `cargo test -p rge-kernel-graph-foundation --test diff_correctness` -> expected exit 0.
- `git diff -- kernel/graph-foundation/tests/diff_correctness.rs` -> expected diff is limited to exactly one additive removed-node payload regression test.
- `git diff --check -- kernel/graph-foundation/tests/diff_correctness.rs` -> expected exit 0.

If a required command cannot be run in the environment, the Executor MUST document the exact command, the failure reason, and any partial verification that was completed.

## Halt Conditions

The Executor MUST halt without modifying runtime behavior and request fresh guidance if any of the following occur:

- The requested removed-node payload regression fails against the existing production implementation and passing it would require changing `Graph`, `GraphDiff`, `GraphSnapshot`, `NodeId`, `EdgeId`, any record type, or any other runtime source.
- Any required edit outside `kernel/graph-foundation/tests/diff_correctness.rs` appears necessary.
- Any new file other than the protocol-required execution report and its finalize-generated sidecar appears necessary.
- The current public integration-test API cannot express the isolated removed-node payload assertion without adding or changing a public API.
- Implementing the requested coverage appears to require changing edge diff behavior, cascading node-removal behavior, snapshot serialization, domain graph behavior, or architecture policy.
- Formatting or tests fail for reasons unrelated to the requested regression and cannot be isolated within the allowed scope.
- The working tree contains conflicting edits in `kernel/graph-foundation/tests/diff_correctness.rs` that make a minimal focused test addition unsafe to apply.

## Planner Notes

This task was filed automatically by `Invoke-AiDispatchAuto.ps1` from `.ai/dispatch.tasks.md` for GitHub issue #83. Revision number: 0. There is no prior Claude gate result. Keep the implementation narrow: add only the removed-node payload preservation regression in the existing `diff_correctness` integration test file, matching the style of the adjacent GraphDiff payload tests from issues #80 and #81.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-83
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

# Task Packet

DISPATCH_ID: ISSUE-5
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-17_22-50-23+0300
RELATED_FILES:
- AI_DISPATCH_AUTOMATION.md
- Invoke-AiDispatchLoop.ps1
- .ai/dispatch.verify.ps1
- .github/workflows/fmt.yml
- .github/workflows/architecture.yml
- .github/workflows/deny.yml
- .github/workflows/tests.yml
- ai_handoffs/ISSUE-5_EXEC_2026-05-17_22-34-41+0300.meta.json
STATUS: OPEN

## Goal

Document the dispatch verification gate for GitHub issue #5 by updating `AI_DISPATCH_AUTOMATION.md` only. The addition must explain `.ai/dispatch.verify.ps1`, the GitHub Actions checks it mirrors, and where the gate runs in the dispatch loop. Prior control feedback says the previous documentation content was acceptable but the run failed because an EXEC `.meta.json` sidecar remained and the EXEC report misstated the worktree; this dispatch must not recreate that sidecar failure.

## Scope

### MAY edit
- `AI_DISPATCH_AUTOMATION.md`

### MAY delete
- `ai_handoffs/ISSUE-5_EXEC_2026-05-17_22-34-41+0300.meta.json`, but only if it exists in the current worktree as stale debris from the prior failed attempt.

### MUST NOT edit
- `Invoke-AiDispatchLoop.ps1`
- `.ai/dispatch.verify.ps1`
- `.github/workflows/fmt.yml`
- `.github/workflows/architecture.yml`
- `.github/workflows/deny.yml`
- `.github/workflows/tests.yml`
- `.gitignore`
- `ai_handoffs/AI_HANDOFF_PROTOCOL.md`
- Any source file, schema, script, Cargo manifest, lockfile, or documentation file other than `AI_DISPATCH_AUTOMATION.md`
- Any existing handoff packet other than the protocol-required EXECUTION_REPORT for this run

### MAY add new files
- One protocol-required `ai_handoffs/ISSUE-5_EXEC_<timestamp>.md` execution report.

### MUST NOT add new files
- Any source, docs, schema, script, Cargo, workflow, or ADR file.
- Any handoff sidecar `.meta.json` for an EXEC packet. The EXEC report for this run must remain Markdown-only unless a human explicitly overrides this packet.

## Deliverables

- A concise `AI_DISPATCH_AUTOMATION.md` subsection, in the document's existing style, for `.ai/dispatch.verify.ps1`.
- The subsection states that `.ai/dispatch.verify.ps1` mirrors the format, architecture lint, cargo-deny, and workspace-test GitHub Actions workflows: `fmt.yml`, `architecture.yml`, `deny.yml`, and `tests.yml`.
- The subsection states that `Invoke-AiDispatchLoop.ps1` runs the verification script after Claude execution and before Codex control.
- The subsection states that a non-zero verification exit fails the dispatch before publish.
- If the stale prior sidecar `ai_handoffs/ISSUE-5_EXEC_2026-05-17_22-34-41+0300.meta.json` exists, it is removed.
- A new EXECUTION_REPORT accurately reports the final `git status --short --branch`, including whether any EXEC `.meta.json` sidecar exists.

## Acceptance Criteria

- `AI_DISPATCH_AUTOMATION.md` contains the requested `.ai/dispatch.verify.ps1` documentation and no unrelated rewrites.
- The changed documentation matches the surrounding document style: concise prose, existing heading pattern, existing terminology, and no new doctrine beyond the verification gate behavior.
- The only intended content change outside protocol packets is `AI_DISPATCH_AUTOMATION.md`.
- No EXEC `.meta.json` sidecar is present for this dispatch after execution; especially `ai_handoffs/ISSUE-5_EXEC_2026-05-17_22-34-41+0300.meta.json` must not remain if it appears.
- The EXECUTION_REPORT's worktree claims match the actual output of `git status --short --branch`.
- The dispatch verification gate passes when the orchestrator runs `.ai/dispatch.verify.ps1` after execution.

## Constraints / Non-Goals

- Do not modify the implementation of the dispatch loop or verification script.
- Do not modify GitHub Actions workflows.
- Do not add tests, crates, dependencies, architecture lint rules, exemptions, schemas, or generated documentation.
- Do not expand the issue into a broader dispatch automation rewrite.
- If `AI_DISPATCH_AUTOMATION.md` already contains an acceptable unstaged addition satisfying the deliverables, leave that doc content unchanged and perform only the stale-sidecar cleanup if needed.

## Verification Gates

The Executor MUST run and document the result of each of these in their `EXECUTION_REPORT`:

- `Select-String -Path AI_DISPATCH_AUTOMATION.md -Pattern '.ai/dispatch.verify.ps1','fmt.yml','architecture.yml','deny.yml','tests.yml'` -> expected matches for all required terms.
- `git diff -- AI_DISPATCH_AUTOMATION.md` -> expected only the concise documentation addition, with no unrelated rewrite.
- `git status --short --branch` -> expected output accurately copied or summarized in the EXECUTION_REPORT.
- `Test-Path -LiteralPath 'ai_handoffs/ISSUE-5_EXEC_2026-05-17_22-34-41+0300.meta.json'` -> expected `False`.
- Do not manually finalize the EXEC packet. The active packet forbids EXEC `.meta.json` sidecar creation; note this deliberate skip in the EXECUTION_REPORT.

The orchestrator will separately run `.ai/dispatch.verify.ps1` after the EXECUTION_REPORT is written and before Codex control. A non-zero exit from that script fails the dispatch before publish.

## Halt Conditions

The Executor MUST halt without committing and request fresh guidance if any of the following occur:

- Satisfying the documentation request appears to require editing any file other than `AI_DISPATCH_AUTOMATION.md` or deleting the single stale sidecar named above.
- The current worktree contains an EXEC `.meta.json` sidecar for this dispatch other than the specifically named stale prior sidecar.
- The verification-script behavior in `.ai/dispatch.verify.ps1` or `Invoke-AiDispatchLoop.ps1` appears inconsistent with the requested documentation.
- `git diff -- AI_DISPATCH_AUTOMATION.md` shows unrelated formatting churn or broad rewrites.
- The executor cannot produce an EXECUTION_REPORT whose worktree claims match `git status --short --branch`.

## Planner Notes

The key issue from the prior failed attempt was not the requested prose; it was publish readiness. Preserve any already-correct documentation wording if present, remove only the named stale EXEC sidecar if present, and make the next EXEC report precise about the actual worktree. This packet intentionally forbids EXEC `.meta.json` sidecar creation so the orchestrator skips auto-finalizing the EXEC report sidecar for this run.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-5
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

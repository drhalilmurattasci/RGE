# Task Packet

DISPATCH_ID: ISSUE-243
AUTHOR: Planner / OpenAI Codex
TIMESTAMP: 2026-05-28_09-50-54+0300
RELATED_FILES:
- AI_DISPATCH_AUTOMATION.md
- ai_handoffs/AI_HANDOFF_PROTOCOL.md
- crates/script-bench/src/wasmtime_cranelift.rs
- crates/script-bench/BASELINE.md
- Status.md
- HANDOFF.md
- change.md
- Cargo.lock
- Cargo.toml
- ai_handoffs/ISSUE-243_*
- ai_dispatch_logs/log_*ISSUE-243*.md
STATUS: OPEN

## Goal

Rebaseline the existing W04 raw Wasmtime Cranelift cold-start measurement on
the recorder host for GitHub issue #243, append the fresh recorder-host-specific
number to `crates/script-bench/BASELINE.md`, and reconcile the forward status
docs so the stale "904 microseconds / wasmtime 23 not revalidated" wording is
superseded without rewriting dated history.

This is a small Phase 9 measurement and documentation dispatch. It must use the
existing raw Wasmtime Cranelift W04 harness. It must not change Rust source,
tests, benches, Cargo metadata, architecture plans, or dispatch automation.

## Scope

### MAY edit

- `crates/script-bench/BASELINE.md`
- `Status.md`
- `HANDOFF.md`
- `change.md`
- `ai_handoffs/ISSUE-243_*.md`
- `ai_handoffs/ISSUE-243_*.meta.json`
- `ai_dispatch_logs/log_*ISSUE-243*.md`

### MUST NOT edit

- Any Rust source, test, bench, or fixture file, including `crates/script-bench/src/**`, `crates/script-bench/tests/**`, and `crates/script-bench/benches/**`.
- `Cargo.toml`, `Cargo.lock`, or any `**/Cargo.toml`.
- Any `crates/**` path outside `crates/script-bench/BASELINE.md`.
- `plans/PLAN.md`, `plans/IMPLEMENTATION.md`, or `plans/BASELINE.md`.
- Any ADR file under `docs/adr/**`.
- Architecture-lint source, fixtures, snapshots, or exemptions.
- `Invoke-AiDispatchLoop.ps1`, `Invoke-AiDispatchQueue.ps1`, `Invoke-AiDispatchAuto.ps1`, `Register-AiDispatchSchedule.ps1`, `Wait-GitHubActions.ps1`, `.ai/dispatch.verify.ps1`, or `.ai/dispatch.tasks.md`.
- Any GitHub workflow under `.github/workflows/**`.
- Existing handoff packets or dispatch logs from earlier issues.

### MAY add new files

- `ai_handoffs/ISSUE-243_*.md`
- `ai_handoffs/ISSUE-243_*.meta.json`
- `ai_dispatch_logs/log_*ISSUE-243*.md`

### MUST NOT add new files

- No new Rust source, tests, benches, fixtures, crates, Cargo metadata, ADRs,
  architecture-lint files, workflow files, automation scripts, schemas, or
  plan docs.
- No new benchmark harness. The existing
  `crates/script-bench/src/wasmtime_cranelift.rs::tests::w04_cold_start_wasmtime_cranelift`
  test is the measurement harness for this dispatch.

## Deliverables

- Recorder-host confirmation evidence in the EXEC packet and in the baseline
  section, or a clean `STATUS: NEEDS_HUMAN` halt if recorder-host status cannot
  be established.
- A fresh W04 raw Wasmtime Cranelift cold-start measurement using the existing
  focused test harness, normally three focused release runs with all
  `duration_ns` and `duration_ms` stdout values recorded.
- A new append-only `RUN 2026-05-28` section in
  `crates/script-bench/BASELINE.md` under the W04 raw Wasmtime Cranelift
  evidence, preserving the older wasmtime 23 904-microsecond history and the
  2026-05-12 wasmtime 44 405,100 ns / 0.405 ms row as dated history.
- The selected min-of-3 cold-start result and its delta versus the 2026-05-12
  wasmtime 44 Cranelift anchor of 405,100 ns. If the fresh minimum is more than
  20 percent slower than that anchor, report it as an observation only.
- Current toolchain and workspace wasmtime version evidence visible from the
  workspace, the exact command or commands run, and the raw sample outputs.
- Concise forward-only updates to `Status.md`, `HANDOFF.md`, and `change.md`
  stating that ISSUE-243 recorded the fresh recorder-host number and that the
  stale "904 microseconds / wasmtime 23 not revalidated" line is superseded
  going forward. Do not rewrite dated historical snapshots.

## Acceptance Criteria

- The dispatch records whether it confirmed recorder-host execution. If
  recorder-host status cannot be established, it halts without recording a new
  performance number.
- The existing W04 raw Wasmtime Cranelift cold-start harness is used. No new
  benchmark harness, Rust source edit, test edit, fixture, or Cargo change is
  introduced.
- `crates/script-bench/BASELINE.md` gains an append-only run section with exact
  command lines, toolchain and wasmtime version evidence, all sample outputs,
  the selected minimum, and the delta versus 405,100 ns.
- The new baseline text clearly distinguishes the fresh number from both the
  older 904-microsecond wasmtime 23 history and the 2026-05-12 405,100 ns
  wasmtime 44 single-run row.
- The new baseline section includes a load-bearing caveat: recorder-host
  specific; does not certify universal performance, vendor parity,
  cold-start behavior on other machines, sustained thermal behavior, or CI
  regression coverage.
- `Status.md`, `HANDOFF.md`, and `change.md` are updated forward-only and
  concisely. Dated historical snapshots remain intact.
- No pass/fail threshold is applied to the performance result. A greater-than
  20 percent slowdown versus 405,100 ns is reported as an observation only.
- Verification commands are run and reported. There is no requirement to run
  the full canonical dispatch gate if the task remains measurement/docs-only
  and source-free.

## Constraints / Non-Goals

- This dispatch is measurement/docs-only. It does not investigate or fix a
  regression, even if the fresh number is slower.
- This dispatch does not change the W04 harness, the wasmtime configuration,
  workspace dependencies, Cargo features, criterion setup, script-host logic,
  or any architecture plan.
- This dispatch does not rebaseline any W04 row other than raw Wasmtime
  Cranelift cold-start.
- This dispatch does not run the one-hour memory soak.
- This dispatch does not run broad `cargo test --workspace` unless the
  Executor has a specific, documented reason.
- This dispatch must run through PR-mode dispatch publication. It must not push
  `origin/main` directly and must not automatically close GitHub issue #243.

## Verification Gates

The Executor MUST run and document the result of each applicable gate in the
`EXECUTION_REPORT`:

- Confirm recorder-host status before measuring. Acceptable evidence must tie
  the current Windows x86_64 workstation to the recorder-host references in
  `HANDOFF.md` / `Status.md`, including the NVIDIA RTX 4060 Ti / Vulkan render
  gate history or equivalent local machine evidence. If this cannot be
  established, halt with `STATUS: NEEDS_HUMAN` and do not record a substitute
  number.
- Inspect the existing W04 raw Wasmtime Cranelift harness and baseline section,
  especially `crates/script-bench/src/wasmtime_cranelift.rs::tests::w04_cold_start_wasmtime_cranelift`
  and the `crates/script-bench/BASELINE.md` "Formal W04 raw WasmtimeCranelift
  gates" section.
- Record current toolchain evidence, at minimum `cargo --version` and
  `rustc --version`, plus the wasmtime version visible from the workspace
  dependency graph or lockfile.
- Run the existing focused cold-start measurement. If the prior exact
  multi-sample methodology is discoverable and compatible, follow it and
  document it. Otherwise run this focused command three times and record every
  printed sample:

```powershell
cargo test -p rge-script-bench --release --lib wasmtime_cranelift::tests::w04_cold_start_wasmtime_cranelift -- --nocapture
```

- `git diff --check`
- `git status --short --branch`

Do not run the one-hour memory soak. Do not run broad workspace tests or the
canonical dispatch gate as an Executor-initiated verification step unless a
specific reason is documented.

## Halt Conditions

The Executor MUST halt and request fresh guidance if any of the following
occur:

- Recorder-host execution cannot be established before measurement. Halt with
  `STATUS: NEEDS_HUMAN`; do not add a new baseline number.
- The focused W04 raw Wasmtime Cranelift cold-start test cannot be run or does
  not print `duration_ns` and `duration_ms` values. Report the failure output
  rather than changing the harness.
- Recording the measurement would require editing Rust source, tests, benches,
  fixtures, Cargo metadata, plans, ADRs, architecture lints, workflows, or
  automation scripts.
- Any required edit would touch files outside the MAY-edit envelope.
- Existing uncommitted changes outside this dispatch's allowed surface would
  make the forward-doc reconciliation ambiguous.
- The Executor determines that a regression investigation, harness redesign, or
  dependency change is needed. That is out of scope for ISSUE-243.

## Planner Notes

This packet follows the orchestrator semantics in `AI_DISPATCH_AUTOMATION.md`:
an orchestrator run writes TASK and EXEC packets, plus CORRECT only if a
correction round is needed. Missing REVIEW or CLOSEOUT packets are normal for
orchestrator dispatches and must not be treated as evidence of an unfinished
run.

The comparison anchor for this dispatch is the 2026-05-12 raw Wasmtime
Cranelift wasmtime 44 row in `crates/script-bench/BASELINE.md`:
`cold_start = 405,100 ns / 0.405 ms`. The older 904-microsecond wasmtime 23
number is historical context only. Preserve both older records and add the new
result append-only.

The intended publication path is queue PR mode, for example
`Invoke-AiDispatchQueue.ps1 -PublishMode pr`, so the dispatch branch is pushed
and a PR is opened for human review without merging to `origin/main` and
without closing issue #243 automatically.

---

HANDOFF_STATUS: COMPLETE
DISPATCH_ID: ISSUE-243
AUTHOR: Planner / OpenAI Codex
NEXT_ROLE: EXECUTOR_AI
EXIT_CODE: 0

---

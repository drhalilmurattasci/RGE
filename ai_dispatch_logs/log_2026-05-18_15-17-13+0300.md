# AI Dispatch Log

- Timestamp: 2026-05-18T15:17:13+03:00
- Issue: #18 - Preserve TransformOp tessellation face labels
- Issue URL: https://github.com/CADRust/RGE/issues/18
- Branch: main
- Mode: manual repair of failed autonomous issue output
- Publish intent: commit and push to origin/main after verification

## Process Trace

1. Reviewed issue #18 state and the failed auto branch output.
2. Re-applied the intended fix on current `main` instead of merging the stale failed branch.
3. Updated Transform behavior so topology-preserving transforms pass face labels through one-for-one.
4. Updated cache-label prediction tests and labeled-pipeline integration coverage to match runtime behavior.
5. Updated topology lineage smoke expectations for the labeled Boolean output path created by label-preserving Transform.
6. Ran focused cad-core verification, then the full workspace test sweep.
7. Wrote this detailed log before staging, committing, and pushing.

## Files Changed / Added / Deleted

### Modified

- `crates/cad-core/src/operators/transform.rs`
  - Preserves upstream `face_labels` by constructing transformed output with `Tessellation::with_labels` when labels exist.
  - Leaves unlabeled inputs unlabeled through `Tessellation::new`.
  - Updates `output_is_labeled` to mirror labeled input state.
  - Adds unit coverage for labeled pass-through, unlabeled output, duplicate labels, and prediction alignment.

- `crates/cad-core/src/graph/operator_graph.rs`
  - Updates the helper test to expect a Cuboid -> Transform graph to predict labeled output.
  - Keeps the test focused on proving `effective_hash_and_label` invokes the operator label predicate.

- `crates/cad-core/tests/labeled_tessellation_pipeline.rs`
  - Updates the integration test from "Transform strips labels" to "Transform preserves labels".
  - Asserts Transform output remains labeled and carries the exact Boolean output label sequence.
  - Refreshes stale cache-key documentation so predicate-vs-reality alignment matches current behavior.

- `crates/cad-core/tests/topology_lineage_smoke.rs`
  - Updates the Union smoke to use the labeled lineage path now produced by Cuboid -> Transform -> Boolean.
  - Replaces the old unlabeled-plane-heuristic Reinterpreted assertion with labeled-path invariants.
  - Updates Difference smoke comments for the label-preserving Transform path.

### Added

- `ai_dispatch_logs/log_2026-05-18_15-17-13+0300.md`
  - This detailed pre-push repair log.

### Deleted

- None.

## Verification

All commands were run from `A:\RCAD\RGE` with:

```powershell
$env:Path = "A:\RustCache\cargo\bin;" + $env:Path
$env:CARGO_HOME = "A:\RustCache\cargo"
$env:RUSTUP_HOME = "A:\RustCache\rustup"
```

Results:

```text
cargo +nightly fmt --all
PASS

cargo +nightly fmt --all -- --check
PASS

cargo test -p rge-cad-core --lib transform
PASS - 18 passed

cargo test -p rge-cad-core --test labeled_tessellation_pipeline -- --nocapture
PASS - 2 passed

cargo test -p rge-cad-core --test topology_lineage_smoke -- --nocapture
PASS - 3 passed

cargo test -p rge-cad-core --all-targets --no-fail-fast -j 1
PASS - cad-core lib and integration targets passed

cargo test --workspace --all-targets --no-fail-fast -j 1
PASS - workspace all-targets passed
```

## Notes

- `crates/cad-core/tests/phase_7_2_gate_closure.rs` appeared in `git status` after formatting due working-copy line ending normalization, but it has no content diff and is not part of the commit.
- Git repeatedly warned that `C:\Users\halil/.config/git/ignore` was not accessible and that several files will be converted from LF to CRLF when Git touches them. These warnings did not affect verification.
